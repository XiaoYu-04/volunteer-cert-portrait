# 前端进展与待办

> **范围声明**
>
> 本文档**只涉及前端**（`volunteer-cert-portrait-web/`）。
>
> 前端有两种跑法，**切换只改环境变量，页面代码一行都不用改**：接口层按真实后端契约书写，
> **纯前端独立演示**走生产构建（`.env.production` 的 `VITE_USE_MOCK=true`，数据由本地 mock 提供）；
> **联调**走 dev server（`.env.development` 的 `VITE_USE_MOCK=false`，请求经 Vite 代理打到 8080 的后端）。
> 因此本文档中的任何事项都不阻塞后端开发，后端也无需为本文档做配合改动。
>
> 后端侧事项见《[后端进展与待办](后端进展与待办.md)》。

- 整理时间：2026-09-22
- 最后更新：2026-09-24（本轮续：29 页 × 2 视口逐页走查（0 信号）、窄屏表格列宽修复、
  mock 学院口径收敛到字典、`verify:mock` 断言 28 → 31，见第二节第 16 条；
  上一轮续：新增「学院管理」页（第 29 页）、三处「学院」筛选改字典下拉、
  30 个文件的去 AI 味文案改造、`verify:mock` 断言 24 → 28；
  上一轮续：C13~C15 三处 mock 缺陷修复 + C16 补 3 条断言，已浏览器端到端验收；
  更早：构建体积核清与拆包、lint 三处 error 修复、ECharts 注册表核对、`.nvmrc`、前端文档同步）
- 2026-09-24 第七轮：活动发布页接入图片上传（1 张封面 + 最多 6 张图文说明），详情页展示
  封面与图集，列表页显示封面缩略图；接口调用 69 → 70，新增 `InkImageUploader.vue`
- 对应提交：`e287c24`（前端全部页面）、`cb5141b`（缺陷修复）、本文件所在提交（枚举对齐）、
  `1138eaa` / `e441135` / `7b362f3`（2026-09-24，已推送 `0a668d7..7b362f3`）；
  学院管理页与文案改造已随 `72dfbd5`（2026-09-24）提交，工作区干净

---

## 一、当前状态总览

| 项目 | 状态 |
|---|---|
| 能否脱离后端独立运行 | ⚠️ 分两种：生产构建（`VITE_USE_MOCK=true`）可纯前端演示；`npm run dev` 已切真实后端（`VITE_USE_MOCK=false`），需 8080 上有后端 |
| 页面 | ✅ 29 个页面全部完成（第 29 页「学院管理」为 2026-09-24 本轮新增，见第二节第 15 条） |
| 生产构建 | ✅ 通过（0 error） |
| 代码检查 | ✅ oxlint + eslint 全过（exit 0）。**注**：此前文档写「无告警」不实 —— 2026-09-24 实跑 `npm run lint` 有 **3 个 eslint error**，已全部修掉，见第二节第 12 条 |
| 模拟数据自洽 | ✅ `npm run verify:mock` **31 项全过**（21 → 24 → 28 → 31，见第二节第 14、15、16 条；2026-09-24 本轮实跑） |
| 与后端的接口契约 | ✅ 状态枚举与接口路径全部对齐（**0 缺失**；后端 **60 条路径 / 73 个操作**、前端 **71 个调用**，2026-09-26 实查 —— B24 新增 `GET /v1/attendance/mine` 与它的前端调用 `listMyAttendance`） |
| 联调数据 | 当前为**全量演示库**（2026-09-25 整库重建并回灌）：1,503 学生 / 389 活动 / 10,719 报名 / 19,319.3 小时，另有 5 个演示账号 + 1 个组织 + 3 张活动图片；`07` 口径为 1,500 / 386 / 10,719 / 19,319.3，**两组别混用** |
| 视觉/交互核对 | ✅ **29 页全部**做过浏览器实测（2026-09-24，1440 + 620 两视口共 58 张截图，逐张看过）；本轮续做了一轮全站逐页走查，56 条记录 0 信号，第 29 页（学院管理）已在其中，见第二节第 16 条；会话 9 又把窄屏走查补到 **375px**（三端 23 页 × 2 视口 46 条，0 溢出），见第二节第 17 条 |
| 自动化测试 | ❌ 无 |

**一句话结论**：前端功能与视觉已完整可用；接口与状态枚举已和后端全部对齐（第四节 P0 两项均已销账），
剩余工作集中在**工程收尾**（测试、体积、逐页目视）与**业务功能缺口**（上传、证书、收藏），都不影响现在拿出去演示。

---

## 二、已完成的工作

### 1. 设计系统移植（新中式水墨风）

从原型 `styles_12-chinese-ink.html` 逐条移植，不是"参考风格重做"：

- 令牌集中在 `src/styles/tokens.css`，页面里不硬编码颜色
- 原型类名 `.od-*` 机械重命名为 `.ink-*`，CSS 规则本身逐条照搬（含 `nth-child(3n)` 去右边框这类细节）
- 三层样式：`tokens.css`（令牌）→ `base.css`（reset/无障碍/容器）→ `ink.css`（组件类），**引入顺序不可颠倒**
- 视觉约束：零圆角、细墨线分隔而非卡片阴影、区块间距 88px、标题走衬线体、数据走等宽体

### 2. 页面（29 个）

| 端 | 数量 | 页面 |
|---|---|---|
| 学生端 | 9 | 首页、活动列表、活动详情、我的报名、我的时长、我的公益画像、通知公告、通知详情、个人中心 |
| 组织端 | 6 | 组织数据、活动管理、发布活动、报名审核、签到管理、时长提交 |
| 学校端 | 10 | 数据看板、时长审核、公益画像、组织管理、分类管理、用户管理、角色管理、学院管理、通知公告、操作日志 |
| 公共 | 4 | 登录、注册、403、404 |

### 3. 手写组件库（21 个）

不引入任何第三方 UI 库，全部按水墨风手写：

- 通用（14）：`InkButton` `InkTable` `InkPagination` `InkDialog` `InkConfirm` `InkToast` `InkField` `InkStat` `InkSection` `InkEmpty` `InkTabs` `InkProgress` `StatusTag` `InkImageUploader`
- 业务（6）：`ActivityCard` `PortraitSeal` `RankList` `OrgList` `NoticeList` `ChangePasswordDialog`
- 图表容器（1）：`InkChart`

**未做**：`InkInput` / `InkSelect` / `InkTextarea` / `InkDate` 这类薄包装组件。表单控件直接用全局的 `.ink-input` / `.ink-select` / `.ink-textarea` 类 —— CSS 已经是这一层的抽象，再包一层组件只是多一层间接。

### 4. 图表层（13 个图表）

- `components/charts/options.js`：把原型 `ODCharts` 的 13 个图表改写成 `(data) => option` 的纯函数
- 配色通过 `getComputedStyle` 从 `:root` 读取，**CSS 是配色的唯一数据源**，改令牌图表跟着变
- `components/charts/echarts.js`：ECharts 按需注册（**新增图表类型必须在这里补注册**，否则运行时静默不渲染）
- `InkChart.vue`：负责挂载、`ResizeObserver` 自适应、`onBeforeUnmount` 销毁

### 5. 接口层与 Mock

- `src/api/*.js` 按后端模块拆分，**按真实后端契约书写**
- `utils/request.js` 按 `VITE_USE_MOCK` 分流：`true` 走 `src/mock`，`false` 走真实 axios
- 响应外壳 `{ code, message, data }`，成功 `code = 0`；分页统一 `{ total, list }`
- 认证失败码 `20001/20002/20003` 命中即清 token 并跳登录
- `src/mock/` 按 `method + path` 分发，支持 `:id` 占位，共 60+ 个接口

### 6. 路由与权限

- 三套布局对应三个角色前缀：`/student/*`、`/org/*`、`/admin/*`
- 路由 `meta` 携带 `{ title, roles, menu, group }`，**侧边栏菜单从路由表派生**，避免菜单和路由两份定义不同步
- 守卫流程：无 token → `/login`（带 redirect 回跳）；有 token 无用户信息 → 拉取；角色不符 → `/403`
- 按钮级权限用 `v-perm` 指令（不满足时把元素移出 DOM，而非隐藏）

### 7. 数据自洽校验

`scripts/verify-mock-data.mjs`（`npm run verify:mock`）把模拟数据的隐性约束写成 31 项断言
（18 → 21 是分类与标签按后端对齐后补的约束，21 → 24 是 2026-09-24 补的「行内字段一致性」三条，见第 14 条；
24 → 28 是本轮补的 4 条学院字典断言，见第 15 条）。
这些约束原本散落在原型注释里，改一个数很容易让不同页面显示的数字互相打架。

### 8. 缺陷修复（第二轮）

依据 `bug/` 下的两份记录，并在验证过程中另发现 2 个：

- 控制台侧边栏改为吸顶，"退出登录"不再需要滚到页面底部
- 注册手机号改为必填 + 11 位校验；邮箱改为必填 + 格式校验（mock 侧同步加了一道）
- 注册后只写了 store 的 state、没写 localStorage，刷新即掉登录态 → 抽成 `user.applySession()`
- 注册产生的新账号跨刷新会丢 → 落盘到 localStorage（见第三节第 7 条）

### 9. 与后端的口径核对（2026-09-24）

- **接口路径层已全对**：`src/api/` 里 64 个调用 vs 后端 OpenAPI 54 条路径 / 66 个「方法 + 路径」，
  **0 缺失**；后端另有 2 个前端没调的（`POST /portraits/recompute`、`PUT /orgs/{id}`）。
  ⚠️ 2026-09-24 本轮按同一口径重数：**上面的 54 / 66 / 64 各少 1** ——
  以 `HEAD`（`7adb427`）为基准逐个数，后端是 55 条路径 / 67 个「方法 + 路径」、前端 65 个调用；
  学院接口后为 **58 / 71 / 69**；2026-09-24 第七轮新增活动图片上传接口后为 **59 / 72 / 70**
  （运行实例 `GET /v3/api-docs` 实测同样是 59 条路径 / 72 个操作），
  2026-09-26 B24 新增 `GET /api/v1/attendance/mine` 后为 **60 / 73 / 71**（同法实查），
  **「0 缺失」的结论不变**。差额出在哪一次改动未查；`README.md`、本文件、`CLAUDE.md`、`docs/答辩前待办.md`
  都已按重数结果改写（`docs/测试报告.md` 的用例数按 D3 回填时一并核）。
- **演示账号口径统一为 `org_admin`**：与 `sql/03_init_data.sql`、`LoginView.vue`、`mock/data/dataset.js`
  三方一致（`admin` / `student` 也已核对无误）；`volunteer-cert-portrait-web/README.md` 的账号表已同步，
  并修掉该文件一处 Markdown 栅栏缺陷（重复的 ```` ```text ```` 开头会被当成代码块内容渲染出来）。
- **A9 销账，用户状态三层天然对齐**：DB `user_status` 存 `SMALLINT` 1/0 → 服务层 `UserStatusEnum`
  双向翻译 → API 返回英文码 → 前端 `DICT_DEFS.user_status` 用英文码。实测字典接口返回
  `ACTIVE` / `DISABLED` 两档（「正常」/「已停用」，色调 `ok` / `mute`），用户列表返回 `status: "ACTIVE"`
  —— **无需改代码**，用户管理页的状态列与启用/停用按钮原样可用。
- **真实数据已可用**：后端演示数据放大后（学生 1,500 / 活动 386 / 报名 10,719 / 累计 19,319.3 小时），
  dev server 重启即可看到真实数字。已实测 `/api/v1/analytics/dashboard`、`/api/v1/portraits/me`
  （student 返回 22.6 小时 / 四星志愿者 / 3 个标签）、`/api/v1/categories`（6 条都带 `remark`）、
  `/api/v1/orgs?college=计算机学院`（从恒 0 条变成 2 条）。
  注意 mock 仍是自成一套的旧量级（12,480 报名 / 86,420 小时），与真实库不是同一套数字，**演示时别混着看**。

### 10. dev server 代理目标改为可配置（2026-09-24）

`vite.config.js` 的 proxy target 原先**写死** `http://10.0.20.91:8080`（某台机器的内网 IP）且已随提交入库，
那台机器一关机或换 IP，所有人的 dev server 就集体连不上（mock 又是关的，没有后端就跑不起来）。现改为：

- 导出改成函数式 `defineConfig(({ mode }) => {...})`，用 `loadEnv(mode, process.cwd(), '')` 读环境变量，
  target 取 `env.VITE_API_TARGET || 'http://127.0.0.1:8080'`
- `.env.development` 补注释说明用法（可选，默认 `http://127.0.0.1:8080`；联调时放 `.env.local` 覆盖，
  **不要把某人的内网 IP 写进入库文件**），`VITE_API_BASE_URL=/api` 与 `VITE_USE_MOCK=false` 保持不动
- 已实测：不设变量时解析出 `http://127.0.0.1:8080`；在 `.env.local` 里另设 `VITE_API_TARGET`
  能覆盖生效（用局域网地址验证过）

> ⚠️ **代理配置不是热更新**：改完 `vite.config.js` 必须让 dev server 重启才会生效 —— 手动停掉再 `npm run dev`
> 一次最保险；不重启的话代理仍是旧目标，会出现「配置改了却还连旧机器」的错觉。

### 11. 口令安全三件套的前端部分（2026-09-24）

后端把口令从明文改成 BCrypt 密文（B15），前端配套三件事：

- **新增共享组件 `components/biz/ChangePasswordDialog.vue`**：`v-model` 控制开关，三个口令框全部
  `type="password"`（原密码 `autocomplete="current-password"`，新密码与确认框 `autocomplete="new-password"`），
  前端校验必填 / 6–32 位 / 两次一致 / 不得与原密码相同。**改密成功后刻意不清 token、不跳登录页** ——
  后端只踢其它设备的会话、保留当前会话，把用户踢回登录页反而像改密失败。
- **入口三处**：学生「个人中心 → 账号安全」面板；组织端 / 学校端控制台侧栏（这两个角色**没有个人中心页**，
  这是他们唯一的自助改密入口）；用户管理页操作列的「重置密码」弹窗（管理员用，留空即重置为默认 `123456`，
  提示语说明该用户所有登录态会立即失效）。
- **`UserManageView.vue` 的初始密码框**由 `type="text"` 改为 `type="password"`（原来会把口令明文回显在屏幕上），
  hint 同步为「留空则使用默认密码 123456，请提醒用户首次登录后自行修改」；操作列由 3 个按钮变 4 个，
  列宽 190px → 250px。

接口层加了 `api/auth.js` 的 `changePassword` 与 `api/system.js` 的 `resetUserPassword`；
`src/mock/data/` 同步补了这两条（mock 里口令仍是明文存储，且**不做**登录失败锁定 —— 演示时被锁住会很困惑，
mock 也不是安全边界，两点都写进了 mock 的注释）。新错误码 `20005 原密码不正确` 刻意**不**放进
`utils/request.js` 的 `UNAUTHORIZED_CODES`：原密码输错只是本次操作失败，把用户踢回登录页属于误伤。

`npm run build` 通过；`npx oxlint .` 无新增告警。

### 12. 工程健康度核清与修复（2026-09-24 续）

这一轮把三处**「文档声称」与「实跑结果」不符**的地方核清并修掉，全部实跑验证：

- **`npm run lint` 其实有 3 个 error**（此前文档写「oxlint + eslint 无告警」）。逐条处理：
  1. `vite.config.js:11` 的 `process is not defined` —— `eslint.config.js` 只给 `scripts/**`
     配了 Node globals，根目录的配置文件没覆盖；已把 `vite.config.js`、`eslint.config.js`
     一并纳入那个 node 覆盖块。
  2. `.tmp-verify/verify-text.mjs` 的 `process is not defined` —— 这是上一个会话留下的
     **本地验证脚本**（验证 `useDashboardText` 在真数据下的文案），被 `.gitignore` 的
     `.tmp-*` 忽略是**有意为之**，不该被 lint。已给 eslint 加 `**/.tmp-*/**` 的
     `globalIgnores`（与 gitignore 规则对齐），本地跑验证脚本不再污染 `npm run lint`。
  3. `src/views/student/HomeView.vue:45` 解构出 `profiles` 却没用到（模板走的是
     `data?.profiles`）—— 删掉该解构项。

  现在 `npm run lint` **exit 0**；`npm run build` 通过；`npm run verify:mock` 21 项全过。

- **构建体积的真实情况与原判断不符**（详见第四节 P1 第 2 条）。核清后顺手拆包。
  另记一条踩坑：**Vite 8 底层是 Rolldown，`manualChunks` 传对象会让构建直接失败**，
  必须用 `advancedChunks.groups`，且正则里路径分隔符要写 `[\\/]`（Windows 上模块 id 是反斜杠）。
  已写进 `CLAUDE.md` 前端踩坑第 8、9 条。

- **ECharts 按需注册表核对无缺口**：`options.js` 用到的 6 类图表
  （`line`/`bar`/`pie`/`gauge`/`heatmap`/`radar`）与全部组件
  （`grid`/`tooltip`/`legend`/`visualMap`/`title`/`radar`/`calendar`）都已在
  `echarts.js` 注册；`axisPointer` 全部嵌在 `tooltip` 内，由 `TooltipComponent` 覆盖，
  不需要单独的 `AxisPointerComponent`。**这条核对是「静默不渲染」类问题的唯一防线** ——
  它不报错，只能靠比对注册表与用法。

- **新增 `.nvmrc`**（内容 `24.20.0`）。不写大版本号 `24` 是因为 `package.json` 的
  engines 下限是 `>=24.12.0`，只写 `24` 可能落到 24.0~24.11 上。
  ⚠️ 同一轮复核**推翻了一条既有结论**：此前多处记「本机实测 Node v24.20.0、`EBADENGINE`
  已消失」，实为**不成立** —— 本机只有一份 node（`/c/Program Files/nodejs/node.exe`，
  无 nvm / fnm / 多版本），`node -v` = **v22.14.0**，低于 engines 下限。
  ✅ **2026-09-25 已销账**：node 已实际升到 **v24.20.0**（`npm -v` = 11.19.0、与 `.nvmrc` 一致），
  `EBADENGINE` 警告消失、`verify:mock` 31 项实跑通过 —— 上面这段「警告当前仍在」只对 **2026-09-24 当天**成立。

- **`vite-plugin-vue-devtools` 不需要补 `apply: 'serve'`**（附录 A 曾这么建议）：
  该插件内部已写死 `apply: "serve"`（`node_modules/vite-plugin-vue-devtools/dist/vite.js`
  的 plugin 定义），构建时本就不生效，且它的 options 里根本没有 `apply` 字段。
  已在 `vite.config.js` 留注释，免得后人再"修"一次。

### 13. C3 逐页走查 + D4 截图（2026-09-24）

**为什么之前做不了**：28 页里只有约 11 页被人在浏览器里看过，其余只是「代码层验证」。
`npm run verify:mock` 的 21 项断言只覆盖**数据自洽**，不碰组件与页面 —— 页面白屏、图表
不渲染、排版崩，静态检查一律看不出来。ECharts 漏注册更是**静默不渲染且不报错**。

**怎么做的**：用 Playwright 驱动**系统已装的 Edge**（`channel: 'msedge'`，不下载 Chromium），
目标服务是 `npm run build && npm run preview`（生产构建 + mock，即答辩演示的那套配置，
不依赖后端、数据确定）。脚本在 `volunteer-cert-portrait-web/.tmp-shots/shoot.cjs`
（`.tmp-*` 已被 gitignore，不入库；同一目录下还有 `probe-profile.cjs` 用于定点复现缺陷）。

**28 页 × 2 视口 = 56 张截图**，同时程序化采集客观信号：

| 信号 | 结果 |
|---|---|
| 控制台 error / 未捕获异常 | **0** |
| 横向溢出（`scrollWidth > clientWidth`） | **0**（含 620px 断点，C4 随之销账） |
| 零尺寸 canvas | **0** |
| 正文出现 `undefined` / `NaN` | **0** |
| 图表页 canvas 数 vs `<InkChart>` 标签数 | **全部对上**（学生首页 5、组织看板 2、学校看板 7 个标签渲染出 9 个 canvas、两个画像页各 1） |

**结论**：页面渲染层没有硬故障，这一层可以放心。**但走查发现了 3 个只有肉眼/实测才能
发现的问题**，见第四节「P1：走查发现的缺陷」。

**顺带排除的疑点**（写下来，免得后人再查一遍）：

- 公益画像的饼图轮廓不规则 —— 是**刻意的玫瑰图**（`options.js:434` 的
  `roseType: rose ? 'radius' : false`），与图注「玫瑰半径与该标签人数成正比」一致，不是 bug
- 操作日志页**没有**「操作对象」列 —— 与后端 `operation_log.target` 从不写入的已知缺口
  （B20-6）一致，页面处理是对的
- `403` / `404` / 注册页渲染正常。唯一小瑕疵：404 的按钮写「去登录」，文案却说「返回首页」——
  登录态下该按钮会被守卫转回本角色首页，功能不错，只是**标签与文案不一致**（低，未单列条目）

### 14. C13~C15 三处 mock 缺陷修复 + C16 补断言（2026-09-24 续）

C3 走查发现的三个缺陷（第四节「P1：走查发现的缺陷」）已全部修掉，改动都在 mock 数据侧：

- **C13 手机号**：`dataset.js` 里 `students[].phone` / `orgList[].phone` / 三个种子账号的 `phone`
  一律改存**完整 11 位号码**（原先存的是 `138****3300` 这类脱敏串，与库里
  `sys_user.phone` 的形态不一致）。个人中心与用户管理都会把 `phone` 预填进可编辑表单，
  脱敏串必然过不了 `/^1\d{10}$/`。顺带把 `UserManageView` 的输入框 placeholder
  由 `如 138****3300` 改成 `如 13800000003` —— 占位符本身不该示范脱敏格式。
- **C14 实得时长**：签到记录原先三处各写各的（签到 `08:45`、签退 `12:05` 却记 4 小时）。
  现在**签退时刻由「签到时刻 + 活动时长」推出**，分钟取同一个错位值（`08:55~08:59` → `12:55~12:59`），
  既恒等于 `activities[0].hours`（4.00），又不至于 40 行时间完全一样。
  选「卡死 4.00」而非「照后端公式算成 3.15~3.97」，是为了**不动任何聚合数字**、
  让既有断言全部不受影响。活动 09:00 开始，取提前 5~9 分钟到场，与活动说明里
  「请提前 15 分钟到达」也对得上。
- **C15 分类说明**：6 个分类的 `remark` 逐字取自 `sql/07_demo_scale.sql` 第一节的回填，
  mock 与真后端下看到的是**同一套文案**（写在 `types` 每项里，与 `code` 同一处 ——
  不另起按下标对齐的数组，见 `types` 的注释）。

**C16：`verify:mock` 从 21 项补到 24 项**，新增的三条正是上面三个缺陷对应的「行内字段一致性」断言。
补它的理由比修数据更重要：原有 21 项覆盖的是**聚合之间**（学院合计 = 累计时长）与
**枚举覆盖**（四种签到状态都出现），**不覆盖同一行内字段之间的一致性** —— 所以 C14 那种
「同一行里时长与时间矛盾」一直没被拦住。这是断言设计上的盲区，不补同类问题还会再漏。

**验证（都是实跑，不是静态检查）**：

| 手段 | 结果 |
|---|---|
| `npm run verify:mock` | **24 项全过**（新增 3 项见上） |
| **反向验证**：把新增 3 条断言原样跑在**改动前**的 `dataset.js`（`git show HEAD:...` 导出）上 | 3 条**全部失败**，分别命中 40 处 / 77 处 / 6 处 —— 证明断言有效，不是写得过松。C14 抓到的样本正是文档里记的那行 `2025-03-22 08:45:00 → 12:05:00 记 4 小时` |
| **端到端验收**（`.tmp-shots/accept-mock-fixes.cjs`，Playwright + 系统 Edge 打 `npm run preview`） | **7 项全过**：个人中心预填 `13833000000` 且**不改任何字段**点「保存修改」出现「保存成功」；签到管理首屏 10 行「签退 − 签到」全部等于「实得时长」（如 `08:55 → 12:55` 记 `4 小时`）；分类管理 6 行「说明」列都有内容；三页均 0 控制台报错 |
| `npm run lint` / `npm run build` | exit 0 / 通过（体积不变：主 chunk 56.70 kB、`echarts` 652.78 kB、`options` 9.90 kB） |

> ⚠️ **写验收脚本时自己踩的一个坑**：第一版脚本把「签退 − 签到」判成 10/10 行不自洽，
> 实际数据是对的 —— 页面把时间显示到**分钟**（`08:55`）不显示秒，而脚本的正则要求秒，
> 解析失败被当成了不自洽。**先怀疑测试再怀疑数据**：失败信息里的样本
> `08:55 → 12:55 记 4 小时` 一减就是 4 小时，说明是判据写错了。

### 15. 学院管理页与去 AI 味文案改造（2026-09-24 本轮）

**两件事放在一轮做的原因**：学院字典本轮有了管理端接口（见 `docs/后端进展与待办.md` 第二节第 12 条），
前端要给它一个页面；同时把 28 个页面与组件 / 登录页 / 错误页的中文文案统一改成人写的样子 ——
两件事都要逐页改文件，合在一轮里做省一次走查。

**① 「学院管理」页（第 29 页）**

- 路由：`src/router/routes/admin.js` 新增 `path: 'colleges'`（路由名 `AdminColleges`，`meta` 为
  `{ title: '学院管理', menu: true, group: '系统管理' }`），排在「角色管理」之后、「通知公告」之前；
  侧边栏菜单由路由表派生，菜单项随之自动出现。
- 页面：`src/views/admin/CollegeManageView.vue` —— 3 个 `InkStat`（学院总数 / 覆盖学生 / 挂靠组织）
  + `panel` 内的 `InkTable`（列：排序 / 学院名称 / 学生 / 组织 / 状态 / 操作）+ 新增用的 `InkDialog`。
  状态列复用 `StatusTag type="user_status"`（学院状态与账号状态同一套语义：1 启用 / 0 停用，
  **不另造字典类型**）；删除走 `useConfirm` 的 `tone: 'danger'`，被后端拒绝时把文案原样 `toast.error` 出来。
- 接口层：`src/api/system.js` 新增 `listColleges` / `createCollege` / `deleteCollege` / `updateCollegeStatus` 四个调用。
  列表**不过滤停用项**（管理页要能看到并重新启用），与注册页那份只取启用项的 `/v1/auth/colleges` 是刻意分开的两条口径。
- 权限：`src/stores/user.js` 的 `SCHOOL_ADMIN` 权限加 `system:college:manage`，与后端 `StpInterfaceImpl` 同步（16 → 17 条）。
- mock 侧（`VITE_USE_MOCK=true` 纯前端演示时走这里）：`mock/data/dataset.js` 新增 `export const collegeDict`
  （5 条 `{ id, name, sort, status }`，与 `sql/10` 的种子逐字一致）；`mock/data/auth.js` 的学院下拉改为
  `collegeOptions()` 从 `collegeDict` 现算（滤掉停用项）；`mock/data/system.js` 新增 `collegeUsage()` / `collegeRows()`
  与 4 个 handler，`GET /v1/system/dicts` 改为返回 `{ ...DICT_DEFS, college: collegeOptions() }`。
  ⚠️ `dataset.js` 里**原有**的 `colleges`（参与 `verify:mock` 的「学院时长合计 = 86,420」断言）
  与新增的 `collegeDict`（5 个）仍是两份数据、不是「重复定义待清理」，但**取值域已在本轮续收敛**
  （见第二节第 16 条）：`colleges` 只列字典里**有数据**的 4 个学院，`students` / `orgList` 的学院
  一律取自 `collegeDict`。
- 同一轮的功能性改动（不只是文案）：`DurationAuditView` / `OrgAuditView` / `PortraitView` 三页的「学院」筛选
  由自由文本输入改成字典下拉（数据源 `getColleges()`，即免登录的 `/v1/auth/colleges`）；
  `UserManageView` 的「新增用户」表单加了学院下拉（只在「新增 + 学生角色」时出现，见后端第二节第 12 条）。

**② 去 AI 味文案改造（30 个文件）**

- 范围：`src/views/{student,org,admin,login,error}/**` 共 28 个页面 + `components/biz/ChangePasswordDialog.vue`
  + `composables/useDashboardText.js`，**只改中文文案，UI 风格与 CSS 一律没动**（逐行核对 diff：无样式规则改动）。
- 规则（口径文件 `.tmp-verify/de-ai-spec.md`，未入库）：副标题 ≤28 汉字、只答「这页能做什么」，
  删因果链 / 排比 / 承诺；`hint` 只写格式约束；`placeholder` 只写示例值；空状态写「暂无 XX」。
- 禁用词表（闭环 / 贯穿 / 环环相扣 / 全流程 / 一站式 / 赋能 / 助力 / 智能化 / 沉淀 / 微光 / 炬 / 被记住 /
  一目了然 / 随时随地 / 让我们 / 为您 / 我们 / 句末「吧」「哦」「啦」/ 感叹号 / emoji）改完在改动文件里
  **0 命中**（本轮实跑检索核对）。
- 例：`以微光聚炬，<br />让每一次<em>服务</em>都被记住` → `全校志愿服务<br /><em>时长认证</em>与公益画像`；
  「汇总全校志愿服务的关键指标：活动规模、服务时长……数据由各组织日常业务沉淀而成」→「全校志愿服务的规模、结构与审核质量。」。
- 扫描清单 `.tmp-verify/copy-inventory.txt` 列了 36 个候选 `.vue`，其中 7 个（`ActivityCard` / `NoticeList` /
  `OrgList` / `InkDialog` / `InkPagination` / `ConsoleLayout` / `StudentLayout`）本来就没有 AI 味文案，未改动。

**③ `verify:mock` 断言 24 → 28**

新增 4 条（都在 `scripts/verify-mock-data.mjs`）：学院字典无重名 / `sort` 互不相同（并列会让下拉顺序不确定）/
与 `sql/10` 的种子逐字一致且顺序一致 / 至少有一个未被占用的学院（学院管理页的删除流程可演示，mock 里是「电子信息学院」）。
**本轮实跑 `npm run verify:mock`：28 项全过**（本轮续又加到 31 项，见第 16 条）。

**④ 补做的浏览器走查**（mock 构建，1440 视口）：学院管理页的列表 / 新增 / 删除（空学院可删、
被占用时被拦）/ 停用启用；用户管理「新增学生」的学院下拉（只列 5 条启用项、缺学院被 `validate()`
拦下并显示「请选择学院」、角色切到组织管理员时该字段隐藏）与提交后该学院学生数 +1；
时长审核 / 组织管理 / 学生画像三处「学院」筛选下拉都已渲染成字典选项（各 5 条）。

`npm run lint` 与 `npm run build` 本轮都重跑过，**两份都 exit 0**，日志留存在
`.tmp-verify/lint.log` 与 `.tmp-verify/build.log`（`dist/` 随之重新生成）。

**未做的**：620 视口未随之重截，C3 那 56 张截图未整套重跑（D4 重截时把第 29 页带上）。

---

### 16. 全站逐页走查（29 页 × 2 视口）+ 窄屏表格修复 + mock 学院口径收敛（2026-09-24 本轮续）

**① 逐页走查（学生端 11 页、组织端 6 页、管理端 10 页，1440 + 620 两视口）**

- 工具：`.tmp-verify/walk.cjs`（playwright，脚本未入库），采集 console error / 未捕获异常 /
  失败请求 / HTTP ≥ 400 / 横向溢出 / 零尺寸 canvas，全页截图落 `.tmp-shots/`。
- 结果：**56 条记录 0 信号**（无报错、无横向溢出、无零尺寸 canvas；管理端 Dashboard 的 7 张 ECharts
  两视口全部正常）。诊断原始数据：`.tmp-verify/walk-{student,org,admin2,student2,org2,admin3}.json`。
- 文案复核：禁用词表（闭环 / 贯穿 / 赋能 / 助力 / 智能化 / 微光 / 炬 / 一目了然 / 为您 / 我们 /
  句末「吧」「哦」「啦」…）在 `src/views`、`src/components` **0 命中**；mock 注释里 2 处「演示闭环」也改了。
- 走查中修掉的缺陷：通知详情「相关操作」提示与同页列表自相矛盾（`NotificationDetailView.vue:62`）、
  个人中心的手机号/邮箱 placeholder 不合口径（`ProfileView.vue:126-135`）、
  组织端三处副标题残留「……后……」因果链、首页流程段 desc 与流程第 4 步语义重复。
- 报告：`.tmp-verify/walk-{student,org,admin2}.md`（逐页结论 + 证据文件名 + 未决项）。

**② 窄屏表格可读性（系统性缺陷，共享层一处修）**

620 视口下各列表页的中文列被压到 1~2 字一列、操作列的按钮被裁（分类管理的「删除」整颗不可见）。
根因是 `InkTable` 的列 `width` 在 auto 布局下只是建议值。修法只动全局样式一处：`src/styles/ink.css`
的 `@media (max-width:1000px)` 里给 `.ink-table th` 96px、`.col-actions` 120px 下限，超出部分由
`.ink-table-wrap` 的 `overflow-x` 兜住（内层滚动，页级仍不横向溢出）。
实测（`.tmp-verify/measure.cjs`）：620 最小列宽 62 → 96px、最大折行 6 → 4 行、被裁单元格 0；
1440 各表宽度与改动前一致（未给 1440 引入任何横向滚动）。

**③ mock 学院口径收敛（`dataset.js`）**

`students` / `orgList` 的学院取值原本来自 `colleges`（原型 8 个学院），于是「环境学院 / 文学院 /
医学院 / 体育学院」有学生有组织、却不在字典里 —— 三个页面的学院筛选（读字典）筛不到这些记录。
现在取值域收敛到 `collegeDict`：新增 `COLLEGE_TRACK`（学院 / 专业 / 班级同下标，`sql/04` 记过这个坑），
4 个学院有实体、「电子信息学院」刻意留空（删除演示要有对象）；`colleges` 同步收敛成字典里
**有数据**的 4 个学院，`hours` 合计仍是 86,420（断言未破），看板从「8 个学院」变成「4 个学院」，
不再与学院管理页的 5 个正面冲突。顺带修掉 42/60 条「学院与专业错配」。

**④ 两处显示口径**

- 百分比统一走 `formatPercent`（1 位小数）：`OrgList.vue` 原先用 `Math.round` 出「94%」，
  同一页还有「94 %」（接口 `unit`）与「94.0%」两种写法，现只余后两种（分属角标与列表，语境不同）。
- 看板图注：加载态不再写「近 12 个月合计 0 场」（新增 `trendTotalText` / `trendHoursText`，无数据给
  `null`，由 `formatNumber` 渲染成「—」）；「全年活动量以…」改为「近 12 个月以…」，与面板角标同口径。

**⑤ `verify:mock` 断言 28 → 31**：新增「学生与组织的学院都在学院字典里」「学院与专业、班级同下标，
无错配」「学院排名的名字都在学院字典里」三条。**实跑 31 项全过。**

**⑥ 学院管理页端到端复测（mock 构建，1440 视口，`.tmp-verify/check-college-flow.cjs`）**：
新增「测试学院」→ 行出现（0 学生 / 0 组织）+ toast「学院已新增」；删除被占用的计算机学院 →
确认框文案正确、被拦并提示「该学院下还有 15 名学生、2 个组织，不能删除」、行仍在；
删除空学院「电子信息学院」→ 成功删除 + toast「学院已删除」。全程 0 console error。

**⑦ 按钮默认外观修复（结构层一处）**：侧边栏底部的「修改密码 / 退出登录」渲染成了浏览器默认按钮
（灰底 `ButtonFace` + `2px outset` 边框 + `1px 6px` 内边距）—— 根因是 `.console-user-out` 只写了
`border-bottom`，从没重置 `<button>` 的 UA 样式。修在 `src/styles/base.css` 的结构层重置里
（`background:none; border:0; padding:0; appearance:none`），两处文字按钮（侧边栏、学员端页脚那个
无类名按钮）随之回到「文字 + 细下划线」的水墨写法；`.btn` / `.ink-tab` / `.ink-pager-btn` /
`.ink-dialog-close` / `.auth-demo` 都自带边框与内边距，逐条核对后不受影响。

`npm run lint` / `npm run build` 本轮复跑均 exit 0（`.tmp-verify/lint2.log`、`.tmp-verify/build2.log`）。

**未做的**：620 下表格改为内层横向滚动后，列多的表要横向滚动才看得到操作列（数据列多的固有取舍）；
活动详情页的 head 段落是活动简介（22~62 字），属正文不属副标题，未改；看板饼图/环图居中而图注左对齐
（`options.js` 的既有实现）未动；看板热力日历只覆盖 mock 的 2025 年 3 月，未造全年数据。

---

### 17. 学生端页脚重做 + 375px 横向溢出修复（2026-09-24 本轮续）

**① 页脚改成单行深色栏（`StudentLayout.vue` + `src/styles/ink.css`）**

原页脚是「12 栏网格 + 两段文字」：左 7 栏放 21 字大标题、右 5 栏放角色与数据说明，
视觉重心比页面主体还重，也与顶部品牌重复。按用户给的参考版式（单行：左品牌 / 中链接 / 右版权）重做：

- 结构：`印章（32×32 朱砂）+ 志愿服务数据志` → `页脚导航` → `© 2026 · 高校志愿服务时长认证与公益画像数据分析系统`。
  导航复用 `useMenu('/student')` 的 7 条（首页 / 志愿活动 / 我的报名 / 我的时长 / 我的公益画像 / 通知公告 / 个人中心），
  与顶部导航同源，不再有第二份菜单定义。
- 样式：`.foot` 内边距由 `56px 0 68px` 收到 `26px 0`；`.foot-in` 由 12 栏 grid 改 flex 单行，
  `justify-content:space-between` + `.foot-nav{margin-right:auto}` 让导航贴着品牌、版权贴右；
  宽度不够时版权自动落到第二行并**左对齐**（`space-between` 下单行元素靠头），因此没有为页脚新增断点。
- 清理：删掉 `.foot-name` / `.foot-meta` / `.foot-meta a` 三条全局样式（全站只有这一处用），
  新增 `.foot-brand` / `.foot-nav` / `.foot-copy`；同时清掉 `StudentLayout.vue` 里上轮删按钮后遗留的
  `.foot-meta-actions` 样式（`rg` 确认 DOM 已无引用）。
- 实测（`.tmp-verify/shot-foot.cjs`，1440 / 1024 / 620 / 375 四档）：页脚高度 84 / 118 / 154 / 207px，
  四档下页脚内 `scrollWidth == clientWidth`、页级横向溢出 0、导航 7 条齐全；
  滚到底页脚完整可见（`.tmp-verify/probe-scroll.cjs`：620 下可视高度 = 页脚高度 154px）。

**② 375px（手机）横向溢出修复**

此前窄屏只走到 620，本轮补到 375（三端 23 页 × 2 视口 = 46 条，`.tmp-verify/check-narrow.cjs`），
查出两处**既有**溢出，都在共享层修掉，46 条现为 0：

- `.hero-in` / `.sec-head` 是 12 栏网格，光列间距就有 11×36px = 396px，超过手机可用宽度（375-56 = 319px），
  首页因此横向溢出 49px。修在 `@media (max-width:620px)`：两处收成一栏，子项 `grid-column:1 / -1`，
  行间距与原来一致，620 的观感不变。
- `.ink-rank-row` / `.ink-org-row` 里的名称与指标都是 `nowrap`，最小内容宽度 270 / 328px 会把所在网格列顶宽
  （`/admin/dashboard` 溢出 6px，`.grid-2` 的单列被撑到 328px 而容器只有 269px）。同样在 ≤620 放开换行
  （`flex-wrap:wrap`，数值列 `margin-left:auto`），**放得下时仍是一行**，620 视觉不变。

**③ 复跑**：`npm run lint` / `npm run build` exit 0；走查 `student` 4 页 + `admin` 2 页 × 2 视口 = 12 条
记录 **0 信号**（`.tmp-verify/walk-foot.json`、`.tmp-verify/walk-foot-admin.json`）。

### 18. 活动图片发布与展示（2026-09-24 第七轮）

- 新增 `InkImageUploader.vue`：支持单张封面和多张图文说明；发布页使用同一组件，
  `max=1` 时替换封面，`max=6` 时追加图片并允许填写说明。
- 发布页提交结构增加 `cover` 与 `images[]`；图片上传成功后才进入表单模型，
  上传中禁用提交与重置，批量上传时已成功的图片不会因后续单张失败而丢失。
- 详情页新增封面大图和图集；学生活动列表、组织活动管理列表增加封面缩略图。
- `utils/request.js` 新增 multipart `upload()`；Vite 增加 `/uploads` 代理，开发环境直接读取后端图片。
- 前端校验与后端一致：JPG/PNG、单张 5MB；纯 mock 模式下上传入口明确禁用，避免误以为上传成功。
- 实测：组织管理员发布页识别到 2 个上传区域；学生活动列表 3 张封面全部加载；
  活动详情封面 `naturalWidth=1536`、图集 1 张且说明正常；`npm run build`、ESLint、Prettier 通过。

### 19. 学生自助签到 / 签退入口（B24，2026-09-26）

- 「我的报名」页新增**「签到」列**（放在「状态」之后）：按 `signupId` 关联本人的签到记录，
  显示状态标签（未签到 / 已签到 / 已签退 / 缺勤 / 异常）；已签退的再显示实得时长；无记录显示 `—`。
  操作列由 180px 放宽到 220px 并补 `cellClass: 'col-actions'`。**未新增页面/路由**（仍是 29 页）。
- 行内新增**「签到」/「签退」按钮**：显隐完全由服务端返回的 `canSignIn` / `canSignOut` 决定
  （后端按 A2 的 30 分钟窗口算好），**前端不重复窗口常量、也不提前拦**；两个标志都为假时不渲染按钮
  —— 渲染一个点了必然报错的按钮比不渲染更糟。点击成功后报名列表与签到记录同时刷新。
- `src/api/volunteer.js` 新增 `listMyAttendance`（`GET /v1/attendance/mine`，第 **71** 个调用）；
  调用时不传 `studentId`（数据范围由后端按登录态裁剪，见 B19）。
- 签到记录**一次取全**（`pageSize: 500`）而不是跟着列表分页走：学生的签到记录量级很小，
  跟着分页会让「我的报名」翻页时按钮时有时无；取失败时只把签到列置空，不打挂整页。
- mock 三处对齐：① 新增 `GET /v1/attendance/mine` handler（**必须排在 `/v1/attendance/:id` 之前**，
  踩坑第 4 条），非学生账号返回 **10003 而非 20003**（20003 会清 token 跳登录）；
  ② `dataset.js` 给演示账号补 4 条 `NOT_SIGNED` 记录（活动 2 / 5 / 7 / 16），
  否则纯前端演示下签到入口**永远没有可点的按钮**；只造 `NOT_SIGNED` —— `SIGNED_OUT`
  要满足「(签退 − 签到) / 3600 = hours」的行内断言（C14），造它既无价值又会被 `verify:mock` 抓住；
  ③ **收掉一处运行期 C14 类矛盾**：原先 `sign-out` 写 `signOutAt = now()` 却记 `item.hours || 4`、
  `PUT /attendance/:id` 同样写 `hours || 4`，现场点一下就会出现「记 4 小时、两个时间只差几秒」；
  现统一到 `resolveMockHours(item)`（`(签退 − 签到) / 3600`，封顶活动预计时长的 1.5 倍），
  签退时刻改为「签到时刻 + 活动时长」，一行自洽、经得起现场减一下。
- ⚠️ **mock 刻意的口径差异（已写进代码注释）**：mock **不做** A2 的 30 分钟窗口校验
  （`canSignIn` 只看状态）—— 演示数据里的活动都在 2025 年，按窗口算的话按钮永远点不了，
  纯前端独立演示（答辩口径）就走不通闭环；真后端会按窗口拒绝并给出具体文案。
- 顺带收掉三处「签到列一加就暴露」的 mock 不自洽：
  ① **取消报名不回收签到记录** —— 真后端 `invalidateBySignupId` 会把签到记录作废，mock 原先不动它，
  于是会出现「已取消 + 未签到」；现在取消时从 `attendance` 里摘掉，`onCancel` 也同时刷新两处数据；
  ② **演示账号在活动 1 的报名是 `PENDING` 却带着「已签退 4 小时」** —— 真后端签到记录只在报名
  **审核通过**时生成，这种组合不可能出现；活动 1 是已结束的活动，按收口逻辑本就该是 `COMPLETED`，
  已改（顺带该行不再显示「取消报名」，与「已完成的活动无法取消」一致）；
  ③ **运行期时长口径**统一到 `resolveMockHours`（见上条 ③）。
- **列宽是量出来的**：「签到」列最初 150px，减去 td 左右内边距各 16px 只剩 118px，而
  「已签退」标签 71 + 间距 8 + 「4 小时」43 = **122px > 118px** → 1440px 下折成两行；
  现取 **160px + 间距 6px**（128px 内容盒 > 71 + 6 + 47，稳定单行）。
- 实测：`npm run verify:mock` **31/31 通过**、`npm run lint` exit 0、`npm run build` exit 0
  （✓ built in 422ms，产物体积与既有记录一致；三道关均为**裸跑**，此前子代理遇到的
  `spawn EPERM` 是文件沙箱限制、不是代码问题）；另有一次直接调 mock 路由表的实跑（18 项全过：
  字段齐全、4 条可签到、传 `studentId`/`orgId` 结果不变、三个筛选生效、非学生 10003、
  `/mine` 在路由表里的索引先于 `/:id`）。
- **浏览器级验收**（系统 Chrome + `playwright-core`，脚本 `.tmp-verify/b24-signups.cjs`）：
  恰好 4 行「未签到」带签到按钮、1 行「已签退 4 小时」、3 行 `—`；点签到 → 已签到 + 出现签退按钮；
  点签退 → 已签退且 **`hours` 与 (签退 − 签到) 严格一致**（4 小时与 5 小时两个活动各验一次）；
  全表按钮计数与状态分布对齐；**0 控制台报错 / 0 未捕获异常 / 0 失败请求**；1440 与 620px 页级溢出 **0 px**。
- ⚠️ **工具链两条**（本机实测，会挡住后续自动化）：`vite preview` 只监听 **IPv6 `[::1]:5199`**，
  必须用 `http://localhost:5199`（`127.0.0.1` 被拒绝）；`.tmp-verify/walk.cjs` 的登录选择器已过期
  （等的是 `placeholder="请输入用户名"`，而登录框现在是「用户名或学号」），复跑前要改成
  `input[type="text"]` + 按按钮文案匹配。

---

## 三、关键决策与理由

### 1. 纯手写组件，不引入 Element Plus

原型是一套**完整的设计系统**（零圆角、墨线分隔、印章式标签），与 Element Plus 的默认视觉冲突很大。
用 Element Plus 需要大量样式覆盖，且很难完全摆脱它的观感。手写组件虽然工作量大，但视觉一致性最好，
答辩时也讲得清"这部分是自己实现的"。

### 2. 保持 JavaScript，不迁 TypeScript

脚手架现成，省下的时间用于多铺页面。架构文档虽规划了 TS，但毕设答辩中 TS 并非必需项。

### 3. 用原生 CSS，不引入 Sass

原型本身就是纯 CSS，加 Sass 只会多一个构建依赖，没有实际收益。

### 4. 图表内的柱状元素保留轻微圆角

原型对柱状图用了 `borderRadius: [6,6,0,0]`，与页面 UI 的"零圆角"看似矛盾。
**这是原型的刻意选择**（页面 chrome 零圆角、图表内元素轻微圆角），已照搬并在代码注释与 README 中标注，不要"统一"掉。

### 5. Mock 分流：切换后端只改一个环境变量

后端当时未就绪，但不能把假数据散落在页面里。做法是 `api/*.js` 按真实契约书写，
由 `utils/request.js` 按 `VITE_USE_MOCK` 分流。后端就绪后改 `.env` 一个开关即可，页面代码零改动
（dev 现已切到真实后端；纯前端演示仍可让 `.env` 走 mock）。

### 6. 状态枚举以后端为权威

本轮核对 `sql/02_schema.sql` 的列注释与 `sql/03_init_data.sql` 的 `sys_dict` 种子数据，
发现前端有 3 处码值对不上，**已按后端对齐**（后端脚本已在 PostgreSQL 18.6 实跑验证，是权威来源）：

| 字典类型 | 前端原值 | 已改为（后端值） |
|---|---|---|
| `attendance_status` | `UNSIGNED` | `NOT_SIGNED` |
| `notification_type` | `SYSTEM`/`ACTIVITY`/`AUDIT` | `SIGNUP`/`DURATION`/`SYSTEM` |
| `audit_action` | 缺 `SUBMIT` | 补上 `SUBMIT` |

顺带确认了两项**本来就一致、无需改动**的对接点：

- 认证头：后端 `sa-token.token-name: Authorization` + `token-prefix: Bearer`，与前端 `Authorization: Bearer <token>` 一致
- 路径前缀：后端未配 `context-path`，前端经 Vite 代理 `/api` → `8080`，到达后端即 `/api/v1/...`，与架构文档一致

### 7. 注册账号跨刷新持久化，其余实体保持内存态

mock 数据活在内存里，整页刷新会重置模块。注册产生的新账号如果不落盘，会出现
"注册 → 刷新 → 被踢回登录页，且新账号再也登不回来"。

只持久化**账号**这一项（`localStorage` 的 `vcp_mock_extra_users`）；其余实体（活动、报名、时长…）
保持内存态，刷新即回到干净的种子数据，方便反复演示同一条流程。想彻底重置清掉该 key 即可。

### 8. 代理目标放进环境变量，而不是写死在 `vite.config.js`

dev server 的 proxy target 曾写死成某台机器的内网 IP，那台机器关机或换 IP 所有人都连不上，
而 `.env.development` 的 `VITE_USE_MOCK=false` 已入库，没有后端页面直接起不来。
改为读 `VITE_API_TARGET`、**默认打本机 `http://127.0.0.1:8080`**：各人跑各自的后端，
要和队友联调时在 `.env.local` 里覆盖（已 gitignore）。

两个坑记在这里：

- `vite.config.js` 里 **`process.env` 拿不到 `.env` 文件里的变量**，必须用 `loadEnv(mode, process.cwd(), '')`
  读；第三个参数传 `''` 是为了把 `VITE_` 前缀之外的变量也一并加载
- **`.env.local` 的优先级低于 `.env.development`**（加载顺序 `.env` → `.env.local` → `.env.[mode]` →
  `.env.[mode].local`，后者覆盖前者，Vite 8.3.0 实测）。所以 `.env.local` 只适合补 `.env.development`
  里没有的变量（如 `VITE_API_TARGET`）；要覆盖 `VITE_USE_MOCK` 这种已有值，得用 `.env.development.local`
  （同样被 gitignore）

---

## 四、未完成 / 待办

### P0：与后端对接 —— ✅ 两项均已销账（2026-09-24）

| # | 待定项 | 结论 | 影响 |
|---|---|---|---|
| 1 | **`sys_user.status` 的类型** | ✅ 三层天然对齐：DB 存 `SMALLINT` 1/0 → 服务层 `UserStatusEnum` 双向翻译 → API 返回英文码 → 前端 `DICT_DEFS.user_status` 英文码；实测字典与用户列表返回的码值/标签/色调逐条一致 —— **无需改代码** | 无 —— 用户管理页的状态列与启用/停用按钮原样可用 |
| 2 | **组织停用/启用** | ✅ 保留：后端已支持（`org_status` 补了 `DISABLED`，`PUT /orgs/{id}/status` 在 `APPROVED` ⇄ `DISABLED` 间启停），前端 `DICT_DEFS.org_status` 与 `updateOrgStatus` 均已就位 | 无 —— 组织管理页的停用按钮保留 |

> 前端与后端之间**不再有状态枚举分歧**（后端侧口径见 `docs/待办清单.md` C-1）。

### P1：前端工程侧

| # | 事项 | 说明 |
|---|---|---|
| 1 | 自动化测试 | 无任何测试配置。`scripts/verify-mock-data.mjs` 只覆盖数据自洽（现 31 项），不覆盖组件与页面 |
| 2 | 打包体积 | ✅ 2026-09-24 已核清并处理。**原判断有误**：实测主 chunk 一直只有 ~57 kB，「超 500 kB」的是**懒加载**的 ECharts chunk。真正的问题只是 `options.js` 的图表配置与 ECharts 被熔进同一个 chunk（rolldown 给它起名 `options`，662.7 kB），已用 `advancedChunks` 拆开为 `echarts` 653 kB + `options` 9.9 kB。**构建警告仍在**（ECharts 本身就 653 kB，属预期），未用 `chunkSizeWarningLimit` 掩盖 |
| 3 | Node 版本 | ✅ **2026-09-25 已销账（结论：已升 node，engines 不动）**。`where.exe node` → `C:\Apps\Dev\node-v24.20.0-win-x64\node.exe`，实测 `node -v` = **v24.20.0**、`npm -v` = 11.19.0，满足 engines 下限 `^22.18.0 \|\| >=24.12.0`、与 `.nvmrc` 的 `24.20.0` 一致，`EBADENGINE` 警告消失；`verify:mock` 在 v24.20.0 上 31 项实跑通过。<br>⚠️ 2026-09-24 那段「本机 v22.14.0、警告当前仍存在、升不升 node 未定」**只对当天成立**，已作废 |
| 4 | 逐页目视核对 | ✅ 2026-09-24 已完成（C3）：**28 页全部**在浏览器里走过（此前只有约 11 页），1440 与 620 两个视口共 56 张截图，逐张看过。做法与证据见第二节第 13 条；走查发现的 3 个缺陷见下方「P1：走查发现的缺陷」 |
| 5 | 响应式走查 | ✅ 2026-09-24 已完成（C4，并入 C3 一起走）：620px 断点下 28 页**全部无横向溢出**（程序化检测 `scrollWidth > clientWidth`），抽查排版正常（侧栏折叠为顶部换行导航、内容转单列） |
| 6 | 键盘可达性 | 有 `:focus-visible` 与 `.skip` 跳转链接，但未做全站 Tab 走查 |
| 7 | `bug/` 下的缺陷笔记 | ✅ 2026-09-24 已收尾：两份笔记随 `5ad6215` / `af7cc16` **整体删除**（`bug/` 目录随之不再存在）—— 笔记里记的问题本身**都已修**（`cb5141b`，见第二节第 8 条）。其中 `sign-out-bug.md` 第 2 条经查 git 历史确认在新增时就是空的、不是丢了内容 |

### P1：走查发现的缺陷（C3 产出，2026-09-24）—— ✅ 三条均已修复

> 三个都是 **mock 数据侧**的问题，真后端不受影响 —— 但 mock 正是「纯前端独立演示」
> （`VITE_USE_MOCK=true`，答辩用）跑的那套，演示时会撞上。
> 共同点：**静态检查看不出来、真跑也不报错**，只有肉眼 / 实测能发现。
> ✅ **2026-09-24 已全部修复并浏览器端到端验收**，修法与证据见第二节第 14 条；
> 对应的可执行条目 `docs/下一步待办.md` 第 24、25、27 项已销账，第 26 项（补断言）一并完成。
> 下表保留缺陷原貌与成因，供论文「系统测试」一章引用。

| # | 缺陷 | 证据与成因 | 修法（已实施） |
|---|---|---|---|
| 1 | **个人中心手机号预填脱敏值 → 不改动就存不了** | 实测：预填 `138****3300`，直接点「保存修改」报「请填写 11 位手机号」，**保存失败**，手动清空后才能存。成因：`mock/data/dataset.js` 的 `users[].phone` 存的就是脱敏串，`ProfileView.vue:19` 把它预填进表单，而 `:31` 的校验是 `/^1\d{10}$/`。真库存的是完整号码（`13800000003`），`SessionVO.phone` 不脱敏 | ✅ mock 的 `phone` 一律改存完整 11 位号码（与真库形态一致；mock 不是安全边界） |
| 2 | **签到管理「实得时长」与签到签退时间不自洽** | 实测：签到 `08:45` → 签退 `12:05`（差 3 小时 20 分），同一行「实得时长」写 **4 小时**。成因：`dataset.js:913` 写死 `hours: status === 'SIGNED_OUT' ? activities[0].hours : 0`，取的是活动声明时长、与钟点无关。**后端口径是 `(签退 − 签到)/3600`**（`AttendancePolicy`，2 位小数、封顶活动预计时长的 1.5 倍），按后端算该行应是 **3.33** | ✅ 让签退时刻由「签到 + 活动时长」推出（`08:55~08:59 → 12:55~12:59`），**保持 4.00 不变** —— 不动聚合数字，比「照后端公式算」风险小 |
| 3 | **mock 分类 `remark` 全为空串** | `dataset.js:163` 写死 `remark: ''` → 分类管理页「说明」列 6 行全 `—`；真库 6 条都有值（B20-2 补列后 `sql/07` 回填过） | ✅ 6 个分类的说明逐字取自 `sql/07` 的回填，与真库同一套文案 |

**为什么第 2 条一直没被拦住**：`verify:mock` 原 21 项断言覆盖的是**聚合之间**（学院合计 =
累计时长）与**枚举覆盖**（四种签到状态都出现），**不覆盖同一行内字段之间的一致性**。
这是断言设计上的一个盲区，不只是「数据填错了」—— 所以修数据的同时必须补断言，
**已补到 24 项**（第二节第 14 条，含反向验证：三条新断言在改动前的数据上全部失败）。

### P2：业务功能（知识库中的 P1/P2 项）

- ~~活动图片上传~~：✅ 2026-09-24 已完成封面 + 最多 6 张图文说明；
  组织资质材料、用户头像上传仍未接
- 证书生成
- 活动收藏
- ~~通知正文~~：✅ 2026-09-23 已完成 —— mock 通知补了 `content`，`NotificationDetailView` 会渲染正文，
  公告管理表单也能填正文

---

## 五、环境与运行信息

| 项 | 值 |
|---|---|
| 技术栈 | Vue 3 + Vite 8 + Pinia + Vue Router + Axios + ECharts 6 |
| 前端开发端口 | 5173 |
| 后端端口（dev server 代理目标） | 8080；由 `VITE_API_TARGET` 决定，默认 `http://127.0.0.1:8080` |
| 演示账号 | `student` / `org_admin` / `admin`，登录口令均为 `123456`（登录页可点击填入）；库里 `sys_user.password` 存的是它的 BCrypt 密文，明文不落库 |
| 环境变量 | `.env.development`（`VITE_USE_MOCK=false`，联调）/ `.env.production`（`true`，纯前端演示），只含接口地址、mock 开关与代理目标，**不含密钥，已入库**；个人覆盖写 `.env.local` / `.env.development.local`（均已 gitignore） |

> ⚠️ **代理配置不是热更新**：改过 `vite.config.js` 或 `.env*` 后，必须让 dev server 重启才会生效 ——
> 手动停掉再 `npm run dev` 一次最保险。

---

## 六、如何复现验证

```bash
cd volunteer-cert-portrait-web
npm install
npm run dev          # 打开 http://localhost:5173
```

接真实后端（`.env.development` 的 `VITE_USE_MOCK` 已经是 `false`）：

```bash
npm run dev        # 默认经代理打到 http://127.0.0.1:8080
# 连队友那台：在 .env.local 里写 VITE_API_TARGET=http://<对方机器>:8080
# 只想纯前端演示（mock）：在 .env.development.local 里写 VITE_USE_MOCK=true
# 改完 vite.config.js / .env* 都要重启 dev server 才生效
```

三条命令覆盖工程健康度：

```bash
npm run verify:mock  # 模拟数据 31 项自洽断言（含行内字段一致性三条 + 学院字典与口径五条）
npm run lint         # oxlint + eslint
npm run build        # 生产构建
```

走一遍三角色主流程：登录 → 学生报名 → 组织审核报名 → 签到 → 组织提交时长 → 学校审核 → 画像/看板更新。

---

## 七、⚠️ 附：公开仓库与数据库凭证（后端事项，2026-09-24 更新）

**这是后端事项，不在前端范围内，但涉及公开仓库的安全，因此在此一并记录。**

- 仓库 `XiaoYu-04/volunteer-cert-portrait` 的可见性是 **public**
- ✅ 2026-09-23 **口令已轮换**（旧口令实测被服务端拒绝）；`application.yml` 里不再有明文，
  改为「环境变量 → `application-local.yml`（已 gitignore）」两级读取；含口令的
  `docs/对话存档/后端/` 已删除；根 `.gitignore` 也已补上 `application-local.yml` 规则（2026-09-24）
- ⚠️ 仍未完成：**通知另外两位同学更新各自本地的 `application-local.yml`**（不更新就连不上库）；
  清理 git 历史里的旧口令建议推迟到答辩之后（旧口令已失效，不做也不影响安全）
- 注意：**仅从文件里删掉不够** —— 旧口令仍留在 git 历史里，任何人在历史提交里都能翻到

> 前端这边不涉及任何密钥：`.env.*` 只有接口地址、mock 开关与代理目标，`src/` 里没有任何凭证。
