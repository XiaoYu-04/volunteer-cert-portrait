# 部署文档

> **范围**：把系统部署到一台 Linux 服务器（Nginx + 后端 jar + PostgreSQL）。
> 配套文件：`deploy/nginx.conf`、`deploy/vcp.service`。
>
> **状态声明（先说清楚，免得误以为已经跑过）**：本文与两个配置文件**是写出来的，不是部署出来的**。
> 见文末「九、本次核实到什么程度」—— 哪些结论有实跑证据、哪些只是照着配置推的，逐条列明。
> **首次部署时请边做边核，发现与本文不符的地方回来改本文。**

---

## 一、目标架构

```
浏览器
  │
  ▼
Nginx（80/443）── 静态文件 /var/www/vcp（前端 dist/）
  │                 └─ /api/ 反代 → http://127.0.0.1:8080
  ▼
Spring Boot jar（JDK 21，端口 8080，无 context-path）
  │
  ▼
PostgreSQL 18（volunteer_cert_portrait 库，16 张表）
```

三个要点决定了下面所有配置：

1. **后端没有配 `context-path`**，接口前缀就是 `/api/v1/...`，所以 Nginx 的 `/api/` 反代要**原样透传路径**。
2. **前端是 SPA，用 history 路由**（`/student/profile` 这种路径磁盘上不存在），Nginx 必须做 `index.html` 回退。
3. **前后端同源**（都从 Nginx 出），所以浏览器不发跨域请求，CORS 在部署形态下实际不会命中。

---

## 二、前置条件

| 组件 | 版本 | 说明 |
|---|---|---|
| JDK | **21** | `pom.xml` 的 `<java.version>21</java.version>`。Boot 4 要求 21，**17 不行** |
| Nginx | 任意较新版本 | 只做静态托管与反代 |
| PostgreSQL | 18 | 复用现有云库，或自建 |
| Node.js | 24（`^22.18.0 \|\| >=24.12.0`） | **只在构建机需要**，服务器上不需要 |

### ⚠️ 坑 1：机器上有多个 JDK 时，`JAVA_HOME` 和 `java` 未必指向 21

实测（2026-09-24，开发机）：`JAVA_HOME` = `C:\Apps\java\jdk-17.0.17`，`mvn -version` 报的也是 17，
而机器上其实另装了一份 JDK 21 —— 只是没被用。部署机上同理：**装完 21 一定要确认**

```bash
java -version        # 必须是 21.x
echo $JAVA_HOME
```

`deploy/vcp.service` 里 `ExecStart` 写的是绝对路径 `/usr/bin/java`，就是为了不吃 PATH 的亏；
若机器上路径不同，改成实际的绝对路径。

---

## 三、数据库

### 3.1 复用现有云库（答辩演示推荐）

`application.yml` 的 `spring.datasource.url` 已指向云库，**数据是定稿的**
（1500 学生 / 386 活动 / 10,719 报名 / 19,319.3 小时，六档等级齐全），不用再做任何事。

只需要拿到凭证 —— 口令不在仓库里，**线下索取**（见根 `README.md` 的「新同学四步上手」）。

### 3.2 自建库

按顺序跑 `sql/` 下的脚本，**顺序不能乱**：

```bash
psql -U postgres -h <主机> -p <端口> -d volunteer_cert_portrait -f sql/01_create_database.sql  # 必须单独执行
psql ... -f sql/02_schema.sql
psql ... -f sql/03_init_data.sql
psql ... -f sql/04_demo_data.sql        # 可选：演示数据
psql ... -f sql/05_backend_gap_fix.sql  # 必执行，缺了登录接口直接报错
psql ... -f sql/06_backend_gap_fix2.sql # 必执行
psql ... -f sql/07_demo_scale.sql       # 可选：演示数据放大（增量、可重复执行）
psql ... -f sql/08_password_bcrypt.sql  # 仅老库需要；新库的种子口令已是密文
psql ... -f sql/09_consistency_check.sql # 只读自检，20 项违规数应全为 0
```

- **`01_create_database.sql` 必须单独执行** —— `CREATE DATABASE` 不能在事务块里跑。
- **Flyway 当前是关闭的**（`spring.flyway.enabled: false`），所以**没有自动建表**，
  必须手工跑上面的脚本；忘了跑会以「表不存在」报错。
- 自建库要改 `spring.datasource.url`（`application.yml` 里写的是云库地址）。
  改法见第 4.3 节，**不要直接改仓库里的 `application.yml`**（那是入库文件，改了就成"本地能跑、别人不能跑"）。

---

## 四、后端

### 4.1 打包（在构建机上）

```bash
cd volunteer-cert-portrait-server
mvn -B package -DskipTests
# 产物：vcp-boot/target/vcp-boot-1.0.0.jar（约 42 MB，fat jar）
```

> **Windows 上打包前先停掉正在运行的实例** —— 运行中的 jar 会锁住文件，repackage 会报
> `Unable to rename 'vcp-boot-1.0.0.jar' to '...jar.original'`。

### 4.2 上传

```bash
sudo useradd -r -s /usr/sbin/nologin vcp
sudo mkdir -p /opt/vcp/config
sudo cp vcp-boot-1.0.0.jar /opt/vcp/
sudo chown -R vcp:vcp /opt/vcp
```

### 4.3 配置凭证（**jar 里不含任何凭证，必须外部提供**）

`application.yml` 的 `spring.config.import` 已按序尝试四个位置，部署场景用**最后一条**：

```
optional:file:./config/application-local.yml
```

配合 `vcp.service` 的 `WorkingDirectory=/opt/vcp`，即读 `/opt/vcp/config/application-local.yml`。
内容照 `vcp-boot/src/main/resources/application-local.yml.example`：

```yaml
vcp:
  db:
    username: postgres
    password: "在这里填口令"    # ⚠️ 必须加引号
```

或者用环境变量（**优先级更高**，也是 `vcp.service` 里采用的方式）：

```bash
# /opt/vcp/vcp.env，权限 600、属主 vcp
VCP_DB_USERNAME=postgres
VCP_DB_PASSWORD=这里填口令
```

> ⚠️ **坑 2：口令必须加引号。** YAML 会把以 0 开头的数字串当**八进制整数**解析
> （实测 `012345` → `5349`），口令就错了。
>
> ⚠️ **坑 3：缺凭证不会启动失败。** 实测确认：`@ConfigurationProperties` 绑定对解析不了的占位符
> **不报错**，会把它当普通字符串用，应用照常启动 —— 要到**第一次访问数据库**才以认证失败暴露。
> 所以「服务起来了但一登录就报错」时，第一件事是检查凭证文件在不在、口令对不对。
>
> ⚠️ **坑 4：凭证不得写回被跟踪的文件。** 仓库是**公开仓库**，明文口令进过 git 历史
> （删文件删不掉历史，只有轮换口令才能让旧口令失效 —— 2026-09-23 已轮换过一次）。
> `application-local.yml` 与 `vcp.env` 都必须 gitignore。

### 4.4 限制后端只监听本机（**推荐做**）

`application.yml` **没有配 `server.address`**，后端默认监听 `0.0.0.0:8080` ——
也就是 **8080 直接暴露在网上**，别人可以绕过 Nginx 直接打后端。二选一：

```bash
# 做法①（推荐）：加进 /opt/vcp/config/application-local.yml
#   server:
#     address: 127.0.0.1
# 或写进启动参数（见 vcp.service 的 ExecStart）
java -jar vcp-boot-1.0.0.jar --server.address=127.0.0.1

# 做法②：用防火墙/云安全组挡掉 8080 的外部访问
```

### 4.5 起服务

```bash
sudo cp deploy/vcp.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable --now vcp
sudo systemctl status vcp
sudo journalctl -u vcp -f
```

`vcp.service` 里设了 `SPRING_PROFILES_ACTIVE=prod`，对应 `application-prod.yml` 的三处上线收口：

| 收口项 | 效果 |
|---|---|
| `vcp.cors.allowed-origin-patterns` | 由 `*` 收紧为白名单（同源反代下不会命中） |
| `knife4j.enable=false` | 文档页 `/doc.html` 与 `/v3/api-docs` **不再免登录** |
| `logging.level.com.vcp` | 由 `debug` 调回 `info`，不再全量打印 SQL |

> ⚠️ **坑 5：不激活 prod，这三项一律不生效。** 开发/演示行为与改动前逐项一致 ——
> 也就是说，忘了加 `SPRING_PROFILES_ACTIVE=prod` 的话，接口文档会对着公网免登录敞开。
>
> ⚠️ **坑 6：`application-prod.yml` 的 `allowed-origin-patterns` 是占位值**
> （`https://vcp.example.edu.cn`），**部署前必改成实际域名**。同源反代下这一项其实不会命中，
> 但留着占位值不体面，且将来若改成前后端分域部署就会真的用上。

---

## 五、前端

### 5.1 ⚠️ 坑 7：构建前必须把 `VITE_USE_MOCK` 改成 `false`

**这是整个部署流程最容易犯的错**，因为**它不报错**：`VITE_USE_MOCK=true` 的产物页面能正常打开、
有数据、有图表，看起来一切正常 —— 只是**根本不请求后端**，数字是 mock 的
（12,480 报名 / 86,420 小时），与真库（10,719 / 19,319.3）不是同一套。演示时和论文里的数字对不上。

```bash
cd volunteer-cert-portrait-web
# 编辑 .env.production：VITE_USE_MOCK=true → false
npm ci
npm run build          # 产物在 dist/
```

`.env.production` 当前是 `true`（纯前端独立演示用，答辩兜底），**部署到真后端时必须改**。
改完的产物应该是这样的：

```bash
grep -rl "vcp_mock_extra_users\|chenshiyuan@example.edu" dist/   # 应当无输出
```

> 实测（2026-09-24）：`VITE_USE_MOCK=false` 构建后产物中**完全不含 mock**，
> `request` chunk 从 165.0 kB 降到 **113.0 kB**（gzip 60.1 → 42.9 kB）。
> 前提是 `src/utils/request.js` 里对 mock 用的是**动态 import** —— 早先写成顶部静态 import 时，
> 即使 `VITE_USE_MOCK=false` 整套 mock（含演示账号口令）也照样被打进产物，
> 详见该文件里的注释。

### 5.2 上传

```bash
sudo mkdir -p /var/www/vcp
sudo cp -r dist/* /var/www/vcp/
sudo chown -R www-data:www-data /var/www/vcp
```

`dist/index.html` 里的引用是**根绝对路径**（`/assets/index-*.js`、`/favicon.ico`），
所以必须部署在域名根路径下。若要部署到子路径（如 `/vcp/`），得先在 `vite.config.js` 里设 `base: '/vcp/'` 重新构建。

---

## 六、Nginx

```bash
sudo cp deploy/nginx.conf /etc/nginx/conf.d/vcp.conf
# 【必改】server_name 与 root 两处
sudo nginx -t
sudo systemctl reload nginx
```

配置里逐条都写了注释，这里只重复三条**不写会出事**的：

| 配置 | 不写会怎样 |
|---|---|
| `try_files $uri $uri/ /index.html;` | **刷新任何深层页面都 404**（history 路由的命门） |
| `location = /index.html` 的 `no-cache` | 发版后浏览器拿旧 index.html 请求已删除的旧 chunk → **白屏** |
| `proxy_pass http://127.0.0.1:8080;`（**结尾不带斜杠**） | 带了斜杠会剥掉 `/api` 前缀，后端**一律 404** |

---

## 七、验收清单

部署完逐条过一遍，全过才算完成（这也是 `docs/答辩前待办.md` 里 D5 的验收标准）：

- [ ] **在一台干净机器上照本文走一遍能起来** —— 这条是 D5 的正式验收方式，别只在开发机上自测
- [ ] `curl -I http://<域名>/` 返回 200，页面能打开
- [ ] **前端路由刷新不 404**：直接在地址栏输入 `http://<域名>/student/profile` 回车，页面正常
- [ ] 三个账号都能登录：`admin` / `org_admin` / `student`（口令 `123456`）
- [ ] **页面数字来自真库**：数据看板显示 1500 学生 / 386 活动 / 10,719 报名 / 19,319.3 小时，
      而不是 mock 的 12,480 / 86,420 —— 这是「`VITE_USE_MOCK` 改对了没有」的唯一可靠判据
- [ ] 走一遍三角色主流程：登录 → 学生报名 → 组织审核 → 签到 → 组织提交时长 → 学校审核 → 画像/看板更新
- [ ] `http://<域名>/doc.html` 要求登录（prod 生效的证据）；若直接能打开说明 **prod profile 没激活**
- [ ] 从外网 `curl http://<域名>:8080/api/v1/categories` 应当**连不上**（第 4.4 节做了的话）

---

## 八、常见故障排查

| 现象 | 最可能的原因 |
|---|---|
| 页面能打开但**全是 mock 数字** | `.env.production` 的 `VITE_USE_MOCK` 没改成 `false`（坑 7） |
| 刷新深层页面 404 | Nginx 少了 `try_files ... /index.html` |
| 页面全白、控制台报 chunk 加载失败 | `index.html` 被缓存了，引用了已删除的旧 chunk |
| 接口 404，路径里 `/api` 丢了 | `proxy_pass` 结尾多写了斜杠 |
| 服务起来了，一登录就报数据库认证失败 | 凭证文件不在或口令写错（坑 3）；或口令被 YAML 当八进制解析了（坑 2） |
| 启动就报找不到表 | 忘了手工跑 `sql/` 脚本（Flyway 是关的） |
| 启动报 class file version 或 `release version 21 not supported` | `java` 不是 21（坑 1） |
| 文档页免登录能打开 | `SPRING_PROFILES_ACTIVE=prod` 没设（坑 5） |
| `mvn package` 报依赖下不下来 | 本机没有配 Maven 镜像（见文末「本次核实到什么程度」最后一条） |

---

## 九、本次核实到什么程度

写这份文档时**没有真正部署过**（团队决定先出文档、机器到位再实操）。为了不让文档里出现
「照着写的但其实是错的」内容，下面把每条结论的来源标清楚 —— **首次部署时请优先怀疑「未核实」那部分**。

### ✅ 有实跑证据

| 结论 | 证据 |
|---|---|
| `VITE_USE_MOCK=false` 构建通过、产物不含 mock、request chunk 113.0 kB | 实跑 `npm run build` + `grep -rl "vcp_mock_extra_users" dist/` 无输出 |
| `VITE_USE_MOCK=true` 构建通过、56 个页面 0 信号（0 报错/0 溢出/0 零尺寸 canvas） | 实跑 `.tmp-shots/shoot.cjs`（28 页 × 1440/620） |
| `dist/index.html` 用根绝对路径引用资源 | 直接读产物 |
| `pom.xml` 要求 Java 21 | 直接读 `volunteer-cert-portrait-server/pom.xml:23` |
| `application.yml` 未配 `server.address`（默认 0.0.0.0） | 直接读配置 |
| `application.yml` 支持 `./config/application-local.yml` | `spring.config.import` 第四条 |
| `application-prod.yml` 三处收口、且不激活则不生效 | 直接读该文件头部注释 |
| Flyway 关闭、后端无 `context-path` | 直接读 `application.yml` |

### ⚠️ 未核实（照配置推的，首次部署时请核对）

| 结论 | 为什么没核实 |
|---|---|
| `nginx.conf` 的语法正确性 | **编写它的机器上没装 nginx**，无法 `nginx -t`。首次部署务必先 `nginx -t` |
| 起 jar + Nginx 反代的实际联调 | 本机没有数据库凭证（`application-local.yml` 不在），起不了后端 |
| `vcp.service` 在 systemd 上的行为 | 同上，没有 Linux 环境可试 |
| 第 4.4 节 `--server.address=127.0.0.1` 的实际效果 | 未实跑 |

### 另外两条环境观察（与部署文档有关，但本身不是文档内容）

1. **开发机当前 `JAVA_HOME` 指向 JDK 17**，而项目要 21（JDK 21 另装在 `C:\Apps\java\jdk-21.0.9+10`）。
   见坑 1。
2. **本机 Maven 目前没有配镜像**：`~/.m2` 与 Maven 安装目录下都没有生效的 mirror 配置，
   而本地仓库的记录显示上次构建（2026-09-23）走的是 `aliyun-public` 镜像。
   本会话尝试 `mvn package` 时依赖解析失败（沙箱拦网，Maven Central 返回 403），
   **这不代表代码有问题，是环境问题** —— 但重新构建前建议先把镜像配回来
   （`~/.m2/settings.xml` 加一个 `aliyun-public` 的 `<mirror>`）。

---

*新增于 2026-09-24。首次实际部署后请把「九、本次核实到什么程度」整节改写成实跑结论。*