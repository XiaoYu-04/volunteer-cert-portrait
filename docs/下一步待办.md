# 下一步待办（2026-09-24 版，2026-09-25 补记）

> 这份清单只回答「**现在还剩什么、按什么顺序做、怎么验收**」。
> 「做过什么、为什么这么做、哪条已销账」在 `docs/待办清单.md`（A–F 组历史总账），两者互补、不重复。
>
> 来源：2026-09-24 三路**只读审计**（后端代码 / 前端 / 仓库与文档）交叉核对，每条都带 `文件:行` 证据；
> 标【新】的是本轮审计才发现的、历史总账里没有的条目。
>
> **2026-09-25 补记**：本轮**云库整库重建 + 全量演示数据回灌 + 后端重新打包重启 + 三角色冒烟**
> 已完成 —— 第 35 项（「演示数据已清空」）与第 37 项（「学院字典只在 `10` 里」）均已销账，
> 下面「数据库」「演示数据」两行已按**实测值**改写。过程与证据见 `docs/会话记录.md` 会话 11。

## 0. 先对齐口径（动手前扫一眼）

| 项 | 当前值 |
|---|---|
| 代码基线 | `7adb427`（`main` 与 `origin/main` 一致）；**活动图片功能已完成后等待本轮提交** |
| 后端 | `mvn -B package -DskipTests` **11 个模块 BUILD SUCCESS**（2026-09-25 重新打包）；**本机 8080 常驻运行新实例**（**pid 32936**，2026-09-25 重启，开发口径即未带 prod profile，5.0 秒启动），四个既有修复 + B22 / B23 实测生效；**2026-09-25 三角色冒烟全过**（`student` / `org_admin` / `admin` 登录均 `code=0`）；端到端脚本 `.tmp-verify/verify.ps1` |
| 数据库 | PostgreSQL 18.6，16 张表；**2026-09-25 已按 `02 → 03 → 04 → 05 → 06 → 07 → 10 → 11 → 12` 整库重建并回灌全量演示数据**，最后跑 `09` 只读复验：**检查项总数 20、违规合计 0**（20 行明细全 0）。行数实查：`sys_user` 1514 / `student_info` **1503** / `student_profile` 1503 / `volunteer_activity` **389** / `activity_signup` **10719** / `attendance_record` 8235 / `service_duration` 7423 / `duration_audit` 13731 / `notification` 15034 / `org_info` 9 / `attachment` 3 / `sys_dict` 38 / `activity_category` 6 / `sys_role` 3 / `operation_log` 1 |
| 演示数据 | **已是全量口径**（2026-09-25 重灌）：`07` 口径 **1500 学生 / 386 活动 / 10719 报名 / 19319.3 小时**；叠加 `12` 后 **1503 学生 / 389 活动**（报名与时长不变 —— `12` 不造报名）。另有图文演示叠加：5 个演示账号 + 1 个组织 + 3 张活动图片（id 387/388/389 带 `cover`）；学院字典 5 条在位（`dict_type='college'`、`sort` 1~5，`GET /api/v1/auth/colleges` 实测返回 5 条）。⚠️ **`07` 口径与叠加 `12` 后的两组数字不要混用** |
| 前端 | 29 页全部完成且全部懒加载；接口层 70 个调用与后端 59 条路径 / 72 个操作全对上；活动发布页已接入封面与图文说明上传，详情页显示图集 |
| 联调 | `.env.development` 的 `VITE_USE_MOCK=false`（开发走真实后端）；**`.env.production` 仍为 `true`**（纯前端独立演示口径 —— 部署到真后端前改成 `false`，见 `docs/部署文档.md` 5.1「坑 7」）；代理默认 `http://127.0.0.1:8080`，可用 `VITE_API_TARGET` 覆盖 |
| 安全 | ✅ B15 口令 BCrypt 密文入库 + 登录失败锁定（5 次 / 15 分钟）+ 本人改密 + 管理员重置，2026-09-24 完成 |
| 所处阶段 | 开发计划共十阶段：一~八已完成，**当前在第九阶段（系统测试与数据完善）**，第十阶段是部署 / 论文 / 答辩 |
| ⚠️ 2026-09-24 第三轮 | **云库已重置**：16 张表全部 `TRUNCATE`，重灌「基础数据 + 2 个测试管理员」（新脚本 `sql/10_base_and_test_accounts.sql`，**不跑 `04` / `07`**）→ 上面「演示数据」那行的 1500 / 386 / 10719 / 19319.3 **已不在库里**；清空前全量备份在 `.tmp-verify/db-backup-20260924-150914/`（16 个 CSV + `RESTORE.md`，还原工具 `.tmp-verify/Restore.java`）。代码基线 = `7732bb5`（**已提交并推送**，16 文件 +693 / −19）；后端**已重新打包并重启**（新 pid 82864），`GET /api/v1/auth/colleges` 与注册落库学院已实测生效（`.tmp-verify/verify-college.ps1` **11/11 PASS**）。详见 `docs/会话记录.md` 会话 6 与下面第 35~39 项。**⚠️ 2026-09-25 已重灌全量演示数据，该行「1500 / 386 / 10719 / 19319.3 已不在库里」的结论只对 2026-09-24 当时成立**（见「下次开工的起点」与第 35 项） |

> ⚠️ 演示时别混数字：当前真实库是**全量演示数据**（1500 / 386 / 10719 / 19319.3，叠加 `12` 后 1503 / 389）；
> 前端 mock 仍是另一套量级（12,480 报名 / 86,420 小时）。两组数字都要标明口径。

---

## 下次开工的起点（2026-09-25 收工时的状态）

**最近三轮是并行推进的**（过程、取舍与证据见 `docs/会话记录.md`）：

- **前端侧**（提交 `a03e0f1`、`b5e5bef`）：构建体积核清与拆包、lint 三处 error 修复、
  ECharts 注册表核对、`.nvmrc`、前端文档同步，以及 **C3 逐页走查**
  （28 页 × 1440/620 两视口 = 56 张截图，C4 随之销账）。
- **文档与 SQL 侧**：清掉 **P1 第 14、15 项**，并把 **P2 第 18 项（D3 硬缺口）** 落地 ——
  `sql/09_consistency_check.sql` 已实跑（20 项、违规合计 0）。
- **本轮（2026-09-24 下半场，独立一轮）**：修 2 个缺陷（**P0 时长可开给未通过审核的报名**、
  **P1 学生注册不建档**）+ 补齐 D 组两份交付物（D2 `docs/测试用例表.md`、D3 `docs/测试报告.md`）
  + 写 D5 部署文档并收口 prod profile 口径 + 本机起 8080 新实例并完成端到端实测
  （含演示数据还原与 `sql/09` 复跑归零）。

**2026-09-24 下半场（第三轮）另起一轮**：**云库重置** —— 清空前全量备份（`.tmp-verify/db-backup-20260924-150914/`）→ 16 张表全部 `TRUNCATE` → 重灌「基础数据 + 2 个测试管理员」（新脚本 `sql/10_base_and_test_accounts.sql`，**刻意不跑 `04` / `07`**）；同时修 **B30 的注册链路**（学院字典 5 条 + 注册表单学院下拉框 + 后端校验并落库 `student_info.college`）并新增**免登录**接口 `GET /api/v1/auth/colleges`。⚠️ 当时库里没有演示数据；**第七轮已补小型图文演示数据**，全量规模仍需另跑 `sql/07`。
→ **2026-09-25 已重灌全量演示数据（`04` / `07` 都跑了），这一段的口径只对 2026-09-24 当时成立**，见下面「2026-09-25（第八轮）」一段。

**2026-09-24 下半场（第四轮）再起一轮**：学院字典有了**管理端** —— `vcp-system` 新增 4 个接口（含删除时的占用校验），
前端新增**「学院管理」页**（第 29 页），并把 **B30 的「管理员新增用户」路径补齐**（`UserSaveDTO.college` +
`DictService.containsEnabled` + `ensureArchive(userId, college)`）；同一轮还做了 30 个文件的**去 AI 味文案改造**、
把 `verify:mock` 补到 **28 项**。学院管理页与「新增学生」表单已在 mock 构建下走过浏览器（1440 视口），
三处「学院」筛选下拉也已确认渲染成字典选项；接口侧由脚本实测
（`.tmp-verify/verify-college-admin.ps1`，**PASS 41 / FAIL 0**），见下面第 40 项。

**2026-09-24 第七轮**：活动图片上传完成 —— 新增 `POST /api/v1/attachments/images`，
发布页支持 1 张封面 + 最多 6 张带说明图片，详情页返回图集；图片本体保存在后端 `uploads/`，
PostgreSQL 只存 `attachment` 元数据。新增 `sql/11_activity_images.sql` 与
`sql/12_activity_images_demo.sql`，并已真实走通上传、创建活动、详情返回和学校管理员上传权限。

**2026-09-25（第八轮，独立一轮）**：**云库整库重建 + 全量演示数据回灌 + 后端重新打包重启 + 三角色冒烟**
（过程与证据见 `docs/会话记录.md` 会话 11）：

- **重建序列**：`02_schema.sql` → `03_init_data.sql` → `04_demo_data.sql` → `05_backend_gap_fix.sql`
  → `06_backend_gap_fix2.sql` → `07_demo_scale.sql` → `10_base_and_test_accounts.sql`
  → `11_activity_images.sql` → `12_activity_images_demo.sql`，最后只读复验 `09_consistency_check.sql`。
  顺序理由：`04` 是 `07` 的硬前置（`07` 的新活动 `org_id = 1 + (g % 6)` 需要 `org_info` 有 id 1~6，
  而 id 2~8 全由 `04` 创建）、`10` 补学院字典、`12` 放最后（避免 `07` 把它的名额从 20/35/18 抬到 46）。
- **结果**：`07` 末尾自检**全部通过**（原样输出：`[07 自检] 全部通过：学生 1500 人、活动 386 场、
  报名 10719 条、累计时长 19319.3 小时（演示账号 22.6 小时 / 标签 热心志愿者,长期坚持型,大型活动型）`）；
  `09` 实跑 **检查项总数 20、违规合计 0**。
- **两组口径**（别混用）：`07` 口径 **1500 学生 / 386 活动 / 10719 报名 / 19319.3 小时**；
  叠加 `12` 后 **1503 学生 / 389 活动**（报名与时长不变）。行数明细见上面「0. 先对齐口径」的数据库行。
- **后端**：`mvn -B package -DskipTests` 11 个模块 BUILD SUCCESS，`vcp-boot-1.0.0.jar` 已重启
  （**pid 32936**，8080 监听，开发口径即未带 prod profile，5.0 秒启动）；三角色冒烟全过
  （`/activities` total=389、`/analytics/dashboard` 活动 389 / 报名 10719 / 时长 19319.3 / 签到率 90.1% /
  审核通过率 75.6% / 5 个学院合计 19319.3 / 8 类标签齐全；`/system/users` total=1514；
  `/portraits/me` 四星志愿者、`/portraits/distribution` 返回 8 条数组）。
- **保险备份**：清库前 `.tmp-verify/db-backup-20260925-pre-reseed/`（16 张表 CSV、196 行、28.5 KB，
  被 `.gitignore` 覆盖、未入库）。
- **本轮踩的坑**：`02_schema.sql` 的 `DROP TABLE` 一开始卡了 **618 秒** —— 被上一次会话遗留的
  两个 `idle in transaction` 连接（pid 5798 / 5801，已挂 18 小时 52 分）持有的 `attachment_id_seq` 锁挡住；
  用 `pg_terminate_backend` 清掉 4 个陈旧会话后 DROP 立刻完成。**动 DDL 前先清陈旧会话**
  （排查法见 `CLAUDE.md` 踩坑第 19 条）。

更早的会话（`00b6d28`~`f818da7`）做完了 **P0 第 1/3/4 项、P1 第 5~13 项、安全第 8~11 项**。
**下次按这个顺序接**：

1. **D4 系统截图**（答辩硬前置，且**必须重截**）—— `.tmp-shots/` 目录已不在仓库里，
   C3 那 56 张没有留存。先拍板「入哪几页、放哪个目录」（第 28 项），再按 D5 口径重截。
   注意：演示走真实后端还是 mock，决定截图里的数字是哪一套（见第 19 项）。
   **2026-09-25 补**：会话 8 的「29 页 × 2 视口、0 信号」走查记录同样只留在 `.tmp-verify/`（未入库），
   结论可用、图仍要重截；另外**现在库里就是全量演示数据，不用再等灌数据**。
2. **D5 收尾** —— 文档与配置已就绪（`docs/部署文档.md` + `deploy/nginx.conf` + `deploy/vcp.service`），
   剩「在干净机器上照文档走一遍 + Nginx 托管与 history 回退实测」。
3. **P0 第 2 项仍未完成**：把云库新口令线下发给两位同学（文档侧已完成，只剩人工传达）。
4. ~~**让 8080 上的四个修复生效**~~ ✅ **2026-09-24 已闭环**：本机此前 8080 无监听，
   本轮已重新打包并起新实例（开发口径），四个修复实测生效
   （文档里「跑的是队友旧实例」指的是**队友那台机器**，不是本机）。
5. **P2 第 21 项（D7 文档同步，已全部 ✅）→ 第 22/23 项（论文五章、答辩材料）** ——
   论文是接下来最大的一块。
6. ~~**P1 第 24、25、27 项**（C3 走查产出的 3 个 mock 缺陷）~~ ✅ **2026-09-24 已全部销账**，
   **第 26 项（给 `verify:mock` 补断言）一并完成**（21 → 24 项，含反向验证）。
   三处改动都在 mock 数据侧，已浏览器端到端验收（7 项全过）。
   证据见 `docs/前端进展与待办.md` 第二节第 14 条；验收脚本 `.tmp-shots/accept-mock-fixes.cjs`（已 gitignore）。
7. ~~⚠️ **先把演示数据灌回来**（第三轮清空的直接后果，**空库上不要截图、不要演示**）~~
   ✅ **2026-09-25 已完成**（不再是「下次开工的第一件事」）：按 `sql/README.md` 的整库重建路径跑完
   `02` → `03` → `04` → `05` → `06` → `07` → `10` → `11` → `12`，`07` 末尾自检全部通过、
   `09` 复跑违规合计 0；实测 **1500 / 386 / 10719 / 19319.3**（叠加 `12` 后 1503 / 389），
   后端已重新打包并重启（pid 32936）。**库里现在就是全量演示数据，可以直接截图 / 演示。**
   （清库前的保险备份仍在 `.tmp-verify/db-backup-20260925-pre-reseed/`，未入库。）

> **D 组交付物进度**：D1 ER 图 ✅、D6 演示数据 ✅、D2 测试用例表 ✅（本轮）、
> D3 测试报告 ✅（本轮）、D7 文档同步 ✅；**只剩 D4 截图与 D5 部署收尾**，
> 以及论文五章与答辩材料。第九阶段（系统测试与数据完善）已基本走完，
> 第十阶段（部署 / 论文 / 答辩）是接下来真正的大头。

---

## P0 —— 今天就做（都很小，但卡着别人）

1. [x] **把这批文档提交并推送**（含本文件）—— 现在 A8/A9/D1/D6 的销账与 `07` 实跑记录**只存在于本地**，
   队友和答辩老师都看不到。文件：`README.md`、`docs/待办清单.md`、`docs/后端进展与待办.md`、
   `docs/前端进展与待办.md`、`sql/README.md`、`docs/下一步待办.md`。
   ✅ 2026-09-24 已销账：已随提交 `00b6d28` 提交并推送，`main` 与 `origin/main` 一致。
2. [ ] **通知另外两位同学更新各自的 `application-local.yml`** —— 云库口令 2026-09-23 已轮换，
   他们那份还是旧口令，**不更新就连不上库，而且会误以为是代码问题**。
   顺手把「新口令从哪拿」写进文档：现在四处都只写「需更新」，没写渠道（`README.md:46`）。
   ✅ 文档侧已完成 —— 「新口令从哪拿」已写进 `README.md` 的「新同学四步上手」一节；
   剩下的只是把口令线下发给两位同学，**本项仍未完成**。
3. [x] **补一份「新同学四步上手」**（clone → 跑 `sql/01`~`07` → 复制 `application-local.yml.example` 填口令 → 起前后端）。
   改动前全仓搜 `git clone` 零命中，步骤散在 `README.md` 的「快速开始」两节里。
   顺带补上 clone URL，以及「直推 main 还是走 PR」（改动前「开发规范」只写了分支前缀）。
   ✅ 2026-09-24 已销账：落在 `README.md` 的「新同学四步上手」一节（含 clone URL 与分支策略）。
4. [x] **删掉 `.gitignore:37` 那行坏规则**：`Userslsy04AppDataLocalTemp*/`（反斜杠被吃掉，只能匹配这个字面量，实际失效）。
   ✅ 2026-09-24 已销账：坏规则已从 `.gitignore` 删除，仓库内 0 个文件匹配它。

---
## P1 —— 本周（正确性与安全；答辩很可能被问到）

### 正确性

5. [x] **B20-1 `signed_count` 口径二选一**（历史遗留，曾是唯一还没收敛的功能性分歧）
   - 运行期实现（`SignupServiceImpl.java:130` / `:205` / `:249`）= **未取消未驳回的报名**（PENDING+APPROVED），审核通过**不加**
   - 演示脚本（`sql/04_demo_data.sql:283`、`sql/07_demo_scale.sql:725`）= `status IN ('APPROVED','COMPLETED')`
   - **改实现**必须同步改 `sql/07_demo_scale.sql:862` 的自检断言（它断言 `signed_count <= max_count`），否则整批回滚；
     **改脚本**则要重跑 `07`。另外 `sql/02_schema.sql:321` 的列注释写反了（写成「审核通过/取消时维护」）。
   ✅ 2026-09-24 已销账（**走的是「改脚本对齐实现」这一支，实现侧一行未改**）：
   口径定为**「报名即占名额」** = 未取消未驳回的报名都计入（`status IN ('PENDING','APPROVED','COMPLETED')`），
   驳回与取消各释放一个，审核通过**不再加**。改了这几处脚本：`sql/04_demo_data.sql:281` 起（第八节回填）
   改成该口径；`sql/02_schema.sql:321` 的列注释改正；`sql/07_demo_scale.sql` 第十一节回填
   （`11.4` 活动已报名人数回填）改成该口径，并把**新活动的名额下限由 40 抬到 46**
   （`40 + ((n.seq * 7) % 36)` → `46 + ((n.seq * 7) % 30)`，即 46..75），另新增 `11.3` 幂等兜底修正
   `UPDATE volunteer_activity SET max_count = 46 WHERE deleted = 0 AND max_count > 0 AND max_count < 46;`。
   抬到 46 的理由：报名数上限 45（`LIMIT 16 + ((a.id * 7919) % 30)`），新口径下「已发布活动」的占位比例
   = 60% `APPROVED` + 35% `PENDING` = 95%，45 × 0.95 = 42.75 > 40 会顶破名额下限、
   使 `sql/07` 末尾自检第 4 项（`signed_count > max_count`）失败并整批回滚；
   **名额 ≥ 46 > 45 是硬保证**，与状态分布无关。`sql/07` 自检第 4 项的口径说明已同步
   （断言本身不变，仍是 `signed_count <= max_count`）；**不需要改实现** ——
   上面「改实现必须同步改 `:862` 断言」那一支的注意事项保留，本次未走这条支路。
   脚本改动**已于 2026-09-24 在云库实跑生效**：`sql/07_demo_scale.sql` 在事务内执行、末尾自检全部通过；
   实测「已报名 > 名额」的活动数 **0**、全库名额范围 **46..75**、与明细按新口径不一致的活动数 **0**
   （按旧口径不一致的有 **36** 场）；报名 10,719 条中占名额 9,201 条，其中 7 场已报名数 > 40（最大 44）。
6. [x] 【新】**新增/修改用户会把明文口令写进操作日志**：`UserController.java:61`、`:76` 的 `@OperationLog`
   没设 `params = false`（登录/注册都设了），而 `UserSaveDTO` 带 `password`，切面原样序列化进
   `operation_log.params`。改法：这两处加 `params = false`，或切面加字段级脱敏。
   ✅ 2026-09-24 已销账：两处已加 `params = false`（现 `UserController.java:66`、`:81`），并在类注释写明原因；
   全模块自查确认再无其他会序列化口令的 `@OperationLog`（带 `password` 的 DTO 只有 3 个，均已关采集）。
7. [x] **A6 拍板（时间类型）**：现状统一 `TIMESTAMP` + `LocalDateTime`，**自洽**；改 `timestamptz` 是
   16 张表 + 全部实体 + 前端渲染 + 演示数据重跑的大改。结论：**不改**（单一部署、无跨时区需求）。
   ✅ 2026-09-24 已销账：结论落在 `README.md` 的「下一步待办」第 3 条、`sql/README.md` 第五节第 6 项、
   `docs/后端进展与待办.md` 第三节决策表与第四节 P0 表第 6 行、`docs/待办清单.md` 的 A 组；A 组已无剩余待定。

### 安全（一次批量做，半天）—— ✅ 2026-09-24 第 8~11 项全部完成

8. [x] 【新】**零登录防护**：无验证码、无失败计数、无锁定（全仓零命中），叠加明文口令与 30 天会话
   （`application.yml:95` 的 `timeout: 2592000`）。至少加「失败计数 + 短时锁定」。
   ✅ 2026-09-24 已销账：新增 `vcp-system/.../service/impl/LoginAttemptGuard.java`（内存 `ConcurrentHashMap`，
   **连续 5 次失败锁 15 分钟**、登录成功清零、15 分钟无新失败则计数作废）。三个刻意取舍写进了类注释：
   ① 单实例内存实现、重启即清空；② 不落表列也不上 Redis（加表列要动 `sql/02` 与 ER 图，工程未引 Redis）；
   ③ **对不存在的用户名同样计数** —— 否则「第 5 次会不会被锁」就成了账号是否存在的探针。
   锁定判定排在**查库与口令校验之前**（否则锁定期间正确口令能登录，且两条路径耗时不同会变成旁路信息）。
   新错误码 `ACCOUNT_LOCKED(20004)`，文案带剩余分钟数。**实测**（云库真后端）：第 5 次错口令返回
   `20004 账号已被锁定，请 15 分钟后再试`、锁定期内用正确口令仍返回 20004、不存在账号 `probe_nobody_xyz`
   第 5 次同样返回 20004（与真实账号表现一致）、admin/student 不受影响照常登录。
   **未做**：验证码（本项目无图片验证码基础设施，锁定已覆盖脚本化爆破）。
9. [x] **B15 密码加密**：`AuthServiceImpl.java:96` 明文比对、`:158` 注册写入、`UserServiceImpl.java:136` 新增用户写入；
   全仓无 BCrypt / `PasswordEncoder` 依赖。**要和第 10 项一起做**，否则加密之后没人能改密码。
   ✅ 2026-09-24 已销账：`vcp-framework/pom.xml` 加 `spring-security-crypto`（版本由 Boot BOM 的
   `spring-security.version` 管，BOM 里无需再加一条），新增 `vcp-framework/.../util/PasswordUtils.java`
   （BCrypt 强度 10，`encode` / `matches` / `isEncoded` / `checkPolicy`，策略 6–32 位）。
   口令的读写**全工程只经这一个类**：登录校验、注册写入、管理员新增用户、本人改密、管理员重置五处。
   `matches` 对「库里还是明文」的行一律返回 false（刻意不做明文兜底，留兜底等于加密白做）。
   种子数据同步换成密文（`sql/03` 三个账号、`sql/04` 的组织管理员与学生、`sql/07` 的批量学生），
   并新增 **`sql/08_password_bcrypt.sql`**（老库增量刷密文，可重复执行，末尾自检残留数）。
   **实测**：云库实跑 `08` → 1509 个账号 100% 为 60 位 `$2a$10$` 密文、明文残留 0；
   后端用明文 `123456` 登录成功（证明比对走的是 BCrypt 而非字符串相等）。
10. [x] 【新】**没有「修改密码」能力**：全仓 `changePassword` 零命中，但 `UserManageView.vue:328` 的提示写着
   「首次登录后请提醒用户修改」——自相矛盾。建议与 B15 合并：加密 + 本人改密 + 管理员重置。
   ✅ 2026-09-24 已销账：**本人改密** `PUT /api/v1/auth/password`（body `{oldPassword,newPassword}`，
   校验旧口令，改完**踢掉该账号其它会话、保留当前会话**，免得用户以为改密失败）；
   **管理员重置** `PUT /api/v1/system/users/{id}/password`（body `{password}`，留空重置为默认 `123456`，
   重置后 `StpUtil.logout(id)` 踢掉该账号**全部**会话 —— 重置的典型场景就是口令外泄）。
   新错误码 `OLD_PASSWORD_ERROR(20005)`；两处 `@OperationLog` 均 `params = false`。
   前端新增共享组件 `src/components/biz/ChangePasswordDialog.vue`，入口三处：学生「个人中心 → 账号安全」、
   组织端/学校端控制台侧栏（这两个角色没有个人中心页，这是他们唯一的自助改密入口）、
   用户管理页操作列的「重置密码」。
   **实测**（云库真后端，12 项断言全过）：改密成功→当前 token 仍可用；原口令错→20005；新旧相同→10001；
   新口令过短→10001；旧口令登录→20001、新口令登录→0；双 token 场景下改密后 A 死 B 活；
   管理员重置后该账号全部 token 失效、默认口令可登录；学生越权重置他人→20003。
   另核对 `operation_log`：「修改密码」「重置密码」两类记录的 `params` 均为空，口令未落日志。
11. [x] 【新】`UserManageView.vue:330` 初始口令输入框是 `type="text"`，**明文回显**在屏幕上 → 改成 `password`。
   ✅ 2026-09-24 已销账：改为 `type="password"` + `autocomplete="new-password"`，hint 文案同步为
   「留空则使用默认密码 123456，请提醒用户首次登录后自行修改」；新增的「重置密码」弹窗同样用 `type="password"`。
   前端 `npm run build` 通过（oxlint 无新增告警；`eslint` 报的 2 处在本文件之外，是既有问题）。
12. [x] 【新】**本机构建的 jar 里含 `application-local.yml`**（已实测：`BOOT-INF/classes/application-local.yml`，196 字节）。
   `vcp-boot/pom.xml` 没有 `<resources><excludes>`。**两条都做**：pom 加排除 + **别把本机构建的 jar 发给别人**。
   ✅ 2026-09-24 已销账：`vcp-boot/pom.xml` 已按父 pom 的两段式结构补 `<resources>` 并精确排除 `application-local.yml`
   （模板 `application-local.yml.example` 仍随第二段进 jar）；构建后已核对 jar 内不再有该文件。**对外分发 jar 的注意事项仍然成立**。
13. [x] 【新】上线口径的三处收紧（代码注释里都自标了「上线前必须收紧」）：
   - CORS 对全网放开且允许携带凭证：`WebMvcConfig.java:47`、`:50`
   - 接口文档无环境区分放行：`SaTokenConfig.java:43`、`:45` + `application.yml:109` 的 `knife4j.enable: true`
   - `application.yml:115` 的 `com.vcp: debug` 会把 SQL 全量打日志（生产同样生效）
   ✅ 2026-09-24 已销账：三处改为由 profile 控制，新增 `vcp-boot/src/main/resources/application-prod.yml`；
   部署时 `--spring.profiles.active=prod` 即收紧（CORS 白名单、`knife4j.enable=false` 并连带不放行文档路径、SQL 日志回 `info`）。
   **不激活 prod 时行为与改动前逐项一致**（默认值仍为 `*` / `enable: true` / `debug`）。

### 口径与文档小修（30 分钟级，但论文引用前必须改）—— ✅ 2026-09-24 三项已全部销账

14. [x] 过期注释与口径：`sql/02_schema.sql:139`（「档位阈值尚未确定」其实已拍板）、`:471`（标签规则同上）、
   `sql/README.md:181`（7 项待定里第 1、2 项代码里已落地，可销账）、`CLAUDE.md:246` 与
   `docs/后端进展与待办.md:75`（仍写「7 项待定」）、`docs/公益等级与标签规则方案.md:97`（逐标签人数「待核实」，
   跑一条 `GROUP BY tags` 就能填）。
   ✅ 2026-09-24 已销账，逐条落地位置：
   - `sql/02_schema.sql:139`、`:469`、`:471` 的「尚未确定 / 见待定项」过时注释已改为已拍板口径
     （等级 6 档阈值、标签 8 类、`total_duration` 以 `student_info` 为准）。
   - `sql/README.md` 第五节已由「7 项待定」修正为「原 9 项，已全部拍板，剩余 0 项待定」
     （恢复了被删掉的原第 1、2 项，逐条标 ✅ 与 A 编号）；`:46` 的「6 项自检」已统一为「6 项 / 9 条断言」。
   - `CLAUDE.md:269` 已改为「待拍板事项（原 9 项已全部拍板，留档备查）」。
   - `docs/后端进展与待办.md` 已修四处自相矛盾（P0 标题、`仍未了结` 条数、第二节编号 5 空缺说明、
     P0「2 项 / 7 项」口径说明）。
   - `docs/公益等级与标签规则方案.md` 已补「逐标签人数」（2026-09-24 实测：热心志愿者 1471 / 长期坚持型 1380 /
     社区服务型 392 / 环保行动型 286 / 大型活动型 231 / 校园服务型 222 / 文化传播型 200 / 助老服务型 165，
     8 类齐全；4 个画像无标签属正常），并区分了 2026-09-22 与 2026-09-24 两期实测。
15. [x] **`docs/ER图.md` 落后于脚本**：`sql/07_demo_scale.sql:69` 加了 `activity_category.remark`，ER 图里没有该列，
   文末还写着「执行过 02 + 05 + 06 之后的状态」。
   ✅ 2026-09-24 已销账：`docs/ER图.md` 已补 `activity_category.remark`（标注「07 补列」），
   文首依据与文末「执行过 02 + 05 + 06」的口径已改为含 `07`（老库含 `08`）。
16. [x] `docs/前端进展与待办.md:228` 写「约 10 页」，实际列了 11 个页面名。
    ✅ 2026-09-24 已销账：`docs/前端进展与待办.md:228` 已写「约 11 页」，与所列 11 个页面名一致；
    同文件 `:32` 与 `docs/待办清单.md:508` 的「约 10 页」本次一并同步为 11。

### 前端走查（C3）产出 —— 2026-09-24 新增（第 24~27 项已全部销账）

> **编号从 24 起、不接在 16 后面**，是为了不动 17~23 的既有编号 ——
> 其他文档与提交信息里有「见第 15 项」「P2 第 18 项」这类交叉引用，重编号会让它们全部失准。
>
> 来源：2026-09-24 的 C3 逐页走查（28 页 × 1440/620 两视口 = 56 张截图，脚本在
> `volunteer-cert-portrait-web/.tmp-shots/shoot.cjs`，已被 gitignore）。
> **客观信号全清**：0 控制台报错、0 未捕获异常、0 横向溢出（含 620px）、0 零尺寸 canvas、
> 正文无 `undefined`/`NaN`；5 个图表页的 canvas 数与 `<InkChart>` 标签数一一对上。
> 下面 3 条是**只有肉眼/实测才能发现**的问题 —— 静态检查看不出来、真跑也不报错。

24. [x] 【新】**个人中心手机号预填脱敏值 → 用户不改动就存不了**（mock 侧，中）
    - **实测复现**：`/student/profile` 手机号框预填 `138****3300`，直接点「保存修改」→
      报「请填写 11 位手机号」，**保存失败**；必须手动清空该框才能存成功。
    - 成因：`src/mock/data/dataset.js` 的 `users[].phone` 存的就是脱敏串，
      `ProfileView.vue:19` 把它预填进表单，而 `:31` 的校验是 `/^1\d{10}$/` —— 必然拒绝。
    - **真后端不受影响**：`sql/03_init_data.sql` 存的是完整号码（`13800000003`），
      `SessionVO.phone` 注释写明「取自 `sys_user.phone`，供个人资料页回显」、不做脱敏。
      但 mock 正是「纯前端独立演示」配置（答辩用），点到个人中心就会撞上。
    - ✅ **2026-09-24 已销账**：mock 的 `students[].phone` / `orgList[].phone` / 三个种子账号的
      `phone` 一律改存完整 11 位号码；`UserManageView` 的 placeholder 由 `如 138****3300`
      改成 `如 13800000003`。**验收通过**：预填 `13833000000`，**不改任何字段**点「保存修改」
      → 出现「保存成功」（浏览器实测，脚本 `.tmp-shots/accept-mock-fixes.cjs`）。

25. [x] 【新】**签到管理「实得时长」与签到签退时间不自洽**（mock 侧，中）
    - **实测复现**：屏幕上签到 `2025-03-22 08:45` → 签退 `12:05`（差 3 小时 20 分），
      同一行「实得时长」写 **4 小时**。
    - 成因：`src/mock/data/dataset.js:913` 写死
      `hours: status === 'SIGNED_OUT' ? activities[0].hours : 0` —— 取的是活动声明时长，
      与钟点无关。
    - **后端口径是 `(签退 − 签到) / 3600`**（`AttendancePolicy.resolveHours`，保留 2 位小数、
      封顶活动预计时长的 1.5 倍）；按后端算该行应是 **3.33**。
    - 影响：这是**系统核心主题**上的数字矛盾，答辩现场一减就看出来。
    - ✅ **2026-09-24 已销账**（走的是推荐的「卡死时长」这一支）：签退时刻由
      「签到时刻 + 活动时长」推出，分钟取同一个错位值（`08:55~08:59` → `12:55~12:59`），
      恒等于 4.00，**聚合数字一个未动**，21 项既有断言不受影响。
      **验收通过**：签到管理页首屏 10 行「签退 − 签到」全部等于「实得时长」。
      ⚠️ 验收脚本自己先踩了一个坑：页面把时间显示到**分钟**、不显示秒，
      第一版脚本的正则要求秒，把 10/10 行误判成不自洽 —— 见 `docs/前端进展与待办.md` 第二节第 14 条。

26. [x] 【新】**`verify:mock` 缺「行内字段一致性」类断言**（工程，中）
    - 现状 21 项断言覆盖的是**聚合之间**（学院合计 = 累计时长）与**枚举覆盖**（四种签到状态
      都出现），但**不覆盖同一行内字段之间的一致性** —— 所以第 25 项那种「同一行里时长与
      时间矛盾」一直没被拦住。
    - ✅ **2026-09-24 已销账**：补到 **24 项**，正是上面列的三条
      （① `SIGNED_OUT` 的 `hours` 与签到签退时间差一致；② `phone` 不得是脱敏串；
      ③ `categories[].remark` 非空）。
      **并做了反向验证**：三条断言原样跑在改动前的 `dataset.js`（`git show HEAD:...` 导出）上
      **全部失败**，分别命中 40 / 77 / 6 处 —— 证明断言有效，不是写得过松。
      这一步是必要的：断言若在旧数据上也通过，等于没写。

27. [x] 【新】**mock 的分类 `remark` 全为空串**（mock 侧，低）
    - `src/mock/data/dataset.js:163` 写死 `remark: ''` → 分类管理页「说明」列 6 行全是 `—`。
    - 真库 6 条都有值（B20-2 补列后 `sql/07` 回填过，实测 `/api/v1/categories` 6 条都带 `remark`）。
    - ✅ **2026-09-24 已销账**：6 个分类的说明**逐字取自** `sql/07_demo_scale.sql` 第一节的回填，
      mock 与真后端下看到的是同一套文案。**验收通过**：分类管理页 6 行「说明」列都有内容。

28. [x] **D4 截图的入库决策**（交付物，**需拍板**）—— ✅ **2026-09-25 已拍板并执行完毕**
    - **决策**：入库目录 **`docs/screenshots/{student,org,admin}/`**（按角色分目录，与三角色架构对齐）；
      **入 22 个关键页**（学生 8 / 组织 6 / 管理 8）+ **6 张 620 视口响应式证据**，共 **28 张**；
      文件名沿用走查产物原名 `<页面>-<路由参数>-<视口>.png`，便于反查 `walk-walk-real-*.json`。
    - **选页口径**：覆盖「首页/看板 + 列表 + 详情 + 表单 + 图表 + 学院管理」六类，优先体现闭环。
      组织端 6 个页面**全入**；学生端舍「通知列表」（信息量低于已入的通知详情）；
      管理端舍「角色管理」（只读权限清单）与「通知公告管理」（与组织端重复）。
    - **体积**：入库 28 张共 **2,859,003 B = 2.73 MB**（原始 5.89 MB，压掉 53.7%），
      **5 MB 上限 PASS、余量 2.27 MB**。索引见 `docs/screenshots/README.md`。
    - **口径**：全部是**真后端**那套（389 活动 / 10,719 报名 / 19,319.3 小时 / 签到率 90.1%），
      **不是** mock 那套（12,480 / 86,420）—— 答辩材料引用时必须标明。
    - 复现：`<python> .tmp-verify/ingest-screenshots.py`；重拍见 `docs/screenshots/README.md` 第四节。
    - 拍板依据（实测）：缩放用 **BOX（区域平均）** 而非 LANCZOS —— 后者振铃在文字边缘造中间色，
      体积反而 +87.7%；且对宽 ≤ 1280 的图**不放大**（620 图上采样会虚增体积）。
      受控实测 28 张：原图 5.89 MB → 仅 optimize 5.77（−2.0%）→ 不缩放+量化 2.26（−61.7%）
      → **BOX→1280+量化 2.09（−64.6%，最优）** → LANCZOS→1280+量化 3.10（−47.5%）。
      量化后同尺寸 PSNR 46.5~51.0 dB；仅含实拍照片的 `student-activities-389-1440.png`（39.59 dB）
      触发兜底**保留 RGB**，多占 0.64 MB（质量取舍，5 MB 预算有富余）。

### 后端摸底（2026-09-24 下半场只读审计）产出 —— 2026-09-24 新增

> **编号从 29 起**，理由同上一节（不动既有编号，免得文档里的交叉引用失准）。
> 来源：2026-09-24 下半场的**只读摸底**（后端代码 / 数据库 / 前端契约三路交叉，逐条带 `文件:行` 证据）。
> 其中两条**已在当场修掉并实测** —— **P0 时长可开给「未通过审核」的报名**、
> **P1 学生注册不建档案**（销账记录见 `docs/待办清单.md` 的 B22 / B23、
> 过程与证据见 `docs/后端进展与待办.md` 第二节第 11 条）；下面 6 条是**尚未做**的。

29. [ ] 【新】**学生自助签到闭环缺两环**（后端 + 前端，中）
    - 后端 `POST /api/v1/attendance/sign-in` / `sign-out` 已实现（只挂 `@SaCheckLogin`，
      靠 `requireOwnRecord` 限本人），签到记录也在报名审核通过时**自动生成**
      （`SignupServiceImpl.java:203` 调 `ensureAttendanceRecord`）—— 链路两头都是通的。
    - **断点①（后端）**：没有「查我自己的签到记录」接口。`GET /api/v1/attendance` 挂的是
      `volunteer:attendance:manage`（组织端 / 管理端权限），**学生拿不到 `attendanceId`**，
      而签到 / 签退都要传它。
    - **断点②（前端）**：`volunteer-cert-portrait-web/src/api/volunteer.js:28`~`:29` 有
      `signIn` / `signOut` 两个函数，**全仓无任何调用**，学生端没有签到页面。
    - 修法（建议）：后端加 `GET /api/v1/attendance/mine`（按登录态过滤），前端在「我的报名」
      行上挂签到 / 签退按钮（mock 侧 handler 本来就齐，只差入口）。
    - **验收**：学生登录 → 在签到窗口内点签到 → 状态 `SIGNED_IN` → 签退 → `SIGNED_OUT`，
      且该记录能开出时长。
    - 注：签到窗口（活动前 30 分钟 ~ 活动后 30 分钟）与「组织管理员调签到接口返回 10003」
      都是**设计内**行为，不是缺陷。

30. [ ] 【新】**取消活动不处理报名**（后端，小-中，**需拍板**）
    - `ActivityServiceImpl.updateStatus`（`:236`）只在流转到 `CLOSED` 时结算报名
      （`completeAttendedSignups`）；流转到 `CANCELED` 时**只改活动状态**。
    - 后果：该活动下 `PENDING` / `APPROVED` 的报名会一直留着 —— 学生端「我的报名」仍显示
      已报名，且按 B20-1 的口径（报名即占名额）它们仍计入 `signed_count`。
    - **需拍板**：取消活动时是否把未完成的报名批量置 `CANCELED`（并同步 `signed_count`）。
      若不改，应在文档里写明「活动取消后报名记录保持不变」的口径。

31. [ ] 【新】**`PENDING_SUBMIT` 没有生产者**（后端 + 文档，小，**需拍板**）
    - `service_duration.status` 的 DB 默认值是 `PENDING_SUBMIT`（`sql/02_schema.sql:398`），
      枚举与多处注释都按「组织还没提交」解释它，**但全仓没有任何代码把它写进去** ——
      开时长时直接落 `PENDING_AUDIT`（`DurationServiceImpl.java:184`）。
    - 后果：该状态恒为空，前端 `duration_status` 字典里的「待提交」是死值；
      `DurationAuditSummaryVO` / `AuditVO` 里「不含 PENDING_SUBMIT」的说明也成了空口径。
    - **两条路（二选一）**：① 补「组织提交」步骤（时长先落 `PENDING_SUBMIT`，组织端提交后进审核）；
      ② 把该状态从枚举、字典与文档里摘掉。当前实现等价于 ②，但文档与字典还写着 ①。

32. [ ] 【新】**默认口令 `123456` 硬编码在两处**（后端 + 前端，10 分钟级，低）
    - 后端 `UserServiceImpl.java:59` 的 `DEFAULT_PASSWORD`；前端 `UserManageView.vue` 的提示文案
      与「重置密码」弹窗也写死同一串。改默认口令要同时改两处（且已入库账号不会变）。
    - 演示项目可接受；正式部署建议改为**首次登录强制改密**。

33. [ ] 【新】**组织列表 N+1**（后端，小，低）
    - `OrgServiceImpl.toVO`（`:181`）对每一行都调 `metricsOf` → `metricsPort.getMetrics`
      （`:196`、`:203`），列表页因此是 **1 + N 次查询**（每行一次聚合）。
    - 组织目前只有 8 条，无感；组织数上百时再改成批量查询。

34. [ ] 【新】**后端零自动化测试**（工程，中）
    - 9 个模块的 `src/test` **全是空目录**，`vcp-boot` 之外也没有测试依赖 ——
      后端目前**没有一行可跑的自动化测试**（前端侧的同源条目是 C7）。
    - 答辩口径（如实说明）：**人工用例表（D2 / D3）+ 一致性自检 SQL（`sql/09`）+
      端到端脚本（`.tmp-verify/verify.ps1`）**，未做单元测试。
    - ✅ **2026-09-25 补**：`verify.ps1` 本轮**被修好并实跑通过** ——
      **PASS 30 / SKIP 2 / FAIL 0**（新增 `Skip()`，签到 / 签退两步因「演示数据里没有进行中的活动」
      按窗口规则记 SKIP 并原样打印后端文案，不再伪装 PASS）。修的三处见第 41 项所在小节的说明与
      `docs/会话记录.md` 会话 11。⚠️ 该脚本**未入库**（`.tmp-verify/` 被 gitignore），
      且**跑完会留下测试残留**（见第 41 项），所以「脚本可复现」这句话要连环境口径一起说。
    - 附录 A 里那条「`src/test` 都是空目录」即本项，不再单列。

### 云库重置与学院链路（2026-09-24 下半场（第三轮））产出 —— 2026-09-24 新增

> **编号从 35 起**，理由同上两节（不动既有编号，免得文档里的交叉引用失准）。
> 来源：2026-09-24 下半场（第三轮）的**云库重置**（全量备份 → 16 张表 `TRUNCATE` → 重灌「基础数据 + 2 个测试管理员」）
> 与随之而来的 **B30 注册链路修复**（学院字典 + 免登录学院接口）。
> ⚠️ 这一轮**把演示数据清掉了**：第 35 项是「下次开工必须先做」的第一件事，别跳过。

35. [x] ⚠️【新】**演示数据已清空，答辩 / 截图前必须重灌**（交付物，**本轮清空的直接后果**）
    - ~~现状：16 张表全部 `TRUNCATE`，库里只剩「基础数据 + 2 个测试管理员」——
      **学生 / 活动 / 报名 / 时长都是 0 行**，看板、档案列表、画像、签到这些页面全是空态。~~
    - ~~两条路：① 按 `sql/README.md` 的顺序跑 `02` → `03` → `04` → `05` → `06` → `07`；
      ② 从 `.tmp-verify/db-backup-20260924-150914/` 用 `.tmp-verify/Restore.java` 还原。~~
      → 本轮走的是**①**（整库重建路径，比只跑 `02`~`07` 多了 `10` / `11` / `12`）。
    - ~~影响面：**D4 截图**（空库截出来全是空态）、**D6 演示数据**（口径没变，但库里现在没有数据）、
      D5 干净机器实测（照 `sql/README.md` 走一遍正好一起验）。~~
    - ~~验收：学生 1500 / 活动 386 / 报名 10,719 / 累计 19,319.3 小时、六档等级齐全
      （`.tmp-verify/row_counts.sql` 可复用来核对行数）。~~ ✅ 2026-09-25 已达成
      （六档等级齐全由 `07` 自检 + `/portraits/me` 四星志愿者 + `/analytics/dashboard` 8 类标签佐证）。
    - ✅ **2026-09-25 已重灌并销账**。重建序列（`sql/README.md` 的整库重建路径，本轮实测顺序）：
      `02_schema.sql` → `03_init_data.sql` → `04_demo_data.sql` → `05_backend_gap_fix.sql`
      → `06_backend_gap_fix2.sql` → `07_demo_scale.sql` → `10_base_and_test_accounts.sql`
      → `11_activity_images.sql` → `12_activity_images_demo.sql`，最后只读复验 `09_consistency_check.sql`。
    - **`07` 末尾自检全部通过**（原样输出）：`[07 自检] 全部通过：学生 1500 人、活动 386 场、
      报名 10719 条、累计时长 19319.3 小时（演示账号 22.6 小时 / 标签 热心志愿者,长期坚持型,大型活动型）`；
      **`09` 实跑：检查项总数 20、违规合计 0**（20 行明细全 0）。
    - **最终行数（实查）**：`sys_user` 1514、`student_info` **1503**、`student_profile` 1503、
      `volunteer_activity` **389**、`activity_signup` **10719**、`attendance_record` 8235、
      `service_duration` 7423、`duration_audit` 13731、`notification` 15034、`org_info` 9、
      `attachment` 3、`sys_dict` 38、`activity_category` 6、`sys_role` 3、`operation_log` 1。
    - ⚠️ **口径必须写清楚（两组别混用）**：`07` 口径 = **1500 学生 / 386 活动 / 10719 报名 /
      19319.3 小时**；再跑 `12` 后 = **1503 学生 / 389 活动**（报名与时长**不变** —— `12` 不造报名）。
      行数比 `07` 提交那一刻各多 3，就是因为 `12` 的图文演示叠加在 `07` 之后。
    - **顺序理由**（都记进 `sql/README.md` 与根 `README.md`）：`04` 是 `07` 的硬前置
      （`07` 的新活动 `org_id = 1 + (g % 6)` 需要 `org_info` 有 id 1~6，而 id 2~8 全由 `04` 创建）；
      `10` 补学院字典（缺了注册必被拒）；**`12` 放最后**，避免 `07` 把它给这三场活动定的名额
      从 20/35/18 抬到 46。
    - **后端**：`mvn -B package -DskipTests` 11 个模块 BUILD SUCCESS → `vcp-boot-1.0.0.jar` 重启
      （**pid 32936**，8080 监听，开发口径即未带 prod profile，5.0 秒启动）；三角色冒烟实测全过
      （`student` / `org_admin` / `admin` 登录 `code=0`；`/auth/me` studentId=1、college=计算机学院；
      `/portraits/me` 四星志愿者、`/durations` total=8；`/activities` total=389；
      `/analytics/dashboard`：活动 389 / 报名 10719 / 时长 19319.3 / 签到率 90.1% / 审核通过率 75.6% /
      5 个学院合计 19319.3 / 8 类标签齐全；`/system/users` total=1514；
      `/portraits/distribution` 返回 8 条**数组**）。活动图片
      `/uploads/demo/activities/{children-reading,community-elder-care,river-cleanup}.png` 均 HTTP 200。
    - **保险备份**：清库前 `.tmp-verify/db-backup-20260925-pre-reseed/`（16 张表 CSV、196 行、28.5 KB，
      被 `.gitignore` 覆盖、未入库）。旧备份 `.tmp-verify/db-backup-20260924-150914/` 仍在。
    - **又重跑了一遍整链（收工前）**：`verify.ps1` 跑完后留下测试残留（见第 41 项），
      于是把 `02 → 03 → 04 → 05 → 06 → 07 → 10 → 11 → 12` **再跑一次**把库恢复干净 ——
      **全程仅约 16 秒**，`07` 末尾自检**再次原样通过、与第一次逐字相同**（可作「演示数据是确定性算法」的实证），
      `02` 这次只花 **208 ms**（对比首次被陈旧会话锁住的 **618 秒**）。终态：`student_info` **1503**、
      `volunteer_activity` **389**、`activity_signup` **10719**、累计 **19319.3** 小时、`attachment` 3、
      `sys_dict` 38、`org_info` 9，`09` 违规合计 0；看板 API 实测「活动 389 / 报名 10719 / 时长 19319.3 /
      签到率 90.1%」且 5 个学院合计 = 19319.3。
      ⚠️ **不要写「verify 跑完库仍是 1503 / 10719 / 19319.3」** —— 跑完是 1505 / 10721 / 19321.8，
      重跑整链之后才回到干净口径。
    - ⚠️ 顺带确认：`sql/10` 头部写明清库后要按 **`05` → `06`** 再跑一遍，
      否则字典 `tone` 退化成灰色、`activity_category.code` 为空（画像的类型标签会取不到）——
      本轮走的是**完整序列**（`05` / `06` 本就在 `10` 之前），不存在这个问题。
    - **本轮踩的坑**：`02_schema.sql` 的 `DROP TABLE` 一开始卡了 **618 秒**，被上一次会话遗留的
      两个 `idle in transaction` 连接（pid 5798 / 5801，已挂 18 小时 52 分）持有的
      `attachment_id_seq` 锁挡住；用 `pg_terminate_backend` 清掉 4 个陈旧会话
      （5792 / 5794 为 `idle in transaction (aborted)`）后 DROP 立刻完成。
      **动 DDL 前先清陈旧会话**，排查法见 `CLAUDE.md` 踩坑第 19 条。

36. [ ] 【新】**B30 残留：`major` / `class_name` 仍无写入入口**（后端 + 前端，中，**需排期**）
    - `college` 的两条写入路径**都已闭环**：注册链路（2026-09-24 第三轮修）+ 管理员新增用户
      （2026-09-24 第四轮修 —— `UserSaveDTO.college` 校验后交 `ensureArchive(userId, college)` 落库，
      前端「用户管理」的新增用户表单也有学院下拉，只在「新增 + 学生角色」时出现；`ensureArchive(Long)` 单参重载已删）。
    - 仍然缺的：`major` / `class_name`（以及同源的 `gender` / `grade`）**没有任何写入入口** ——
      注册表单不收集、管理端也不收集，恒为 null（详见 `docs/待办清单.md` 的 B30）。
    - 要闭环需要一个**「管理员补录学生档案」的页面**（或至少把这几列加进管理端表单）。
    - 口径：学院是分组键，新增入口时**同样要走字典校验**，不要存自由文本。

37. [x] 【新】**学院字典只在新脚本 `sql/10` 里，新环境跑到 `09` 拿不到**（SQL / 文档，小）
    - `sys_dict` 的 5 条学院字典**不在** `sql/03_init_data.sql` 里（那里的 8 类字典没有 `college`；
      清空前的备份 `sys_dict` 33 行也证实了这一点），它由 `sql/10_base_and_test_accounts.sql` 落库。
    - 后果：新环境按 `sql/README.md` 跑到 `09` 时，注册页下拉框是空的、注册必被拒（「请选择学院」）。
      要么把 5 条学院补进种子脚本（`03`），要么在 `sql/README.md` / 根 `README.md` 的「新同学四步上手」里
      写明「必须跑到 `10`」（四步上手现在写的是 `sql/01`~`07`）。
    - ✅ **2026-09-25 已销账（走的是第二条路：写进文档，没有动 `sql/03`）**：
      根 `README.md` 的「新同学四步上手」第 2 步与「快速开始（后端）」第 1 步都已写明
      **`10` 是学院字典的唯一来源、必执行**（缺了注册必被拒），并把它排进脚本序列。
      **剩余部分（如实记录）**：`sql/03_init_data.sql` 里**仍然没有**这 5 条学院 ——
      也就是说「学院字典的种子在哪」这件事，答案仍是「在 `10` 里」，只是文档不再漏掉它。
      （本轮实测：库里 5 条在位、`sort` 1~5，`GET /api/v1/auth/colleges` 返回 5 条。）

38. [ ] 【新】**全量备份与还原工具都在 `.tmp-*` 里，换机器就没了**（工程，小，低）
    - `.tmp-verify/db-backup-20260924-150914/`（16 个 CSV / 9.3 MB）+ `Backup.java` / `Restore.java`
      都被 `.gitignore` 的 `.tmp-*` 覆盖，**未入库**（同 `docs/待办清单.md` P3 的既有口径）。
    - 演示数据随时可用 `sql/02`~`07` 重灌，所以**不必入库**；但**答辩前建议把这份备份另存到仓库之外**，
      它是「万一灌不回来」的最后一道保险。

39. [x] 【新】**学院链路的端到端证据已补**（测试，小，2026-09-24 完成）
    - 脚本已就位：`.tmp-verify/verify-college.ps1`（免登录可访问 + 5 个学院顺序 + 缺 / 非法 `college` 都被拒
      + 合法注册后会话与 `/auth/me` 回显 `college`），落库核对用 `.tmp-verify/check_college.sql`；
      前端侧另有 `.tmp-verify/check-register-college.mjs`（mock handler）与
      `volunteer-cert-portrait-web/.tmp-verify/check-register-view.mjs`（SFC 编译 + 结构断言），
      两个脚本的输出都**没有落盘**。
    - ✅ **已补**：后端重新打包（11 模块 BUILD SUCCESS）并重启（**新 pid 82864**），`.tmp-verify/verify-college.ps1`
      **11/11 PASS**（免登录取 5 个学院且顺序一致、缺 / 非法 `college` 都被拒且文案「请选择学院」一字不差、
      注册后会话与 `/auth/me` 都回显 `college`、`studentId` 非空）；落库核对 `student_info.college = 计算机学院`、
      占位学号 `S000003`；前端 `npm run build` 通过。（这名测试学生已在第四轮收尾时清掉，
      库里 `student_info` 现为 0 行 —— 见 `docs/会话记录.md` 会话 7 的「环境」一节。）
    - 验收：重新打包重启 → 跑 `verify-college.ps1` → 结论回填 `docs/测试报告.md`（D3）。

40. [ ] 【新】**学院管理端（接口 + 页面）的收尾：页面走查**（测试 / 文档，小）
    - 已完成：`vcp-system` 的 4 个学院接口、前端「学院管理」页（第 29 页）、用户管理「新增学生」的学院下拉、
      时长审核 / 组织管理 / 学生画像三处「学院」筛选下拉；`npm run verify:mock` **28 项全过**（实跑）。
    - ✅ **接口侧已实测**（`.tmp-verify/verify-college-admin.ps1`，**PASS 41 / FAIL 0**）：列表 / 新增（含重名、
      空名、超 30 字）/ 启停（含停用后注册页下拉不含它）/ 删除（含占用校验的两条文案）/ 管理员新增学生的学院校验 /
      `student_info.college` 落库 / 脚本自清理回基线（2 个账号 / 0 份学生档案 / 5 个学院）。
      见 `docs/后端进展与待办.md` 第二节第 12 条。
    - ✅ **页面级走查已补做**（mock 构建、1440 视口）：学院管理页的增 / 删 / 启停（含被占用时删除被拦）、
      用户管理「新增学生」的学院下拉与必填校验、三处「学院」筛选下拉渲染成字典选项。
    - ✅ `npm run lint` 与 `npm run build` 本轮都重跑过、**都 exit 0**（日志留在 `.tmp-verify/lint.log`
      与 `.tmp-verify/build.log`）。
    - ~~**未做**：620 视口未随之重截、C3 那 56 张截图未整套重跑。~~
      ✅ **2026-09-25 补记：已被会话 8 的「29 页 × 2 视口、0 信号」走查覆盖** ——
      会话 8 把逐页走查从 28 页扩到**全站 29 页**（含「学院管理」），1440 与 620 两视口各一遍，
      记录 0 信号（0 控制台报错 / 0 未捕获异常 / 0 横向溢出 / 0 零尺寸 canvas），
      见 `docs/会话记录.md` 会话 8 与 `docs/前端进展与待办.md` 第二节第 16 条。
      **剩余只是截图未入库**（走查截图与报告都在 `.tmp-verify/`，被 `.gitignore` 覆盖）——
      与第 28 项「D4 入哪几页」是同一件事，不再是「未重截」。
    - ✅ **2026-09-25 补记：真后端逐页点检也已做完**（会话 8 那轮走的是 mock 构建）——
      起 dev server（`http://localhost:5173`，`VITE_USE_MOCK=false`，代理打本机 8080），
      走 **25 个角色页 × 2 视口 = 50 条记录、0 信号**，且全站 0 处出现 mock 数字
      （12,480 / 86,420 零命中），页面数字即真库数字（看板 389 / 10,719 / 19,319.3 / 签到率 90.1%，
      学生端 22.6 小时 + 四星画像，3 张活动封面正常渲染）。
      ⚠️ 工具链变了：原先依赖 codex 运行时里的 playwright，该运行时**已不存在**；
      现改为 `npm i playwright-core --prefix .tmp-verify/pw`（不下载浏览器）+ **系统 Chrome**
      （`C:\Program Files\Google\Chrome\Application\chrome.exe`），
      `walk.cjs` 已支持 `WALK_BASE` / `WALK_BROWSER` 两个环境变量（默认仍是 5199 + 系统 Chrome）。
    - 验收：浏览器里把上面几条点一遍；把接口脚本的 41 项结论回填 `docs/测试报告.md`（D3）。
    - 顺带：**D4 重截时把第 29 页带上**（第 19 项按 28 页描述的是 C3 那一批，重截要覆盖 29 页）。

### 云库重建后的新发现（2026-09-25 第八轮）产出 —— 2026-09-25 新增

> **编号从 41 起**，理由同上面几节（不动既有编号，免得文档里的交叉引用失准）。
> 来源：2026-09-25 的**云库整库重建 + 全量演示数据回灌**过程中跑端到端脚本 `verify.ps1` 时暴露 ——
> 脚本连跑两轮后看板的数字**自己涨了**，追下去才定位到这一条。**本轮只把库恢复干净，缺陷未修。**
>
> 同批还修好了 `verify.ps1` 本身（**旧脚本跑不通**，三处原因都在脚本侧、不是产品缺陷）：
> ① 注册请求体**没带 `college`**（B30 之后注册必填学院）→ 从第 3 步起级联失败
> （`10001 请选择学院`），已补 `"college":"计算机学院"`；
> ② 第 4 步选活动**没限定「本组织」**，取列表第一条 `PUBLISHED` 会拿到 `demo_org_01` 的图文演示活动，
> 后面用 `org_admin` 开时长被 `10001 活动「…」不属于本组织` 正确拒绝，已加 `$_.orgId -eq $orgId`；
> ③ 签到 / 签退受 A2 窗口约束（活动开始前 30 分钟 ~ 结束后 30 分钟），而演示数据里
> **没有任何进行中的活动**（`PUBLISHED` 的都在 2026-09-27 之后），这两步改为记 `SKIP`
> 并原样打印后端文案「签到尚未开放，活动开始前 30 分钟才可签到」，**不再伪装 PASS、也不算 FAIL**。
> 修完实跑：**PASS 30 / SKIP 2 / FAIL 0**。

41. [x] 【新】**删除用户不级联：业务记录留在库里并继续计入看板**（后端 + SQL，中-高，**2026-09-25 发现、当日已修并实测闭环**）
    - 现象（修复前）：`DELETE /api/v1/system/users/{id}` 只把 `sys_user.deleted` 置 1；
      该学生的 `student_info`（仍 `deleted = 0`）、`activity_signup`、`service_duration`
      **全部留在库里，并继续参与统计**（看板、档案列表、画像分布都会带上他）。
    - **选定的口径**：删用户时**同事务级联** —— 软删 `student_info`；
      **逻辑删除**其全部报名（`deleted = 1`，**保留原 `status`**）并按 `sql/09` 第 ⑦ 项的同一谓词
      **重算** `signed_count`；作废其签到记录；软删其 `service_duration`。
      ⚠️ **不用「置 CANCELED」**：看板 `enrolled` 的口径是
      `COUNT(*) FROM activity_signup WHERE deleted = 0`（`AnalyticsMapper.java:84`），
      `CANCELED` 行仍会被数进去 —— 实测只改状态的话看板照样 +1，验收过不了。
    - **实现**：vcp-system 声明 `StudentSignupPurgePort` / `StudentDurationPurgePort` 两个端口
      （照 `OrgLookupPort` / `OrgCollegeCountPort` 的既有套路，**必需注入**而非可缺省 ——
      缺实现时安静跳过就是静默的半个级联），分别由 vcp-volunteer / vcp-certification 实现；
      编排在 `StudentArchivePurger.purgeByUserId`，由 `UserServiceImpl.deleteUser` 在同一个
      `@Transactional` 里调用。新增 SQL 只有两条：`VolunteerActivityMapper.refreshSignedCountByStudent`、
      `AttendanceRecordMapper.invalidateByStudentId`。
    - **验收（已实跑）**：造学生 → 报名 → 开时长 → 审核通过 → 删该用户 → 看板数字**回到删除前**
      （389 / **10719** / **19319.3** / 签到 8235·7423 / 审核 7423·5614），
      库内 `student_info` / `activity_signup` / `service_duration` / `attendance_record`
      的 `deleted = 1` 行数全 0，`sql/09` **20 项违规合计 0**。
      脚本：`.tmp-verify/verify.ps1` **PASS 30 / SKIP 2 / FAIL 0**；
      构建：`mvn -B package -DskipTests` **11 个模块 BUILD SUCCESS**。
    - **顺带闭环**：`PortraitAggregateMapper.selectTagCounts` 补 `si.deleted = 0`
      （`student_profile` 无 `deleted` 列，被删账号的快照此前仍计入标签分布；同 Mapper 其它查询
      一直都是这么过滤的）。
    - **仍未做**：① `sql/09` **未加**「业务记录属于已逻辑删除的用户」这一检查项
      （检查项总数仍是 20，因该数字被文档大量引用，留给主会话统一评估）；
      ② 修复**不追溯**存量脏数据 —— 本轮是靠重跑整条重建链（实测 **9.7 秒**）恢复干净口径的。
    - ⚠️ **一处如实说明**：`/portraits/distribution` 跑完 `verify.ps1` 后仍有 6 个标签各 −1 ——
      这是**既有**的「脚本口径 vs 运行期算法」差异（脚本第 5 步会调 `POST /portraits/recompute`
      重写 4 份画像的 `tags`），**不是 B31 的漂移**（修复前那一轮同样存在这 6 个 −1，可对照）。
    - 详细证据与改动清单：`docs/_handoff/agent-2-b31.md`。

42. [ ] 【新】**`verify.ps1` 跑完会让画像标签分布漂 6 个 −1**（测试脚本 vs 运行期算法口径差，小）
    - 现象：`verify.ps1` 第 5 步调 `POST /api/v1/portraits/recompute`，运行期算法重写了 4 份画像的
      `tags`，于是 `/portraits/distribution` 有 6 个标签各 −1（社区服务型 393→392、环保行动型 287→286、
      大型活动型 232→231、校园服务型 223→222、文化传播型 201→200、助老服务型 166→165）。
    - 性质：**既有的「脚本口径 vs 运行期算法」差异**，**不是 B31**（修复前那一轮同样存在这 6 个 −1）。
      影响：跑过 e2e 的库会与 `sql/07` 生成的口径不一致 —— 答辩前若跑过 e2e，**记得重跑整链**（约 10 秒）。
    - 修法二选一：① 让 `sql/04` / `sql/07` 的标签生成与 `PortraitTagEnum` 的判定逐条对齐（根治，但要重跑演示数据）；
      ② 在 `verify.ps1` 末尾补一步重算、或在脚本头部写明该差异（低成本）。
    - 发现于 2026-09-25（代理 2 做 B31 回归对照时发现，证据见 `docs/_handoff/agent-2-b31.md` ③-3.4 末注）。

---
## P2 —— 答辩前必须产出（D 组交付物）

> 依据 `docs/高校志愿服务时长认证与公益画像数据分析系统_开发计划与分工.md`（第九、十阶段）。

17. [x] **D2 测试用例表** —— ✅ 2026-09-24 已产出 `docs/测试用例表.md`：按计划书 `:824`~`:853` 的
    功能 / 流程 / 异常三组编排，共 **73 条**用例（功能 48 + 流程 10 + 异常 15），
    预期结果里带真实错误码（取自 `ErrorCodeEnum`）。
18. [x] **D3 测试报告** —— 有个**硬缺口**：仓库里没有「20 项一致性自检」的**可执行脚本**，
   这个数字只存在于提交信息和 `docs/后端进展与待办.md:81`。论文要写它，就得先把它落成一个能跑的 SQL
   （脚本侧目前只有 `07` 的 6 项断言、前端 `verify:mock` 的 21 项）。
   ✅ 2026-09-24 已销账：硬缺口已补 —— `sql/09_consistency_check.sql` 已落地（只读、可重复执行，
   **正好 20 项检查**；输出 20 行 + 1 行汇总，汇总行 `check_no = 99`、文案形如「检查项总数 20，违规合计 N」），
   并于 2026-09-24 在云库（PostgreSQL 18.6）实跑：20 项违规数全部为 0，汇总行「检查项总数 20，违规合计 0」。
   **数字 20 现在有脚本可当场复现，论文可放心引用**。
   ✅ 2026-09-24 报告本体也已产出 `docs/测试报告.md`：覆盖 D2 的用例结论，并**如实记录
   5 项受限 / 未通过项与 4 项未执行项**；本轮 2 个缺陷（B22 / B23）的修复证据也收在报告里。
19. [x] **D4 系统截图** —— ✅ **2026-09-25 完成，仓库不再是 0 张图片**。
    - **产物**：`docs/screenshots/{student,org,admin}/` 共 **28 张**（22 个关键页 × 1440 +
      6 张 620 响应式证据），**2.73 MB**，索引 `docs/screenshots/README.md`。
    - **来源**：2026-09-25「真后端逐页点检」，25 个角色页 × 2 视口 = 50 条记录、**0 信号**
      （0 控制台报错 / 0 未捕获异常 / 0 失败请求 / 0 个 ≥400 响应 / 0 横向溢出 / 0 零尺寸 canvas），
      报告 `.tmp-verify/walk-walk-real-{student,org,admin}.json`，原始素材 50 张 / 8.93 MB（未入库）。
    - **口径**：截的是**真后端**那套（389 活动 / 10,719 报名 / 19,319.3 小时 / 签到率 90.1%），
      走查时全站 **0 处**命中 mock 数字（12,480 / 86,420）。
      **⚠️ 答辩材料里的数字必须标明是哪一套** —— mock 口径仍是 12,480 报名 / 86,420 小时。
    - 说明：本轮走的是**前端 dev server（`localhost:5173`，`.env.development` 为
      `VITE_USE_MOCK=false`）+ 真后端**这条路，**没有**动 `dist/`，
      所以「`.env.production` 仍是 `VITE_USE_MOCK=true`、现存 `dist/` 是 mock 口径」这两条
      **依然成立**，D5 构建时仍要记得改。重拍命令见 `docs/screenshots/README.md` 第四节。
    - 未入库（如实记录）：620 视口只入 6/25 张、1440 有 3 页未入（通知列表 / 角色管理 / 通知公告管理），
      理由见 `docs/screenshots/README.md` 第五节。
20. [ ] **D5 部署** —— 后端 jar + 前端 `npm run build` + Nginx 托管并反代 `/api`；部署文档也要写。
    **2026-09-25 进展**：文档与配置这一层已经收口，**本机能实测的都实测掉了**，
    正式验收（干净机器照文档走一遍）**仍未做**。
    - ✅ **前端真后端口径构建**：用 `.env.production.local` 覆盖 `VITE_USE_MOCK=false`（不动入库的
      `.env.production`）后 `npm run build`；`dist/` 里 **mock chunk 彻底消失**
      （`mock-DAfFHmNx.js` 45.7 kB 已随旧产物被覆盖；`grep -rl vcp_mock_extra_users dist/` 无输出）。
      实测体积：入口 `index-C47X2cye.js` 22.93 kB / gzip 7.87 kB、`request` 113.14 kB / gzip 42.91 kB、
      `echarts` 652.78 kB / gzip 219.43 kB（懒加载）、`index.css` 28.68 kB；**首屏 JS 合计 65.66 kB**。
      ⚠️ `dist/` 现在留的就是这一套（真后端口径），旧的 mock 产物没了。
    - ✅ **静态托管 + history 回退 + `/api` 反代 + `/uploads` alias**：本机没有 nginx，用了一个按
      `deploy/nginx.conf` 语义实现的 Node 替身实测 —— 4 条深层路由（`/student/profile`、
      `/student/activities/389`、`/admin/colleges`、`/org/activities`）全部 200 且内容 = `index.html`；
      `/api/v1/categories` 到后端返回 `{"code":20001}`；`/uploads/demo/activities/children-reading.png`
      200 + `image/png`。**替身不是 nginx**，`nginx -t` 与真机行为仍未验（见 `docs/部署文档.md` 第九节差异清单）。
    - ✅ **新增 `deploy/verify-deploy.ps1`**（入库）：照验收清单写成一键脚本，PASS/FAIL/SKIP 分明、
      有 FAIL 则退出码 1。本机实跑 **PASS 9 / FAIL 0 / SKIP 4 / INFO 1，退出码 0**；
      反向自检（对着只有后端的 8080 跑）**FAIL 4、退出码 1**，证明它不是永远绿。
    - ✅ **修了 3 个真问题**：① `deploy/vcp.service` 的 `JAVA_OPTS` 是死配置（`ExecStart` 没引用
      `$JAVA_OPTS` → 堆上限与 `-Duser.timezone` 都没生效，而本项目时间字段是 `TIMESTAMP` 无时区，
      时区错了会影响看板与签到窗口），已修并补依据；② 验收清单里「用 `http://<域名>/doc.html`
      验 prod 收口」**根本测不出来**（`/doc.html` 不经 Nginx，会被 try_files 回退成前端页面，
      实测 200 + index.html），已改为**直连后端**验；③ 文档 3.1/3.2 的库数据现状与脚本顺序过期
      （缺新环境必需的 `sql/10`）。
    - ⏭️ **剩下的（必须在真机上做）**：干净机器照文档走一遍、`nginx -t`、systemd 启停与
      `$JAVA_OPTS` 实际展开、prod profile 实跑（`/doc.html` 应 401）、从外网确认 8080 连不上。
      复跑命令见 `docs/_handoff/agent-3-d5.md` 的 3.5 节。
    - 历史（2026-09-24）：文档与配置部分先完成 —— `docs/部署文档.md` + `deploy/nginx.conf` +
      `deploy/vcp.service`，含**验收清单**与**故障排查表**（9 条现象 → 原因）；根 `README.md`
      另新增「生产部署」一节，写明**必须带 `--spring.profiles.active=prod`**（不带则文档页免登录 /
      CORS 全开 / SQL 全量打印三项收紧全部失效）。
21. [x] **D7 文档同步** —— ✅ 2026-09-24 已**全部完成**（附录 B 现逐条 ✅）。
22. [x] **论文材料：需求分析、概要设计、详细设计、系统实现、系统测试 五章** —— ✅ **2026-09-25 已产出初稿**
    位置：`docs/论文/`（`01-需求分析.md` / `02-概要设计.md` / `03-详细设计.md` /
    `04-系统实现.md` / `05-系统测试.md` / `README.md`）。
    规模：五章合计 **5.4 万汉字**（9,182 / 8,221 / 14,417 / 10,570 / 11,242，合计 53,632），
    **18 个 Mermaid 图**，每条结论都带 `文件:行` 出处。
    口径：**章节要求照《开发计划与分工》第九、十阶段**（计划书 `:812-877`、`:880-930`）。
    已合并导出 **`docs/论文/论文初稿.docx`**（164 页 / 244 KB / 167 张表，
    结构校验 **verdict = pass**），生成脚本 `docs/论文/build_docx.py` 可重跑。
    **数据口径已按三套分别标注、不混用**：`sql/07` 口径 1500 / 386 / 10719 / 19319.3；
    叠加 `sql/12` 后 1503 / 389（报名与时长不变）；前端 mock 12480 / 86420（**非真库数字**）。
    状态字一律用英文码 + `sys_dict` 翻译。
    **如实收录**：功能/流程/异常三组用例、**5 项受限 / 4 项未执行**、
    已知限制（后端零单元测试、`operation_log.target` 恒空、**B31 删除用户不级联**、
    组织资质与头像上传未接、证书生成与活动收藏未做、**6 档阈值仍硬编码在 3 处**）。
    ⬅️ **仍剩**（见 `docs/论文/README.md` 第五节）：
    ① DOCX **排版未做视觉确认** —— 捆绑 LibreOffice 预览渲染对中文右侧越界，
       已用最小复现证明是**渲染管线问题而非 DOCX**（5 种字体 × 3 种页边距结果一致；
       XML 里页宽 12240 twips = Letter、左右边距各 1587 twips 均正确），
       **建议用 Microsoft Word 打开核对排版**；
    ② 摘要 / 关键词 / 英文摘要 / 绪论（含文献综述）/ 结论与展望 / 参考文献 / 致谢 —— 均未写（素材缺口，非本批范围）；
    ③ 需本人确认的信息：学校/学院/专业/班级/学号/姓名/指导教师/完成日期、
       三人分工的真实姓名对应关系、院系论文格式规范。
    （原「系统截图（D4）」这条已由 D4 完成闭环：`docs/screenshots/` 28 张真后端截图已入库。）
23. [x] **答辩材料** —— ✅ **2026-09-25 已产出 6 份，全部落在 `docs/答辩材料/`**：
    `答辩PPT.pptx`（**18 页**，每页带讲者备注，共 1.6 万字，含 Q&A 与数字出处）、
    `演示脚本.md`（389 行，三幕 + 现场闭环 + 四套降级方案）、
    `演示视频分镜.md`（205 行，25 个镜头 / 4 分 20 秒，**视频本身未录制**）、
    `数据库说明.md`（399 行，16 表 + 23 关系 + 索引约束现状，如实写明无 CHECK/触发器/视图/乐观锁列）、
    `技术亮点.md`（552 行，12 条「问题→方案→证据」）、
    `成员分工.md`（244 行，3 人分工，**姓名占位待本人确认**）。
    校验：`check_office.py` → `verdict: pass` / `slides: 18`；LibreOffice 逐页渲染 18/18 页 0 越界、无缺字。
    **PPT 的 7 张图全部取自 `docs/screenshots/`（真后端口径），无文字占位。**
    **剩余**：① 演示视频未录制（分镜已就绪，需真人录制）；② 姓名与指导教师待本人确认后替换占位符。
    详见 `docs/_handoff/agent-5-defense.md`。

---

## P3 —— 明确不做 / 推迟（写下来，省得反复纠结）

- **git 历史里的旧口令**：`161e16c`、`0704662` 两个提交仍能翻到旧口令（值已失效）。
  清理需要 force push，会波及两位同学的本地仓库 —— **推迟到答辩后**，但要在文档里留一句。
- **C9 文件上传的剩余场景 / C11 证书生成 / C12 活动收藏**：活动图片上传已完成；
  组织资质材料与用户头像上传仍未接，证书生成、活动收藏仍为零代码。属计划书里的「可选完成」，
  **除非答辩要演示，否则不做**。
- **C7 自动化测试（vitest 等）**：工作量大，用 D2 / D3 的人工用例表替代。
- **`.tmp-archive-backup/`、`.tmp-db-rotate/`**：本地敏感文件（会话存档备份、口令轮换脚本），未入库，
  答辩后删掉。
- **内网 IP `10.0.20.91`**：源码里 0 处，只在文档里作为「已修历史」出现，保留不动。

---

## 附录 A：10 分钟级小项（顺手做，不做也不影响答辩）

- ~~前端 `vite.config.js` 缺 `build.rollupOptions.output.manualChunks`（ECharts 未单独拆包，即 C6 体积）~~
  ✅ 2026-09-24 已处理，**但原判断有误**：ECharts **本来就是**懒加载的独立 chunk（不在入口
  modulepreload 列表里），主 chunk 一直只有 ~57 kB。真正熔在一起的是 `options.js` 的图表配置
  + ECharts（662.7 kB，rolldown 命名为 `options`，极易误读成主包），已用 `advancedChunks`
  拆成 `echarts` 653 kB + `options` 9.9 kB。构建警告仍在（ECharts 本身就 653 kB），属预期。
  ⚠️ **不要用 `manualChunks: { echarts: [...] }` 对象写法** —— Vite 8 底层是 Rolldown，
  传对象**会让构建直接失败**，正则里路径分隔符要写 `[\\/]`（Windows 上是反斜杠）。
- ✅ 前端 `.nvmrc` 已加（内容 `24.20.0`，经 npm registry 核实该版本**确实存在**且满足
  `>=24.12.0`；不写大版本 `24` 是因为 engines 下限是 `>=24.12.0`）。
  ⚠️ 但原记的「web `README.md:22` 的 v22.14.0 已过时（实测 v24.20.0）」**不成立**：
  本机只有一份 node（`/c/Program Files/nodejs/node.exe`，无 nvm / fnm），
  实测 `node -v` = **v22.14.0**，**低于** engines 下限，`EBADENGINE` 警告**当前仍然存在**。
  `build` / `lint` / `verify:mock` 在该版本上均实跑通过，但属「碰巧能跑」。
  README 的 Node 说明已按实情改写（此前被写成 v24.20.0，是错的）。
  ✅ **2026-09-25 已销账（C8 关闭，结论：已升 node、engines 不动）**：本机 node 已升级到
  **v24.20.0**（`where.exe node` → `C:\Apps\Dev\node-v24.20.0-win-x64\node.exe`，`npm -v` = 11.19.0），
  满足 `engines.node` 的 `^22.18.0 || >=24.12.0`，与 `.nvmrc` 的 `24.20.0` 一致，
  `EBADENGINE` 警告随之消失；`npm run verify:mock` 在 v24.20.0 上**实跑通过、全部 31 项**
  （含学院字典相关的 4 条断言）。上面那段 v22.14.0 的复核结论**只对 2026-09-24 当时成立**。
- ~~`vite-plugin-vue-devtools` 无条件注册（`vite.config.js:14`），建议加 `apply: 'serve'`~~
  ✅ **该建议不成立**：插件内部已写死 `apply: "serve"`（`dist/vite.js` 的 plugin 定义），
  构建时本就不生效；且它的 options 里根本没有 `apply` 字段，传了也是无效参数。
  已在 `vite.config.js` 留注释，免得后人再"修"一次。
- 后端有接口但前端无入口的三处（**仍未做**）：画像重算 `POST /v1/portraits/recompute`（小）、
  组织资料编辑 `PUT /v1/orgs/{id}`（中）、学生自助签到/签退（小-中，`src/api/volunteer.js`
  有 `signIn`/`signOut` 函数但全仓无调用 —— mock 侧 handler 是齐的，只差页面入口）。
  ⚠️ 2026-09-24 复核：学生自助签到**不止缺页面入口** —— 后端也缺「查我自己的签到记录」接口
  （学生拿不到 `attendanceId`），两处合起来才算闭环，见第 29 项。
- `LogView.vue:15` 把 7 个模块名写死（**仍未做**；与后端当前 7 个 module 值一致，但后端加模块会漏）。
- ~~`bug/` 下两份缺陷笔记~~ ✅ 2026-09-24 已收尾：笔记里记的问题本身**都已修**（`cb5141b`）；
  其中 `sign-out-bug.md` 第 2 条经查 git 历史确认在新增时**就是空的**、不是丢了内容。
  两份笔记已随 `5ad6215` / `af7cc16` **整体删除**（`bug/` 目录随之不再存在）。
  缺陷内容与修复提交号在 git 历史（`cb5141b`）里可查，前端侧的对应记录见
  `docs/前端进展与待办.md` 第二节第 8 条。
- 9 个模块的 `src/test` 都是**空目录**，`vcp-boot` 之外也没有测试依赖（已单列为第 34 项）。
- `application.yml:74` 的 `# TODO 实体类包路径确定后补上`（`type-aliases-package` 仍被注释）。
- B21 `StudentInfo.gender`（DB 列已在 `sql/06_backend_gap_fix2.sql:118`，Java 实体没接，**当前无消费方**）。
- B13 MapStruct 仍是「只做版本管理、无模块依赖、无转换器」；真要用必须先配 `annotationProcessorPaths`。
- B17 索引缺口**已闭环**（`sql/06_backend_gap_fix2.sql:182`~`202`）；仍缺 CHECK / 触发器 / 视图 / 乐观锁列，可不做。
- `sql/07` 的自检有两个口径在混用：「6 项」vs 实际 9 条 `RAISE EXCEPTION` 断言，建议统一成「6 项 / 9 条」。
  ✅ 2026-09-24 已完成：`sql/README.md:46` 已统一为「6 项 / 9 条断言」。

## 附录 B：文档同步（D7）逐条状态

| 位置 | 状态 |
|---|---|
| 根 `README.md` 的进度表、`docs/` 结构、`07` 执行顺序、下一步待办 | ✅ 已随 `00b6d28` 与 `962b630` 提交推送 |
| `docs/待办清单.md` 的 D7 指控（「README 仍把 A8/A9 列为待定、sql 清单停在 `01`~`06`」） | ✅ 已不成立，本批已销账 |
| 一致性自检项数 | ✅ 已统一为 20 项（`docs/后端进展与待办.md`、`docs/公益等级与标签规则方案.md`、`CLAUDE.md`）；2026-09-24 已固化为 `sql/09_consistency_check.sql`（云库实跑，20 项违规合计 0） |
| `CLAUDE.md:221`~`228` | ✅ 2026-09-24 已完成：现文已写明「前后端联调（B12）已于 2026-09-23 审计通过」，B20-2、A8、A9 均已闭环（集成发现的 4 个问题已全部闭环，含 B20-1）。原指控：仍把「与前端联调 B12」列为下一步，并把 B20-2、A8、A9 列为未做/待定。本轮另修 `CLAUDE.md:269` 的「待拍板事项」口径（原 9 项已全部拍板，留档备查） |
| `volunteer-cert-portrait-web/README.md:38`、`:110` | ✅ 2026-09-24 已完成（前端侧提交 `a03e0f1`）：`:38` 改为「后端已就绪，开发环境已切真实后端，纯前端演示走 mock」；`:110` 的「另有两处前端与后端尚未对齐」改为「已逐条核对、全部对齐」。同文件另修两处：`org_status` 枚举表漏了 `DISABLED`；Node 版本说明按实情改写 —— **本机确为 v22.14.0、低于 engines 下限 `^22.18.0 \|\| >=24.12.0`，`EBADENGINE` 警告真实存在**（此前「已升到 v24.20.0」的说法经复核**不成立**，见 C8 与前端 P1 第 3 项） |
| `.env.development:2`、`.env.production:3` 的头部注释 | ✅ 2026-09-24 已完成（前端侧提交 `a03e0f1`）：`.env.development` 原注释说「走本地 mock」却配着 `VITE_USE_MOCK=false`（**注释与值自相矛盾**），已改为「联调走真实后端」并补上 `.env.local` 优先级说明；`.env.production` 的「后端尚未开发」已改为说明两种部署口径（mock 演示 vs Nginx 反代） |
| `docs/知识库已确认项目选题与技术背景.md:273` | ✅ 「学校端 7 页」已改为 9 页 |
| `docs/公益等级与标签规则方案.md:97` | ✅ 2026-09-24 已补逐标签人数（8 类齐全：热心志愿者 1471 / 长期坚持型 1380 / 社区服务型 392 / 环保行动型 286 / 大型活动型 231 / 校园服务型 222 / 文化传播型 200 / 助老服务型 165；4 个画像无标签属正常），并区分 2026-09-22 与 2026-09-24 两期实测 |
| `docs/ER图.md` | ✅ 2026-09-24 已补 `activity_category.remark`（标注「07 补列」），文首依据与文末「执行过 02 + 05 + 06」的口径已改为含 `07`（老库含 `08`）（见第 15 项） |
| `sql/README.md` 的脚本清单、根 `README.md` 的「新同学四步上手」、`docs/测试报告.md` 的「数据库」行 | ✅ **2026-09-25 已同步**（原 ⏳ 2026-09-24 第三轮待同步）：根 `README.md` 的「新同学四步上手」第 2 步与「快速开始（后端）」第 1 步都已补上 `09`（只读自检）与 `10`（学院字典来源、必执行），并写明 **`04` 是 `07` 的硬前置**；`docs/测试报告.md` 的「数据库」行与行数核对表已按 2026-09-25 重灌后的实测值改写（两组口径）。**收尾时已一并修正 `sql/README.md`**：`:16` 的 `04_demo_data.sql` 由「可选」改为「**跑 `07` 时必需**」、`:19` 的 `07` 补「必须先执行 `04`」、`:22` 的 `10` 由「可选」改为「**新环境必需（`college` 学院字典的唯一来源）**」、`:26` 起的重建段落与「执行方式」两段补成完整九脚本序列并加上只读的 `09`（含 16 秒实测与「DDL 前先清陈旧会话」的提示）。**本项已无剩余** |

---

*本清单由 2026-09-24 的三路只读审计汇总；【新】= 本轮审计新发现，历史总账 `docs/待办清单.md` 里没有。*
*2026-09-25 补记：第 41 项（删除用户不级联）与 `verify.ps1` 的修复来自**云库整库重建**那一轮，过程见 `docs/会话记录.md` 会话 11。*
