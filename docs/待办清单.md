# 待办清单

> **这是历史总账（唯一总入口）。** 后端细节见 `docs/后端进展与待办.md`，前端见 `docs/前端进展与待办.md`，
> 数据库见 `sql/README.md`；**只想看「接下来做什么」直接看 `docs/下一步待办.md`**（2026-09-24 版）。
>
> 约定：完成的项**打勾并注明日期，不要删行** —— 便于回顾进度，也便于写论文时说明工作量。

最后更新：2026-09-24（**本轮：A8 实跑销账、A9 销账、D1 ER 图产出、A 类演示数据实跑成功、两处工程风险收口**。
A9 三层天然对齐（DB `SMALLINT` 1/0 / API 英文码 / 前端 `DICT_DEFS` 英文码）——
**实测已对齐，无需改代码**，前端 C1 页的阻塞解除；`docs/ER图.md` 已产出（646 行 / 16 表 / 23 关系）；
`sql/07_demo_scale.sql` **已实跑成功**：首次实跑暴露的 3 处脚本缺陷（第 108 行 `date + bigint`、
每场报名上限 8..39 导致五星档恒空、第十/十之二节缺幂等守卫）已全部修复并重跑，**末尾 6 项自检全部通过**，
实测学生 1500 / 活动 386 / 报名 10719 / 累计 19319.3 小时，六档等级齐全（A8 与 D6 销账）；
根 `.gitignore` 已补 `application-local.yml` 规则；前端代理改为读 `VITE_API_TARGET`
（默认 `http://127.0.0.1:8080`）；本批提交 `1138eaa`（fix: sql/07）、`e441135`（docs）、
`7b362f3`（chore），已推送 `0a668d7..7b362f3`。上一版：2026-09-23 前后端联调（B12）审计完成、
数据库口令已轮换、`docs/对话存档/后端/` 已删除）

---

## ⚠️ 需要尽快处理：公开仓库里有明文数据库凭证

- [x] **【唯一真正的修复：轮换数据库口令】仓库 `XiaoYu-04/volunteer-cert-portrait` 是 public，
      而云上 PostgreSQL 的口令进过 git 历史（提交 `161e16c`）与当前工作区文件**
      —— ✅ 2026-09-23 **已轮换**，旧口令实测被服务端拒绝

  ✅ 2026-09-23 已完成「配置外置」：`application.yml` 里不再有明文，改为
  **环境变量 → `application-local.yml`（已 gitignore）** 两级读取，模板见同目录
  `application-local.yml.example`；`CLAUDE.md`、`docs/后端进展与待办.md` 里被当作示例的
  口令也已换成中性值。

  ✅ 2026-09-23 **已完成轮换**：经 JDBC 执行 `ALTER USER postgres WITH PASSWORD ...`；
  服务端 `password_encryption = scram-sha-256`（不是 `trust`，所以「旧口令被拒绝」是真的生效，
  不是假象）；新口令只写进 `application-local.yml`，未回写任何被跟踪的文件。当时步骤留档：

  1. 数据库侧改口令：`ALTER USER postgres WITH PASSWORD '<新口令>';`
  2. 新口令写进 `application-local.yml`（或设环境变量 `VCP_DB_PASSWORD`）
  3. **通知另外两位同学更新各自的本地凭证** —— 他们不更新就连不上库  ⬅️ **仍未完成**
     新口令的获取渠道已写进 `README.md` 的「新同学四步上手」一节。
  4. 轮换后不要把新口令写回任何被跟踪的文件

  > 前端不涉及任何密钥：`.env.*` 只有接口地址与 mock 开关，`src/` 里没有凭证。

- [x] **同一问题的第二处：`docs/对话存档/后端/` 目录** —— ✅ 2026-09-23 已删除（在轮换之后）

  该目录下的 jsonl 记录了后端搭建会话的完整工具调用，其中含数据库连接串与明文口令。
  目录自己的 `README.md` 写着「本仓库是私有的，但若日后转为公开，必须先删除本目录」——
  **当时仓库已公开而目录还在，口令就摆在 HEAD 里（不是只在历史里）。**
  已按正确顺序处理：**先轮换口令，再删除该目录**。本地留了一份备份在 `.tmp-archive-backup/`
  （gitignored，写论文要翻当时的决策记录时用）。

  > 注意：清理 git 历史需要 force push，会波及另外两位同学的本地仓库，动手前先在群里打招呼。
  > 若暂时不动历史，至少先轮换密码——轮换后历史里的旧口令即失效。

---

## 🚩 下一步该做什么（2026-09-24 版）

> B1–B11 全部做完，**后端不再是瓶颈**；联调审计也做完了（见 F 组）。
> 2026-09-24：A 类演示数据已实跑成功、D1 ER 图已产出、两处工程风险已收口；
> 真正卡住的只剩 P0 的「通知同学」与 P2 的「重启后端」。

**P0 —— 只剩「通知同学」**

1. ~~轮换云数据库口令~~ ✅ 2026-09-23 完成
2. ~~删除 `docs/对话存档/后端/`~~ ✅ 2026-09-23 完成
3. ⬅️ **通知另外两位同学更新各自本地的 `application-local.yml`** —— 不更新就连不上库。
   **清理 git 历史里的口令**仍建议推迟到答辩之后（旧口令已失效，不做也不影响安全）。
4. ✅ 2026-09-24 **已给根 `.gitignore` 补 `application-local.yml`**（同批提交；
   2026-09-23 发现，一分钟的事）—— 原因仍值得记：
   `application.yml` 支持 `optional:file:./application-local.yml`（**仓库根**），
   而根 `.gitignore` 原本只有 `*.local` / `.env.local`，**没有这条规则** ——
   谁在根目录建了这个文件，git 就会跟踪它，等于把口令提交到公开仓库。
   （`vcp-boot/src/main/resources/` 下的那份一直是安全的：`vcp-volunteer`… 同级的
   `volunteer-cert-portrait-server/.gitignore:17` 覆盖了它；敞着的只有根目录那份。）

**P1 —— A 类演示数据放大：✅ 已实跑成功，末尾 6 项自检全部通过**

5. ✅ 2026-09-24 **`sql/07_demo_scale.sql` 已实跑成功** —— 916 行（`0a668d7` 初版 797 行，本次修复后 903 行，口径收口后 916 行）、幂等、包事务、
   末尾带 6 项自检（`DO` 块断言，不满足即整批回滚），**6 项全部通过**。
   实测：学生 **1500** 人、活动 **386** 场、报名 **10719** 条、累计时长 **19319.3** 小时；
   六档等级齐全：普通 118 / 一星 168 / 二星 299 / 三星 687 / 四星 207 / 五星 21
   （规模口径见 F 组的「量级对齐、口径自洽」决策）。
   **但首次实跑暴露了脚本自身的 3 个缺陷 + 1 个幂等性漏洞，全部已修**（所以**不能说「一次跑通」**）：
   ① 第 108 行 `DATE '2015-01-01' + ((id * 137) % 2920)` 是 `date + bigint` ——
   PostgreSQL 没有这个运算符（只有 `date + integer`），**脚本第一条 UPDATE 就报错**；
   已改成 `(((id * 137) % 2920)::int)`；
   ② 每场报名上限原为 `8 + ((a.id * 7919) % 32)`（8..39），导致人均只参与 5.5 场、最高 9 场
   ≈ 37.2 小时，「五星志愿者（≥40 小时）」一档**恒为空**，末尾自检第 3 项必然失败；
   已改为 `16 + ((a.id * 7919) % 30)`（16..45）；名额下限 2026-09-24 已由 40 抬到 **46**，
   46 > 报名数上限 45，自检第 4 项（`signed_count <= max_count`）恒成立（口径见 B20-1）；
   ③ 新增「十之二、把高活跃学生定向补进五星档」一节：一次报名要过三层折算
   （85% COMPLETED × 90% 非缺勤 × 75% 审核通过 ≈ 57%）才变成一条有效时长，每条约 4 小时，
   40 小时需约 18 次报名；光靠调大报名数会把总量推到 3 万条（原型只有 12,480），
   故改为对累计 32..40 小时的学生定向补 4 场 ≥4 小时的活动；
   ④ 另修幂等性漏洞：第十节与第十之二节原先没挂 `should_scale` 守卫，
   重跑会给学生 1 再补 6 场、五星档持续膨胀。
   *（2026-09-23 原状态：脚本已写好但一次都没跑过，等数据库凭证；项目约定「数据改动必须实跑验证」，
   所以当时是未验证状态。现已实跑成功，等级分布问题已修复。）*
   ✅ **后续顺序**：`07` 已重跑完成 → `mvn package` → 重启后端 —— `07` 第 69 行补了
   `activity_category.remark` 列，不重启后端的话分类接口会因查不存在的列而报错（见 P2）。

**P2 —— C 类后端改动要生效，需重新构建并重启后端**

6. ⬅️ 我改的 4 个功能缺口（分类 `remark`、公告正文、`StudentVO.grade`、
   `SessionVO` 的 phone/email）已构建通过，但 **8080 上跑的是队友的实例**，
   他那边还是旧代码。要么请他 `mvn package` + 重启，要么用本地新构建的
   `vcp-boot/target/vcp-boot-1.0.0.jar` 起一个。
   ✅ 2026-09-24 前置条件已满足：`sql/07` 里的 `activity_category.remark` 补列**已执行**。
   执行顺序固定为：**跑 `07`（已完成）→ `mvn package` → 重启后端**（顺序颠倒会因查不存在的列报错）。

**P3 —— 收尾与交付物**

7. ~~密码加密（B15）~~ ✅ 2026-09-24 已完成：BCrypt 密文 + 本人改密 + 管理员重置 + 登录失败锁定
   （老库补跑 `sql/08_password_bcrypt.sql`，新库不用）。**剩余**：Flyway（B14）、文件上传（C9）、
   证书生成（C11）、活动收藏（C12）。
8. 交付物 D1–D7：ER 图、测试用例表、测试报告、系统截图、部署、演示数据定稿、文档同步。
   其中 **D1 ER 图已产出**、**D6 演示数据已实跑成功**（5 条缺口闭环 4 条，
   `operation_log.target` 属后端代码问题、仍单独待办）（见 D 组）；
   **D7 文档同步**里还列着几处已知过时（根 `README.md` 的「下一步待办」一节等；
   一致性自检项数已核实并统一为 20）。
   **D4 系统截图**：C3 走查已产出 **56 张**（28 页 × 1440/620 两视口），但存在被 gitignore 的
   `volunteer-cert-portrait-web/.tmp-shots/out/`、**尚未入库**（仓库里仍是 0 张图片），
   需先拍板入库范围与目录 —— 见 `docs/下一步待办.md` 第 28 项。
   **C3 / C4 已销账**（28 页全部浏览器实测、620px 无横向溢出），走查发现的 3 个 mock 缺陷
   见 C-2b（C13~C16）。

**A 组已无剩余待定**：~~A6~~ ✅ 2026-09-24 已定：**不改**（单一部署、无跨时区需求，见 A 组）。
~~A8~~ ✅ 已定：量级对齐、口径自洽（见 F 组）。~~A9~~ ✅ 已解决（三层天然对齐，**无需改代码**）。

**⚠️ 曾需要你确认的提交内容（2026-09-24 已处理）**

9. ✅ 2026-09-24 **硬编码已消除**：`volunteer-cert-portrait-web/vite.config.js` 的代理目标
   曾从 `127.0.0.1:8080` 改成写死的 **`http://10.0.20.91:8080`**（某台机器的内网 IP），
   且 `.env.development` 的 `VITE_USE_MOCK=false` 已随提交入库。
   **风险**：那台机器一关机或换 IP，所有人的 dev server 就集体连不上，且 mock 已关，
   没有后端就跑不起来。
   现改为读 `VITE_API_TARGET`，**默认 `http://127.0.0.1:8080`**（默认打本机，各自跑各自的后端）；
   需要联调队友那台时，在本地 `.env.local` 里覆盖 `VITE_API_TARGET` 即可，不要改回 `vite.config.js`。

---

## A. 需要先拍板（不定就无法开工）

> 2026-09-23：为解锁四路并行开发（`vcp-volunteer` / `vcp-certification` / `vcp-portrait` /
> `vcp-analytics`），**A1–A5、A7 已按下列口径先行拍板并写进代码**。这些是工程上可用的默认值，
> **请三人复核**：若有异议，改动点集中在 `vcp-volunteer` 与 `vcp-certification` 两个模块，量可控。
> A6 已拍板（**不改**，单一部署、无跨时区需求，见 A 组）；A8 已拍板（量级对齐、口径自洽，见 F 组）、A9 已解决（三层天然对齐，**无需改代码**）。

- [x] 2026-09-23 **A1 服务时长如何计算**
      结论：`hours = (签退时间 − 签到时间) / 3600`，保留 2 位小数；**缺签退或签到状态为
      `ABNORMAL` 时取活动预计时长**；**上限为活动预计时长的 1.5 倍**（超出按 1.5 倍封顶）。
      理由：异常场景按预计时长计比按 0 计更贴近真实服务量，1.5 倍封顶则防止签到窗口被滥用。

- [x] 2026-09-23 **A2 签到/签退的时间窗口**
      结论：**活动开始前 30 分钟开放签到**；**活动结束后 30 分钟内必须签退**；
      未签退记 `ABSENT`，实现为**查询时惰性判定**（不引入定时任务，降低部署复杂度）。
      30 分钟为可调常量，集中定义在 `vcp-volunteer` 的常量类里。

- [x] 2026-09-23 **A3 报名人数上限的并发控制**
      结论：用 `UPDATE volunteer_activity SET signed_count = signed_count + 1
      WHERE id = ? AND signed_count < max_count` 原子更新，**影响行数为 0 即「已满」**。
      不做「先查再写」（存在 TOCTOU 窗口），也不用行锁（`volunteer_activity` 没有 `version` 列）。

- [x] 2026-09-23 **A4 审核驳回后如何重新提交**
      结论：`REJECTED` **允许重新提交**，回到 `PENDING_AUDIT`，
      并往 `duration_audit` 写一条新的 `SUBMIT` 流水（保留驳回历史，不覆盖）。

- [x] 2026-09-23 **A5 `total_duration` 以哪张表为准**
      结论：**以 `student_info.total_duration` 为准**，时长审核通过时由后端累加；
      `student_profile` 仅作画像快照，可随时由前者重算。

- [x] 2026-09-24 **A6 时间类型是否改 `timestamptz`** —— ✅ **已拍板：不改**
      结论：**不改** —— 继续用 `TIMESTAMP` + `LocalDateTime`。理由：单一部署、无跨时区需求；
      改成 `timestamptz` 要动 16 张表 + 全部实体 + 前端渲染 + 演示数据重跑，收益与代价不匹配。
      现按原始设计用 `TIMESTAMP`（无时区），单时区部署无影响，跨时区才需改。

- [x] 2026-09-23 **A7 逻辑删除与唯一约束的冲突**
      结论：**「取消 = 改 `status = 'CANCELED'`、复用同一行」**，`deleted` 保持 0。
      不改唯一约束 —— 改部分唯一索引要动 `02_schema.sql` 的既有约束，
      而「同一学生对同一活动只保留一行」本来就是想要的语义。

- [x] 2026-09-23 **A8 演示数据尺度对齐** —— ✅ **已拍板：量级对齐、口径自洽**
      （不追求与原型逐字相等）。理由见 F 组：原型的 `SUM(profiles.count)` 与 `enrolled`
      都写 12,480 是**冻结值的巧合**，一个学生最多 3 个标签，标签计数之和必然大于报名数，
      这个等式在真实数据下不可复现；`trend` 窗口又锚定「本月往前 12 个月」，
      与原型的 2025-01~12 也不可能相等。目标规模改为
      **386 场活动 / 约 1,500 学生 / 约 1 万报名 / 约 2 万小时**，脚本见 `sql/07_demo_scale.sql`。
      ✅ 2026-09-24 **已实跑销账**：`07` 重跑成功、末尾 6 项自检全绿，实测学生 1500 / 活动 386 /
      报名 10719 / 累计 19319.3 小时，六档等级齐全（详见 P1、F4）。

- [x] 2026-09-23 **A9 `sys_user.status` 用什么类型** —— ✅ **已解决：三层天然对齐，无需改代码**。
      DB `user_status` 存 `SMALLINT` 1/0；API 返回英文码（`UserStatusEnum` 做双向翻译）；
      前端 `DICT_DEFS.user_status` 用英文码 —— 三层本来就一致，**没有一处需要改**。
      实测字典接口返回 `[{value:'ACTIVE',label:'正常',tone:'ok'},{value:'DISABLED',label:'已停用',tone:'mute'}]`，
      用户列表接口返回 `status: "ACTIVE"`，与前端静态定义**值/标签/色调逐条一致**。
      因此前端 `C1` 页的阻塞解除，无需为这个字段开特例。
      *（本次审计时发现后端侧早已改完，只是没打勾，补记。）*

- [x] 2026-09-23 **A10 组织「停用/启用」是否保留**
      结论：**保留**。`org_info.status` 在资质审核态之外增加 `DISABLED`：
      审核结果仍是 `PENDING` / `APPROVED` / `REJECTED`，
      学校管理员可在 `APPROVED` 与 `DISABLED` 之间往返切换。
      这样能保留组织历史数据，暂停其发布活动与提交时长，不必把资质改成"驳回"。

  > A9 已销账：状态枚举口径**全部对齐**（`user_status` 三层天然一致；其余 `attendance_status`
  > 的 `NOT_SIGNED`、`notification_type` 的三值、`audit_action` 的 `SUBMIT` 前端也已按后端对齐），
  > 前端与后端之间不再有状态枚举分歧。

- [x] 2026-09-23 **A11 演示账号名不一致**（状态枚举之外的另一处口径）
      结论：**统一为 `org_admin`**（后端种子为准），已由提交 `9443b0f` 改了前端
      `src/mock/data/dataset.js`、`src/views/login/LoginView.vue` 与根 `README.md`。
      ✅ 2026-09-24 收口：`volunteer-cert-portrait-web/README.md` 的演示账号表里组织管理员
      也由 `org` 改成 `org_admin`（该文件原先漏改）。
      *（本次复核时发现这项其实早已做完，只是没打勾，补记。）*

- [x] **A12 活动分类的前后端口径不一致（2026-09-23 新发现）**
      库里的 6 条 `activity_category` 与前端 mock 的 6 条**名称与顺序都不同**：

      | id | 库（`sql/03_init_data.sql`） | 前端 mock（`dataset.js`） |
      |---|---|---|
      | 1 | 校园服务 | 社区服务 |
      | 2 | 社区服务 | 环保行动 |
      | 3 | 环保公益 | 支教助学 |
      | 4 | 大型赛事 | 大型赛会 |
      | 5 | 助老服务 | 校园服务 |
      | 6 | 文化传播 | 敬老助残 |

      影响不只是文案：`sql/06_backend_gap_fix2.sql` 给分类补 `code` 时只能**按名称语义**映射
      （校园服务→`CAMPUS`、社区服务→`COMMUNITY`、环保公益→`ENVIRONMENT`、大型赛事→`EVENT`、
      助老服务→`ELDERLY`、文化传播→`CULTURE`），而 `docs/公益等级与标签规则方案.md`
      的标签规则用的是**库里的中文名**（「助老服务型」「文化传播型」），
      前端 mock 里根本没有「文化传播」这一项。接上真实接口后，前端分类筛选器的文案会整体换一套。

      **建议以后端（库 + 标签规则文档）为准，改前端 mock 的 6 个名称与顺序**；
      同时「分类→标签映射」必须按 `code` 而不是中文名做（见 B18 的脆弱点）。

      ✅ 2026-09-23 **已按建议改完前端**：`dataset.js` 的 `types`/`categories` 换成库里那套
      名称与顺序（校园服务 / 社区服务 / 环保公益 / 大型赛事 / 助老服务 / 文化传播，id 即 1~6），
      `code` 直接写在每项里（原来是按下标对齐的编码数组，改名即静默错位）；
      24 场活动的 `type` 与 `categoryId` 同步重映射。三处附带处理：
      ① 原型里的「支教助学」在库的 6 个分类里没有对应项，三条支教类活动按「文化/教育类」
      归到「文化传播」，标题保留原样（若要更贴切可改成非遗、讲解一类的内容）；
      ② `DashboardView` / `HomeView` 里写死分类名的无障碍描述已改；
      ③ `scripts/verify-mock-data.mjs` 新增断言「每场活动的分类 id 与类型名一致」。
      「分类→标签映射按 code 做」后端 `PortraitTagEnum` 已是这么实现的（B10 落地时做的）。
      ✅ 已完成（2026-09-24 复核：`volunteer-cert-portrait-web/src/mock/data/dataset.js:68-75` 的 6 条分类名与顺序已与 `sql/03_init_data.sql:60-66` 逐条一致、`code` 写在每项里；`sql/06_backend_gap_fix2.sql:62-91` 补 `code` 并按名称回填、`sql/07_demo_scale.sql:69-84` 按 `code` 回填 `remark`；后端类型标签按 `code` 映射（`volunteer-cert-portrait-server/vcp-portrait/src/main/java/com/vcp/portrait/enums/PortraitTagEnum.java` 的 `byCategoryCode`）；`node scripts/verify-mock-data.mjs` 21 项断言全过，含「每场活动的分类 id 与类型名一致」）

---

## B. 后端工程

### B-1 基础设施（一切的前提）

- [x] 2026-09-23 **B1 `vcp-common`**：统一返回 `R<T>`、`PageResult`、`BaseEntity`、异常体系、
      常量、枚举（10 个状态枚举，码值与 `03_init_data.sql` 的 `sys_dict` 种子逐条核对过）
- [x] 2026-09-23 **B2 `vcp-framework`**：Sa-Token 配置 + `StpInterface`、全局异常处理、
      MyBatis-Plus 分页插件、字段自动填充（`create_time`/`update_time`）、跨域
      （另外做了：操作日志切面 + `@OperationLog` 注解、Knife4j 的 Bearer 鉴权配置、`PageUtils`）
- [x] 2026-09-23 **B3 确定 MyBatis 扫描方式**：定为**每个 Mapper 接口加 `@Mapper`**，不改配置。
      MyBatis-Plus 自动扫 `com.vcp` 包，启动日志里那句
      "No MyBatis mapper was found in '[com.vcp]' package" 是正常的，等第一个 Mapper 出现即消失
- [x] 2026-09-23 **B4 Sa-Token 拦截器放行文档路径**：已实测 `/doc.html` 与 `/v3/api-docs`
      返回 200；放行清单集中在 `security/SaTokenConfig` 的 `EXCLUDE_PATHS` 常量数组
- [x] 2026-09-23 **B5 登录认证打通**：跑通「登录 → 拿 token → 访问受保护接口」，
      这一步能验证整套基础设施是否真的可用
      （2026-09-23 进展：拦截器已实测生效——未登录访问任意路径返回 20001；
      只差一个真实登录接口，需要先有 `vcp-system` 的用户查询）
      （2026-09-23 完成：`/api/v1/auth` 五个接口全部实测通过——登录、注册、退出、
      查本人信息、改本人资料。端到端 42 项断言全绿，含「未登录 20001 / 停用 20002 /
      越权 20003」三条认证路径。**踩坑记录**：Sa-Token 的 `SaSession.set` 不接受 null 值，
      最初把空 orgId 直接写进会话，导致学校管理员与学生登录报「系统繁忙」、
      而组织管理员正常，表现为「只有部分账号登不进去」；已改为值为 null 时不写该键）

### B-2 业务模块

- [x] 2026-09-23 **B6 `vcp-system`**：用户 / 角色 / 学生档案 / 字典 / 通知 / 操作日志 / 附件（8 张表）
      （2026-09-23 完成：entity 8 / mapper 8 / dto 10 / vo 8 / service 6 + 实现 7 /
      controller 7 / 事件监听器 1，全部实测通过。`/api/v1/system/*` 七个接口可用。
      附件表只建了实体与 Mapper，暂无接口——文件上传要等 `vcp-org` 的资质材料场景一起做。
      **前置依赖**：需先执行 `sql/05_backend_gap_fix.sql` 补齐前端要而库里缺的列，
      否则登录接口会因 `sys_user.last_login_at` 不存在而直接报错）
- [x] 2026-09-23 **B7 `vcp-org`**：组织信息维护 + 资质审核（1 张表）
      （2026-09-23 完成：entity 1 / mapper 1 / dto 4 / vo 1 / service 接口 1 + 实现 2 /
      controller 1，接口为组织列表、详情、资料维护、资质审核、启停。
      登录时的 `OrgLookupPort` 已接上，组织管理员登录会写入 `orgId`；
      审核结果会给负责人落一条系统通知。实测通过：8 个组织分页返回、
      组织管理员读取/更新本组织、学生读取/更新被拒 20003、启停往返恢复、
      非法审核动作不改变原状态。聚合指标通过 `OrgMetricsPort` 预留，
      未接统计实现时返回 0，不阻塞基础资料接口。资质材料文件上传仍归 C9：
      当前前端没有上传组件，后端附件表已建但不把文件存储混进 B7）
- [x] 2026-09-23 **B8 `vcp-volunteer`**：活动分类 / 活动发布 / 报名审核 / 签到签退（4 张表）
      （52 个文件，18 个接口，含 4 个 Mapper XML。A2/A3/A7 按本次拍板的口径实现。
      **签到/签退接口只挂 `@SaCheckLogin`**：学生那 5 条权限里没有签到相关项，
      补一条要改 `vcp-framework` 的 `StpInterfaceImpl`；真正的门禁放在 Service 的
      `requireOwnRecord()`（只能操作自己的签到记录，越权返回 30011「不存在」）。
      `GET /categories` 用 `volunteer:activity:list` 而非 `category:manage` ——
      三个角色都要读分类，挂 `category:manage` 会让学生与组织端直接 20003）
- [x] 2026-09-23 **B9 `vcp-certification`**：时长提交 + 学校审核（2 张表）
      （24 个文件，6 个接口。A1/A4/A5 按本次拍板的口径实现：`REJECTED` 可重提回
      `PENDING_AUDIT` 并补一条 `SUBMIT` 流水；审核通过用一条原子
      `UPDATE ... SET total_duration = COALESCE(total_duration,0) + ?` 累加，
      避免读-改-写丢更新。`signup_id` 由 `studentId + activityId` 反查，查不到报 30008）
- [x] 2026-09-23 **B10 `vcp-portrait`**：画像生成 / 等级 / 标签（1 张表，规则见
      `docs/公益等级与标签规则方案.md`）
      （20 个文件，5 个接口。**B18 的处理：`public_welfare_level` 保留中文等级名**
      —— 前端把 `level` 当展示文案直接渲染，改存码值页面会显示 `FIVE_STAR`；
      折中是 VO 里一并返回 `levelCode`，枚举同时识别中文名与英文码。
      已实测：Java 判定与 `04_demo_data.sql` 的 `CASE` 逐字节一致，31 名学生
      等级/标签/描述三项 mismatch 全为 0。重算走「手动接口 + 每日 02:00 定时兜底」）
- [x] 2026-09-23 **B11 `vcp-analytics`**：看板统计接口（只读聚合，无独立表）
      （21 个文件，10 个接口，11 条单条 SQL 聚合。`/dashboard` 刻意只挂
      `@SaCheckLogin`：学生首页、组织看板、学校看板三处都调它，挂
      `analytics:dashboard:view` 会把学生端与组织端打成 20003。
      无数据月份用 `generate_series` 补 0 行，不把明细捞进内存）
- [x] 2026-09-23 **B12 与前端联调** —— ✅ **审计阶段完成**（处置见 F 组）。
      方式：前端 `VITE_USE_MOCK=false` + 代理指向真实后端，三个角色真登录打 92 次接口，
      再与 `src/mock/data/*` 逐字段比对；另加 8 次越权探针。
      结论：**后端代码基本完备**，未发现字段名错位类硬伤；
      问题集中在演示数据规模与前端写死的 mock 数字（已分别归入 A 类与 B 类）。
      两条重要「误报已排除」：`PortraitVO.tag`（单数）是后端有意提供的主标签；
      `20003` 计入「清 token 跳登录」与后端 `ErrorCodeEnum` 的冻结契约一致，均非 bug。

### B-3 收尾

- [ ] **B13 MapStruct 注解处理器配置**（写第一个对象转换器时加，否则实现类静默不生成）
- [ ] **B14 开启 Flyway**（表结构稳定后，把 `02`/`03` 复制进 `db/migration/` 改名 `V2__`/`V3__`）
- [x] **B15 接入密码加密** ✅ 2026-09-24 已完成：`vcp-framework/pom.xml` 加 `spring-security-crypto`
  （版本由 Boot BOM 管），新增 `vcp-framework/.../util/PasswordUtils.java`（BCrypt 强度 10，
  `encode`/`matches`/`isEncoded`/`checkPolicy`），口令读写全工程只经这一个类（登录、注册、新增用户、改密、重置五处）。
  种子数据 `sql/03` / `04` / `07` 已换成密文，另新增 `sql/08_password_bcrypt.sql` 并在云库实跑
  （1509 个账号 100% 为 60 位密文、明文残留 0）。配套的「本人改密 / 管理员重置 / 登录失败锁定」
  见 `docs/下一步待办.md` 第 8~11 项。`matches` 对明文记录一律返回 false，**刻意不做明文兜底**。

### B-4 审计发现的既有缺口（写业务代码前先处理）

> 2026-09-22 用 4 路并行审计（工程现状 / 文档待办 / 前端接口反推 / 数据库闭环）交叉核对得出：
> 前端 28 个页面已经把字段契约写死，而 16 张表的列对不上，数据库层也有若干会影响实现的缺口。
> 这些不是"新功能"，是**写接口前必须先处理或明确绕开**的问题。

- [ ] **B16 前端要的字段，库里没有**（需扩表，或明确映射与 JOIN 方案）
      **（部分完成：数据库列已全部补齐、后端映射也已全部落地；唯一仍缺的是 `operation_log.target` 的写入侧，已归 B20-6 单列）**
      **2026-09-23 进展：列已由 `sql/06_backend_gap_fix2.sql` 补齐并执行**
      （活动 `deadline`/`contact`、报名 `reason`、分类 `code`、学生 `gender`/`grade`、
      时长 `proof`/`org_id`/`activity_type`），四个新模块已按补列后的契约实现。
      **2026-09-24 口径收敛（本条内部曾自相矛盾，以此处为准）**：
      **数据库列这一层已经补齐**（上列各列都在库里），不再作为缺口；
      后端**实体类**的缺口只剩 `StudentInfo.java` 缺 `gender`（DB 列 `student_info.gender` 已存在），
      该条已单列在 **B21**（无消费方、低优先级），本条不再重复。
      下面是**补列解决不了、仍需处理**的部分：
      - 签到：`attendance_record` 没有 `activity_id`/`student_id`/`hours`，
        需经 `signup_id` 两次 JOIN 才能拼出前端表格要的字段
      - 时长：`signup_id` 是 NOT NULL UNIQUE，而前端只给 `studentId` + `activityId`，
        **后端必须自己反查 `signup_id`**（已按此实现，见 B9）
      - 操作日志：`role`/`module`/`target`/`result` **四列全不存在**，
        而日志页 8 列都要，`module` 还是中文全等筛选（时长认证/志愿活动/活动报名/…）
        ✅ 2026-09-24 复核：`sql/05_backend_gap_fix.sql:145-149` 已补 `module`/`action`/`target`/`result`/`cost` 五列；
        `role` 是**刻意不落列**（由 `user_id` 关联角色推导，见该文件 `:142-143` 与 `LogServiceImpl.java:87`）。
        **仍缺**：`operation_log.target` 写入侧恒空（`@OperationLog` 注解与 `OperationLogEvent` 都还没有 `target` 属性），已单列在 B20-6
      - 通知：`from`/`top` 两列不存在；且语义冲突 —— 表是「按收件人一行 + `is_read`」，
        前端既要"学校管理员发布公告"又要"学生看未读列表"，
        需决定公告是群发插行还是另建公告表
        ✅ 2026-09-24 复核：已按「群发插行」落地 —— `sql/05_backend_gap_fix.sql:124-135` 补了 `source`
        （前端字段名 `from`，SQL 关键字冲突故改名）/`is_top`（前端 `top`）/`batch_no`（群发批次号），
        实体 `Notification.java:47,51,54` 已接上；`NotificationServiceImpl.java:151,170` 生成并写入批次号，`:217-218` 按批次整批删除
      - 组织：`college`/`member_count`/`founded_at`/`code` 原本没有，✅ 已由 `05_backend_gap_fix.sql` 补齐，
        并额外补了 `audit_remark`
      - 学生档案：`gender`/`grade` 两列 ✅ 已由 `05`/`06` 补齐；
        `phone` 在 `sys_user`、姓名在 `sys_user.real_name`
      - 分类：`activity_category.code` ✅ 已由 `06` 补齐（按名称语义映射，见 A12）；
        `remark` ✅ 已由 `07` 补齐（见 B20-2）
      - 字典：前端要 `{dict_type: [{value, label, tone}]}` 的 **Map 形态 + tone 色调**，
        表是平铺行且**无 tone 列**，后端需 reshape（tone 可省略，前端缺省为 mute）
      - 权限：`RoleManageView` 要 `role.perms`（前端静态定义了 **28** 条权限标识：
        学生 5 / 组织管理员 7 / 学校管理员 16，已同步进 `StpInterfaceImpl`）与 `userCount`，
        **后端没有权限表、`sys_role` 没有 perms 列**
        ✅ 2026-09-24 复核：接口侧已落地 —— `RoleVO.java:32,37` 有 `userCount` 与 `List<String> perms`，
        `RoleServiceImpl.java:51-52` 分别取角色用户数与 `StpInterfaceImpl.getPermsByRoleCode(roleCode)`。
        权限表**仍不存在**（perms 是代码内静态定义，改权限要改代码）

- [ ] **B17 数据库层缺口**（`sql/README.md` 之外的审计发现）
      **（部分完成：索引缺口与 `update_time` 两项已闭环；仍缺 CHECK/触发器/视图、`version` 乐观锁列、两处 UNIQUE、`volunteer_activity` 四列可空）**
      **2026-09-23 进展：索引缺口已由 `sql/06_backend_gap_fix2.sql` 补 12 条并执行**
      （含 `attendance_record` 的两条、6 处外键索引）。**仍未处理**：全库零 CHECK/触发器/视图、
      无 `version` 乐观锁列（`update_time` 一条已闭环，见下）。
      - **`update_time` 永远不会自动刷新**：PostgreSQL 没有 `ON UPDATE CURRENT_TIMESTAMP`，
        全库也没有 `BEFORE UPDATE` 触发器；`04_demo_data.sql` 的三条回填 UPDATE 也从不写它。
        只能靠尚未实现的 `MetaObjectHandler`（B2）兜住，否则"最后修改时间"全部失真
        ✅ 2026-09-24 复核：**该指控已不成立** —— `MetaObjectHandler` 已实现（B2 已销账），
        `VcpMetaObjectHandler.java:47-50` 的 `updateFill` 会填 `updateTime`，字段策略声明在
        `BaseEntity.java:30-32`（`FieldFill.INSERT_UPDATE`），11 个实体继承生效；自定义 SQL
        （如 `VolunteerActivityMapper.java:35`）与 9 处 `update(null, wrapper)` 调用点也都逐条显式写了 `update_time`。
        **残留**：DB 层仍无触发器，`sql/04` 与 `sql/07` 的回填 UPDATE 仍不写它 —— 将来新增自定义 UPDATE 若忘记显式写，仍会失真
      - 全库**零 CHECK、零触发器、零视图** → 状态机流转、时长非负、时间窗口全靠应用层守
      - **无乐观锁列**（无 `version`）→ MyBatis-Plus 的 `@Version` 用不了，
        A3 只能走行锁或 `UPDATE ... WHERE signed_count < max_count` 原子更新
      - **FK 列缺索引 7 处**（PostgreSQL 不为外键列自动建索引，与 MySQL 不同）：
        `duration_audit.duration_id`（已有 128 条流水）、`sys_user_role.role_id`、
        `org_info.contact_user_id` ✅ 已补部分唯一索引、`volunteer_activity.category_id` 等
        ✅ 2026-09-24 复核：6 处已在 `sql/06_backend_gap_fix2.sql:182-187` 建索引
        （`idx_user_role_role` / `idx_activity_category` / `idx_signup_audit_user` / `idx_duration_audit_user` /
        `idx_duration_audit_duration` / `idx_duration_audit_auditor`），第 7 处由 `sql/05` 的部分唯一索引覆盖
      - **看板聚合所需索引缺口**：`attendance_record` 除唯一键外**无任何索引**，
        而 analytics 有 10 个接口要按签到时间聚合它；
        `notification(user_id, is_read)`、`activity_signup(signup_time)`、
        `service_duration(create_time)` 也未建
        ✅ 2026-09-24 复核：4 项均已在 `sql/06` 补上 —— `:156`/`:157`（`attendance_record` 两条）、`:198`、`:200`、`:202`
      - `org_info.org_name`、`activity_category.category_name` 无 UNIQUE；
        `volunteer_activity` 的 `category_id`/`org_id`/`start_time`/`end_time` 均可空

- [ ] **B18 公益等级/标签规则「已拍板但无处落库」**
      **（部分完成：分类→标签映射的脆弱点已修（改按 `code`）；6 档阈值仍是 3 处硬编码、无单一事实来源）**
      6 档阈值只硬编码在 `04_demo_data.sql:271-279` 的 `UPDATE ... CASE` 里，
      **无配置表、无字典类型**；`public_welfare_level` 直接存中文等级名，
      与「状态一律英文码 + `sys_dict` 翻译」的约定不一致。
      最脆弱的一处：**分类→标签映射以中文分类名为键**，而分类名可被
      `PUT /v1/categories/{id}` 改名 → 改名即静默丢标签，DB 层无任何保护。
      在 B10 落地时需确定单一事实来源（方案文档与 SQL 的 CASE 目前是两处重复定义）。
      ✅ 2026-09-24 复核：**该脆弱点已修** —— `PortraitTagEnum.java:92-103` 的 `byCategoryCode` 以 `code` 为键，
      中文名只作兜底（`:114-125`），`:134-137` 的 `resolveTypeTag` 是「有 `code` 用 `code`、没有才按名兜底」。
      **仍缺**：6 档阈值仍是 3 处重复硬编码（`sql/04_demo_data.sql:271-279`、`sql/07_demo_scale.sql:713-721`、
      `PublicWelfareLevelEnum.java:43-58`），无配置表、`sys_dict` 里也没有对应类型；
      `sql/04` 的标签生成仍以中文分类名为键（改名会让演示脚本的数据静默失配）

- [x] **B19 前端契约的隐性要求**（写接口时必须遵守，否则联调即报错）
      - **越权风险（最重要）**：前端把 `studentId` / `orgId` 当查询参数自己传，
        mock 里 `eq(undefined)` 直接放行返回全量。**后端必须以登录态为准、忽略前端传入的
        这两个参数**，否则任意学生可查他人报名与时长
      - **`pageSize` 前端会传 500**（`DurationSubmitView`），不能把上限卡在 100，
        否则"我的时长"汇总与"时长提交"候选名单会缺数据（前端靠全量取回自己算汇总）
      - **静态段路由必须优先匹配**：`/activities/org-overview`、`/portraits/me`、
        `/portraits/distribution`、`/durations/batch-audit`
      - **错误码语义已固化**：`20001` 未登录/过期、`20002` 账号停用 ——
        前端命中即清 token 并跳登录，不要复用这两个码做别的错误
      - `unreadOnly` 可能是布尔 `true` 或字符串 `'true'`；空字符串筛选参数必须忽略
      - **`@OperationLog` 的 `module()` 必须用前端 `LogView.vue` 里写死的中文筛选项**：
        `时长认证 / 志愿活动 / 活动报名 / 用户与权限 / 签到签退 / 志愿组织 / 认证` ——
        前端是中文全等匹配，写错不报错但永远筛不出来。另外切面发出的 `OperationLogEvent`
        里 `username` 通常为空（登录会话只存 roleCode/orgId/studentId），
        **vcp-system 侧的落库监听器需按 `userId` 反查 `sys_user.username` 回填**
      ✅ 已完成（2026-09-24 复核：7 条要求逐条落地 —— 数据范围只认登录态（`SignupServiceImpl.java:84,107,218`、`AttendanceServiceImpl.java:62`、`DurationServiceImpl.java:72-80` 与 `DurationScopeResolver`）；`PageQuery.java:29` 上限 1000，前端传 500 不被截断；四个静态段路由都排在 `/{id}` 之前（`PortraitController.java:52,63`、`ActivityController.java:47`、`DurationController.java:107`）；`ErrorCodeEnum.java:39-40` 固化 20001/20002；`NotificationQuery` 把 `unreadOnly` 当字符串、mapper XML 一律 `!= null and != ''` 忽略空串；`@OperationLog` 的 `module` 与 `LogView.vue:15` 的 7 个中文筛选项逐字一致；`OperationLogListener.java:81-92` 按 `userId` 反查 `sys_user.username` 回填）

- [x] **B20 2026-09-23 集成验证新发现的 4 个问题**（四模块落地后实跑时暴露，原记「均未修」已过时）
      **✅ 2026-09-24 已完成：4 项全部闭环 —— 第 2/3/4 项此前已 ✅；第 1 项 `signed_count` 口径也已收敛
      （选「改脚本对齐实现」这一支，见下）**
      1. **`signed_count` 两处口径不一致**：`vcp-volunteer` 的实现是「提交报名即占名额
         （含 `PENDING`），驳回与取消各释放一个，审核通过不再加」（与前端 mock 一致），
         而 `sql/04_demo_data.sql` 是按「`APPROVED` + `COMPLETED`」回填的 →
         **演示数据的计数与明细对不上**，看板/列表的「已报名/剩余名额」会偏。
         二选一：改实现，或改 `04` 的回填口径并重跑。
        ⚠️ 2026-09-23 追加、2026-09-24 修订：`sql/07_demo_scale.sql` 里**沿用了 `04` 的口径**
        （`signed_count = APPROVED + COMPLETED`），并在末尾自检里断言
        `signed_count <= max_count`；新活动的**名额下限仍是 40**。
        **单场报名上限已由 39 改成 45**（`8 + ((a.id * 7919) % 32)` → `16 + ((a.id * 7919) % 30)`，
        即 16..45）—— 原上限 8..39 导致人均只参与 5.5 场、最高 9 场 ≈ 37.2 小时，
        「五星志愿者（≥40 小时）」一档**恒为空**，末尾自检第 3 项必然失败；
        改后的安全边界是「已结束活动约 85% 的报名变 `COMPLETED`，45 × 0.85 = 38.25 < 40」，
        所以这条断言仍恒成立（自检第 4 项安全）。
        **注意口径本身没变**：`07` 依然沿用 `04` 的「`signed_count = APPROVED + COMPLETED`」，
        所以上面那两条出路（改实现 / 改 `04` 回填口径并重跑）依然成立，
        **改实现时必须同步改 `07` 的自检口径**，否则整批回滚。
        ✅ 2026-09-24 **已销账 —— 选「改脚本对齐实现」这一支，实现侧一行未改**：
        口径定为**「报名即占名额」** = **未取消未驳回的报名都计入**
        （`status IN ('PENDING','APPROVED','COMPLETED')`），驳回与取消各释放一个，
        审核通过**不再加** —— 即 `vcp-volunteer` 运行期实现
        （`SignupServiceImpl.java:130` / `:205` / `:249`）与前端 mock 的行为。
        脚本侧已按此对齐三处：① `sql/04_demo_data.sql:281` 起（第八节「汇总字段回填」）
        的回填口径改成 `status IN ('PENDING','APPROVED','COMPLETED')`；
        ② `sql/02_schema.sql:321` 的 `signed_count` 列注释原写反了（写成「审核通过/取消时维护」），已改正；
        ③ `sql/07_demo_scale.sql` 第十一节 `11.4`（活动已报名人数回填）的口径同上，
        并把**名额下限由 40 抬到 46**（第四节活动 INSERT 的 `40 + ((n.seq * 7) % 36)`
        → `46 + ((n.seq * 7) % 30)`，即 46..75），同时新增 `11.3` 幂等兜底修正
        `UPDATE volunteer_activity SET max_count = 46 WHERE deleted = 0 AND max_count > 0 AND max_count < 46;`
        （只抬不降，重跑安全）。
        **为什么必须 ≥ 46**：单场报名数上限是 45（`LIMIT 16 + ((a.id * 7919) % 30)`），
        新口径下「已发布活动」的占位比例 = 60% `APPROVED` + 35% `PENDING` = **95%**，
        45 × 0.95 = 42.75 > 40 会顶破原名额下限，使 `07` 末尾自检第 4 项
        （`WHERE deleted = 0 AND signed_count > max_count`）失败并整批回滚；
        **名额 ≥ 46 > 45 是硬保证，与状态分布无关**。
        这条结论**取代**了上面 ⚠️ 段里按旧口径算的「新活动的名额下限仍是 40」
        与「45 × 0.85 = 38.25 < 40」两句（旧算法只数 `APPROVED + COMPLETED`，新口径下不再成立）。
        `SignupServiceImpl` 的类注释里「与 `04_demo_data.sql` 的差异」段已改成「口径一致」；
        **实现代码不需要改**，`07` 自检第 4 项的口径说明已同步（断言本身不变，仍是 `signed_count <= max_count`）。
        脚本改动**已于 2026-09-24 在云库实跑生效**。实跑结果（`sql/07_demo_scale.sql`，
        事务内执行、末尾自检全部通过）：自检第 4 项（`signed_count > max_count` 的活动数）实测 **0**；
        全库 `max_count` 范围 **46..75**（下限已抬到 46）；与明细逐场比对 —— 按新口径不一致的活动数
        **0**、按旧口径不一致的有 **36** 场（说明这次确实改了 36 场的计数）；报名 10,719 条中
        占名额 9,201 条，其中 **7** 场活动的已报名数 > 40（最大 **44**，活动 332 / `PUBLISHED`）
        —— 若不抬名额下限，这些场次必然触发自检失败。
      2. ✅ 2026-09-23 **已修**（原：`activity_category` 没有 `remark` 列，
         而前端分类管理页有备注输入框 → 后端 `CategorySaveDTO.remark` 收下但不落库、
         VO 恒返回 null，前端渲染「—」）。
         **这是本次审计里最严重的一条**：页面提示「分类已保存」，用户重填的说明
         刷新即消失 —— 假成功 + 静默丢数据。
         改动：`sql/07` 补列（ALTER → COMMENT → 回填 → 自检四段式）；
         `ActivityCategory` 实体、`CategoryRow`、`ActivityCategoryMapper.xml` 的 SELECT、
         `ActivityCategoryServiceImpl` 的 `createCategory`/`updateCategory`/`toVO` 四处接上；
         `CategorySaveDTO`/`CategoryVO` 的过期注释一并更正。
         **注意 `remark` 在实体上声明了 `FieldStrategy.ALWAYS`**：MyBatis-Plus 默认
         NOT_NULL 策略写不了 null，用户清空 textarea 后旧值会留在库里；声明 ALWAYS 后
         语义变成「按传入值覆写，空串即清空」。该实体的 `updateById` 只有一处调用点，
         所以没有「别处的更新顺手把说明抹掉」的风险 —— 将来新增调用点需重新评估。
      3. **画像等级色调失效**：前端 `PortraitView.vue` 的 `levelTone` 以
         「五星 / 四星 / 三星 / 二星」为键，而接口返回完整等级名「五星志愿者」→
         等级列色调退化成灰色。修法：前端改用接口已返回的 `levelCode`（推荐，不动数据），
         或补全 `levelTone` 的键。
         ✅ 2026-09-23 已按推荐修法改完：`levelTone` 改以 `levelCode` 为键（六档全覆盖），
         模板读 `row.levelCode`。**顺带发现 mock 侧的 `level` 是短名「五星」**——
         它只在 mock 下能命中色调，正是这个 bug 的成因，故一并改成与后端一致的完整等级名
         并补上 `levelCode`。⚠️ 遗留：mock 的等级是按 `i` 循环取的、**与 totalHours 不挂钩**，
         后端是按 `total_duration` 定档（3/6/10/20/40）。要一致就得按时长定档，
         但那会把演示账号（12 小时）从五星降到三星，是否要改请团队定（已写进
         `dataset.js` 的注释）。
      4. **画像标签分布是 8 类不是 6 类**：`/portraits/distribution` 按拍板后的标签体系
         返回 8 类（含新增的「助老服务型」「文化传播型」），而前端页面标题写死
         「六类画像标签分布」，需改文案。
         ✅ 2026-09-23 已改完：文案改「八类」，mock 的 `profiles` 由 6 类补齐到 8 类
         （`desc` 逐字取自 `PortraitTagEnum`），人数仍合计 12,480 ——
         新增两类时**类型标签合计 6,840 不变**，只是按各分类活动数重新分摊；
         玫瑰图的无障碍描述改为从接口数据拼接（原来写死了 6 个标签与人数）；
         `PortraitSeal` 的印章网格由 6 列改 4 列（8 项正好两行，6 列会留 4 个空位）。

      > 另外 `sql/06` 把 6 个分类的 `code` **按名称语义**映射（校园服务→`CAMPUS`、
      > 社区服务→`COMMUNITY`、环保公益→`ENVIRONMENT`、大型赛事→`EVENT`、
      > 助老服务→`ELDERLY`、文化传播→`CULTURE`）—— 因为库与前端 mock 的分类名
      > 本就不一致，见 A12。

- [x] **B20-5 管理员发布的公告没有正文**（2026-09-23 联调新发现）
      `NotificationCreateDTO` 只有 title/type/from/top，**没有 content**；
      而 `NotificationVO` 有 content，学生端公告详情页会渲染它 →
      管理员发的公告学生点进去看不到正文。管理端本页不展示 content，
      所以「管理端看起来是好的」，问题只在跨端暴露。
      ✅ 2026-09-23 **已修**：`NotificationCreateDTO` 加 `content`；
      `NotificationServiceImpl.create()` 照 `createForUser` 的写法落库
      （空串统一存 null）；前端 `NoticeManageView` 的 form/payload/对话框补正文字段；
      **`NotificationDetailView` 原本根本没有渲染 content，已新增一个正文面板**
      （`white-space: pre-wrap` 保留换行）；mock 的 `system.js` 与 `dataset.js` 种子公告同步。
      ✅ 已完成（2026-09-24 复核：`NotificationCreateDTO.java:26` 已有 `content`，`NotificationServiceImpl.java:150,165` 照 `createForUser` 的写法落库（空串存 null）；前端 `NoticeManageView.vue:59,78,192` 与 `NotificationDetailView.vue:137-141,187`（`pre-wrap` 正文面板）已接上；mock 的 `system.js:158-159`、`dataset.js:795-802` 种子公告同步）

- [ ] **B20-6 操作日志「操作对象」列恒空**（2026-09-23 联调新发现，**只做了止血**）
      `operation_log.target` 这一列存在，但**写入侧从来不填**
      （`OperationLogEvent` 注释：「留库时留空，待 `@OperationLog` 增加 target 属性后再填」），
      实测 56 条日志 0 条有该键。而 `LogView` 里既有「操作对象」列、
      搜索框又承诺「操作人或操作对象」，两处都是空承诺。
      ✅ 2026-09-23 **止血**：`LogView` 撤掉该列、placeholder 改成「操作人」。
      ⬅️ **根治仍待做**：给 `@OperationLog` 加 `target` 属性，让各 Controller 传入
      业务对象名（如「分类 · 社区服务」「用户 · stu0030」），
      切面采集后落库，再把这一列加回来。涉及面广，单独排期。
      ⚠️ 2026-09-24：`sql/07_demo_scale.sql` **不覆盖此项**（它只管演示数据规模），
      所以本项**仍单独保留为待办**，别以为跑完 `07` 就顺带解决了。

- [ ] **B21 `StudentInfo.java` 没有 `gender` 字段**（2026-09-24 新发现，低优先级）
      DB 侧 `student_info.gender` 已由 `sql/05_backend_gap_fix.sql` / `06_backend_gap_fix2.sql` 补列，
      但 Java 实体没跟上。**当前没有任何消费方，暂不影响功能** —— 等有页面真正要用「性别」时再补，
      补的时候顺带确认 `StudentVO` 是否需要透出。

---

## C. 前端（`volunteer-cert-portrait-web/`）

> **前端已可脱离后端独立运行**（`npm install && npm run dev`，数据走本地 mock），
> 本节事项**都不阻塞后端开发**。现状与决策理由见 `docs/前端进展与待办.md`。

### C-1 与后端对齐（A9、A10 均已拍板，条目已关闭）

- [x] 2026-09-23 **C1 统一 `sys_user.status` 类型**（依赖 A9）—— ✅ **实测已对齐，无需改代码**
      （三层天然一致：DB `SMALLINT` 1/0、API 英文码、前端 `DICT_DEFS` 英文码；
      实测 `status: "ACTIVE"`，`user_status` 字典齐全且与前端静态定义逐条一致），
      `UserManageView` 的状态列与启用/停用按钮无需再改。
- [x] 2026-09-23 **C2 组织「停用/启用」去留**（依赖 A10）——结论为保留；
      后端已支持 `DISABLED`，前端页面无需再改

### C-2 工程收尾

- [x] **C3 逐页目视核对** —— ✅ 2026-09-24 已完成：28 页**全部**在浏览器里走过（此前只有约 11 页），
      1440 + 620 两视口共 56 张截图并逐张看过。做法：Playwright 驱动系统已装的 Edge
      （不下载 Chromium）打 `npm run preview`（生产构建 + mock，即答辩演示配置），
      脚本在 `volunteer-cert-portrait-web/.tmp-shots/shoot.cjs`（已 gitignore）。
      **客观信号全清**：0 控制台报错 / 0 未捕获异常 / 0 横向溢出 / 0 零尺寸 canvas /
      正文无 `undefined`·`NaN`；5 个图表页的 canvas 数与 `<InkChart>` 标签数一一对上。
      另补做 ECharts 注册表核对（`CLAUDE.md` 前端踩坑第 2 条）无缺口。
      **走查发现 3 个缺陷** → 见下方 C-2b。详见 `docs/前端进展与待办.md` 第二节第 13 条
- [x] **C4 响应式走查** —— ✅ 2026-09-24 已完成（并入 C3 一起走）：620px 断点下 28 页
      **全部无横向溢出**（程序化检测 `scrollWidth > clientWidth`），抽查排版正常
      （控制台侧栏折叠为顶部换行导航、内容转单列）
- [ ] **C5 键盘可达性走查**：已有 `:focus-visible` 与 `.skip` 跳转链接，但未做全站 Tab 走查
- [x] **C6 打包体积** —— ✅ 2026-09-24 核清并处理，**原描述有误**：实测**主 chunk 一直只有 ~57 kB**，
      「超 500 kB」的是**懒加载**的 ECharts chunk，且它**不在** `dist/index.html` 的 modulepreload
      列表里（只在打开图表页时下载）。真正该修的是 `options.js` 的图表配置与 ECharts 被熔进
      同一个 662.7 kB 的 chunk（rolldown 把它命名为 `options`，极易误读成主包）；
      已用 Rolldown 的 `advancedChunks` 拆成 `echarts` 653 kB + `options` 9.9 kB。
      构建警告仍在（ECharts 本身就 653 kB），属预期，未用 `chunkSizeWarningLimit` 掩盖。
      踩坑：Vite 8 底层是 Rolldown，`manualChunks` **传对象会让构建直接失败**，
      正则路径分隔符要写 `[\\/]` —— 见 `CLAUDE.md` 前端踩坑第 8、9 条
- [ ] **C7 自动化测试**：目前无任何测试配置。`npm run verify:mock` 只覆盖数据自洽，
      不覆盖组件与页面
- [ ] **C8 Node 版本**：`package.json` 要求 `^22.18.0 || >=24.12.0`，当前 v22.14.0
      （安装有 `EBADENGINE` 警告，实测构建与运行正常）
      ⚠️ **2026-09-24 复核：原先记的「已完成、实测 v24.20.0」不成立** ——
      本机只有一份 node（`/c/Program Files/nodejs/node.exe`，无 nvm / fnm），
      实测 `node -v` = **v22.14.0**，低于 engines 下限，**`EBADENGINE` 警告当前仍然存在**。
      `build` / `lint` / `verify:mock` 在 v22.14.0 上均实跑通过，但属「碰巧能跑」。
      已补 `.nvmrc`（`24.20.0`，经 npm registry 核实该版本存在且满足 `>=24.12.0`）。
      **仍未销账**：升 node 还是放宽 engines 下限，二选一 —— 属团队决策

### C-2b 走查发现的 mock 缺陷（C3 产出，2026-09-24 新增）

> 三条都是 **mock 数据侧**的问题，真后端不受影响 —— 但 mock 正是「纯前端独立演示」
> （`VITE_USE_MOCK=true`，答辩用）跑的那套，演示时会撞上。
> 共同点：**静态检查看不出来、真跑也不报错**，只有肉眼 / 实测能发现。
> 可执行条目与验收方式见 `docs/下一步待办.md` 第 24~27 项。

- [ ] **C13 个人中心手机号预填脱敏值 → 不改动就存不了**：实测预填 `138****3300`，
      直接点「保存修改」报「请填写 11 位手机号」、**保存失败**，手动清空后才能存。
      成因：`mock/data/dataset.js` 的 `users[].phone` 存的就是脱敏串，`ProfileView.vue:19`
      预填进表单，而 `:31` 的校验是 `/^1\d{10}$/`。真库存完整号码（`13800000003`）、
      `SessionVO.phone` 不脱敏。修法：mock 改存完整号码（与真库一致，mock 不是安全边界）
- [ ] **C14 签到管理「实得时长」与签到签退时间不自洽**：实测签到 `08:45` → 签退 `12:05`
      （差 3 小时 20 分），同一行「实得时长」写 **4 小时**。成因：`dataset.js:913` 写死
      `hours: status === 'SIGNED_OUT' ? activities[0].hours : 0`，取活动声明时长、与钟点无关。
      **后端口径是 `(签退 − 签到)/3600`**（`AttendancePolicy`，2 位小数、封顶 1.5 倍），
      按后端算该行应是 **3.33**。影响：系统核心主题上的数字矛盾，答辩现场一减就看得出来。
      修法：让生成的签到/签退时间正好卡住活动时长（保持 4.00，不动聚合断言）
- [ ] **C15 mock 分类 `remark` 全为空串**：`dataset.js:163` 写死 `remark: ''` →
      分类管理页「说明」列 6 行全 `—`；真库 6 条都有值。修法：填上与真库同风格的说明
- [ ] **C16 `verify:mock` 缺「行内字段一致性」类断言**：现有 21 项只覆盖**聚合之间**
      与**枚举覆盖**，不覆盖**同一行内字段之间的一致性** —— 所以 C14 那种矛盾一直没被拦住。
      建议至少加：① `SIGNED_OUT` 的 `hours` 必须与签到签退时间差一致；
      ② 会被回填进可编辑表单的字段不得是脱敏串；③ `categories[].remark` 非空。
      **修完 C13~C15 必须一起补**，否则同类问题还会再漏

### C-3 业务功能（知识库里的 P1/P2 项，本轮未做）

- [ ] **C9 文件上传**（后端 `attachment` 表已建，前端未实现上传组件）
- [x] **C10 通知正文** —— ✅ 2026-09-23 已修（提交 `996f1ce`）：后端 `NotificationCreateDTO` 补了
      `content` 并照 `createForUser` 的写法落库；前端 mock 的通知数据补上正文，
      `NotificationDetailView` 新增正文面板（`white-space: pre-wrap` 保留换行）。
- [ ] **C11 证书生成**
- [ ] **C12 活动收藏**

---

## D. 交付物（论文 / 答辩）

- [x] 2026-09-24 **D1 ER 图**（《开发计划》第三阶段的明确产出要求）—— ✅ 已产出 `docs/ER图.md`：
      646 行、16 张表、23 条关系（= 18 条物理外键 + 5 条业务关联），Mermaid 已做解析级语法校验
- [ ] **D2 测试用例表**
- [ ] **D3 测试报告**
- [ ] **D4 系统截图**
- [ ] **D5 部署**：`mvn -pl vcp-boot -am clean package` → 可执行 jar；
      前端 `npm run build` → Nginx 托管并反代 `/api`
- [x] 2026-09-24 **D6 演示数据定稿**（答辩现场用的那份，依赖 A8）—— ✅ 已实跑定稿：
      `sql/07_demo_scale.sql` 重跑成功、末尾 6 项自检全绿，实测学生 1500 / 活动 386 /
      报名 10719 / 累计 19319.3 小时，六档等级齐全（普通 118 / 一星 168 / 二星 299 /
      三星 687 / 四星 207 / 五星 21）。
      F4 的 5 条缺口已闭环 4 条（规模、演示账号 `student` 有参与记录、`org_info` 四字段、
      `activity_signup.reason`）；**`operation_log.target` 未闭环，但它属后端代码问题
      （`@OperationLog` 缺 `target` 属性），`07` 覆盖不到，仍单独保留为待办**（见 B20-6）。
      接口实测：`/api/v1/orgs?college=计算机学院` 由**恒 0 条**变成 **2 条**（不筛选 8 条）；
      `/api/v1/categories` 6 条都带上 `remark`；`/api/v1/analytics/dashboard` 活动 386、
      报名 10719、时长 19319.3 小时、签到率 90.1%、审核通过率 75.6%。
      执行顺序：**跑 `07`（已完成）→ `mvn package` → 重启后端**（`07` 第 69 行补
      `activity_category.remark` 列，不重启后端的话分类接口会查不存在的列报错）。

- [ ] **D7 文档同步**（多处文档已过时，写论文引用前需更新）
      - 根 `README.md` ✅ 本批已改完（进度表、`docs/` 结构、PostgreSQL 18.6、演示账号 `org_admin`、
        「下一步待办」一节、`sql/` 脚本清单含 `07`）；仍未改的是 `CLAUDE.md`、web `README.md` 与
        `docs/ER图.md`（缺 `activity_category.remark`）—— 逐条状态见 `docs/下一步待办.md` 附录 B
      - 一致性自检项数**已核实并统一为 20 项**（依据提交 `9424841` 的提交信息原文；
        18 是公益等级与标签规则落地之前的旧数字）—— `docs/后端进展与待办.md`、
        《公益等级与标签规则方案.md》与根 `CLAUDE.md` 本批均已改成 20，四处一致
      - 《公益等级与标签规则方案.md》§二 的人数分布是改进演示数据**前**的旧数，
        文末「落地后的实测效果」才是当前值 —— 引用时别取错
      - 《知识库》§4.3 写"学校端 7 页"，实际是 9 页（多出公益画像、角色管理）

---

## E. 已完成（存档，不用再做）

- [x] 2026-09-22 后端工程可构建可启动（修复了 BOM 循环依赖与 Boot 4 的 aop 改名问题）
- [x] 2026-09-22 数据库 16 张表建成并实跑验证（20 项一致性自检全为 0）
- [x] 2026-09-22 接口文档 `/doc.html` 可用
- [x] 2026-09-22 公益等级与标签规则确认并落地（含演示数据的偏好分类改进）
- [x] 2026-09-22 后端文档体系建立（进展与待办、公益规则方案、CLAUDE.md、对话存档）
- [x] 2026-09-22 **前端 28 个页面全部完成**（学生端 9 / 组织端 6 / 学校端 9 / 登录注册 / 403 / 404），
      纯手写水墨风组件库，不依赖第三方 UI 库；可脱离后端独立运行
- [x] 2026-09-22 前端图表层完成（13 个 ECharts 图表，配色由 CSS 令牌驱动）
- [x] 2026-09-22 前端 mock 层完成（60+ 接口，切换后端只需改 `VITE_USE_MOCK`）
- [x] 2026-09-22 `scripts/verify-mock-data.mjs` 建立，模拟数据 18 项自洽断言
      （这是 **mock 数据**的断言数、与 D7 的「一致性自检 20 项」不是同一回事；该脚本现已扩到 21 项，见 F5）
- [x] 2026-09-22 修复 `bug/` 下记录的缺陷 + 验证中另发现 2 个（注册掉登录态、注册账号不持久化）
- [x] 2026-09-22 **前端状态枚举按后端对齐**（`NOT_SIGNED`、`notification_type` 三值、`audit_action` 的 `SUBMIT`）
- [x] 2026-09-22 前端文档体系建立（进展与待办、CLAUDE.md 前端踩坑小节）
- [x] 2026-09-22 **后端缺口全面审计**（4 路并行：工程现状 / 文档待办 / 前端接口反推 / 数据库闭环），
      结论已落进 A11、B16–B19、D7 与本清单开头的警示块
- [x] 2026-09-23 **B1+B2 基础设施完成并实跑验证**：vcp-common（`R<T>`/`PageResult`/`BaseEntity`/
      错误码体系/10 个状态枚举）+ vcp-framework（Sa-Token 认证、全局异常处理、分页插件、
      字段自动填充、跨域、操作日志切面、Knife4j Bearer 鉴权）。验证方式：编译打包通过、
      应用启动成功（3.3 秒）、`/doc.html` 与 `/v3/api-docs` 返回 200、未登录访问返回 20001

---

## F. 前后端联调（B12）审计与处置 —— 2026-09-23

> 这一节记录联调怎么做的、结论是什么，供复核与写论文引用。
> 待办条目已分别归入 A/B/C 各组，这里只做汇总与留证。

### F1 方法（**全部结论都是实测得来，不是静态阅读**）

| 手段 | 内容 |
|---|---|
| 前端接真后端 | `VITE_USE_MOCK=false`，Vite 代理 `/api` → 真实后端；已实测打通 |
| 三角色真登录打接口 | admin / org_admin / student 各登录后打 92 次接口，响应原样落盘 |
| 契约差分 | 把 `src/mock/data/*` 的 handler 直接跑一遍，与真实响应**逐字段比对** |
| 越权探针 | 8 次：以学生身份读他人报名/时长、以学生身份替他人报名、以组织身份读他组织活动 |
| 逐页字段核对 | 28 个页面读取的每个字段 vs 真实返回形状，按学生/组织/学校三端分工 |

探针脚本与全部响应样本留在 `.tmp-lianTiao/`（`.tmp-*` 已被 gitignore，可复跑）。

### F2 结论：后端代码基本完备，问题不在契约层

- **未发现字段名错位类硬伤**：28 个页面里绝大多数字段与真实返回逐条吻合。
- **权限集一致**：前端 `ROLE_PERMS` 与后端 `StpInterfaceImpl` 三角色逐条相同；
  学生端 9 页 + 组织端 6 页**零 20003 命中**。
- **9 个字典完全一致**：值 / 标签 / 色调与前端 `DICT_DEFS` 逐条相同（含 `user_status`）。
- **B19 的越权风险不存在**：后端一律以登录态为准，忽略前端传入的 `studentId` / `orgId`，
  实测无 IDOR。
- **比率口径一致**：mock 与后端同为 0~1 小数（`signin.rate`、`orgs[].signRate`）。

### F3 两条「疑似 bug」经实测**推翻**（留档，免得后人再查一遍）

1. **`row.tag` 不是字段名写错**：`PortraitVO` 同时有 `tag`（单主标签，供表格一列/印章一枚）
   与 `tags`（全部标签）。后端类注释写明这个分工是有意的，前端读 `tag` 是对的。
   空白的真正原因是**演示学生没有标签**（见 F4）。
2. **`20003` 计入「清 token 跳登录」不是 bug**：`ErrorCodeEnum` 的类注释明确写着
   20001/20002/20003 命中即清 token 跳登录，是**从前端 mock 反推并冻结的契约**。
   但要知道后果：任何页面误调越权接口，表现是**掉线**而不是「某块空白」。

> 这两条正好印证 `CLAUDE.md` 里「数据相关改动必须实跑验证，不能只看静态检查」。

### F4 真正的问题：演示数据缺口（A 类）—— ✅ 2026-09-24 已由 `sql/07_demo_scale.sql` 实跑处理（4/5 条闭环）

1. ✅ **规模差约 500 倍**（真实 156.2 小时 / 149 报名 / 20 场 vs 原型 86,420 / 12,480 / 386）：
   `07` 已把数据放大到目标量级并**实跑达标**：实测学生 1500 / 活动 386 / 报名 10719 /
   累计 19319.3 小时，六档等级齐全（普通 118 / 一星 168 / 二星 299 / 三星 687 / 四星 207 / 五星 21）。
   接口实测：`/api/v1/analytics/dashboard` 返回活动 386、报名 10719、时长 19319.3 小时、
   签到率 90.1%、审核通过率 75.6%。
2. ✅ **演示账号 `student` 完全没有参与记录**（最伤的一条）：0 条时长、0 小时、0 标签、
   等级「普通志愿者」（它那 2 条已通过报名都在**未来**的 10 月活动上，所以没有已完成记录 →
   学生端「我的服务时长」「我的公益画像」是空的）——`07` 已给它补上参与记录，
   末尾自检第 6 项（演示账号必须有可展示的时长与标签）通过。
   根因：`04` 的报名用「偏好分类 + LIMIT」选人，学生 1 一次都没被选中。
3. ✅ **`org_info` 的 `code` / `college` / `founded_at` / `member_count` 全为 NULL/0**：
   `07` 已回填（`OrgVO` 与 `toVO()` 早就实现了这四个字段，是 `03`/`04` 的 INSERT 从没写过它们，
   又因 `default-property-inclusion: non_null` 响应里连键都不存在）——
   「挂靠学院」筛选随之恢复：`/api/v1/orgs?college=计算机学院` 由**恒 0 条**变成 **2 条**
   （不筛选时 8 条）。
4. ✅ **`activity_signup.reason` 从不回填** → `07` 已回填，组织端审核弹窗的「报名理由」
   不再恒显示「学生未填写」。
5. ⬅️ **`operation_log.target` 写入侧从不填** —— **不在 `07` 的覆盖范围内，仍单独待办**，见 B20-6。

处置：`sql/07_demo_scale.sql`（增量，不重建库、幂等、包事务、末尾带 6 项自检）
**已于 2026-09-24 实跑成功，末尾 6 项自检全部通过**。但**首次实跑并不顺利**，
暴露并修掉了**脚本自身的 3 个缺陷 + 1 个幂等性漏洞**（所以**不能说「一次跑通」**）：
① 第 108 行 `DATE '2015-01-01' + ((id * 137) % 2920)` 是 `date + bigint`
（PostgreSQL 无此运算符，只有 `date + integer`），**脚本第一条 UPDATE 就报错**，
已改成 `(((id * 137) % 2920)::int)`；
② 每场报名上限原为 `8 + ((a.id * 7919) % 32)`（8..39），人均只参与 5.5 场、最高 9 场 ≈ 37.2 小时，
「五星志愿者（≥40 小时）」一档**恒为空**，自检第 3 项必然失败；已改为 `16 + ((a.id * 7919) % 30)`
（16..45），名额下限 46 > 报名数上限 45，自检第 4 项恒成立；
③ 新增「十之二、把高活跃学生定向补进五星档」一节：一次报名要过三层折算
（85% COMPLETED × 90% 非缺勤 × 75% 审核通过 ≈ 57%）才成为一条有效时长，每条约 4 小时，
40 小时需约 18 次报名，光靠调大报名数会把总量推到 3 万条（原型只有 12,480），
故改为对累计 32..40 小时的学生定向补 4 场 ≥4 小时的活动；
④ 另修幂等性漏洞：第十节与第十之二节原先没挂 `should_scale` 守卫，
重跑会给学生 1 再补 6 场、五星档持续膨胀。
执行顺序：**跑 `07`（已完成）→ `mvn package` → 重启后端**（`07` 第 69 行补 `activity_category.remark` 列，
不重启后端的话分类接口会查不存在的列报错）。

### F5 前端写死的 mock 数字（B 类）—— 已修完

`HomeView` 与 `DashboardView` 把原型的数字**当文案写死**在图注 / 正文 / `aria-label` 里
（386 场、86,420 小时、15,240 小时、「体育学院 7980 小时最少」、92.3%、2480/2290…），
接真后端后**图里画真数据、旁边文字断言 mock 数**，同一屏自相矛盾；
`aria-label` 更是读屏用户唯一能听到的内容，写死等于报假数。

✅ 已改为全部从接口现算：新增 `src/composables/useDashboardText.js`（两个页面共用，
照 `admin/PortraitView.vue` 的 `distributionLabel` 既有范例）。
另修两处无标签学生渲染成**空印章方块**（`PortraitView` / `MyPortraitView`）。

**验证**：`npm run build` 通过、`verify:mock` 21/21 通过，
另用一个脚本把 composable 喂真实 dashboard 响应跑 17 项 × 3 角色断言全绿。

### F6 环境与工程留证（省得下一个人再踩）

1. **本机 Maven Central 直连返回 403**，阿里云公共仓库可达。
   本次用 `.tmp-lianTiao/settings-aliyun.xml`（gitignored）显式 `-s` 指定，
   **没有改全局 `~/.m2/settings.xml`**。
2. **本机默认 `JAVA_HOME` 指向 JDK 17，而项目要求 JDK 21**（`pom.xml:23`）。
   `C:\Apps\java\jdk-21.0.9+10` 可用，构建时临时覆盖 `JAVA_HOME` 即可。
3. **`04_demo_data.sql` 没有 `BEGIN`/`COMMIT`**：每条语句各自自动提交，
   中途失败会留下半套数据，且重跑会撞唯一约束。
   `07` 已显式包事务；将来若重跑 `04`，建议加 `psql --single-transaction`。
4. **`04` 的报名排序键 `(si2.id*13 + a.id*29) % 37` 只有 37 个取值**：
   学生数超过 37 后大量并列，`LIMIT` 取哪几行由执行计划决定 ——
   三个人在不同机器上会拿到**不同的报名数据**，违背「三人结果一致」的前提。
   `07` 一律改用 `md5(学生id || 活动id)` 或大范围线性式。
5. **`duration_audit` 没有 `audit_time` 列**（只有 `create_time`，建表见 `02_schema.sql:428`），
   写流水时别顺手加上它。
6. 根 `.gitignore` 缺 `application-local.yml` 规则 —— ✅ 2026-09-24 已补，
   见「下一步该做什么」P0 第 4 条。
7. **前端 dev server 的代理目标不要写死**：`vite.config.js` 曾把 target 写死成
   `http://10.0.20.91:8080`（某台机器的内网 IP），而 `.env.development` 的
   `VITE_USE_MOCK=false` 已随提交入库 —— 那台机器一关机或换 IP，所有人的 dev server
   就集体连不上，且 mock 已关、没有后端就跑不起来。
   现改为读 `VITE_API_TARGET`，**默认 `http://127.0.0.1:8080`**；
   联调队友那台时用本地 `.env.local` 覆盖，别改回 `vite.config.js`
   （见「下一步该做什么」第 9 条）。
