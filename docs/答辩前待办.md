# 答辩前待办（2026-09-24 版）

> 本页是**单页执行清单**，只回答「接下来做什么、做到什么算完成」。
> **状态与历史以 `docs/下一步待办.md`（还剩什么、按什么顺序）与 `docs/待办清单.md`（历史总账）为准**，
> 本页每轮收工同步一次，不单独记历史。
> 项目当前在开发计划的**第九阶段（系统测试与数据完善）**，下一步是**第十阶段（部署 / 论文 / 答辩）**。

## 0. 已经就绪，不用再动

- 后端 6 个业务模块全部完成（66 个接口），与前端 64 个调用逐项对上；`mvn package` 11 模块全过。
- 安全三件套完成：BCrypt 密文入库 + 登录失败锁定（5 次 / 15 分钟）+ 本人改密 / 管理员重置。
- 演示数据定稿：学生 1500 / 活动 386 / 报名 10719 / 累计 19319.3 小时，六档等级齐全。
- **一致性自检可跑**：`sql/09_consistency_check.sql`（只读、正好 20 项），2026-09-24 云库实跑违规合计 0。

## 1. 立即（今天，卡着别人）

- [ ] **把云库新口令线下发给两位同学** —— 不更新就连不上库，而且会误以为是代码问题。
      验收：两人能跑通「起后端 → 用种子账号登录」。
- [ ] **让 8080 跑上最新代码** —— 现在跑的是队友的旧实例，`remark` / 公告正文 / `grade` / `phone` 四个修复没生效。
      顺序固定：跑 `sql/07`（已完成）→ `cd volunteer-cert-portrait-server && mvn -B package -DskipTests` → 重启。
      验收：分类管理页能显示并编辑「说明」；公告有正文；学生列表有年级；个人中心显示手机号 / 邮箱。

## 2. 本周（答辩演示的前置）

- [ ] **D5 部署** —— 后端 jar + 前端 `npm run build` + Nginx 托管并反代 `/api`；部署文档也要写。
      验收：在一台干净机器上照文档走一遍能起来；前端路由刷新不 404（history 回退）。
- [ ] **D4 系统截图** —— 仓库里目前 0 张图片。**必须在 D5 之后做**，否则要重截。
      验收：三个角色各一套关键页面截图，命名规整、随仓库提交。
- [ ] **D2 测试用例表** —— 计划书 `:824`~`:853` 已给好范围（功能 / 流程 / 异常），照着列即可。
- [ ] **D3 测试报告** —— 硬缺口已补，报告本体待写。
      验收：引用 `sql/09_consistency_check.sql` 的 20 项结果（实跑 0 违规），并覆盖 D2 的用例结论。
- [ ] **D7 文档同步（剩余两处）** —— `volunteer-cert-portrait-web/README.md:38`、`:110` 仍写「后端尚未开发」；
      `.env.development:2`、`.env.production:3` 的头部注释仍写「后端未就绪 / 尚未开发」。

## 3. 论文与答辩材料

- [ ] 论文五章：需求分析、概要设计、详细设计、系统实现、系统测试。
- [ ] 答辩材料：PPT、演示脚本、演示视频、数据库说明、技术亮点说明、成员分工说明。

## 4. 明确不做（写进论文「已知限制」即可）

- `B13` MapStruct（只有版本声明、无转换器）、`B14` Flyway（待表结构稳定）、
  `B17` 的 CHECK / 触发器 / 视图 / 乐观锁列、`B18` 的阈值配置表（6 档阈值仍硬编码在 3 处）、
  `B21` `StudentInfo.gender`（当前无消费方）、`C9` 文件上传 / `C11` 证书生成 / `C12` 活动收藏（全链路零代码）。
- git 历史里的旧口令：清理需 force push、会波及两位同学的本地仓库，**推迟到答辩之后**。

## 附：可直接引用的证据（论文 / 答辩）

| 结论 | 证据 |
|---|---|
| 20 项一致性自检全 0 | `sql/09_consistency_check.sql`（只读，可当场跑给人看） |
| 演示数据规模与口径 | `sql/07_demo_scale.sql` 末尾 6 项 / 9 条断言 |
| 口令全为密文、明文残留 0 | `sql/08_password_bcrypt.sql` 末尾自检 |
| 等级 / 标签规则落地 | `docs/公益等级与标签规则方案.md` 的两期实测 |
| 后端接口完备性 | `docs/后端进展与待办.md` 第二节（`vcp-system` 42 项断言 + 四模块 20 个请求实测） |
