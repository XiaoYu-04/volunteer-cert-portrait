# 答辩前待办（2026-09-24 版，2026-09-25 补记）

> 本页是**单页执行清单**，只回答「接下来做什么、做到什么算完成」。
> **状态与历史以 `docs/下一步待办.md`（还剩什么、按什么顺序）与 `docs/待办清单.md`（历史总账）为准**，
> 本页每轮收工同步一次，不单独记历史。
> 项目当前在开发计划的**第九阶段（系统测试与数据完善）**，下一步是**第十阶段（部署 / 论文 / 答辩）**。

## 0. 已经就绪，不用再动

- 后端 6 个业务模块全部完成（**73 个「方法 + 路径」/ 60 条路径**，2026-09-26 实查），与前端 **71 个调用**逐项对上；
  `mvn package` 11 模块全过。
- **本轮（2026-09-24）新修复并实测 2 个缺陷**：① 时长可开给「未通过审核」的报名（P0，会让
  `total_duration` 被错误累加且不可撤销）→ 反查 SQL 加报名状态过滤；② 学生注册后不建档案，
  学生端整条链路报 10003 → 注册与管理员新增学生用户时同事务建档。两处均已端到端验证。
- 安全三件套完成：BCrypt 密文入库 + 登录失败锁定（5 次 / 15 分钟）+ 本人改密 / 管理员重置。
- 当前演示数据：**已是全量口径**（2026-09-25 整库重建并回灌）—— `07` 口径 **1500 学生 / 386 活动 /
  10719 报名 / 19319.3 小时**；叠加 `12` 后 **1503 学生 / 389 活动**（报名与时长不变）。
  另有图文演示叠加：5 个演示账号 + 1 个组织 + 3 张带图片的活动（id 387/388/389）；
  学院字典 5 条在位。**两组数字别混用**，详见 `docs/下一步待办.md` 第 35 项。
- **活动图片上传已完成**：封面 + 最多 6 张图文说明，PostgreSQL 存元数据、文件落盘。
- **一致性自检可跑**：`sql/09_consistency_check.sql`（只读、正好 20 项），2026-09-24 云库实跑违规合计 0；
  **2026-09-25 在重灌后的全量库上复跑：检查项总数 20、违规合计 0**。
- **前端 29 页全部已浏览器实测**（C3 / C4 那批是 28 页 × 2 视口 = 56 张截图逐张看过；
  2026-09-24 会话 8 又按**全站 29 页 × 2 视口**（含第 29 页「学院管理」）走查一遍，
  记录 **0 信号**：0 控制台报错 / 0 未捕获异常 / 0 横向溢出 / 0 零尺寸 canvas）。
- **走查发现的 3 个 mock 缺陷已全部修复并验收**（2026-09-24）：`verify:mock` 24 项全过
  （含反向验证）+ 浏览器端到端 7 项全过。详见第 2 节与 `docs/前端进展与待办.md` 第二节第 14 条。
  **2026-09-25 补**：断言已在会话 8 补到 **31 项**，并在升级后的 node **v24.20.0** 上实跑通过
  （C8 随之销账：已升 node、`engines` 不动）。
  ⚠️ 该批截图的存放目录 `volunteer-cert-portrait-web/.tmp-shots/` 已不在仓库中（未入库），
  结论仍成立但**证据不在仓库里**；要引用截图请按第 2 节 D4 重截。

## 1. 立即（今天，卡着别人）

- [x] **把云库新口令线下发给两位同学** —— ✅ **2026-09-26 已关闭（不再需要）**：演示库口径改为
      **明文 `123456`**（写进 `application.yml`，零配置连库）。代价与「答辩后必须轮换」见
      `docs/待办清单.md` 的「明文凭证」一节顶部。
      验收改为：clone 后直接 `cd volunteer-cert-portrait-server && java -jar ...` 能连上库。
- [x] **让 8080 跑上最新代码** —— ✅ 2026-09-24 已完成并**实测四个修复全部生效**。
      事实澄清：这台机器上当时 **8080 没有任何监听、也没有 java 进程**（`netstat` 无 LISTENING），
      「跑的是队友旧实例」指的是队友那台机器，不是本机。
      本轮动作：`mvn -B package -DskipTests`（11 模块 BUILD SUCCESS）→ 本机起新实例（pid 46468）。
      验收证据（真实后端口径）：分类「说明」= 校园服务 / 校园内的日常服务…；公告正文 27 字；
      学生列表年级 = 2022；个人中心 phone=13800000003 / email=student@example.com。
      ⚠️ 该实例是**开发口径**（未带 prod profile），本机演示够用；正式部署按 `docs/部署文档.md` 带 profile 起。
      ✅ **2026-09-25 再更新一次**：云库整库重建后**重新打包并重启** —— `mvn -B package -DskipTests`
      11 个模块 BUILD SUCCESS，`vcp-boot/target/vcp-boot-1.0.0.jar` 重启为 **pid 32936**
      （8080 监听，开发口径即未带 prod profile，**5.0 秒启动**），三角色冒烟实测全过
      （`student` / `org_admin` / `admin` 登录均 `code=0`；`/activities` total=389、
      `/analytics/dashboard` 活动 389 / 报名 10719 / 时长 19319.3 / 签到率 90.1%、
      `/system/users` total=1514）。**收工时 pid 32936 仍在 8080 运行、库里是全量演示数据，可直接演示。**

## 2. 本周（答辩演示的前置）

- [ ] **D5 部署** —— 文档与配置已就绪，**2026-09-25 又补了一轮「本机能实测的都实测掉」**，
      但**仍未在真机上部署过**（团队决定先出文档、机器到位再实操）：
      `docs/部署文档.md` + `deploy/nginx.conf` + `deploy/vcp.service` + **`deploy/verify-deploy.ps1`（新增，验收一键跑）**。
      **本轮已实测（本机，无 nginx）**：① 前端**真后端口径**构建 —— `dist/` 里 mock chunk 彻底消失
      （`grep -rl vcp_mock_extra_users dist/` 无输出），入口 22.93 kB / `request` 113.14 kB /
      `echarts` 652.78 kB，首屏 JS 合计 65.66 kB；② 用一个按 `nginx.conf` 语义实现的 Node 替身实测
      **history 回退**（4 条深层路由 200 且内容 = index.html）、**`/api` 反代**（到后端返回 `code=20001`）、
      **`/uploads` alias**（200 + `image/png`）、`index.html` 的 no-cache 头；
      ③ 验收脚本实跑 **PASS 9 / FAIL 0 / SKIP 4、退出码 0**（反向自检 FAIL 4、退出码 1）；
      ④ 修掉 3 个真问题（`vcp.service` 的 `JAVA_OPTS` 死配置、验收清单里测不出来的 `/doc.html` 那条、
      文档 3.1/3.2 过期内容）。
      **仍未实测（首次部署时优先怀疑）**：`nginx -t` 语法、真 nginx 运行时行为、systemd 单元行为、
      Nginx ↔ 后端真机联调、prod profile 实跑、公网 8080 是否真的连不上、**干净机器照文档走一遍**。
      逐条差异见 `docs/部署文档.md` 第九节（含「替身 vs 真 nginx」对照表）。
      ⚠️ **构建口径**：`dist/` 是**可切换**的 —— 2026-09-25 曾留真后端那一套（`VITE_USE_MOCK=false`），
      **2026-09-26 重新 `npm run build` 之后现存 `dist/` 又是 mock 口径**（含 `mock-*.js` chunk 约 48 KB：
      `.env.production.local` 已不在，入库的 `.env.production` 是 `true`）。要出真后端包，
      先建 `.env.production.local` 写 `VITE_USE_MOCK=false` 再构建。见文档 5.1「坑 7」。
- [x] **B24 学生自助签到闭环** —— ✅ **2026-09-26 完成**：后端新增 `GET /api/v1/attendance/mine`
  （学生本人的签到记录，数据范围只看登录态；`AttendanceVO` 补 `canSignIn` / `canSignOut` 等服务端判定字段），
  前端在「我的报名」行上挂签到 / 签退按钮。接口规模随之 **59 → 60 条路径 / 72 → 73 个操作**、
  前端调用 **70 → 71 个**。
  ⚠️ **现场演示签到的前置条件（务必记住）**：演示库当前**没有任何活动的签到窗口覆盖此刻**
  （`sql/07_demo_scale.sql` 把 PUBLISHED 活动放在 3~17 天后），而且**全新重建的库里演示学生连一条
  `NOT_SIGNED` 签到记录都没有**（种子只为已结束活动上 `COMPLETED` 的报名生成签到记录）——
  因此**真后端口径下「我的报名」的签到按钮不会出现**。现场三条路（2026-09-26 全部实测）：
  ① **（推荐，纯 UI 可达）先造数据、再改时间**：组织端发布一场**明天开始**的活动（报名截止日期填
  **今天**，UI 只支持日期粒度）→ 学生端报名 → 组织端审核通过（此时自动生成 `NOT_SIGNED` 签到记录）→
  组织端**编辑该活动**：开始时间改成**今天「当前时间 + 30 分钟」**、报名截止日期填**昨天**
  （UI 校验与后端校验都会放行 —— 昨天 23:59:59 ≤ 今天的开始时间）→ 保存后签到窗口**立刻开放** →
  学生端「我的报名」点签到 / 签退。实测该步 `GET /attendance/mine` 返回 `canSignIn=true`
  （活动 id 413 探针，已清理）。
  ⚠️ **不能一步到位**：UI 的「报名截止日期」只有日期粒度（`input[type=date]`），**当天开始的活动
  发不出去**（后端把它当 23:59:59 > 开始时间而拒绝）—— 见 B34 记录的既有口径限制。
  ② **接口一条龙**（不想来回改表单时用）：`time = 现在+30 分钟`、`deadline = 现在+20 分钟`
  （完整日期时间，**B33 修复后才支持**）→ 报名 → 审核 → 签到；验收脚本
  `.tmp-verify/verify-attendance-mine.ps1` 的 A1–A8 就是这条路径（**65 PASS / 0 FAIL**）。
  ③ 走**纯前端 mock 口径**（`VITE_USE_MOCK=true` 的 `dist/`）—— mock 刻意不做 30 分钟窗口校验，
  点签到一定能过，无需任何前置。
- [x] **D4 系统截图** —— ✅ **2026-09-25 完成**。验收对照：
      「三个角色各一套关键页面截图」→ `docs/screenshots/{student,org,admin}/` 学生 10 张 /
      组织 8 张 / 管理 10 张，共 **28 张**；「命名规整」→ 沿用 `<页面>-<路由参数>-<视口>.png`，
      并配 `docs/screenshots/README.md` 索引表（文件名/页面/路由/视口/说明/口径）；
      「随仓库提交」→ 已落在 `docs/screenshots/`，等主会话统一提交。
      **体积**：**2.73 MB**（原始 5.89 MB，压掉 53.7%），5 MB 上限 PASS。
      **口径 = 真后端**：389 活动 / 10,719 报名 / 19,319.3 小时 / 签到率 90.1%；
      与 mock 口径（12,480 报名 / 86,420 小时）**不是同一套**，**答辩材料里的数字必须标明是哪一套**。
      ✅ 已覆盖此前两处遗留：「620 视口未走查」「C3 那 56 张未整套重跑」——
      本轮 25 页 × 2 视口、0 信号，其中 620 视口入 6 张作响应式证据。
      ⚠️ 本轮走的是**前端 dev server（`localhost:5173`，`VITE_USE_MOCK=false`）+ 真后端**，
      **未动 `dist/`**；所以「`.env.production` 仍是 `VITE_USE_MOCK=true`」这条**依然成立**，
      D5 构建前仍要改（见上一节）。
- [x] **D2 测试用例表** —— ✅ 2026-09-24 已产出 `docs/测试用例表.md`：
      按计划书 `:824`~`:853` 的功能 / 流程 / 异常三组编排，共 **73 条**用例
      （功能 48 + 流程 10 + 异常 15），预期结果里带真实错误码（取自 `ErrorCodeEnum`）。
- [x] **D3 测试报告** —— ✅ 2026-09-24 已产出 `docs/测试报告.md`：
      引用 `sql/09_consistency_check.sql` 的 20 项结果（**2026-09-24 云库实跑违规合计 0**），
      覆盖 D2 的用例结论，并如实记录 5 项受限 / 未通过项与 4 项未执行项。
      本轮同时修复并验证了 2 个缺陷：时长可开给未通过审核的报名（P0）、注册不建档（P1）。
- [x] **D7 文档同步** —— ✅ 2026-09-24 已**全部完成**：最后两处（`volunteer-cert-portrait-web/README.md:38`、
      `:110` 的「后端尚未开发」/「另有两处尚未对齐」，以及 `.env.development:2`、`.env.production:3`
      的头部注释）已在前端侧提交 `a03e0f1` 修掉。逐条状态见 `docs/下一步待办.md` 附录 B（现全部 ✅）。
- [x] **修 C3 走查发现的 3 个 mock 缺陷**（`docs/下一步待办.md` 第 24、25、27 项）—— ✅ **2026-09-24 已完成**。
      三处都在 mock 数据侧：个人中心手机号改存完整号码、签到管理签退时刻由「签到 + 活动时长」
      推出（恒等于 4.00、聚合数字未动）、分类说明逐字取自 `sql/07`。
      **`verify:mock` 的断言一并补到 24 项**（第 26 项），并做了反向验证 —— 三条新断言跑在
      改动前的数据上全部失败，证明断言有效。
      **2026-09-25 补**：会话 8 又补了 3 条口径断言，现为 **31 项**；在 node **v24.20.0** 上实跑通过。
      验收：`npm run verify:mock` 24 项全过（**现为 31 项全过**）；浏览器端到端 7 项全过
      （个人中心不改动即保存成功、签到页 10 行时长自洽、分类页 6 行说明非空）。
      证据见 `docs/前端进展与待办.md` 第二节第 14 条。

## 3. 论文与答辩材料

- [x] **论文：五章正文 + 摘要 / 绪论 / 结论 / 参考文献 / 致谢 / 附录** —— ✅ 2026-09-25 已全部产出初稿
  - 位置：`docs/论文/`（**11 块正文 + README**）；总览与引用来源清单见 `docs/论文/README.md`
  - 规模：正文 11 块 **83,481 汉字 / 20 个 Mermaid 图**（最初五章 53,632，本日补齐的 6 块约 2.9 万）/ 每条结论带 `文件:行` 出处
  - DOCX：`docs/论文/论文初稿.docx`（**345 KB / 3,236 段 / 220 张表**，按「合稿顺序」并入 11 块），`docs/论文/build_docx.py` 可重跑
  - ⚠️ **DOCX 排版未做视觉确认**：捆绑 LibreOffice 预览渲染中文右侧越界，
    已用最小复现判定为**渲染管线问题而非 DOCX**（5 种字体 × 3 种页边距结果一致；XML 页宽 12240 twips 正确），
    **请用 Microsoft Word 打开核对排版**
  - ⚠️ **口径三套别混用**：`07` 口径 1500 / 386 / 10719 / 19319.3；
    叠加 `12` 后 **1503 / 389**（报名与时长不变）；前端 mock **12480 / 86420**（**不是真库数字**）
  - ⬅️ **仍缺 / 待本人**（见 `docs/论文/README.md` 第五节）：
    ① **参考文献部分完成** —— 29 条中 25 条已在线复核、4 条抄自仓库并标 `*`、**0 条编造**，
       另 5 条出版项标 `〔出版项待核〕`；需按该文件第六节补检（从 `[30]` 起）；
    ② **两块超字数**：`绪论.md` 7,330 汉字（原定 3,500~5,500）、`结论与展望.md` 3,542 汉字（超 42 字）；
       另**英文摘要未做母语润色**；
    ③ **致谢通篇 `【…】` 占位符**待填；
    ④ 学校/学院/专业/班级/学号/姓名/指导教师/完成日期、三人分工的真实姓名对应、院系格式规范、
       **GB/T 7714 用 2015 还是 2025 版**（2025 版已替代 2015 版，现按 2015 版著录）
- [x] **答辩材料** —— ✅ 2026-09-25 已产出 6 份，全部落在 `docs/答辩材料/`：

  | # | 文件 | 规模 | 说明 |
  |---|---|---|---|
  | 1 | `答辩PPT.pptx` | **18 页** / 325.6 KB / 讲者备注 **16,075 字** / 内嵌截图 7 张 | 封面（姓名与指导教师**留占位待本人确认**）→ 背景 → 三角色 → 架构选型 → 数据库 → 七步闭环 → 关键实现 ①②③④⑤ → 系统演示（学生/组织/学校）→ 数据看板 → 测试 → **已知限制 7 条** → 分工总结。**每页都有讲者备注**（要点 + 可能被问到的 Q&A + 数字出处） |
  | 2 | `演示脚本.md` | 389 行 | 三幕（学生 → 组织管理员 → 学校管理员）逐步写清「点哪个菜单 → 点什么 → 预期看到什么」；含**现场可复现闭环**（报名 → 组织审核 → **签到窗口说明** → 组织开时长 → 学校终审 → 学生时长增加 → 画像重算）与**四套降级方案**（后端挂切 mock / 签到窗口未开 / 时间不够 / 误删用户） |
  | 3 | `演示视频分镜.md` | 205 行 | **镜头级** 25 个分镜（镜号/时长/画面/操作/旁白/字幕），总时长 **4 分 20 秒**；含录制环境（1920×1080、账号、起服务命令）与 7 条录制红线。**⚠️ 视频本身未录制，如实标注** |
  | 4 | `数据库说明.md` | 399 行 | 16 张表逐表（用途/关键列/关联/是否逻辑删除）+ 23 条关系 + 索引与约束现状（**如实写明无 CHECK / 无触发器 / 无视图 / 无乐观锁列**）+ 两组数据口径 + 与 `docs/ER图.md` 的 3 处口径差异 |
  | 5 | `技术亮点.md` | 552 行 | **12 条**，每条按「问题 → 方案 → 证据」写，证据给 `文件:行号` / 脚本 / 实测输出 |
  | 6 | `成员分工.md` | 244 行 | 3 人小组按模块与交付物分工；**姓名用「成员 A / B / C」占位并标「待本人确认」**，并写明小组自报姓名与学号的出处、以及指导教师无出处 |

  **校验证据**：`check_office.py` → `verdict: pass`、`slides: 18`；捆绑 LibreOffice 逐页渲染
  **18/18 页 0 越界、`missingFonts` 为空**；python-pptx 遍历确认 **18/18 页有讲者备注**。
  **PPT 的 7 张内嵌图全部取自 `docs/screenshots/`（真后端口径），无文字占位。**
  **未做项**：演示视频未录制（脚本已就绪）；姓名与指导教师留占位。

## 4. 明确不做（写进论文「已知限制」即可）

- `B13` MapStruct（只有版本声明、无转换器）、`B14` Flyway（待表结构稳定）、
  `B17` 的 CHECK / 触发器 / 视图 / 乐观锁列、`B18` 的阈值配置表（6 档阈值仍硬编码在 3 处）、
  `B21` `StudentInfo.gender`（当前无消费方）、`C9` 的组织资质/头像上传剩余场景、
  `C11` 证书生成 / `C12` 活动收藏（活动图片上传已完成）。
- git 历史里的旧口令：清理需 force push、会波及两位同学的本地仓库，**推迟到答辩之后**。
- ✅ **2026-09-25 新发现的缺陷已修复**（见 `docs/下一步待办.md` 第 41 项 / `docs/待办清单.md` 的 B31）：
  ~~**删除用户不级联**~~ —— `DELETE /api/v1/system/users/{id}` 原先只置 `sys_user.deleted = 1`，
  该学生的 `student_info` / 报名 / 时长仍留在库里并**继续计入看板**（复现时实测 1503→1504 学生、
  10719→10720 报名、19319.3→19321.8 小时，而 `sql/09` 20 项仍全 0）；
  现已**同事务级联软删**四张表并按 `sql/09` 第 ⑦ 项口径重算 `signed_count`，
  重跑 `verify.ps1` 看板**跑前跑后不漂**、`sql/09` 仍 20 项 0。
  **演示时仍建议不要删用户**（删了不会漂，但会永久少一个演示账号）。

## 附：可直接引用的证据（论文 / 答辩）

| 结论 | 证据 |
|---|---|
| 20 项一致性自检全 0 | `sql/09_consistency_check.sql`（只读，可当场跑给人看）；2026-09-24 与 **2026-09-25** 两次云库实跑均为「检查项总数 20，违规合计 0」 |
| 测试用例与结果 | `docs/测试用例表.md`（73 条）+ `docs/测试报告.md`（含 5 项受限项如实记录） |
| 端到端闭环可复现 | `.tmp-verify/verify.ps1`（真实后端口径；2026-09-25 修好并实跑 **PASS 30 / SKIP 2 / FAIL 0**）—— ⚠️ 脚本未入库，且**跑完会留下测试残留**（见第 4 节），环境口径要一起说 |
| 部署步骤与验收 | `docs/部署文档.md` + `deploy/`（Nginx + history 回退 + 验收清单 + 故障排查表） |
| 演示数据规模与口径 | `sql/07_demo_scale.sql` 末尾 6 项 / 9 条断言（2026-09-25 实跑原样通过：学生 1500 / 活动 386 / 报名 10719 / 累计 19319.3 小时）；叠加 `12` 后 1503 学生 / 389 活动，**两组别混用** |
| 口令全为密文、明文残留 0 | `sql/08_password_bcrypt.sql` 末尾自检 |
| 等级 / 标签规则落地 | `docs/公益等级与标签规则方案.md` 的两期实测 |
| 后端接口完备性 | `docs/后端进展与待办.md` 第二节（`vcp-system` 42 项断言 + 四模块 20 个请求实测） |
| 论文五章初稿与引用来源 | `docs/论文/`（README 含**引用来源清单**，逐条 `文件:行`）；合并稿 `docs/论文/论文初稿.docx`（164 页，结构校验 pass） |
| 系统截图（三个角色） | `docs/screenshots/{student,org,admin}/`（**28 张 / 2.73 MB**，真后端口径；索引与口径警示见 `docs/screenshots/README.md`） |
