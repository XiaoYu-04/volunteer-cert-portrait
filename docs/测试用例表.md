# 系统测试用例表（D2）

> 范围与分类照 `docs/高校志愿服务时长认证与公益画像数据分析系统_开发计划与分工.md`
> 第九阶段（约 812~877 行）的要求编排：**功能测试 / 流程测试 / 异常测试**三组。
> 本表只列**用例与预期结果**；实际执行结果与证据见 `docs/测试报告.md`。
>
> 预期结果里的错误码取自 `vcp-common/.../ErrorCodeEnum.java`，是后端真实返回的码值，
> 答辩时可直接对照。

## 一、测试环境与执行口径

| 项 | 取值 |
|---|---|
| 后端 | Spring Boot 4.1.1 / Java 21，端口 8080，连接云上 PostgreSQL 18.6 |
| 前端 | Vue 3 + Vite，`npm run dev`（开发口径）或 Nginx 托管 `dist/`（部署口径） |
| 演示账号 | `student`（学生）/ `org_admin`（组织管理员）/ `admin`（学校管理员），口令均为 `123456` |
| 数据规模 | 当前小型图文演示：5 个演示账号 / 3 个活动 / 3 张图片；全量 1500 学生规模可按 `sql/07` 恢复 |
| 数据自检 | `sql/09_consistency_check.sql`（只读，20 项） |

**执行方式的两种口径**（结论必须标明是哪一种）：

- **真实后端**：前端请求经 `/api` 打到 8080，数据与云库一致。
- **纯前端 mock**：`.env.production` 的 `VITE_USE_MOCK=true`，走 `src/mock` 本地数据。
  ⚠️ mock 的数字（12,480 报名 / 86,420 小时）与真实库**不是同一套**。

## 二、功能测试

### 2.1 登录与账号（计划书「登录测试」）

| 编号 | 角色 | 前置条件 | 操作步骤 | 预期结果 |
|---|---|---|---|---|
| TC-F-01 | 学生 | 库中存在 `student` 账号 | 用正确口令登录 | 返回 `code=0` 与 token，进入学生首页 |
| TC-F-02 | 组织管理员 | 同上 | 用正确口令登录 | 进入组织端看板 |
| TC-F-03 | 学校管理员 | 同上 | 用正确口令登录 | 进入管理端看板 |
| TC-F-04 | 任意 | 未登录 | 直接访问任一受保护接口 | 返回 `20001`「登录已过期，请重新登录」 |
| TC-F-05 | 任意 | 未登录 | 浏览器直接打开受保护前端路由 | 被路由守卫拦回登录页 |
| TC-F-06 | 任意 | 账号 `student` | 用错误口令登录 | 返回认证失败，提示口令错误 |
| TC-F-07 | 任意 | 账号 `student` | 连续输错口令 5 次 | 第 5 次起返回 `20004`「账号已被锁定，请稍后再试」 |
| TC-F-08 | 学生 | 本人已登录 | 修改本人资料（手机号/邮箱） | 保存成功，重新进入页面可见新值 |
| TC-F-09 | 学生 | 本人已登录 | 本人改密：原密码正确、新密码符合格式 | 改密成功，其它会话被踢下线，当前会话保留 |
| TC-F-10 | 学生 | 本人已登录 | 本人改密：原密码填错 | 返回 `20005`「原密码不正确」 |
| TC-F-11 | 学校管理员 | 已登录 | 在用户管理重置某用户口令 | 重置成功，该用户全部会话被踢下线 |
| TC-F-12 | 学校管理员 | 已登录 | 新增一个学生角色的用户 | 新增成功，该账号可用默认口令登录 |
| TC-F-13 | 学校管理员 | 已登录 | 停用某用户后，用该账号登录 | 返回 `20002`「账号已停用，请联系学校管理员」 |

### 2.2 活动与报名（计划书「报名测试」）

| 编号 | 角色 | 前置条件 | 操作步骤 | 预期结果 |
|---|---|---|---|---|
| TC-F-14 | 组织管理员 | 已登录 | 发布一个新活动 | 发布成功，状态为「报名中」 |
| TC-F-15 | 组织管理员 | 已登录 | 编辑活动的说明、名额 | 保存成功，列表回显新值 |
| TC-F-16 | 学生 | 已登录 | 浏览活动列表并查看详情 | 列表与详情正常返回，无报错 |
| TC-F-17 | 学生 | 活动处于报名中且未满 | 提交报名 | 报名成功，状态为「待审核」，活动已报名数 +1 |
| TC-F-18 | 学生 | 已有该活动的有效报名 | 再次提交报名 | 返回 `30007`「你已报名该活动，请勿重复提交」 |
| TC-F-19 | 学生 | 活动名额已满 | 提交报名 | 返回 `30006`「名额已满」 |
| TC-F-20 | 学生 | 活动处于「已结束/已关闭」 | 提交报名 | 返回 `30005`「该活动当前不接受报名」 |
| TC-F-21 | 学生 | 已报名且未被审核 | 取消报名 | 取消成功，状态变「已取消」，名额释放 |
| TC-F-22 | 学生 | 报名对应活动已完成 | 取消报名 | 返回 `30010`「已完成的活动无法取消」 |
| TC-F-23 | 学生 | 已登录 | 查看「我的报名」列表 | 只返回本人的报名，不出现他人记录 |
| TC-F-24 | 组织管理员 | 已登录 | 查看本组织报名列表 | 只返回本组织活动的报名 |

### 2.3 审核（计划书「审核测试」）

| 编号 | 角色 | 前置条件 | 操作步骤 | 预期结果 |
|---|---|---|---|---|
| TC-F-25 | 组织管理员 | 存在待审核报名 | 审核通过 | 成功，报名状态变「已通过」，自动生成签到记录，学生收到通知 |
| TC-F-26 | 组织管理员 | 存在待审核报名 | 审核驳回 | 成功，状态变「已驳回」，名额释放 |
| TC-F-27 | 组织管理员 | 报名已审核过 | 再次审核同一条 | 返回 `30009`「该报名已审核，无法重复操作」 |
| TC-F-28 | 组织管理员 | 已登录 | 对非本组织的报名执行审核 | 返回 `20003`「没有操作权限」 |
| TC-F-29 | 学校管理员 | 存在待审核组织 | 审核组织资质 | 成功，组织状态变为已通过 |
| TC-F-30 | 学校管理员 | 组织已审核过 | 再次审核 | 返回 `10006`「该组织已审核，无法重复操作」 |
| TC-F-31 | 组织管理员 | 已登录 | 尝试审核组织资质 | 返回 `20003`（该操作仅学校管理员可用） |

### 2.4 签到（计划书「签到测试」）

| 编号 | 角色 | 前置条件 | 操作步骤 | 预期结果 |
|---|---|---|---|---|
| TC-F-32 | 组织管理员 | 报名已通过，存在签到记录 | 在签到管理页执行签到 | 成功，状态变「已签到」，记录签到时间 |
| TC-F-33 | 组织管理员 | 已签到 | 执行签退 | 成功，状态变「已签退」，记录签退时间 |
| TC-F-34 | 组织管理员 | 未签到 | 直接执行签退 | 返回 `30013`「请先签到再签退」 |
| TC-F-35 | 组织管理员 | 已登录 | 查看签到列表与状态筛选 | 列表正确，异常状态可人工修正 |
| TC-F-36 | 学生 | 已登录 | 在页面自助签到 | **当前学生端无入口**（后端 `POST /api/v1/attendance/sign-in` 需先取得 `attendanceId`，而该 id 只出现在管理员可访问的签到列表）——列为已知限制 |

### 2.5 时长审核（计划书「时长审核测试」）

| 编号 | 角色 | 前置条件 | 操作步骤 | 预期结果 |
|---|---|---|---|---|
| TC-F-37 | 组织管理员 | 学生报名已通过 | 提交服务时长（含活动、学生、时长、证明） | 提交成功，状态「待审核」 |
| TC-F-38 | 学校管理员 | 存在待审核时长 | 审核通过 | 成功，学生 `total_duration` 增加对应小时数 |
| TC-F-39 | 学校管理员 | 存在待审核时长 | 审核驳回 | 成功，学生累计时长不变 |
| TC-F-40 | 学校管理员 | 已登录 | 批量审核多条时长 | 成功，逐条结果正确 |
| TC-F-41 | 学校管理员 | 时长已审核过 | 再次审核同一条 | 返回 `40002`「该记录已审核，无法重复操作」 |
| TC-F-42 | 学校管理员 | 已登录 | 查看时长审核汇总（三态条数） | 汇总数与列表条数一致 |
| TC-F-43 | 组织管理员 | 已登录 | 提交时长时选择非本组织活动的学生 | 返回 `20003` 或 `30008`（越权/无有效报名） |

### 2.6 公益画像与数据看板（计划书「数据看板测试」）

| 编号 | 角色 | 前置条件 | 操作步骤 | 预期结果 |
|---|---|---|---|---|
| TC-F-44 | 学生 | 有累计时长 | 打开「我的公益画像」 | 展示累计时长、公益等级、标签；等级与 `<3/3-6/6-10/10-20/20-40/≥40` 六档口径一致 |
| TC-F-45 | 学生 | 无累计时长（0 小时） | 打开「我的公益画像」 | 等级为「普通志愿者」（0 小时落在 `<3` 档），页面不报错 |
| TC-F-46 | 学校管理员 | 已登录 | 打开画像管理页，按标签/学院筛选 | 列表与筛选生效，分页正确 |
| TC-F-47 | 学校管理员 | 已登录 | 打开数据看板 | 概览、趋势、类型占比、学院排名、组织活跃度、标签分布、审核三态、签到率、热力图均正常渲染 |
| TC-F-48 | 任意 | 已登录 | 打开首页看板并观察数字 | 数字与库中数据一致（真实后端口径） |

## 三、流程测试（端到端闭环）

计划书给定的流程（约 833~844 行）：

> 学生注册 → 学生报名 → 组织审核通过 → 学生签到签退 → 组织提交时长 → 学校审核通过 → 学生时长增加 → 看板数据更新

| 编号 | 步骤 | 操作 | 预期结果 |
|---|---|---|---|
| TC-E-01 | ① 学生注册 | 在 `/register` 注册一个新学生账号 | 注册成功并自动登录，进入学生首页 |
| TC-E-02 | ① 校验 | 用新账号打开「我的报名」「我的时长」「我的画像」 | 三个页面均可用（不出现 `10003`「当前账号未关联学生档案」） |
| TC-E-03 | ② 学生报名 | 用新账号报名一个报名中的活动 | 报名成功，状态「待审核」 |
| TC-E-04 | ③ 组织审核 | 用 `org_admin` 审核通过该报名 | 成功，状态「已通过」，生成签到记录 |
| TC-E-05 | ④ 签到签退 | 对该报名执行签到、签退 | 两步均成功，时间被记录 |
| TC-E-06 | ⑤ 提交时长 | 用 `org_admin` 为该学生提交服务时长（如 2.0 小时） | 提交成功，状态「待审核」 |
| TC-E-07 | ⑥ 学校审核 | 用 `admin` 审核通过该时长 | 成功 |
| TC-E-08 | ⑦ 时长增加 | 用新账号查看「我的时长」与「我的画像」 | 累计时长增加 2.0 小时，等级按新时长更新 |
| TC-E-09 | ⑧ 看板更新 | 用 `admin` 打开数据看板 | 相关统计数字随之变化（报名数、时长合计） |
| TC-E-10 | 回归 | 用 `student` 种子账号重复 ②~⑧ | 同样走通，且种子账号原有时长不被破坏 |

## 四、异常测试（计划书给定 6 条 + 补充）

| 编号 | 场景 | 操作 | 预期结果 |
|---|---|---|---|
| TC-X-01 | 未登录访问页面（计划书） | 未登录直接打开受保护前端路由 | 被拦回登录页，不泄露任何数据 |
| TC-X-02 | 重复报名（计划书） | 同一学生对同一活动二次报名 | `30007`，库中不产生第二条有效报名 |
| TC-X-03 | 活动已满继续报名（计划书） | 对名额已满的活动报名 | `30006`，不超卖（`signed_count` 不超过 `max_count`） |
| TC-X-04 | 未签到直接签退（计划书） | 未签到状态下签退 | `30013` |
| TC-X-05 | 时长重复提交（计划书） | 对同一报名二次提交时长 | 被拒（待审核中提示「请勿重复提交」；已通过提示无法重复提交） |
| TC-X-06 | 审核驳回后再次提交（计划书） | 时长被驳回后重新提交 | 允许，重新进入待审核 |
| TC-X-07 | 越权访问（补充） | 学生调用管理员接口 | `20003` |
| TC-X-08 | 越权改他人数据（补充） | 学生取消他人的报名 | `30008`（按「不存在」处理，不泄露他人记录是否存在） |
| TC-X-09 | 资源不存在（补充） | 查询不存在的活动 / 报名 / 时长 | `30001` / `30008` / `40001` |
| TC-X-10 | 参数不合法（补充） | 提交缺必填项、时长非正数、时间区间倒挂 | `10001`「参数不合法」 |
| TC-X-11 | 无档案学生（补充） | 学生账号无学生档案时调用学生端接口 | `10003`「当前账号未关联学生档案，请联系学校管理员」 |
| TC-X-12 | 画像未生成（补充） | 查询尚无画像快照的学生画像 | `50001`「画像尚未生成」，前端展示空状态 |
| TC-X-13 | 重复点击（补充） | 快速重复提交同一审核操作 | 第二次返回已审核类错误码（`30009` / `40002`），数据不被重复计算 |
| TC-X-14 | 分类约束（补充） | 新增重名分类 / 删除仍有活动的分类 | `30002` / `30004` |
| TC-X-15 | 口令安全（补充） | 直接查库看 `sys_user.password` | 全部为 BCrypt 密文，无明文残留（`sql/08` 末尾自检 0 条） |

## 五、活动图片补充用例（2026-09-24）

| 用例 | 场景 | 预期结果 |
|---|---|---|
| TC-IMG-01 | 组织管理员上传 JPG/PNG | `code=0`，返回 `fileUrl/fileName/fileSize/contentType` |
| TC-IMG-02 | 上传非图片或伪扩展名文件 | `10001`，不落盘 |
| TC-IMG-03 | 上传超过 5MB 图片 | `10001`，提示大小限制 |
| TC-IMG-04 | 创建活动时提交封面和 1~6 张图片 | 活动与 `attachment` 同事务保存，详情返回 `cover` 与 `images[]` |
| TC-IMG-05 | 更新活动时传 `images=[]` | 清空活动图片关联，不删除磁盘文件 |
| TC-IMG-06 | 学校管理员上传图片 | `code=0`（`activity:create OR update`） |
| TC-IMG-07 | 未登录或学生上传 | `20001` / `20003` |
| TC-IMG-08 | 直接访问 `/uploads/...` | HTTP 200，带 `X-Content-Type-Options: nosniff` |

## 六、测试数据（对照计划书要求）

| 计划书要求 | 实际 | 结论 |
|---|---|---|
| 3 个角色账号 | `student` / `org_admin` / `admin` | ✅ |
| 5~10 个组织 | 见 `sql/03_init_data.sql` + `sql/04_demo_data.sql` | ✅ |
| 10~20 个活动 | 386 个 | ✅ 超出 |
| 若干学生账号 | 1500 个 | ✅ 超出 |
| 50~200 条报名数据 | 10,719 条 | ✅ 超出 |
| 对应签到和时长数据 | 由 `sql/07_demo_scale.sql` 生成，含六档等级齐全 | ✅ |

> 说明：演示数据规模**刻意超出**计划书要求，目的是让看板与画像的图表有足够层次
> （六档公益等级齐全、学院排名不退化）。
