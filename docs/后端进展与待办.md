# 后端进展与待办

> **范围声明**
>
> 本文档**只涉及后端**（`volunteer-cert-portrait-server/` 与仓库根目录 `sql/`）。
> **前端（`volunteer-cert-portrait-web/`）无需参与**，也不需要为本文档中的任何事项做配合改动。
> 文末附有一份状态枚举码表，仅因前端渲染中文标签时会用到而列出，属**参考信息，不是待办**。

- 整理时间：2026-09-23（**2026-09-24 增补**：第二节第 10 条；口令 BCrypt 加密（B15）与
  「本人改密 / 管理员重置 / 登录失败锁定」三件套；第四节 P1 表第 11~12 行；
  新增只读脚本 `sql/09_consistency_check.sql`（20 项一致性自检）并接入第四、六节）
  （**2026-09-24 下半场再增补**：第二节第 11 条 —— 两个缺陷修复（B22 / B23）与端到端实测；
  第四节 P1 表第 13~18 行；第三节决策表新增「时长只能开给已通过审核的报名」一行）
  （**2026-09-24 下半场（第四轮）再增补**：第二节第 12 条 —— 学院字典管理接口 4 条、
  `OrgCollegeCountPort` 端口与 `DictService.containsEnabled`；「管理员新增用户」路径补学院；学院管理端 41 项脚本实测）
  （**2026-09-24 第七轮增补**：第二节第 13 条 —— 活动图片上传落地：文件落盘 + PostgreSQL
  只存附件元数据，新增 `POST /api/v1/attachments/images`；活动支持封面与最多 6 张图文说明；
  SQL 新增 `11_activity_images.sql`、`12_activity_images_demo.sql`）
- 对应提交：`161e16c`、`a467176`、`53cf140`、`9424841`（数据库脚本）；
  `e1405b2`、`a447460`、`ebde572`、`2877b65`（后端模块）；
  `0704662`、`f556683`（凭证外置与口令轮换）；`4752214`（前端按后端对齐）；
  `1138eaa`（`sql/07` 缺陷修复并实跑）、`e441135`（文档与 ER 图）、`7b362f3`（`.gitignore` 与前端代理）
  （2026-09-24 续）：`f818da7`（B15 口令加密与登录防护）、`4698739`（B20-1 `signed_count` 口径收口）、
  `e545ba9`、`e7275b5`（待办清单复核与销账）、`d7e5ef0`（会话记录）
  （2026-09-24 下半场的工作**尚未提交**，留在工作区：2 处代码修复 + 5 份文档 + `.env.production`，
  见 `docs/会话记录.md` 会话 3）

---

## 一、当前状态总览

| 项目 | 状态 |
|---|---|
| 后端工程能否构建 | ✅ 可以（`mvn package` 通过，11 个模块全过） |
| 后端应用能否启动 | ✅ 可以（`java -jar` 启动成功，Tomcat 监听 8080；2026-09-24 重新打包后重启，PID 157500，启动耗时 6.6 秒）。**2026-09-24 下半场再次打包（11 模块 BUILD SUCCESS）并起新实例（PID 46468）**，端到端实测通过；该实例是**开发口径**（未带 prod profile），正式部署按 `docs/部署文档.md` 带 profile 起 |
| 数据库 | ✅ 已建成并验证（16 张表，20 项一致性自检全部通过；`04` 基线 843 行；`activity_category.remark` 已由 `07` 补列并回填 6 条；20 项自检已固化为 `sql/09_consistency_check.sql`，2026-09-24 云库实跑违规合计 0） |
| 演示数据 | ⚠️ **不再是 1500 / 386 的全量演示库**：当前为「基础数据 + 5 个图文演示账号 + 1 个演示组织 + 3 个已发布活动 + 3 张活动图片」。新增账号为 `demo_stu_01`~`demo_stu_03`、`demo_org_01`、`demo_school_01`，口令均为 `123456`；由 `sql/12_activity_images_demo.sql` 可重复执行生成 |
| 接口文档 | ✅ 可用（`/doc.html`） |
| **业务代码** | ✅ **6 个业务模块全部落地**（2026-09-23）：`vcp-system`、`vcp-org`、`vcp-volunteer`、`vcp-certification`、`vcp-portrait`、`vcp-analytics` |

**一句话结论**：地基已打好，6 个业务模块已全部落地；`mvn package` 11 个模块全过，
启动后用三个角色实打 20 个接口、**20 个全部符合预期**（含 1 个刻意的 20003 权限拒绝），
日志零异常。见第二节第 8 条。
**2026-09-24 补**：`sql/07` 的三处缺陷已修并实跑通过，演示数据放大到「学生 1500 /
活动 386 / 报名 10719 / 累计 19319.3 小时」，六档等级齐全；后端已重新打包重启（PID 157500）。
见第二节第 10 条。
**2026-09-24 再补**：口令已全部改为 BCrypt 密文（B15），并做完「本人改密 / 管理员重置 / 登录失败锁定」
三件套（见第四节 P1 表第 12 行）；老库需补跑 `sql/08_password_bcrypt.sql`（新库不用）。
下一步是**收尾项 Flyway（B14）** 与 **P2 交付物**（测试用例表、测试报告、系统截图、部署、
论文与答辩材料，见 `docs/下一步待办.md` 的「P2 —— 答辩前必须产出（D 组交付物）」）。
**与前端联调（B12）已于 2026-09-23 审计通过**；**活动图片上传已于 2026-09-24 完成**，
组织资质材料与用户头像上传仍未接入。
**2026-09-24 下半场补**：一次只读摸底（后端代码 / 数据库 / 前端契约）后**修掉 2 个缺陷** ——
时长可开给「未通过审核」的报名（P0）、学生注册不建档案（P1，学生端整条链路报 10003）；
两处均已端到端实测（见第二节第 11 条）。摸底另发现 6 项未完成（学生自助签到缺两环、
取消活动不处理报名、`PENDING_SUBMIT` 无生产者、默认口令硬编码、组织列表 N+1、后端零自动化测试），
已编号落进 `docs/待办清单.md` 的 **B24~B29**。
**2026-09-24 第四轮补**：学院字典有了**管理端** —— `vcp-system` 新增 4 个接口
（`GET` / `POST /api/v1/system/colleges`、`PUT /api/v1/system/colleges/{id}/status`、`DELETE /api/v1/system/colleges/{id}`），
列表带的学生数 / 组织数走「端口接口 + 实现」（`OrgCollegeCountPort`，实现落在 `vcp-org`，依赖方向仍是 `vcp-org → vcp-system`）；
同时补齐 **B30 的「管理员新增用户」路径**（`UserSaveDTO.college` + `DictService.containsEnabled` + `ensureArchive(userId, college)`）。
见第二节第 12 条。
**2026-09-24 第七轮补**：活动图片上传落地 —— 图片本体写文件系统，PostgreSQL 只存
`attachment` 元数据；活动发布页支持 1 张封面 + 最多 6 张带说明图片，详情页返回图集。
当前 OpenAPI 为 **60 条路径 / 73 个操作**（2026-09-26 起；B24 新增
`GET /api/v1/attendance/mine`），前端接口调用 **71 个**，仍为 0 缺失。
见第二节第 13 条。

---

## 二、已完成的工作

### 1. 修复后端工程（此前完全无法构建）

| 问题 | 说明 |
|---|---|
| **依赖管理循环（致命）** | `vcp-dependencies` 作为 BOM 被聚合根导入，而它的 `parent` 又指向聚合根，互相引用成环。Maven 直接拒绝读取整个工程，11 个模块一个都读不出来。已改为独立 BOM（不继承聚合根），并在 pom 里加注释说明**不能改回去** |
| **`spring-boot-starter-aop` 不存在** | Boot 4 已将其改名为 `spring-boot-starter-aspectj`，旧坐标在 4.x 下没有发布（最后版本停在 3.5.16）。已修正 |
| **启动类缺失** | pom 里配了 `mainClass=com.vcp.VcpApplication`，但模块里只有 IntelliJ 模板 `Main.java`。注意：这不影响构建（repackage 照样成功），只在 `java -jar` 时才 ClassNotFound。已补上启动类 |
| **10 个模板 `Main.java`** | IntelliJ 自动生成的 `Hello and welcome!` 模板，10 个文件内容完全相同，会让每个 jar 里都出现 `com.vcp.Main`。已全部删除 |
| **无 `.gitignore`** | 构建产物 `target/`（33MB）会被误提交。已补 |

### 2. 配置文件 `application.yml`

已配好数据源与连接池、MyBatis-Plus、Sa-Token、Knife4j、日志。其中三处值得注意：

- **凭证已外置（2026-09-23 改）**：原先把云数据库口令直接写在 `application.yml`，而仓库是公开仓库，口令因此进了 git 历史。现改为**环境变量 → `application-local.yml`（已 gitignore）** 两级读取，`application.yml` 里只剩占位符，模板见同目录 `application-local.yml.example`。已实测：重新打包启动后调 `/api/v1/auth/login` 返回 200 并拿到真实 token。**注意两点**：①改文件不等于修好，只有轮换口令才能让历史里的旧口令失效（**2026-09-23 已轮换**，旧口令实测被服务端拒绝，见第二节第 9 条）；②**缺凭证不会启动失败**（实测确认，`@ConfigurationProperties` 对解析不了的占位符不报错），要到第一次访问数据库才暴露，排查连不上库时先看 `application-local.yml` 是否存在
- **口令必须加引号**：`application-local.yml` 里的口令不加引号会被 YAML 当作**八进制整数**解析（实测 `012345` → `5349`），口令就错了
- **`id-type` 必须是 `auto`**：MyBatis-Plus 默认是 `assign_id`（雪花 ID），会由应用生成主键、绕过数据库序列，与建表脚本的 `BIGSERIAL` 以及初始数据里显式写入的 `id=1,2,3` 冲突

### 3. 接口文档 Knife4j

已接入并**实测通过**：`/doc.html` 返回 200、`/v3/api-docs` 的标题/描述/版本正确、`/knife4j/config` 返回 200。

一个坑记录在此：`knife4j.enable` 默认为 `false` 且没有 `matchIfMissing`，不显式开启的话**增强能力（接口调试、过滤）会静默失效，但 `/doc.html` 仍能打开**，很容易误判为已接好。

### 4. 数据库脚本 `sql/`

| 文件 | 内容 |
|---|---|
| `01_create_database.sql` | 建库 `volunteer_cert_portrait` |
| `02_schema.sql` | **建表**：16 张表 + 表/字段注释 + 索引 |
| `03_init_data.sql` | **初始化数据**：角色、3 个角色账号、学生档案、组织、活动分类、数据字典 |
| `04_demo_data.sql` | 演示数据（可选）：8 组织 / 20 活动 / 31 学生 / 149 条报名及对应签到与时长 |
| `05_backend_gap_fix.sql` | **补列脚本（2026-09-23 新增）**：前端要而 `02` 里没有的列 —— `sys_dict.tone`、`sys_user.last_login_at`、`notification.source/is_top/batch_no`、`operation_log.module/action/target/result/cost`、`org_info.code/college/member_count/founded_at/audit_remark`，以及相关索引。只做加法、可重复执行。**不执行它 `vcp-system` 与 `vcp-org` 的接口会直接报错** |
| `06_backend_gap_fix2.sql` | **补列脚本（第二批，2026-09-23 新增）**：活动报名截止/联系方式、报名理由、分类编码、学生性别年级、时长证明与所属组织，并补齐签到聚合与外键列索引。纯增量、可重复执行。**不执行它，活动/报名/时长/学生档案相关接口会报错** |
| `07_demo_scale.sql` | **演示数据放大（可选，2026-09-24 实跑）**：把演示数据从 20 活动 / 31 学生放大到 **386 活动 / 1500 学生 / 10719 条报名 / 19319.3 小时**，并回填 `org_info` 的 `code`/`college`/`founded_at`/`member_count`、`activity_signup.reason`、`activity_category.remark`。纯增量、不重建库、可重复执行（整脚本一个事务 + 末尾 6 项 / 9 条断言）。**含 `remark` 补列，执行后需重启后端**。详见第二节第 10 条 |
| `08_password_bcrypt.sql` | **口令刷密文（2026-09-24 新增）**：把老库里仍是明文的口令批量刷成 BCrypt 密文（强度 10），可重复执行、末尾自检明文残留数。**新库不用跑**（`03`/`04`/`07` 的种子数据已是密文）；老库不跑则登录一律失败（`PasswordUtils.matches` 对明文记录刻意返回 false，不做明文兜底） |
| `09_consistency_check.sql` | **一致性自检（2026-09-24 新增，20 项，只读、可重复执行）**：外键悬空 6 / 汇总字段与明细不符 4 / 状态与审核字段矛盾 4 / 学院专业错配 1 / 时间异常 2 / 演示数据完整性 3，输出 **20 行 + 1 行汇总**（汇总行 `check_no = 99`，文案形如「检查项总数 20，违规合计 N」）。**2026-09-24 在云库（PostgreSQL 18.6）实跑：20 项违规数全 0、违规合计 0**。用法：`psql -U postgres -d volunteer_cert_portrait -f 09_consistency_check.sql`；前置条件：`01`~`06` 必须已执行（用到 `05` 补的 `org_info.college`），`07` 可选（没跑过 `07` 的库也能执行不报错，只是第 15、20 项会命中非 0，属演示数据规模不足的正常现象） |
| `10_base_and_test_accounts.sql` | 清库后的基础数据重灌脚本（3 角色 / 2 管理员 / 1 组织 / 6 分类 / 学院字典），增量可重复执行 |
| `11_activity_images.sql` | **活动图片字段（2026-09-24 新增）**：`attachment.content_type/caption/sort_order` + `idx_attachment_biz_sort` + 附件字典补齐；增量可重复执行 |
| `12_activity_images_demo.sql` | **少量图文演示（2026-09-24 新增）**：5 个账号 + 1 个组织 + 3 个已发布活动 + 3 张图片元数据；增量可重复执行 |
| `README.md` | 执行顺序、设计约定、与原始脚本的差异、**待拍板事项（原 9 项，已全部拍板，留档备查）** |

**已在 PostgreSQL 18.6 上实跑验证**：

```
16 张表 / 843 行数据（2026-09-22 全量实跑值，见提交 `9424841`）
20 项一致性自检全部为 0（外键悬空、汇总字段与明细不符、状态与审核字段矛盾、
                          学院专业错配、审核时间落在未来 等）
看板聚合与画像标签均能正常产出
20 项自检已固化为 `sql/09_consistency_check.sql`（2026-09-24 云库实跑，违规合计 0）
```

验证过程发现并修复了 11 处缺陷，其中 3 处是实跑才能暴露的：

- **学号冲突**：`04` 生成的学号与 `03` 的基础学生重复，导致 `04` 整批回滚（多语句隐式事务），演示数据一条都进不去
- **审核人条件写反**：`service_duration` 状态是 `<15 已通过 / <18 待审核`，判定却用 `>=15`，结果 55 条已通过的没有审核人、9 条待审核的反倒有
- **学院与专业错配 24/30**：学院用 `g%5`、专业用 `g%6`，两个下标互质，造出"经济管理学院-电子信息工程技术"这类不存在的组合

其余修复（未来审核时间、异常签到数据与备注矛盾、`tags` 空字符串、标签阈值失效、排序依赖数据库 collation、`ROUND` 的 PG 版本兼容等）详见 `sql/README.md` 与提交 `a467176`。

> 编号说明：本节编号 5 空缺（历史原因），为保持与既有文档引用一致未重排。

### 6. `vcp-system` 模块（2026-09-23 新增）

这是第一个真正跑起来的业务模块，也是「登录 → 拿 token → 访问受保护接口」主链路的验证。

**代码规模**：entity 8 / mapper 8 / dto 10 / vo 8 / service 接口 6 + 实现 7 / controller 7 / 事件监听器 1。

**接口清单**（前缀 `/api/v1`）：

| 路径 | 方法 | 权限 | 说明 |
|---|---|---|---|
| `/auth/login` | POST | 放行 | 登录，写 token 并往会话写 roleCode / orgId / studentId |
| `/auth/register` | POST | 放行 | 学生自助注册，注册即登录 |
| `/auth/logout` | POST | 登录 | 退出 |
| `/auth/me` | GET / PUT | 登录 | 查 / 改本人资料（只能改姓名、手机号、邮箱） |
| `/system/users` | GET / POST | `system:user:list` / `:create` | 用户列表与新增 |
| `/system/users/{id}` | PUT / DELETE | `system:user:update` / `:delete` | 修改与删除（逻辑删除） |
| `/system/users/{id}/status` | PUT | `system:user:update` | 启用 / 停用 |
| `/system/roles` | GET | `system:role:list` | 角色 + 权限清单（只读，角色不可增删改） |
| `/system/dicts` | GET | **仅需登录** | 字典，三种角色都要用，不能挂 `system:*` |
| `/system/students` | GET | 仅学校管理员 | 学生档案（前端暂无页面调用，为后续预留） |
| `/system/notifications` | GET / POST / DELETE | 读**仅需登录**；写 `system:notice:manage` | 通知公告 |
| `/system/notifications/{id}` `/read` | GET / PUT | 仅需登录 | 详情（顺带标已读）与标已读 |
| `/system/logs` | GET | `system:log:list` | 操作日志（只读，无写接口） |

**几个刻意的设计取舍**：

- **`sys_dict` 必须返回 `tone`**：前端 `stores/dict.js` 的 `load()` 是**整体替换**语义，
  接口不返回 `tone` 会把所有状态标签刷成灰色。这是「不报错但页面全变样」的典型静默失效。
- **通知按收件人一行存，用 `batch_no` 串批次**：`is_read` 天然是按人记的；
  删除时只有自己那一行的 id，不按批次整批删就会「删了但学生端还在」。
- **`operation_log` 不存 `role` 列**：由 `user_id` 实时关联推导，冗余存一列在角色调整后会失真。
- **登录 / 注册 / 本人改密 / 管理员重置口令都不采集请求参数**（`@OperationLog(params = false)`）：
  请求体里是明文口令，采集会把口令序列化进 `operation_log.params` 永久留存。
  2026-09-24 实测确认这四类日志的 `params` 均为空。
- **停用 / 删除自己会被拒绝**：管理员一旦把自己停掉，登录接口会以 20002 拒绝，
  而恢复又需要登录，等于系统再也进不去。
- **越权访问别人的通知报 10004 而不是 20003**：报「无权限」等于确认这条通知存在。
- **时间字段在服务端格式化成字符串**：日志页与通知页直接渲染 `{{ row.time }}` / `{{ row.date }}`，
  返回 `LocalDateTime` 会带上 ISO 的 `T`。

**验证方式**：本机启动后跑了 42 项端到端断言，全部通过——覆盖登录 / 注册 / 退出、
三类角色登录、按角色筛选用户、字典分组、日志落库与中文模块筛选、公告群发与整批删除、
学生越权被拒 20003、停用账号登录 20002、退出后 token 失效 20001、逻辑删除后列表不可见等。

**新增的前置脚本 `sql/05_backend_gap_fix.sql`**：前端要而 `02_schema.sql` 里没有的列，
全部是 `ADD COLUMN IF NOT EXISTS` + `ON CONFLICT DO NOTHING`，只做加法、可重复执行。
**不执行它，登录接口会直接报错**（`sys_user.last_login_at` 不存在）。

其中一处值得记下：字典色调必须按 `(dict_type, dict_key)` **精确指定**，不能只按 `dict_key` 匹配 ——
不同字典类型里有同名键（`CANCELED` 同时属于 `activity_status` 与 `signup_status`），
只按 `dict_key` 更新会把它们刷成同一种色调，而前端的取值并不相同（一个是 bad 红、一个是 mute 灰）。
这类错配不报错，只表现为「这个标签的颜色跟别处不一样」。

---

### 7. `vcp-org` 模块（2026-09-23 新增）

这是第二个真正跑起来的业务模块，负责组织基础信息、资质审核与启停。

**代码规模**：entity 1 / mapper 1 / dto 4 / vo 1 / service 接口 1 + 实现 2 / controller 1。

**接口清单**（前缀 `/api/v1`）：

| 路径 | 方法 | 权限 | 说明 |
|---|---|---|---|
| `/orgs` | GET | `org:info:audit` | 学校管理员分页查询组织 |
| `/orgs/{id}` | GET | 登录 | 详情；学校管理员可查任意，组织管理员只能查本组织 |
| `/orgs/{id}` | PUT | 登录 | 更新组织资料；数据范围同详情 |
| `/orgs/{id}/audit` | PUT | `org:info:audit` | 资质审核，`APPROVE` / `REJECT`，驳回必须填理由 |
| `/orgs/{id}/status` | PUT | `org:info:audit` | 已审核组织在 `APPROVED` / `DISABLED` 间启停 |

**几个刻意的设计取舍**：

- **`DISABLED` 与 `REJECTED` 分离**：停用不改变资质结论，历史活动与时长数据继续保留；
  只有已通过审核的组织允许启停，待审核和驳回组织不能直接「启用」。
- **登录归属用端口接回**：`vcp-system` 定义 `OrgLookupPort`，`vcp-org` 提供实现；
  组织管理员登录后，会话写入 `orgId`，前端组织端页面不再只能回退到 id 1。
- **审核结果通知负责人**：新增单用户通知方法，不复用群发公告逻辑；
  审核通过/驳回各落一条系统通知，驳回理由写入正文。
- **组织资料更新不带状态字段**：状态、负责人账号、审核备注只能走对应接口，
  避免资料维护绕开状态机。
- **聚合指标通过 `OrgMetricsPort` 预留**：`vcp-org` 不直接访问活动与认证表；
  后续统计模块实现该端口后可填活动场次、签到率、通过率，未实现时返回 0。

**数据库补齐**：`05_backend_gap_fix.sql` 为 `org_info` 补了
`code` / `college` / `member_count` / `founded_at` / `audit_remark`，
并增加状态查询索引与「在用组织负责人」部分唯一索引。

**验证方式**：11 个 Maven 模块 `package` 全部通过；数据库脚本已在当前库实跑；
启动后实测 8 个组织分页返回、组织管理员登录得到 `orgId=1`、
组织管理员读取与更新本组织成功、学生读取/更新返回 20003、
启停往返后状态恢复 `APPROVED`、非法审核动作返回 10001 且状态保持 `PENDING`。

### 8. 四个业务模块（2026-09-23 新增，5 路并行开发）

`vcp-volunteer`、`vcp-certification`、`vcp-portrait`、`vcp-analytics` 一次性落地，
外加补列脚本 `sql/06_backend_gap_fix2.sql`。

**代码规模**：`vcp-volunteer` 52 个文件（含 4 个 Mapper XML）、`vcp-certification` 24、
`vcp-portrait` 21、`vcp-analytics` 21（均为 `src/main` 下的 `.java` + `.xml` 文件数）。

**接口清单**（前缀均为 `/api/v1`）

| 模块 | 路径 | 接口数 | 权限 |
|---|---|---|---|
| `vcp-volunteer` | `/activities`、`/categories`、`/signups`、`/attendance`、`/attendance/mine` | **19** | 活动/报名/签到各自授权；**签到签退与「我的签到记录」仅需登录**（学生无签到权限码，数据范围按登录态裁剪） |
| `vcp-certification` | `/durations`、`/duration-audits` | 6 | 提交 `certification:duration:submit`、审核 `:approve`；列表仅需登录 |
| `vcp-portrait` | `/portraits`、`/portraits/me`、`/portraits/distribution` | 5 | 本人画像 `portrait:profile:mine`，其余 `portrait:profile:list` |
| `vcp-analytics` | `/analytics/*` | 10 | `/dashboard` 仅需登录，其余 `analytics:dashboard:view` |

**实跑验证（2026-09-23）**：`mvn package` 11 个模块全过；`sql/06` 已在库上执行并提交
（9 列 + 1 唯一约束 + 12 索引，回填 20 活动 / 69 时长 / 31 学生）；启动后用
admin / org_admin / student 三个账号实打 20 个请求，**20 个全部符合预期、日志零异常**：

- `GET /analytics/dashboard` 三个角色都通 —— 刻意**不挂** `analytics:dashboard:view`，
  否则学生端与组织端会被打成 20003
- 学生打 `GET /analytics/overview` 正确返回 **20003**（权限门禁生效）
- `GET /portraits` total=31、`/durations` total=69、`/activities` total=20、
  `/signups` total=33、`/attendance` total=11
- `GET /portraits/distribution` 首轮抛 NPE（`code=10000`），**已修复并复验通过**
  —— 根因是 MyBatis 的 `returnInstanceForEmptyRow` 默认为 false，一行所有映射列都是 NULL 时
  会往 `List` 里塞 `null`；`.select(StudentProfile::getTags)` 只投影一列，
  而 `student_profile` 有 2 行 `tags` 为 NULL。改为 `GROUP BY tags` + `COUNT(*)` 聚合下推，
  并顺带修掉同源的 `selectActivitySpan`（无已完成活动的学生会返回 `null` 而非空对象）。
  **教训已写进 `CLAUDE.md` 第 18 条：投影查询至少要带一个恒非空列**

**几个刻意的设计取舍**

- **数据范围一律以登录态为准，忽略前端传的 `studentId` / `orgId`** —— B19 列的第一位越权风险
- **越权与数据范围外返回「不存在」（30001/30008/30011），不复用 20003** ——
  前端把 20003 当登录失效，会清 token 跳登录
- **`signed_count` 口径**：提交报名即占名额（含 `PENDING`），驳回与取消各释放一个，
  审核通过不再加。与前端 mock 一致；`04_demo_data.sql` / `07_demo_scale.sql` 也已按
  同一口径回填（见下第 1 项，B20-1 已闭环）
- **`vcp-analytics` 直接只读跨域表**：一次看板跨 8 张表，走各域 Service 会退化成
  「查明细再内存聚合」，取舍写在 `AnalyticsMapper` 类注释里
- **`public_welfare_level` 暂存中文等级名**（B18）：前端把 `level` 当展示文案直接渲染，
  改存码值页面会显示 `FIVE_STAR`；折中是 VO 里一并返回 `levelCode`，
  枚举同时识别中文名与英文码，将来字典化不必再推码表

**`sql/06_backend_gap_fix2.sql` 补了什么**

- 列：`volunteer_activity.deadline` / `contact`、`activity_signup.reason`、
  `activity_category.code`（+ `uk_category_code`）、`student_info.gender` / `grade`、
  `service_duration.proof` / `org_id` / `activity_type`
- 索引 12 条，最关键的是 `attendance_record(sign_in_time)` —— 该表原先除唯一键外
  **没有任何索引**，而 analytics 有 10 个接口要按签到时间聚合它
- 回填全部确定性派生（无 `random()`）；`activity_signup.reason` 与
  `service_duration.proof` **刻意不回填**（库里无来源，编造会误导）

**本次集成新发现的 4 个问题**（已记进 `docs/待办清单.md`；**4 个已全部闭环** —— 3、4 已于 2026-09-23 在前端侧修掉，
2 已由后端补 `activity_category.remark` 列修掉，1 已于 2026-09-24 由脚本侧对齐实现口径闭环，详见下）

1. `signed_count` 两处口径不一致：实现是「提交即占名额」，而 `04_demo_data.sql`
   按「`APPROVED` + `COMPLETED`」回填 → 演示数据的计数与明细对不上
   → ✅ 2026-09-24 已闭环（收口方向：**脚本对齐实现，实现未改**）：
   `sql/02_schema.sql` 的列注释（原写成「审核通过/取消时维护」）与 `sql/04_demo_data.sql`、
   `sql/07_demo_scale.sql` 的回填已统一为「未取消未驳回的报名」
   （`PENDING` + `APPROVED` + `COMPLETED`）口径；
   `sql/07` 的新活动名额下限由 40 抬到 **46**（公式 `40 + ((n.seq * 7) % 36)` →
   `46 + ((n.seq * 7) % 30)`，即 46..75），并在第十一节补了一条幂等修正
   `UPDATE volunteer_activity SET max_count = 46 WHERE deleted = 0 AND max_count > 0 AND max_count < 46;`。
   理由：报名数上限是 45，新口径下已发布活动占位 60% + 35% = 95%，45 × 0.95 = 42.75 > 40
   会顶破名额下限、使 `sql/07` 末尾自检第 4 项失败；名额 ≥ 46 > 45 是硬保证
    （结论见 `docs/待办清单.md` 的 B20-1）。**2026-09-24 已在云库实跑**：`sql/07` 事务内执行、
    末尾自检全部通过；实测「已报名 > 名额」的活动数 0、全库名额范围 46..75、与明细按新口径
    不一致的活动数 0（按旧口径不一致的有 36 场）；报名 10,719 条中占名额 9,201 条。
2. `activity_category` 没有 `remark` 列，而前端分类管理页有备注输入框
3. 画像等级：前端 `PortraitView.vue` 的 `levelTone` 以「五星 / 四星 / …」为键，
   而接口返回完整等级名「五星志愿者」→ 等级列色调会退化成灰色
   → ✅ 2026-09-23 已修（提交 `4752214`）：`levelTone` 改以接口返回的 `levelCode` 为键
4. 画像标签分布返回 **8 类**（含拍板新增的「助老服务型」「文化传播型」），
   而前端页面标题写死「六类画像标签分布」
   → ✅ 2026-09-23 已修（提交 `4752214`）：文案改「八类」，mock 同步补齐 8 类

### 9. 安全事件闭环（2026-09-23）

- **云数据库口令已轮换**：旧口令实测被服务端拒绝（`password_encryption = scram-sha-256`），
  所以进过 git 历史的旧口令已经失效；本地 `application-local.yml` 已同步新口令
- **含明文口令的会话存档已删除**：`docs/对话存档/后端/`（6 个 jsonl + README，内含数据库
  连接串与明文口令）已随提交 `f556683` 删除并推送。**该目录已不存在，不要再引用它**
- 凭证外置的做法与两个注意点见第二节第 2 条；团队内其余成员需同步更新各自本地的
  `application-local.yml`（不更新就连不上库）
- 清理 git 历史里的口令需要 force push、会波及两位同学的本地仓库，**建议推迟到答辩之后**：
  旧口令已失效，不做也不影响安全

---

### 10. 演示数据放大与 `sql/07` 修复（2026-09-24）

`sql/07_demo_scale.sql` 是同学在提交 `0a668d7` 新增的演示数据放大脚本（初版 797 行，本次修复后 903 行，口径收口后 916 行），
**入库后从未执行过**。同学的 F4 审计指出 5 个演示数据缺口：规模差约 500 倍、
演示账号 `student` 没有参与记录、`org_info` 四字段全空、`activity_signup.reason`
从不回填、`operation_log.target` 从不填。本节是**首次实跑**的经过与结果。

**脚本自身的三处缺陷（此前从未跑过，一跑就暴露）**

| # | 位置 | 缺陷与后果 | 修法 |
|---|---|---|---|
| 1 | `sql/07_demo_scale.sql:108` | `DATE '2015-01-01' + ((id * 137) % 2920)` 是 `date + bigint`，而 PostgreSQL 只有 `date + integer`、没有这个运算符 —— **脚本第一条 UPDATE 就报错**，整批回滚 | 改为 `(((id * 137) % 2920)::int)` |
| 2 | `sql/07_demo_scale.sql:384`（第五节报名上限） | 原为 `8 + ((a.id * 7919) % 32)`（8..39 场）→ 人均只参与 5.5 场、最高 9 场 ≈ 37.2 小时，**「五星志愿者（≥40 小时）」一档恒为空**，末尾自检第 3 项必然失败 | 改为 `16 + ((a.id * 7919) % 30)`（16..45 场） |
| 3 | 新增一节（`sql/07_demo_scale.sql:598` 起） | 只靠调大报名数补不出五星档：总报名会冲到约 3 万条，而前端原型只有 12,480 条 | 新增「十之二、把高活跃学生定向补进五星档」 |

- **缺陷 2 的安全边界**（口径已改，见第二节第 8 条的 4 个问题第 1 项）：`signed_count`
  改成「未取消未驳回的报名」后，已发布活动的占位比例是 60% + 35% = 95%，
  45 × 0.95 = 42.75 会顶破原先的名额下限 40；故 `sql/07` 把新活动名额下限抬到 **46**（46..75），
  而第五节报名数上限仍是 45，46 > 45 是硬保证，自检第 4 项
  （`signed_count > max_count` 的活动数为 0）恒成立
- **第十之二节为什么必须「定向补」**：一次报名要过三层折算
  （85% COMPLETED × 90% 非缺勤 × 75% 时长审核通过 ≈ 57%）才变成一条有效时长，
  每条约 4 小时，40 小时需要约 18 次报名；改为对累计 32..40 小时的学生
  定向补 4 场 ≥4 小时的活动，做法与第十节给演示账号补记录一致
- **另修一处幂等性漏洞**：第十节（演示账号补记录）与新增的第十之二节原先没挂
  `should_scale` 守卫，重跑会给学生 1 再补 6 场、五星档持续膨胀（现已挂上，见
  `sql/07_demo_scale.sql:539`、`:623`）

**实跑结果（脚本末尾自检全部通过）**

| 指标 | 实测值 |
|---|---|
| 学生 / 活动 / 报名 | 1500 人 / 386 场 / 10719 条 |
| 累计时长 | 19319.3 小时 |
| 六档等级人数 | 普通 118 / 一星 168 / 二星 299 / 三星 687 / 四星 207 / 五星 21 |

- 演示账号 `student`（学生档案 id=1，姓名 张同学）：22.6 小时、四星志愿者、9 场活动、
  标签「热心志愿者,长期坚持型,大型活动型」
- `org_info` 四字段全填：code `ORG001`–`ORG008`、college（5 个学院）、
  founded_at 2015–2018、member_count 97–282
- `activity_signup.reason` 空值从 149/149 降到 49/10719；`activity_category.remark`
  补列并回填 6 条
- `signed_count > max_count` 的活动 0 条

**重新打包与重启**：旧后端 PID 124316 先停 —— 它锁着 `target/vcp-boot-1.0.0.jar`，
导致 `spring-boot:repackage` 报 `Unable to rename ... .jar.original`；
`mvn -DskipTests package` **BUILD SUCCESS**（11 个模块全 SUCCESS）；
新进程 PID 157500 监听 8080，启动耗时 6.6 秒。

**接口实测**

| 接口 | 结果 |
|---|---|
| `GET /api/v1/categories` | 6 条分类都带上 `remark` |
| `GET /api/v1/analytics/dashboard` | 活动 386、报名 10719、时长 19319.3 小时、签到率 90.1%、审核通过率 75.6%（5614 通过 / 1115 待审 / 694 驳回）、5 个学院各约 300 人、8 类画像标签、12 个月趋势、签到热力图 |
| `GET /api/v1/portraits/me`（student） | `activityCount` 9、`totalHours` 22.6、`level` 四星志愿者、`levelCode` `FOUR_STAR`、3 个标签 |
| `GET /api/v1/orgs?college=计算机学院` | 从**恒 0 条**变成 2 条（不筛选时 8 条） |

**同批完成的配置与文档收尾**

- 根 `.gitignore` 补 `application-local.yml` 规则：原规则只有 `*.local`，而它只匹配
  「以 `.local` 结尾」，不覆盖 `application-local.yml`；`application.yml` 支持
  `optional:file:./application-local.yml`（**仓库根**），谁在根目录建这个文件就会被跟踪。
  已验证 `.example` 模板仍可入库
- `volunteer-cert-portrait-web/vite.config.js` 的 proxy target 原写死
  `http://10.0.20.91:8080`（某台机器的内网 IP）且已随提交入库；改为用 `loadEnv` 读
  `VITE_API_TARGET`，默认 `http://127.0.0.1:8080`
- `.env.development` 补注释说明 `VITE_API_TARGET` 用法（联调时放 `.env.local` 覆盖）
- `volunteer-cert-portrait-web/README.md`：组织管理员演示账号 `org` → `org_admin`，
  并修掉一处 Markdown 栅栏缺陷
- `docs/ER图.md` 新增：16 张表、23 条关系（18 条物理外键 + 5 条业务关联）
- **A9 销账**：DB 存 `SMALLINT` 1/0、API 返回英文码、前端 `DICT_DEFS` 用英文码，
  三层天然对齐；实测字典接口返回 `ACTIVE`/`DISABLED`、用户列表返回 `status:"ACTIVE"`，
  **无需改代码**
- 「一致性自检项数」统一为 **20 项**（依据提交 `9424841`；18 是公益等级与标签规则
  落地前的旧数字）；已固化为 `sql/09_consistency_check.sql`（2026-09-24 云库实跑，违规合计 0）

**提交（已推送）**

| 提交 | 说明 |
|---|---|
| `1138eaa` | fix: 修复 `sql/07` 演示数据放大脚本的三处缺陷并实跑（A8/F4） |
| `e441135` | docs: 更新待办清单与项目说明（A9 销账、D1 产出 ER 图、一致性自检统一为 20 项） |
| `7b362f3` | chore: 补齐 `application-local.yml` 忽略规则并让前端代理可配置 |

推送范围 `0a668d7..7b362f3`，`main` 与 `origin/main` 一致。

**环境留证（省得下一个人再踩）**

- **本机没有 `psql`**：执行 SQL 用 JDBC 单文件程序，靠 `preferQueryMode=simple`
  让 PostgreSQL 自己解析多语句，`DO $$...$$` 里的分号不会被切坏
- **运行中的后端会锁住 jar**：`mvn package` 的 repackage 会报
  `Unable to rename ... .jar.original`，必须先停进程
- **想在不改数据的前提下看脚本结果**：把 `RAISE EXCEPTION` 改成 `RAISE NOTICE`，
  并在末尾 `COMMIT` **之前**插入诊断 DO 块让它 `RAISE EXCEPTION`，
  这样既能拿到完整数据又自动回滚
- **沙箱里跑 Maven 需设** `MAVEN_OPTS=-Duser.home=<家目录> -Dmaven.repo.local=<本地仓库>`

**仍未了结**：见第四节 P1 第 8~10 条（旧口令同步、`operation_log.target`、`StudentInfo.gender`）
与第 13~18 条（2026-09-24 下半场摸底新增：学生自助签到缺两环、取消活动不处理报名、
`PENDING_SUBMIT` 无生产者、默认口令硬编码、组织列表 N+1、后端零自动化测试）；
同表第 11、12 条已于 2026-09-24 完成（本机构建的 jar 内含本地配置、口令 BCrypt 加密与登录防护）。

### 11. 两个缺陷修复与端到端实测（2026-09-24 下半场）

本轮先做了一次**只读摸底**（后端代码 / 数据库 / 前端契约三路交叉，过程见 `docs/会话记录.md` 会话 3），
挑出 2 个必修缺陷当场修掉，其余未完成项已编号落进 `docs/待办清单.md` 的 B24~B29。
修复按**互不重叠的模块**切给两个并行子任务：一个只碰 `vcp-certification`，一个只碰 `vcp-system`。

| # | 缺陷 | 后果 | 修法 | 实测 |
|---|---|---|---|---|
| 1 | `DurationRefMapper.selectSignupId` 反查报名时**只过滤 `deleted = 0`**（P0） | 「取消报名」与「审核驳回」按 A7 的口径只改 `status`、**不软删** → **待审核 / 已驳回 / 已取消的报名也能开时长**，审核通过后 `total_duration` 被错误累加，且当时没有任何撤销接口 | 反查 SQL 加 `AND status IN ('APPROVED', 'COMPLETED')`（`DurationRefMapper.java:176`）；反查失败文案改为「该学生在此活动下的报名未通过审核（可能为待审核、已驳回、已取消，或该学生未报名）…」（`DurationServiceImpl.java:158`~`:163`），错误码 `SIGNUP_NOT_FOUND(30008)` 不变 | 待审核报名开时长 → 30008 被拒；同一条报名审核通过后再开 → 成功，该学生时长 0.0 → 2.5 小时 |
| 2 | 学生注册**不建 `student_info`**（P1） | 会话里的 `studentId` 取自 `student_info` → 新注册学生 `studentId` 恒为 null，学生端「我的报名 / 我的时长 / 我的画像」**整条链路全报 10003** | 新增 `StudentArchiveRegistrar`（入口 `ensureArchive`），在注册（`AuthServiceImpl.java:208`）与管理员建号（`UserServiceImpl.java:166`，仅学生角色）两处**同事务**建档 | 新注册学生 `studentId=1503`、学号 `S001513`、画像接口返回 50001（画像未生成）而**不再是 10003** |

**建档的口径**（论文引用按这个）：占位学号 `S` + 六位零填充 userId（如 `S001513`）；
`total_duration` 写 0；`public_welfare_level` **留空** —— 等级由每日画像重算补写，
这样 `vcp-system` 不必反向依赖 `vcp-portrait`；**不建 `student_profile`**（画像首次生成时再建）。
幂等实现：先按 `user_id` 查，并发撞唯一约束时捕获 `DuplicateKeyException` 转可读文案
（catch 里**刻意不回查**：PostgreSQL 事务此时已 aborted，回查必然失败）。

**端到端实测**（真实后端 + 云库，脚本 `.tmp-verify/verify.ps1`）：三个角色登录、此前 4 个功能缺口修复、
本轮 2 个修复、异常用例 4 项**全部通过**；组织端人工修正签到可用。
测试产生的账号与数据已清理，**演示数据还原到 1500 / 386 / 10,719 / 19,319.3**，
`sql/09_consistency_check.sql` 复跑**违规合计 0**。
两项**设计内**的未通过项：签到窗口（活动未开始返回「签到尚未开放」）、
组织管理员调学生签到接口返回 10003。

**踩坑留证**：`signed_count` 是**应用层维护的派生列** —— 直接用 SQL 删报名行不会回退它，
必须同时修正（本轮用过一次 `fix7.sql` 才把偏差归零）。
### 12. 学院字典管理接口与「管理员新增用户」补学院（2026-09-24 下半场（第四轮））

学院**不是独立表**，是 `sys_dict` 里 `dict_type = 'college'` 的字典行（`dict_key` = `dict_value` = 学院名）。
上一轮只做了「读」（注册页下拉 + 注册时校验），这一轮把**管理端**补齐，并顺手收口了 B30 的另一条写路径。

**新增的 4 个接口**（`vcp-system` 的 `CollegeController`，前缀 `/api/v1`，全部挂 `system:college:manage`）：

| 路径 | 方法 | 说明 |
|---|---|---|
| `/system/colleges` | GET | 列表，返回 `CollegeVO`：`id` / `name`（取 `dict_key`）/ `sort` / `status`（1 启用 / 0 停用）/ `studentCount` / `orgCount`。**不过滤停用项** —— 管理页要能看到并重新启用，只取启用项的是注册页那份 `/auth/colleges` |
| `/system/colleges` | POST | 新增，body `CollegeSaveDTO { name, sort }`：名称去空格后必填、最长 30 字、同类型下不可重名（10001「该学院已存在」）；`sort` 留空或 ≤ 0 时取现有最大 `sort` + 1（**不用行数 + 1** —— 删过学院后行数与最大 `sort` 会脱节，补位会插到列表中间） |
| `/system/colleges/{id}/status` | PUT | 启停，body 复用 `StatusUpdateDTO`：`ACTIVE` / `"1"` → 1、`DISABLED` / `"0"` → 0，其余值 10001「学院状态不合法」（**不猜默认值**：猜错会把停用做成启用，表现为「点了没反应」）；**刻意不校验占用** —— 学院合并 / 停招时硬删会被占用校验挡住，只能靠停用让它从注册页下拉里消失 |
| `/system/colleges/{id}` | DELETE | 物理删除（`sys_dict` 没有 `deleted` 列，字典行的下架手段本来是停用），只用于「建错了、还没被引用」的行；**学院下还有学生或组织时拒绝**，文案带具体数量：「该学院下还有 N 名学生、M 个组织，不能删除」（只有一方时也有对应文案） |

**几个刻意的取舍**：

- **学院即字典行，不新建表**：注册页下拉、注册校验、按学院筛学生与组织，全都按「学院名」这一列匹配；多一张表只会多一份可能与字典漂移的名字清单。
- **学生数 / 组织数各一次 GROUP BY**：学生数走 `student_info` 一次聚合（排除 `college` 为 null / 空串的行）；组织数走**端口** —— `vcp-system` 定义 `OrgCollegeCountPort`、`vcp-org` 提供 `OrgCollegeCountPortImpl`，调用方用 `ObjectProvider.getIfAvailable()` 取，取不到或查询失败时 `orgCount` 一律按 0（缺这一列只影响展示，不该让整个列表页打不开）。依赖方向仍是 `vcp-org → vcp-system`，**没有反向依赖**（与 `OrgLookupPort` 同一套路）。
- **权限码只发学校管理员**：`system:college:manage` 加在 `StpInterfaceImpl` 的 `SCHOOL_ADMIN` 组（16 → 17 条），前端 `stores/user.js` 的 `ROLE_PERMS` 同步（两边不一致会出现「按钮点得到、接口报 20003」）。
- **三个写操作都记操作日志**：`@OperationLog(module = "用户与权限", action = 新增学院 / 启停学院 / 删除学院)`；刻意**不写** `params = false` —— 请求体里只有学院名 / 排序 / 状态，没有口令一类要避开的字段。

**「管理员新增用户」补学院（B30 的第二条写路径）**：

- `UserSaveDTO` 加 `String college`（仅「新增 + 学生角色」时读；修改用户忽略它 —— 学院落在 `student_info` 上）。
- `UserServiceImpl.createUser`：学生角色下校验学院非空且命中字典的启用项，否则 10001「请选择学院」（与注册逐字一致），然后 `studentArchiveRegistrar.ensureArchive(userId, college)` 落库。
- `DictService.containsEnabled(dictType, key)` 是新增的一处判定（COUNT、只认 `status = 1`、任一参数为空直接判否），**注册（`AuthServiceImpl`）与新增用户（`UserServiceImpl`）共用它** —— 原先注册那份私有 `isCollegeInDict` 已删，免得两边慢慢漂移出「注册能选、管理端不能选」。空值判定刻意不下推给 SQL：`eq(column, null)` 在 MyBatis-Plus 里是「不加条件」，传空值会退化成「这个类型下有没有任意一条启用项」。
- `StudentArchiveRegistrar.ensureArchive(Long)` 单参重载**已删**（改完就没有调用方了），两条路径都走双参版本。
- 前端配套：`UserManageView.vue` 的「新增用户」表单加学院下拉（`needCollege = mode === 'create' && role === 'STUDENT'`），数据源取管理端 `listColleges()` 并自行滤掉停用项；mock 的 `POST /v1/system/users` 补同一条校验并同步建学生档案。

**实测**（2026-09-24 本轮，运行实例 pid 102528，jar 16:26:08 打、16:33:57 起）：

| 手段 | 结果 |
|---|---|
| `GET /api/v1/system/colleges`（带 admin token） | `code 0`，5 行按 `sort` 升序（`id` 28~32），字段齐全；`studentCount` / `orgCount` 现全 0（库中无演示数据，收尾时已把上一轮留下的测试学生删净） |
| 非法状态 `PUT /{id}/status`（body `{"status":"BOGUS"}`） | 10001「学院状态不合法」，**且未改动任何行** |
| 免登录的 `GET /api/v1/auth/colleges` | `code 0`，5 个学院顺序一致（免登录放行未受影响） |
| 不带 token 调 `GET /api/v1/system/colleges` | 20001（路由存在且被鉴权拦住） |
| 操作日志 | `operation_log` 留下 `module = 用户与权限` / `action = 启停学院` / `result = FAIL` 一行，证明 `@OperationLog` 生效 |
| 新增 / 启停 / 删除三个写操作 | ✅ 脚本实跑（`.tmp-verify/verify-college-admin.ps1`，**PASS 41 / FAIL 0**）：新增（不传 `sort` 自动补 6、重名 / 空名 / 超 30 字都被拒）、启停（停用后注册页下拉不含它、管理端列表仍含停用项、`"1"` 也能启用）、删除（空学院可删、重复删除 10001「学院不存在」）、占用校验（有学生 / 有学生 + 组织都被拒，文案带两个数）、管理员新增学生缺 / 非法学院都被拒、`student_info.college` 落库核对 |
| 脚本自清理 | 收尾把测试账号与档案删净，库回到基线：2 个账号 / 0 份学生档案 / 5 个学院 |
| 未测 | 前端页面级（浏览器）走查只在 **mock 构建**下做过（1440 视口），未对着真后端逐页点；写操作在 `operation_log` 里的成功落痕未逐条核对 |

**残留**：`student_info` 的 `major` / `class_name`（以及同源的 `gender` / `grade`）**仍然没有写入入口**，B30 只算部分销账，见 `docs/待办清单.md` 的 B30。

### 13. 活动图片上传（2026-09-24 第七轮）

本轮把原来只有实体和 Mapper、没有实际接口的附件能力接通到活动发布场景。
存储策略按「数据库存元数据、文件系统存本体」实现，不把图片写进 `BYTEA`：

- 上传接口：`POST /api/v1/attachments/images`，`multipart/form-data` 字段名 `file`。
- 文件目录：开发环境 `volunteer-cert-portrait-server/uploads/activities/年/月/UUID.jpg|png`；
  演示图片固定放在 `uploads/demo/activities/`。
- 数据库：
  - `attachment` 保存 `biz_type=ACTIVITY`、`biz_id=活动 id`、`file_url`、`file_size`、
    `content_type`、`caption`、`sort_order`；
  - `volunteer_activity.cover` 保存封面 URL。
- 校验：仅 JPG/PNG，单张不超过 5MB；通过 Java `ImageIO` 识别真实格式并限制最大像素；
  文件名由服务端生成，不采用客户端原始路径。
- 权限：拥有 `volunteer:activity:create` 或 `volunteer:activity:update` 的角色均可上传，
  因此组织管理员和学校管理员都能在编辑活动时补图。
- 静态访问：`/uploads/**` 由后端资源映射提供，Sa-Token 放行；生产 Nginx 可直接
  `alias /opt/vcp/uploads/`。
- 事务：活动保存失败会回滚 `attachment` 记录；文件系统不在数据库事务内，当前保留一个
  “失败上传或替换后的旧文件不自动删除”的已知限制，后续可加定时清理。

新增/修改的数据库脚本：

| 文件 | 内容 |
|---|---|
| `sql/11_activity_images.sql` | `attachment.content_type/caption/sort_order`、业务排序索引、附件字典补齐；增量可重复执行 |
| `sql/12_activity_images_demo.sql` | 5 个演示账号 + 1 个组织 + 3 个已发布活动 + 3 张图片元数据；增量可重复执行 |

实测：后端 11 模块 `mvn package` / `mvn test` 通过；组织管理员完整走通
「上传 → 创建活动 → 详情返回封面和图片」；学校管理员上传返回 `code=0`；
`/uploads/demo/activities/*.png` 返回 200 且带 `X-Content-Type-Options: nosniff`；
`sql/09_consistency_check.sql` 20 项违规合计 0。

### 14. B24 学生自助签到闭环 + 两个横切缺陷（B32 / B33）（2026-09-26）

**背景**：学生自助签到链路此前断在「拿不到 `attendanceId`」——`GET /api/v1/attendance` 挂的是
组织端权限 `volunteer:attendance:manage`，学生访问会拿到 20003（而前端把 20003 当登录失效，
表现是掉线）。本轮补上这一环，并在验收过程中挖出两个**既有横切缺陷**一并修掉。

**① 新增 `GET /api/v1/attendance/mine`**（`AttendanceController.mine`）：

- 只挂 `@SaCheckLogin`、**不挂权限码** —— 学生的 5 条权限里没有签到项，新增权限码要改
  `vcp-framework` 的静态权限表；真正的门禁是「数据范围只取登录态」：学生档案 id 取
  `StudentIdentityUtils.requireCurrentStudentId()`，**前端传的 `studentId` / `orgId` 一律忽略**。
- 非学生账号调用返回 **10003**（刻意不用 20003：前端命中 20003 会清 token 跳登录）。
- 参数 `page` / `pageSize` / `status` / `activityId` / `keyword`；返回 `R<PageResult<AttendanceVO>>`，
  排序 `r.id DESC`，过滤与列表逐字一致（`r.deleted = 0 AND s.deleted = 0 AND a.deleted = 0`）。
- 取数：`AttendanceRecordMapper.selectMyAttendancePage` + XML `<select id="selectMyAttendancePage">`，
  复用 `attendanceColumns` / `attendanceFrom` 两个片段，`status` 的惰性判定 CASE 分支与列表相同。

**② `AttendanceVO` 新增 4 个字段**（纯增量，`vcp-certification` 的时长提交页按字段名取数不受影响）：

| 字段 | 含义 |
|---|---|
| `activityStartAt` / `activityEndAt` | 活动开始 / 结束时间（`yyyy-MM-dd HH:mm:ss`），学生端用来解释窗口 |
| `canSignIn` | `Boolean`：惰性判定后状态为 `NOT_SIGNED`/`ABSENT` 且 `AttendancePolicy.isSignInOpen` |
| `canSignOut` | `Boolean`：状态为 `SIGNED_IN` 且 `AttendancePolicy.isSignOutOpen` |

两个布尔**与 sign-in / sign-out 接口的准入条件逐条对应**（同一份 `AttendancePolicy`），
所以前端按钮显隐与接口受理同源，不会出现「按钮能点、点了必然报错」。

**③ B32 关键字查询全线 `code=10000`**（横切，非本轮引入）：

`ILIKE CONCAT('%', ?, '%')` 中 `CONCAT` 是 `VARIADIC "any"`，连接串带 `stringtype=unspecified`
→ 参数以 unknown 下发 → `42P18 could not determine data type of parameter $1` → 被兜成 10000。
**直连执行同一 SQL 却成功**，所以纯 SQL 复核会漏。修法 `CONCAT('%', #{x}::text, '%')`，
共 **11 处**：`VolunteerActivityMapper.xml`、`ActivitySignupMapper.xml`、
`AttendanceRecordMapper.xml`（4 处）、`PortraitAggregateMapper.java`（3 处，含 tag 筛选）。
实测修后：`/attendance/mine?keyword=社区`→2、`/activities?keyword=社区`→65、`/attendance?keyword=张`→74、
`/signups?keyword=社区`→4、`/portraits?keyword=张`→150、`/portraits?tag=热心志愿者`→**1471**（与文档逐标签人数吻合）。

**④ B33 `toDeadline` 把 deadline 的时分秒恒抹成 23:59:59**（横切，非本轮引入）：
判据 `value.length() <= DATE.toString().length()` 里的 `DATE.toString()` 是
`DateTimeFormatter` 的描述串（**78 字符**）而非模式串（10），条件恒真 →
「进行中且可报名」的活动用接口造不出来。改为看输入串是否含时间部分后，
**纯 API** 即可建出 `start=12:25 / deadline=12:24` 的活动。

**验收**（独立子代理，脚本 `.tmp-verify/verify-attendance-mine.ps1`）：正向 A1–A8 全 PASS
（含人工修正到 +2 小时后 `hours=2.00`、并进入 `/attendance?status=SIGNED_OUT` 候选名单）、
负向 B1–B7 全 PASS、**零漂移 PASS**（看板 389 / 10719 / 19322.1 —— 收尾把上一会话遗留的 +2.8 存量还原后为
**19319.3**，`sql/09` 复跑违规 0；存活行数 389 / 10719 / 8235，
前后逐项相同）。`mvn -B package -DskipTests` 11 模块 BUILD SUCCESS，后端已重启（开发口径）。

> ⚠️ **行数验收口径**：B31 的刻意软删会让 `activity_signup` / `attendance_record` 的 `COUNT(*)`
> 比文档口径**各多 2 行**（10721 / 8237，多出的两行 `deleted = 1`）。**请用 `WHERE deleted = 0`**。

---

## 三、关键决策与理由

| 决策 | 理由 |
|---|---|
| `vcp-dependencies` **不继承**聚合根 | 否则与聚合根的 BOM 导入形成循环，Maven 无法读取工程 |
| **Flyway 暂时关闭** | 表结构在开发期会频繁变动，而 Flyway 的已执行脚本受 checksum 保护、不可再改，此时开启会处处报错。待表结构稳定后，把 `02_schema.sql`/`03_init_data.sql` 复制到 `db/migration/` 并改名为 `V2__`/`V3__` 即可开启 |
| 主键用 `BIGSERIAL` + `id-type: auto` | 数据库自增，与建表脚本和初始数据一致 |
| 时间类型用 `TIMESTAMP`（非 `timestamptz`） | 沿用原始设计。单时区部署无影响；若将来跨时区需改 |
| **并非所有表都有 `deleted`** | 关联表、流水/日志表加逻辑删除没有意义、只增加查询负担。详见 `sql/README.md` 第三节 |
| 演示数据用**确定性算法**（取模而非 `random()`） | 保证三个成员执行后看到完全一致的演示效果 |
| ~~数据库口令直接写在 `application.yml`~~ **2026-09-23 已撤销** | 当初是团队决定，**代价是口令进了 git 历史**。现已改为环境变量 / `application-local.yml` 外置（见第二节第 2 条），并已于 2026-09-23 **轮换口令**：旧口令实测被服务端拒绝，历史里的旧口令已失效（见第二节第 9 条） |
| **时长只能开给已通过审核的报名** | 反查报名的 SQL 带 `status IN ('APPROVED','COMPLETED')`（`DurationRefMapper.java:176`）。理由：`deleted` 与 `status` 是**两套语义** —— 取消 / 驳回只改 `status`、不软删（A7），只过滤 `deleted` 会把待审核、已驳回、已取消的报名一并放进来，直接污染 `total_duration`（B22）。副作用：**已取消报名的学生开不出时长** —— 这是刻意口径，取消报名不产生服务时长 |

---

## 四、未完成 / 待办

### P0：原定需拍板的 7 项（已全部拍板，无阻塞项）

> ✅ **已解决 2 项**：公益等级的档位阈值与公益标签的判定规则已于 2026-09-22 确认并落地。
> **口径说明**：这「2 项」指 `sql/README.md` 第五节那批待定项里已解决的两项，
> 与本表 P0 的 7 项**不是同一个集合**（前者 2 项、后者 7 项），两个数字不要混算。
> 依据见 `docs/公益等级与标签规则方案.md`，实现见 `sql/04_demo_data.sql`。
> 结论：等级采用**演示尺度阈值**（普通 <3 / 一星 3-6 / 二星 6-10 / 三星 10-20 / 四星 20-40 / 五星 ≥40 小时，实际部署需上调）；
> 标签**新增**「助老服务型」「文化传播型」两个；类型标签**不设**参与次数下限，并已改进演示数据为学生预设偏好分类。

以下事项**在《开发计划》《知识库》《选题对话》三份文档里都没有定义**，不定就无法落笔；`sql/README.md` 第五节有更完整的说明。✅ 2026-09-24 复核：这 7 项已全部在 `docs/待办清单.md` 的 A 组（A1–A7）拍板并落地到代码，本表已无阻塞项。

| # | 待定项 | 现状 | 阻塞的模块 |
|---|---|---|---|
| 1 | **服务时长如何计算** | ✅ 2026-09-24 已完成：A1 已拍板并落地（结论见 `docs/待办清单.md` 的 A1）—— `AttendancePolicy.resolveHours` 按「(签退时间 - 签到时间)/3600、保留 2 位小数；缺签退或签到状态为 `ABNORMAL` 时取活动预计时长；上限为活动预计时长的 1.5 倍」实现，见 `vcp-volunteer/src/main/java/com/vcp/volunteer/support/AttendancePolicy.java:121`、常量 `vcp-volunteer/src/main/java/com/vcp/volunteer/constant/VolunteerConstants.java:26`。原疑问：是否取 `签退时间 - 签到时间`？是否受活动预计时长约束？异常签到的时长怎么算？ | `vcp-certification` |
| 2 | **签到/签退的时间窗口** | ✅ 2026-09-24 已完成：A2 已拍板并落地（结论见 `docs/待办清单.md` 的 A2）—— 活动开始前 30 分钟开放签到、结束后 30 分钟内必须签退，逾期不引入定时任务、由查询时惰性判定为 `ABSENT`，见 `vcp-volunteer/src/main/java/com/vcp/volunteer/support/AttendancePolicy.java:85`、常量 `vcp-volunteer/src/main/java/com/vcp/volunteer/constant/VolunteerConstants.java:20`、惰性判定 SQL `vcp-volunteer/src/main/resources/mapper/volunteer/AttendanceRecordMapper.xml:75`。原疑问：活动开始前多久可签到？结束后多久必须签退？逾期是否自动记缺勤？ | `vcp-volunteer` 签到 |
| 3 | **报名人数上限的并发控制** | ✅ 2026-09-24 已完成：A3 已拍板并落地（结论见 `docs/待办清单.md` 的 A3）—— 用 `UPDATE volunteer_activity SET signed_count = signed_count + 1 WHERE id = ? AND signed_count < max_count` 原子更新、影响行数为 0 即「已满」，不做「先查再写」、不用行锁，见 `vcp-volunteer/src/main/java/com/vcp/volunteer/mapper/VolunteerActivityMapper.java:35`（调用点 `SignupServiceImpl.java:130`）。原疑问：`signed_count` 是冗余计数列，与报名明细可能漂移。测试项提到"活动已满继续报名"，但没说怎么防超卖 | `vcp-volunteer` 报名 |
| 4 | **审核驳回后如何重新提交** | ✅ 2026-09-24 已完成：A4 已拍板并落地（结论见 `docs/待办清单.md` 的 A4）—— `REJECTED` 允许重新提交、回到 `PENDING_AUDIT`；因 `service_duration.signup_id` 是 UNIQUE 只能更新原行，且每次提交都往 `duration_audit` 写一条新的 `SUBMIT` 流水（保留驳回历史），见 `vcp-certification/src/main/java/com/vcp/certification/service/impl/DurationServiceImpl.java:173`。原疑问：测试项提到该场景，但无状态机定义 | `vcp-certification` |
| 5 | **`total_duration` 以哪张表为准** | ✅ 2026-09-24 已完成：A5 已拍板并落地（结论见 `docs/待办清单.md` 的 A5）—— 以 `student_info.total_duration` 为准，时长审核通过时由后端原子累加，`student_profile` 仅作画像快照（画像与看板侧同样只读 `student_info`），见 `vcp-certification/src/main/java/com/vcp/certification/mapper/DurationRefMapper.java:240`。原疑问：`student_info` 与 `student_profile` 各有一个同含义字段 | 画像、看板 |
| 6 | **时间类型是否改 `timestamptz`** | ✅ 2026-09-24 已完成：A6 已拍板**不改**（结论见 `docs/待办清单.md` 的 A6）—— 继续用 `TIMESTAMP` + `LocalDateTime`（单一部署、无跨时区需求）。理由见第三节决策表「时间类型用 `TIMESTAMP`（非 `timestamptz`）」一行 | 全局 |
| 7 | **逻辑删除与唯一约束的冲突** | ✅ 2026-09-24 已完成：A7 已拍板并落地（结论见 `docs/待办清单.md` 的 A7）—— 「取消 = 改 `status = 'CANCELED'`、复用同一行」，`deleted` 保持 0，不改唯一约束；重新报名时把该行改回 `PENDING` 而不是软删除后插新行，见 `vcp-volunteer/src/main/java/com/vcp/volunteer/service/impl/SignupServiceImpl.java:147`。原疑问：`activity_signup` 有 `uk_activity_student` 唯一约束，同时又有 `deleted` 软删除。若"取消报名"用软删除实现，同一学生再次报名会撞唯一约束 | `vcp-volunteer` 报名 |

### P1：工程侧待办

| # | 事项 | 说明 |
|---|---|---|
| 1 | 各模块业务代码 | ✅ **6 个模块全部完成**（2026-09-23）：`vcp-system`、`vcp-org`、`vcp-volunteer`、`vcp-certification`、`vcp-portrait`、`vcp-analytics`。见第二节第 8 条 |
| 2 | `vcp-common` 基础设施 | ✅ 已完成。另于 2026-09-23 补了 `PageQuery`（分页基类，字段名必须是 `page`/`pageSize`，上限 1000 而非 100）与 `DateTimeUtils` |
| 3 | `vcp-framework` 基础设施 | ✅ 已完成。另于 2026-09-23 补了 `PageUtils.toPage`，并给 `@OperationLog` 加了 `params()` 属性（登录/注册必须传 `false`，否则明文密码会落进 `operation_log.params`） |
| 4 | MyBatis 扫描方式 | ✅ 已定为每个接口加 `@Mapper`（见 `docs/待办清单.md` 的 B3） |
| 5 | **Sa-Token 拦截器要放行文档路径** | ✅ 已完成并实测（见 `docs/待办清单.md` 的 B4）。放行清单集中在 `SaTokenConfig.EXCLUDE_PATHS`，新增放行项请加到那里 |
| 6 | MapStruct 注解处理器 | BOM 里已声明版本但无模块使用。Boot 4 的父 pom **不再配置 `annotationProcessorPaths`**，将来加 MapStruct 时必须显式配 `annotationProcessorPaths`（lombok + mapstruct-processor + lombok-mapstruct-binding 三者缺一不可），否则 mapper 实现类会静默不生成 |
| 7 | Flyway 开启 | 待表结构稳定后（见第三节） |
| 8 | **通知另外两位同学更新 `application-local.yml`** | 云库口令 2026-09-23 已轮换，他们本地那份还是旧口令 —— 不更新就连不上库，且**会误以为是代码问题**（见第二节第 9 条）。清理 git 历史里的口令仍建议推迟到答辩之后。新口令的获取渠道已写进 `README.md` 的「新同学四步上手」一节 |
| 9 | **`operation_log.target` 写入侧从不填** | 后端代码问题，`sql/07` 覆盖不到（它只管演示数据规模）。需给 `@OperationLog` 加 `target` 属性、让各 Controller 传入业务对象名，再把日志页那一列加回来；涉及面广，单独排期（见 `docs/待办清单.md` 的 B20-6） |
| 10 | **`StudentInfo.java` 缺 `gender` 字段** | DB 侧 `student_info.gender` 已由 `sql/05` / `06` 补列，Java 实体没跟上；**当前无消费方**，等有页面要用「性别」时再补，顺带确认 `StudentVO` 是否透出（见 `docs/待办清单.md` 的 B21） |
| 11 | ~~本机构建的 jar 里含 `application-local.yml`~~ | ✅ 2026-09-24 已完成：`vcp-boot/pom.xml:69` 起按父 pom 的两段式结构补了 `<resources>`，并在 `vcp-boot/pom.xml:81` 精确排除 `application-local.yml`；clean 重打后实测 `BOOT-INF/classes/` 下只剩 `application.yml`、`application-prod.yml`、`application-local.yml.example`。**对外分发 jar 前仍建议自查一遍** |
| 12 | **口令加密与登录防护** | ✅ 2026-09-24 已完成。`vcp-framework/pom.xml` 加 `spring-security-crypto`（版本由 Boot BOM 的 `spring-security.version` 管），新增 `vcp-framework/.../util/PasswordUtils.java`（BCrypt 强度 10；`encode`/`matches`/`isEncoded`/`checkPolicy`，策略 6–32 位），**口令读写全工程只经这一个类**（登录校验、注册写入、管理员新增、本人改密、管理员重置五处）；`matches` 对明文记录一律返回 false，刻意不做明文兜底。新增 `LoginAttemptGuard`（内存 `ConcurrentHashMap`，连续 5 次失败锁 15 分钟、成功清零、15 分钟无新失败则计数作废，**对不存在的用户名同样计数**以防枚举）。新增接口 `PUT /api/v1/auth/password`（本人改密，校验旧口令，踢其它会话保留当前会话）与 `PUT /api/v1/system/users/{id}/password`（管理员重置，留空为默认 `123456`，踢全部会话）；新错误码 `ACCOUNT_LOCKED(20004)`、`OLD_PASSWORD_ERROR(20005)`，**刻意不放进前端 `UNAUTHORIZED_CODES`**。种子脚本 `sql/03`/`04`/`07` 已换密文，新增 `sql/08_password_bcrypt.sql`（老库增量刷密文，可重复执行、末尾自检）。**实测**：云库实跑 `08` → 1509 账号 100% 密文、明文残留 0；真后端 12 项断言全过（明文 `123456` 登录成功、第 5 次错口令返回 20004 且锁定期内正确口令仍拒绝、不存在账号同样在 5 次后被锁、双 token 改密后 A 死 B 活、管理员重置后全部 token 失效、越权重置 20003、改密/重置日志 `params` 为空） |
| 13 | **学生自助签到闭环缺两环** | 签到记录本身由「报名审核通过」自动生成（`SignupServiceImpl.java:203`），后端 `sign-in`/`sign-out` 也已实现（只挂 `@SaCheckLogin`）；缺的是两环：① **没有「查我自己的签到记录」接口** —— `GET /api/v1/attendance` 挂 `volunteer:attendance:manage`，学生拿不到 `attendanceId`；② 前端 `src/api/volunteer.js:28`~`:29` 的 `signIn`/`signOut` **全仓无调用**、学生端无签到页面（见 `docs/待办清单.md` 的 B24） |
| 14 | **取消活动不处理报名** | `ActivityServiceImpl.updateStatus`（`:236`）只在流转到 `CLOSED` 时结算报名；`CANCELED` **只改活动状态** —— 该活动下 `PENDING`/`APPROVED` 的报名仍留在学生端「我的报名」，并按 B20-1 的口径继续计入 `signed_count`。需拍板是否批量置 `CANCELED`（见 B25） |
| 15 | **`PENDING_SUBMIT` 无生产者** | DB 默认值是它（`sql/02_schema.sql:398`）、枚举与多处注释都按「组织还没提交」解释它，但**全仓无代码写入**（开时长即落 `PENDING_AUDIT`，`DurationServiceImpl.java:184`）→ 该状态恒为空、字典里的「待提交」是死值。二选一：补「组织提交」步骤，或把该状态从枚举 / 字典 / 文档里摘掉（见 B26） |
| 16 | **默认口令 `123456` 硬编码在两处** | 后端 `UserServiceImpl.java:59` 的 `DEFAULT_PASSWORD`，以及前端 `UserManageView.vue` 的提示文案与重置弹窗。正式部署建议改为首次登录强制改密（见 B27） |
| 17 | **组织列表 N+1** | `OrgServiceImpl.toVO`（`:181`）逐行调 `metricsOf` → `metricsPort.getMetrics`（`:196`、`:203`），列表页 1 + N 次查询；组织仅 8 条，暂无感（见 B28） |
| 18 | **后端零自动化测试** | 9 个模块 `src/test` 全空、`vcp-boot` 之外无测试依赖。答辩口径：人工用例表（D2 / D3）+ 一致性自检 SQL（`sql/09`）+ 端到端脚本（`.tmp-verify/verify.ps1`），并如实说明未做单元测试（见 B29） |

---

## 五、环境与连接信息

| 项 | 值 |
|---|---|
| Spring Boot | 4.1.1（Spring Framework 7.0.9、Tomcat 11） |
| JDK | Java 21 |
| 构建 | Maven 3.9.16 |
| 数据库 | PostgreSQL 18.6 |
| 库名 | `volunteer_cert_portrait` |
| 主机 | `103.40.14.100:19476`（家庭服务器，经内网穿透暴露） |
| 用户 | `postgres`，口令不在仓库里：见 `application-local.yml`（本地，已 gitignore）或环境变量 `VCP_DB_PASSWORD` |
| 后端端口 | 8080 |
| 接口文档 | `http://localhost:8080/doc.html` |

**数据库脚本的执行方式**见 `sql/README.md` 第二节。

**默认账号**（登录口令均为 `123456`；库里 `sys_user.password` 存的是它的 **BCrypt 密文**，明文不落库）：

| 账号 | 角色 |
|---|---|
| `admin` | 学校管理员 |
| `org_admin` | 组织管理员 |
| `student` | 学生 |
| `org_admin2` ~ `org_admin8` | 组织管理员（演示数据） |
| `stu0001` ~ `stu0030` | 学生（演示数据） |

---

## 六、如何复现验证

```bash
# 1. 构建（11 个模块应全部 SUCCESS）
cd volunteer-cert-portrait-server
mvn -B package -DskipTests

# 2. 启动
java -jar vcp-boot/target/vcp-boot-1.0.0.jar
# 期望：Started VcpApplication，Tomcat 监听 8080

# 3. 验证接口文档
curl -o /dev/null -w "%{http_code}\n" http://localhost:8080/doc.html   # 期望 200

# 4. 重建数据库（可选，会清空数据）
cd ../sql
psql -U postgres -h 103.40.14.100 -p 19476 -d volunteer_cert_portrait -f 02_schema.sql
psql -U postgres -h 103.40.14.100 -p 19476 -d volunteer_cert_portrait -f 03_init_data.sql
psql -U postgres -h 103.40.14.100 -p 19476 -d volunteer_cert_portrait -f 04_demo_data.sql
# 05、06 是后端接口依赖的补列脚本（纯增量、可重复执行），也必须执行
psql -U postgres -h 103.40.14.100 -p 19476 -d volunteer_cert_portrait -f 05_backend_gap_fix.sql
psql -U postgres -h 103.40.14.100 -p 19476 -d volunteer_cert_portrait -f 06_backend_gap_fix2.sql

# 5. 一致性自检（只读、可重复执行；20 项违规数全 0 即通过）
psql -U postgres -h 103.40.14.100 -p 19476 -d volunteer_cert_portrait -f 09_consistency_check.sql
```

`04_demo_data.sql` 末尾附有验证查询（各表行数、看板自检），可直接复制执行。

`09_consistency_check.sql` 是**只读自检**（20 项检查 + 1 行汇总，汇总行 `check_no = 99`），
可在任意阶段执行、不改变数据：跑完看汇总行「检查项总数 20，违规合计 N」，**20 项违规数全 0 即通过**
（2026-09-24 在云库实跑，违规合计 0）。前置条件：`01`~`06` 必须已执行，`07` 可选
（没跑过 `07` 的库也能执行不报错，只是第 15、20 项会命中非 0）。

---

## 七、附：状态枚举码表（参考信息，前端无需参与）

数据库统一存**英文大写码**，中文由 `sys_dict` 表翻译。

| 字典类型 | 码 → 中文 |
|---|---|
| `activity_status` | `DRAFT` 草稿 / `PUBLISHED` 已发布 / `CLOSED` 已结束 / `CANCELED` 已取消 |
| `signup_status` | `PENDING` 待审核 / `APPROVED` 已通过 / `REJECTED` 已驳回 / `CANCELED` 已取消 / `COMPLETED` 已完成 |
| `attendance_status` | `NOT_SIGNED` 未签到 / `SIGNED_IN` 已签到 / `SIGNED_OUT` 已签退 / `ABNORMAL` 异常 / `ABSENT` 缺勤 |
| `duration_status` | `PENDING_SUBMIT` 待提交 / `PENDING_AUDIT` 待审核 / `APPROVED` 已通过 / `REJECTED` 已驳回 |
| `org_status` | `PENDING` 待审核 / `APPROVED` 已通过 / `REJECTED` 已驳回 / `DISABLED` 已停用（2026-09-23 由 `sql/05` 补） |
| `notification_type` | `SIGNUP` 报名结果 / `DURATION` 时长审核 / `SYSTEM` 系统公告 |
| `audit_action` | `SUBMIT` 提交 / `APPROVE` 通过 / `REJECT` 驳回（注意与状态值的 `APPROVED`/`REJECTED` 不同形） |
| `attachment_biz_type` | `ACTIVITY` 活动图片 / `ORG` 组织资质 / `AVATAR` 用户头像 |
| `user_status` | `ACTIVE` 正常 / `DISABLED` 已停用（2026-09-23 由 `sql/05` 补；`sys_user.status` 库里存 1/0，由服务层翻译） |

> ⚠️ 《开发计划与分工.md》第八阶段的示例统计 SQL 是错的：它从 `service_duration` 里 `SELECT college`，但该表**没有**这个字段（学院在 `student_info` 里，需 JOIN），且状态存的是 `APPROVED` 而非中文 `已通过`。正确写法见 `sql/README.md` 第六节。
