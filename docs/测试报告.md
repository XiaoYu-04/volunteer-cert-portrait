# 系统测试报告（D3）

> 用例编号（`TC-F-*` / `TC-E-*` / `TC-X-*`）与预期结果见 `docs/测试用例表.md`。
> 本报告只记录**实际执行过**的结果，每条都给出可复现的证据；未执行的部分单独列在
> 第 6 节，不写成「已通过」。

## 1. 测试概况

| 项 | 内容 |
|---|---|
| 测试时间 | 2026-09-24（**2026-09-25 在整库重建后的全量库上复验**：`sql/09` 20 项违规合计 0、三角色冒烟全过、`verify.ps1` 修好后 PASS 30 / SKIP 2 / FAIL 0） |
| 执行方式 | **真实后端**（前端 `VITE_USE_MOCK=false`，请求经 `/api` 打到 8080，数据取自云库） |
| 后端 | Spring Boot 4.1.1 / Java 21，`mvn -B package` **11 个模块 BUILD SUCCESS**（2026-09-25 重新打包并重启为 pid 32936） |
| 前端 | Vue 3 + Vite，`npm run build` 通过（产物 `dist/`） |
| 数据库 | 云上 PostgreSQL 18.6；**2026-09-25 已整库重建并回灌全量演示数据**（`02 → 03 → 04 → 05 → 06 → 07 → 10 → 11 → 12`，随后 `09` 只读复验）。口径两组：**`07` 口径 1500 学生 / 386 活动 / 10719 报名 / 19319.3 小时**；**叠加 `12` 后 1503 学生 / 389 活动**（报名与时长不变）。另有 5 个图文演示账号 / 1 个组织 / 3 张活动图片 |
| 接口规模 | 后端 **59 条路径 / 72 个「方法 + 路径」**（运行实例 `GET /v3/api-docs` 实查，2026-09-24）；前端 `src/api/` **70 个调用**；差额 2 个是后端有、前端没调的 `POST /portraits/recompute` 与 `PUT /orgs/{id}`（见 `docs/前端进展与待办.md` 第二节第 9 条） |
| 自动化脚本 | `.tmp-verify/verify.ps1`（端到端断言，非入库脚本；**2026-09-25 修好并实跑 PASS 30 / SKIP 2 / FAIL 0**）、`sql/09_consistency_check.sql`（只读 20 项自检）、`.tmp-verify/verify-college.ps1`（注册链路 11 项）、`.tmp-verify/verify-college-admin.ps1`（学院管理 41 项，见第 3.7 节） |

> **库状态注记（2026-09-24 补）**：第 2 节的 `sql/09` 20 项自检结论、第 3.6 节的
> `scanned=1503`、第 4.2 节的 `studentId=1503`、第 8 节的还原值，都是**本报告实测当时**的云库环境。
> 该批全量演示数据曾在第三轮被整体 `TRUNCATE`；第七轮已补入小型图文演示数据
> （5 个账号 / 1 个组织 / 3 个活动 / 3 张图片），并实跑 `sql/09` 违规合计 0。
> 需要复现 1500 学生规模的旧结论时，再按 `sql/README.md` 的顺序执行 `sql/07_demo_scale.sql`。
>
> ✅ **库状态注记（2026-09-25 补，本注记覆盖上面那段）**：云库已按 `sql/README.md` 的**整库重建路径**
> 重灌 —— `02 → 03 → 04 → 05 → 06 → 07 → 10 → 11 → 12`，随后只读复验 `09_consistency_check.sql`：
> **检查项总数 20、违规合计 0**（20 行明细全 0）。上面「需要复现时再跑 `07`」这一步**已经做过**，
> 库里现在就是全量演示数据（1503 学生 / 389 活动 / 10719 报名 / 19319.3 小时），可以直接复现本报告。
> 两组口径的差别见第 1 节「数据库」行与第 8 节。

## 2. 结论摘要

| 组别 | 用例数 | 通过 | 受限/未通过 | 说明 |
|---|---|---|---|---|
| 功能测试 | 48 | 43 | 5 | 未通过项集中在「签到窗口」与「学生自助签到无入口」，见第 5 节 |
| 流程测试 | 10 | 9 | 1 | 第 ④ 步「学生签到签退」受签到窗口限制，已用组织端人工修正路径补齐 |
| 异常测试 | 15 | 15 | 0 | 全部按预期返回错误码 |
| 数据一致性 | 20 | 20 | 0 | `sql/09` 云库实跑，**违规合计 0** |

**本轮同时修复了 2 个缺陷并当场验证通过**（第 4 节），其中 1 个是 P0 级数据正确性问题。
**2026-09-24 第四轮又补测了学院链路**（用例表外的补充实测，见第 3.7 节）：学院管理接口脚本
**PASS 41 / FAIL 0**、注册链路 **11 项全过**，前端 `lint` / `build` / `verify:mock` 三份日志也都通过。

## 3. 功能测试结果

### 3.1 登录与账号（TC-F-01 ~ TC-F-13）

实测：三个角色均可用 `student` / `org_admin` / `admin` + 口令 `123456` 登录成功
（`code=0`，学生会话带 `studentId=1`、组织管理员带 `orgId=1`）；未登录访问受保护接口返回
`20001`；学生调用管理端接口返回 `20003`。口令密文化、改密、重置、登录失败锁定已在此前批次
（B15）验证，本批次未重复执行。

### 3.2 活动与报名（TC-F-14 ~ TC-F-24）

实测：报名成功（状态「待审核」）；重复报名返回 `30007`；活动已结束返回 `30005`；
「我的报名」只返回本人记录；组织端只看到本组织报名。

### 3.3 审核（TC-F-25 ~ TC-F-31）

实测：组织管理员审核通过后，报名状态转「已通过」且**自动生成签到记录**
（`attendanceId` 随之出现，状态 `NOT_SIGNED`）；学校管理员审核组织资质、时长审核均正常；
越权与重复审核按预期拒绝。

### 3.4 签到（TC-F-32 ~ TC-F-36）

实测：

- 组织端「签到管理」的人工修正接口 `PUT /api/v1/attendance/{id}` 可用 —— 把记录修正为
  `SIGNED_OUT` 并写入签到签退时间，回查状态正确。
- **签到窗口规则生效**：窗口外调用 `POST /api/v1/attendance/sign-in` 返回
  `10001 签到尚未开放，活动开始前 30 分钟才可签到`。
- **签到接口是学生专属**：用组织管理员 token 调用返回 `10003`（该接口按会话里的
  `studentId` 定位本人，与 `StudentIdentityUtils` 的越权防护一致）。
- `TC-F-36`（学生自助签到）**仍未通过**：学生端没有页面入口，且报名 VO 不返回
  `attendanceId`，学生无从取得该参数。属已知限制，见第 5 节。

### 3.5 时长审核（TC-F-37 ~ TC-F-43）

实测：组织端提交时长成功（`code=0`）；学校端能看到待审记录（`hours=2.5`）并审核通过；
学生累计时长由 **0.0 变为 2.5**；重复提交被拒
（`10001 该学生此活动的服务时长已通过审核，无法重复提交`）。

### 3.6 公益画像与数据看板（TC-F-44 ~ TC-F-48）

实测：

- 手动重算接口 `POST /api/v1/portraits/recompute` 可跑：`scanned=1503, updated=3`
  —— **只写真正变化的行**，幂等性符合设计。
- 重算后新学生的公益等级被自动补写为「普通志愿者」（2.5 小时落在 `<3` 档），
  证明「等级由重算按 `PublicWelfareLevelEnum.of()` 口径补写」这条设计成立。
- 数据看板 `/api/v1/analytics/dashboard` 正常返回。

### 3.7 学院字典与学院管理（用例表外的补充实测，2026-09-24 第四轮）

本节不在 `docs/测试用例表.md` 的编号内 —— 学院管理接口与「注册 / 新增用户必选学院」是本轮新增的功能，
用例表（73 条）尚未补对应条目，所以只按脚本断言口径记录，不折算成 TC 编号。本节实测跑在**清库之后**
的云库上（脚本基线：2 个账号 / 0 份学生档案 / 1 个组织 / 5 个学院），与第 3.1 ~ 3.6 节的环境不同。

被测对象：

- 后端 `vcp-system` 新增 4 个学院管理接口，全部挂权限码 `system:college:manage`（只发学校管理员）：
  `GET` / `POST /api/v1/system/colleges`、`PUT /api/v1/system/colleges/{id}/status`、
  `DELETE /api/v1/system/colleges/{id}`。学院不是独立表，是 `sys_dict` 里 `dict_type = 'college'`
  的字典行；列表带 `studentCount` / `orgCount`，组织数经 `OrgCollegeCountPort` 端口由 `vcp-org` 统计。
- 学院校验收敛到 `DictService.containsEnabled(dictType, key)` 一处，**注册**（`AuthServiceImpl`）与
  **管理员新增用户**（`UserServiceImpl`）两条写路径共用，失败文案逐字一致（「请选择学院」）。
- 前端新增「学院管理」页（第 29 页，`src/views/admin/CollegeManageView.vue`）与「用户管理 → 新增学生」
  的学院下拉；时长审核 / 组织管理 / 学生画像三处「学院」筛选由自由文本改成字典下拉。

实测（脚本在运行实例上实跑，脚本与输出留存在 `.tmp-verify/`）：

- `.tmp-verify/verify-college-admin.ps1` —— **PASS 41 / FAIL 0**（`.tmp-verify/verify-college-admin.out`）。
  覆盖到：列表（5 个学院、按 `sort` 升序、首个是计算机学院、字段齐全、学生数 / 组织数）；权限
  （组织管理员读写都被拒 20003、未登录被拒 20001）；新增的 3 类非法输入（重名「该学院已存在」、空名
  「请填写学院名称」、超 30 字「学院名称最多 30 个字」）与不传 `sort` 时自动补 6；启停（停用后注册页
  下拉不含它、管理端列表仍含停用项、`"1"` 也能启用、非法状态码「学院状态不合法」、不存在的学院被拒）；
  删除（空学院可删、重复删除「学院不存在」、占用校验两种文案：有学生、有学生 + 组织）；管理员新增学生的
  学院校验（缺学院 / 非法学院都被拒、组织管理员不要求学院）；`student_info.college` 落库与学号
  `S` + 六位占位口径；收尾自清理后回到基线（2 账号 / 0 档案 / 5 学院）。
- `.tmp-verify/verify-college.ps1` —— **11 项全过**（`.tmp-verify/verify-college.out`）。覆盖到：免登录
  `GET /auth/colleges` 可访问、5 个学院顺序一致；注册缺 `college` / 非法学院都被拒且文案为「请选择学院」；
  合法注册后会话与 `GET /auth/me` 都回显学院、会话带 `studentId`。
- 前端：`npm run lint` 与 `npm run build` 两份日志都 exit 0（`.tmp-verify/lint.log` / `.tmp-verify/build.log`，
  构建产物里有 `CollegeManageView` 的独立 chunk）；`npm run verify:mock` **28 项全过**
  （`.tmp-verify/verify-mock.log`），其中本轮新增的 4 条是学院字典断言（无重名 / `sort` 互不相同 /
  与 `sql/10` 的种子逐字一致且顺序一致 / 至少有一个未被占用的学院）。
  **后续（同日第五轮）**又加了 3 条口径断言，现为 **31 项全过**（`.tmp-verify/verify-mock2.log`）：
  学生与组织的学院都在字典里 / 学院与专业、班级同下标无错配 / 学院排名的名字都在字典里。
  同轮还做了全站 29 页 × 2 视口的逐页走查（56 条记录 0 信号），见
  `docs/前端进展与待办.md` 第二节第 16 条。

未覆盖 / 未复核（如实记录）：

- 后端 `src/test` 仍为空目录，**学院接口没有单元测试**（与第 5 节「零自动化测试」同一条）。
- 页面级走查只在 **mock 构建**（`VITE_USE_MOCK=true`）下做过（1440 视口），未对着真后端逐页点；
  620 视口未重截（依据 `docs/前端进展与待办.md` 第二节第 15 条）。
- 三个写操作在 `operation_log` 里的**成功落痕未逐条核对** —— 只核过一条失败记录
  （`module = 用户与权限` / `action = 启停学院` / `result = FAIL`，见 `docs/后端进展与待办.md` 第二节第 12 条）。
- 脚本的占用场景与收尾清理是 **SQL 直改 / 直删**（`org_info.college` 直改、测试账号直删），不是走业务
  接口造数与清理；「组织占用」这条校验因此只验到拒绝分支的文案与计数。

### 3.8 活动图片上传与展示（2026-09-24 第七轮）

本轮新增 `POST /api/v1/attachments/images`，并接入活动发布、列表与详情：

- 存储：PostgreSQL `attachment` 只保存 `file_url` / `file_size` / `content_type` /
  `caption` / `sort_order`；图片本体保存到后端 `uploads/`，活动封面另存于
  `volunteer_activity.cover`。
- 校验：JPG/PNG、单张不超过 5MB、Java `ImageIO` 识别真实格式、最大像素限制；
  `/uploads/**` 静态响应带 `X-Content-Type-Options: nosniff`。
- 功能：发布页 1 张封面 + 最多 6 张图文说明；详情页显示封面与图集；列表页显示封面缩略图。

实测结果：

| 检查 | 结果 |
|---|---|
| 组织管理员上传 PNG | `code=0`，文件写入 `uploads/activities/2026/09/` |
| 创建带图活动 | `code=0`，详情返回 `cover` 和 `images[1]`，说明字段一致 |
| 学校管理员上传 | `code=0`（权限 `create OR update` 生效） |
| 静态图片访问 | HTTP 200，响应头 `X-Content-Type-Options: nosniff` |
| 浏览器渲染 | 发布页 2 个上传区域；列表 3 张封面全部加载；详情封面 `naturalWidth=1536`、图集说明正常 |
| 工程检查 | 后端 `mvn package` / `mvn test`、前端 build / ESLint / Prettier 全过 |
| 数据一致性 | `sql/09_consistency_check.sql` 20 项违规合计 0 |

已知限制：替换或删除活动图片时只清理数据库关联，磁盘旧文件暂不自动删除，后续可加定时清理。

## 4. 本轮修复的缺陷与验证证据

### 4.1 P0：时长可以开给「未通过审核」的报名

- **缺陷**：`DurationRefMapper.selectSignupId` 只过滤 `deleted = 0`，而取消报名/驳回只改
  `status` 不做逻辑删除，于是「待审核 / 已驳回 / 已取消」的报名也能反查成功 → 组织端可提交
  时长 → 学校审核通过后直接累加 `student_info.total_duration`，且**没有撤销接口**。
- **修复**：反查 SQL 增加 `AND status IN ('APPROVED','COMPLETED')`，并纠正了该处 javadoc
  里「取消 = 软删除」的错误前提；提交失败文案改为点明「报名未通过审核」。
- **验证**：端到端脚本断言 —— 对一条**待审核**报名提交时长，返回
  `30008 该学生在此活动下的报名未通过审核（可能为待审核、已驳回、已取消，或该学生未报名）`；
  同一条报名经组织审核通过后，同一请求返回 `code=0` 成功。修复前后行为差异明确。

### 4.2 P1：学生注册后没有学生档案，学生端整条链路不可用

- **缺陷**：注册只建 `sys_user` 与角色关联，不建 `student_info`，会话里 `studentId` 恒为
  `null`，新学生的「我的报名 / 我的时长 / 我的画像」全部返回
  `10003 当前账号未关联学生档案`。而开发计划的**流程测试第一环就是「学生注册」**
  （见计划书第 833~844 行），这一环断了整条闭环走不通。
- **修复**：新增建档入口 `StudentArchiveRegistrar.ensureArchive(userId)`，在「学生注册」与
  「管理员新增学生用户」两条路径上调用，与账号插入同一事务；占位学号为
  `S + 六位零填充 userId`（注册表单不收集学号，而该列 `NOT NULL UNIQUE`）。
- **验证**：注册新学生后 —— 会话直接带 `studentId=1503`、`studentNo=S001513`；
  `GET /auth/me` 有档案；`GET /portraits/me` 返回 `50001 画像尚未生成`（不再是 `10003`）；
  `GET /signups` 正常返回 `code=0`。随后该学生完成了完整的报名 → 审核 → 时长 → 审核流程，
  累计时长从 0.0 增加到 2.5。

## 5. 未通过 / 受限项（如实记录）

| 编号 | 现象 | 判定 |
|---|---|---|
| TC-F-36 | 学生自助签到无页面入口，且拿不到 `attendanceId` | **未实现**，属已知限制；后端接口存在，仅缺入口 |
| TC-F-32/33 | 窗口外无法签到（活动未开始时返回「签到尚未开放」） | **符合设计**，非缺陷；窗口内可签到，窗口外走组织端人工修正 |
| TC-F-14 | 活动发布后状态为 `PUBLISHED`，但 `ActivityVO.status` 返回英文码 | 契约一致，非缺陷（状态机统一「英文码存库 + 字典翻译」） |
| — | `PENDING_SUBMIT`（待提交）状态全链路无生产者，组织端「时长提交」页的状态分布真后端恒为 0 | **未实现**，已知限制 |
| — | 操作日志「操作对象」列恒空（`@OperationLog` 未采集 `target`） | **部分实现**，已在前端止血 |
| — | 取消活动不释放名额、不终止报名；活动结束后 PENDING 报名无终态 | **未实现**，已知限制 |
| — | 后端 9 个模块 `src/test` 为空目录，**零自动化测试** | 已拍板用本用例表 + 本报告 + `sql/09` 替代（C7） |
| —（**2026-09-25 新发现，用例表外**） | **删除用户不级联**：`DELETE /api/v1/system/users/{id}` 只置 `sys_user.deleted = 1`，该学生的 `student_info` / 报名 / 时长仍留在库里并**继续计入看板**（实测 1503→1505 学生、10719→10721 报名、19319.3→19321.8 小时，而 `sql/09` 20 项仍全 0） | **未修复**，已知限制；见 `docs/下一步待办.md` 第 41 项 / `docs/待办清单.md` 的 B31。**演示时不要删用户**；删了要重跑整条重建链（约 16 秒）才回到干净口径 |

> 以上「已知限制」与 `docs/答辩前待办.md` 第 4 节「明确不做」一致，答辩时按「已知限制」陈述即可。
> **2026-09-25 补**：本表新增了最后一条「删除用户不级联」——它**不在 D2 的 73 条用例编号内**
> （是云库重建那轮跑端到端脚本时才暴露的），所以第 2 节的用例统计（功能 48 / 流程 10 / 异常 15）
> **不受影响**；如实记在这里是为了不让它只存在于会话记录里。

## 6. 未执行的部分（下次接着做）

1. **浏览器实测**：本轮未做前端页面的浏览器走查（上一轮 C3/C4 的 56 张截图所在的
   `volunteer-cert-portrait-web/.tmp-shots/` 已不在仓库中，需重截）。功能验证通过接口层完成，
   页面渲染未在本轮覆盖。（2026-09-24 第四轮补：新增的第 29 页「学院管理」已在 mock 构建下走查过一遍，
   见第 3.7 节；真后端逐页点检与 620 视口重截仍未做。）
   **2026-09-25 补**：会话 8 已按**全站 29 页 × 2 视口**（含第 29 页、含 620 视口）走查一遍、
   记录 **0 信号**（见第 3.7 节），所以「620 视口未走查」这一条**已不成立**；
   **仍未做的是「对着真后端逐页点检」**（会话 8 走的是 mock 构建），以及**截图入库**（D4）。
2. **D5 部署验收**：部署文档与配置已写（`docs/部署文档.md` + `deploy/`），但「在干净机器上照文档走一遍」尚未执行，
   Nginx 托管与 history 回退未实测。
3. **prod profile 三项收紧**：只做了配置层核对（文档 + `application-prod.yml` 逐项对照），
   未实际以 `--spring.profiles.active=prod` 启动验证。
4. **异常测试的并发分支**（TC-X-13 快速重复点击）未做并发压测，只做了串行重复提交。

## 7. 复现步骤

```bash
# 1) 后端
cd volunteer-cert-portrait-server
mvn -B package -DskipTests
java -jar vcp-boot/target/vcp-boot-1.0.0.jar        # 默认 8080

# 2) 端到端断言（真实后端口径）
powershell -File .tmp-verify/verify.ps1

# 3) 只读数据一致性自检（20 项，期望汇总行 violations = 0）
java -cp <postgresql-42.7.13.jar> .tmp-run-sql/RunSql.java . sql/09_consistency_check.sql

# 4) 前端生产构建
cd volunteer-cert-portrait-web && npm run build
```

> 脚本说明：`.tmp-verify/` 下的脚本是本轮验证用的临时产物（已被 `.gitignore` 的 `.tmp-*`
> 规则忽略）。`verify.ps1` 会新建测试学生账号并在结尾自动清理；
> `cleanup.sql` / `fix7.sql` 用于把测试数据彻底还原（含 `signed_count` 派生列回退），
> 两者都可重复执行。
>
> **复现前提（2026-09-25 已改写）**：本报告实测所用的演示数据（1500 学生 / 386 活动等）
> **已在 2026-09-25 整库重建时灌回**，`student` 演示账号也在库里 —— 现在**可以直接复现本报告**，
> 不必再先跑一遍 `02`~`07`。重建序列：`02 → 03 → 04 → 05 → 06 → 07 → 10 → 11 → 12`，
> 随后只读复验 `09`（检查项总数 20、违规合计 0）。
>
> ⚠️ **`verify.ps1` 本身在 2026-09-25 被修好过**（旧版跑不通，三处都在脚本侧）：
> ① 注册请求体没带 `college`（B30 之后注册必填学院）→ 从第 3 步起级联失败；
> ② 选活动没限定「本组织」，会拿到 `demo_org_01` 的图文演示活动 → 用 `org_admin` 开时长被
> `10001 活动「…」不属于本组织` 正确拒绝（是脚本的选择逻辑问题，不是产品缺陷）；
> ③ 签到 / 签退受 A2 窗口约束，而演示数据里没有进行中的活动 → 这两步改为记 `SKIP`
> 并原样打印后端文案，不再伪装 PASS。修完实跑：**PASS 30 / SKIP 2 / FAIL 0**。
> ⚠️ **该脚本跑完会留下测试残留**（见第 5 节新增条目与 `docs/下一步待办.md` 第 41 项），
> 本轮是**重跑整条重建链**才把库恢复成干净口径的 —— 所以不要写「verify 跑完库仍是 1503 / 10719 / 19319.3」。

## 8. 演示数据还原核对

本轮端到端测试会写入真实数据，测试后已用 `cleanup.sql` + `fix7.sql` 完全还原并复核
（下表是 **2026-09-24 当时**的核对结果）：

| 指标 | 测试后还原值 | 文档口径 | 结论 |
|---|---|---|---|
| 学生档案 | 1500 | 1500 | ✅ |
| 活动 | 386 | 386 | ✅ |
| 报名 | 10,719 | 10,719 | ✅ |
| 累计时长 | 19,319.3 | 19,319.3 | ✅ |
| `sql/09` 20 项自检违规合计 | 0 | 0 | ✅ |

**2026-09-25 整库重建后的行数核对**（只读实查，两组口径别混用）：

| 指标 | `07` 口径 | 叠加 `12` 后（**当前库中值**） | 结论 |
|---|---|---|---|
| 学生档案 | 1500 | **1503** | ✅ 差 3 = `12` 新建的 3 个图文演示学生 |
| 活动 | 386 | **389** | ✅ 差 3 = `12` 新建的 3 场带图活动（id 387/388/389） |
| 报名 | 10,719 | 10,719 | ✅ 不变（`12` 不造报名） |
| 累计时长 | 19,319.3 | 19,319.3 | ✅ 不变（`12` 不造时长） |
| `sql/09` 20 项自检违规合计 | 0 | **0**（2026-09-25 复跑，检查项总数 20） | ✅ |

> `07` 末尾自检在 2026-09-25 实跑时**原样通过**，逐字输出：
> `[07 自检] 全部通过：学生 1500 人、活动 386 场、报名 10719 条、累计时长 19319.3 小时（演示账号 22.6 小时 / 标签 热心志愿者,长期坚持型,大型活动型）`。
> 该脚本重跑第二遍时输出**与第一遍逐字相同**，可作「演示数据是确定性算法、不是 `random()`」的实证。

> 还原过程中发现一处需要注意的机制：`volunteer_activity.signed_count` 是**应用层维护的派生列**，
> 直接用 SQL 删 `activity_signup` 行不会回退它，会让 `sql/09` 第 7 项报出不一致。
> 手工清理数据后必须同步重算该列（`fix7.sql` 已按第 7 项的口径写成通用语句）。

> 库状态注记（2026-09-24 补）：上表是**当时**的还原核对结果；该批演示数据随后在 2026-09-24 第三轮被整体清空
> （16 张表 `TRUNCATE`，只重灌「基础数据 + 2 个测试管理员」，`sql/10_base_and_test_accounts.sql`），
> **当时云库不含上表的任何数字**（补注时只读实查：四张业务表均 0 行，见第 1 节注记）。
> 本节结论只对当时的环境成立。
>
> ✅ **库状态注记（2026-09-25 补，本注记覆盖上面那段）**：2026-09-25 已整库重建并回灌全量演示数据，
> **上面那张「行数核对」表的报名与时长两行已经回到库里**（10,719 / 19,319.3）；
> **学生与活动两行现在是 1503 / 389**（比上表各多 3 —— `12` 的图文演示叠加在 `07` 之后，
> 明细见上一张 2026-09-25 的表）。口径提醒：**1500 / 386 是 `07` 提交那一刻的口径**，
> **1503 / 389 才是现在库里的值**，两组别混用。
