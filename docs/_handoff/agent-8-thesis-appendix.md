# Agent-8 交接：论文附录（`docs/论文/附录.md`）

> 日期：2026-09-25 ｜ 代理 C ｜ 任务：为毕业论文初稿补写**附录**
> **只写了 1 个新文件**（`docs/论文/附录.md`）+ 本交接文件，**未改任何其它文件**
> （未动 7 个共享总账、未动 01~05 章、未动 `docs/论文/README.md`、未跑构建、未连数据库、无 git 写操作）。

---

## ① 一句话结论

**附录 A 的接口清单是从运行中后端的 OpenAPI 取到的（`http://127.0.0.1:8080/v3/api-docs`，
实测 59 路径 / 72 个「方法 + 路径」），并用 18 个 Controller 源码解析做了交叉校验，两路集合完全一致；
摘要与权限码两列因为 OpenAPI 里没有（本项目 `@Operation` 是自研的 `OperationLog` 日志注解、
不是 Swagger 的），改由 Controller 源码的 javadoc 首句与 `@SaCheckPermission` 提供。**

其余六个附录分别取自脚本原文与文档实查记录，**全程未连数据库**
（云库连接被打满，且脚本是更可靠的权威来源）。

---

## ② 产物清单

| 文件 | 行数 | 字符数 | 汉字数（全文，口径同 `docs/论文/README.md:26-28`） | 汉字数（**不含表格行**） | 表格数 | Mermaid |
|---|---|---|---|---|---|---|
| `docs/论文/附录.md` | **889** | **50,974** | **11,740** | **4,056** | **44** | 0 |
| `docs/_handoff/agent-8-thesis-appendix.md` | 279 | — | — | — | — | — |

> **两个字数的区别**（请按需取用）：
> - **11,740** = 全文 U+4E00–U+9FFF 计数，**口径与 `docs/论文/README.md` 的"章节总览"表一致**（该表含表格与代码块）→ **填 README 表用这个**；
> - **4,056** = **剔除以 `|` 开头的表格行**后的汉字数，即任务要求的"正文论述"口径（要求 3,000~6,000）→ **落在区间内**。
>
> 附录本身是**索引型**的（44 张表），所以两个数差得较多，这是设计使然。

**核验命令**（可复跑，统计脚本在 `.tmp-verify/_agent8_count.py`，未入库）：

```powershell
& "<python>" .tmp-verify\_agent8_count.py
# 输出：行数 889 / 字符 50974 / 汉字 11740（正文 4056 + 表格 7684）/ 表格 44
```

**结构自检**：44 个 Markdown 表格块**列数全部一致**（0 处错列），校验脚本 `.tmp-verify/_agent8_validate.py`。

---

## ③ 每个附录的数据来源与核对方式（逐条）

### 附录 A 接口清单

| 项 | 来源 / 命令 |
|---|---|
| 59 路径 / 72 方法+路径 | **本次实测**：`Invoke-WebRequest -Uri 'http://127.0.0.1:8080/v3/api-docs'`（HTTP 200 / 50.4 KB），`@($j.paths.PSObject.Properties).Count` → **59**；遍历 6 个 HTTP 动词计数 → **72** |
| 逐条「方法+路径」 | 解析 `.tmp-verify/api-docs.json`（落盘的 OpenAPI 原文） |
| 模块分组 | 按 Controller 所在 Maven 模块（`volunteer-cert-portrait-server/<module>/.../controller/*.java`），18 个文件 |
| **用途（摘要）** | Controller 方法的 **javadoc 首句**。⚠️ **OpenAPI 的 `summary` 实测为空**（72 条里 0 条）—— 本项目 `@Operation` 是 `com.vcp.framework.log.OperationLog` 日志注解，不是 Swagger 的 `@Operation` |
| **权限码** | Controller 的 `@SaCheckPermission` 注解值 |
| 鉴权三档分布 | 52 条 `@SaCheckPermission` + 2 条 `@SaCheckRole`（`StudentController.java:41`、`:53`）+ 7 条 `@SaCheckLogin`（`AnalyticsController.java:60`、`DurationController.java:56`/`:68`、`OrgController.java:43`/`:49`、`AttendanceController.java:62`/`:76`）+ 11 条无方法级注解 = **72** |
| **交叉校验** | 源码解析出的 72 条与 OpenAPI 的 72 条做**集合比对**：`源码有 OpenAPI 无 = []`、`OpenAPI 有源码无 = []`、`两边完全一致 = True` |

**取数脚本**（均未入库，在 `.tmp-verify/`）：
`_agent8_openapi.py`（拉 OpenAPI + 按 tag 分组）、`_agent8_controllers.py`（解析 18 个 Controller + 集合比对）、`_agent8_mktables.py`（生成表格）、`_agent8_multi.py`（多权限码清单）。

**⚠️ 过程中修正的一个解析缺陷（值得记一笔）**：初版解析脚本的正则对
`@SaCheckPermission(value = {"a","b"}, mode = SaMode.OR)` 这种**多权限码 OR** 形态
**只抓到了第一个码**。复核时发现全库共 **2 处**这种写法，已修正并重新统计：

| 接口 | 真实注解 | 出处 |
|---|---|---|
| `POST /api/v1/attachments/images` | `volunteer:activity:create` **OR** `volunteer:activity:update` | `AttachmentController.java:34` |
| `GET /api/v1/signups` | `volunteer:signup:create` **OR** `volunteer:signup:audit` | `SignupController.java:44` |

修正后**总数仍是 72 / 52，不受影响**（只是这 2 条的权限码从单码变成 OR 双码）。
附录 A.3 已把这个 OR 语义与它的业务含义（"学校管理员也能传图"、"学生与管理员共用报名列表接口"）写清楚。

### 附录 B 数据库表清单与关键列

| 项 | 来源 |
|---|---|
| 16 张表 + 149 个基线列 | 解析 `sql/02_schema.sql` 的 16 个 `CREATE TABLE` 块（行号 `:51` `:80` `:100` `:117` `:145` `:168` `:190` `:213` `:244` `:279` `:298` `:337` `:369` `:398` `:434` `:459`） |
| **真实补列 25 个** | **逐条解析 `ALTER TABLE … ADD COLUMN`**：`05` 补 15、`06` 补 9、`07` 补 1 |
| 最终列数 174 | 149 + 25（**与按 `docs/ER图.md` 逐表字段表统计的结果一致**，两条路径互相印证） |
| 9 表有 `deleted` / 7 表无 | `sql/02_schema.sql` DDL + `sql/README.md:165-179` 的原因说明 |
| 23 条关系（18 物理外键 + 5 业务关联） | `docs/ER图.md:499-520`、`:522-530`；N:N 1 组见 `:532-536` |
| 索引 29 条语句 → 27 个不同名 | `docs/答辩材料/数据库说明.md:132-140` |
| 唯一约束 11 个 + 部分唯一索引 1 个 | `docs/答辩材料/数据库说明.md:150-179` |

**取数脚本**：`.tmp-verify/_agent8_schema.py`、`_agent8_er.py`、`_agent8_addcols.py`。

**⚠️⚠️ 本附录最重要的一处更正（请主会话注意）**：
`docs/ER图.md:12`、`:305-307` 称 `attachment` 的 `content_type` / `caption` / `sort_order`
是"**11 补列**"，但**这 3 列在 `sql/02_schema.sql:220-222` 的建表语句里已经有了**
（已逐行读过确认），`sql/11:18-20` 的 `ALTER … IF NOT EXISTS` 对**新建库是空操作**。

- 因此**真实补列是 25 而不是 28**（ER 图的标注若照抄会多算 3 个）；
- 这处差异**已被 `docs/答辩材料/数据库说明.md:381` 记录**为 ER 图的 3 处已知差异之一；
- **附录 B 一律以脚本原文为准**，并在 B.2 用引用块显式写出这处更正。
- 同源的第二处：`docs/ER图.md:8` 称 `02` 含"**10 个**命名索引"，实为 **11 条** `CREATE INDEX`
  （多出 `idx_attachment_biz_sort`，与 `11:26` 同名重申）—— 见 `数据库说明.md:380`。附录 B.5 按 **11 条**标注。

### 附录 C 角色权限矩阵

| 项 | 来源 |
|---|---|
| 权限静态表 | `volunteer-cert-portrait-server/vcp-framework/src/main/java/com/vcp/framework/security/StpInterfaceImpl.java:39-75`（`ROLE_PERMS`） |
| 为什么写死 | 同文件 `:19-27`（后端**无权限表**：`sys_role` 只有 `role_code`/`role_name`，无 perms 列、无关联表） |
| 前后端一致性要求 | 同文件 `:24-27`（与 `volunteer-cert-portrait-web/src/stores/user.js` 的 `ROLE_PERMS` 同步） |
| 递归坑 | 同文件 `:127-144`（`StpInterfaceImpl` 里不能调 `StpUtil.getRoleList()`） |
| 29 个页面 | `volunteer-cert-portrait-web/src/router/routes/{student,org,admin,common}.js`（学生 9 / 组织 6 / 学校 10 / 公共 4；`common.js` 的 `Root` 是 redirect，不计页面） |

**取数脚本**：`.tmp-verify/_agent8_perms.py`。

**⚠️ 一处口径更正（任务书里写的是"三角色 17 条权限码"，实测不是）**：

| 角色码 | 权限码数 | 出处 |
|---|---|---|
| `STUDENT` | **5** | `StpInterfaceImpl.java:40-46` |
| `ORG_ADMIN` | **7** | `:47-55` |
| `SCHOOL_ADMIN` | **17** | `:56-74` |
| **授予关系合计** | **29** | 三角色相加 |
| **不同权限码（去重）** | **24** | 4 个码被多角色共用 |

即 **17 是"学校管理员"一个角色的条数，不是三角色合计**。
`CLAUDE.md` 与 `docs/论文/README.md` §3.3（快照 `:204`）记的"学生 5 / 组织 7 / 学校 17"本身没错，
但**不要把 17 当成三角色总数**。附录 C.2 已把这个更正显式写在正文里。

**被多角色共用的 4 个码**：`volunteer:activity:list`（三角色都有）、
`volunteer:activity:update`、`volunteer:signup:audit`、`volunteer:attendance:manage`（后三个是组织+学校）。

### 附录 D 测试用例索引

| 项 | 来源 |
|---|---|
| 73 条（功能 48 + 流程 10 + 异常 15） | `docs/测试用例表.md`：功能 `:26-104`、流程 `:106-123`、异常 `:125-143` |
| 8 条图片用例（**不计入 73**） | `docs/测试用例表.md:145-156` |
| 错误码 | 用例表原文（其源头是 `vcp-common` 的 `ErrorCodeEnum.java`） |
| 20 项一致性检查 / 93 项合计 / 87 通过 6 受限 | 正文 `05-系统测试.md:881-883`（附录只做交叉引用，不重复论证） |

**方式**：逐节提取编号、把"操作步骤"压缩成"名称"、预期结果保留原文（含错误码）。

### 附录 E SQL 脚本与验证脚本清单

| 项 | 来源 |
|---|---|
| 12 个脚本的用途 | `sql/README.md:11-24` 的清单表 |
| **是否幂等** | **逐脚本核对正文**，不是照抄 README：<br>· `01` ❌ 不可重复（头部 `:8-9` 明说 `CREATE DATABASE` 无 `IF NOT EXISTS`）<br>· `02` ✅（16 条 `DROP TABLE IF EXISTS … CASCADE`）<br>· `03` ❌（15 条 `INSERT`、**0 条 `ON CONFLICT`**）<br>· `04` ❌（16 条 `INSERT`、**0 条 `ON CONFLICT`**）<br>· `05`/`06`/`11` ✅（`ADD COLUMN IF NOT EXISTS` + `ON CONFLICT DO NOTHING`）<br>· `07` ✅（`should_scale` 守卫，`sql/07:58`）<br>· `08` ✅（只更新口令恰为明文 `123456` 的行）<br>· `09` ✅（全文只有 `SELECT`/`WITH`）<br>· `10` ✅（13 条 `INSERT` 带 **16 处 `ON CONFLICT`** + `setval`）<br>· `12` ✅（增量） |
| `10` 的前置条件 | ⚠️ **`10` 自身不 TRUNCATE** —— 头部 `:6-7` 写的是"**清库（16 张表全部 TRUNCATE）之后**，用本脚本一次性灌回"；且 `:100-101` 提醒清库会连带清掉 `05`/`06` 回填的值，跑完须按 `05` → `06` 重跑 |
| 执行顺序 + 三条顺序理由 | `sql/README.md:26-44`、`CLAUDE.md` 踩坑第 20 条 |
| `verify.ps1` 七步 | `.tmp-verify/verify.ps1` 的 `=====` 标题行（`:29` `:41` `:58` `:75` `:94` `:148` `:161`） |
| `verify.ps1` 未入库 | `git check-ignore -v .tmp-verify/verify.ps1` → `.gitignore:36:.tmp-*`（**实测确认**） |
| `verify-deploy.ps1` 的两个坑 | `deploy/verify-deploy.ps1:10-14`（未登录返回 HTTP 200 + `code=20001`；反代没配好会被 `try_files` 回退成 200 + index.html） |
| `09` 只读保证与前置条件 | `sql/09_consistency_check.sql:11-29`（无写操作、无临时表、无 psql 元命令；前置 `01`→`06`） |

### 附录 F 已知限制与未做项汇总

| 来源 | 取用内容 |
|---|---|
| `docs/论文/05-系统测试.md:781-876`（5.11 节） | 8 行受限项、4 项未执行、明确不做清单、其他技术问题 |
| `docs/答辩前待办.md:134-145`（第 4 节） | "明确不做"的 `B13`/`B14`/`B17`/`B18`/`B21`/`C9`/`C11`/`C12` |
| `docs/待办清单.md:710-735`（B31） | **最新**销账状态 |
| `docs/下一步待办.md:576-581`（第 42 项） | `verify.ps1` 跑完画像标签分布漂 6 个 `−1` |

**⚠️⚠️ 两处"章节已过时"必须让主会话知道**（附录 F.1 已显式标注，并给出两方出处）：

| # | 第五章 5.11 的写法 | **最新状态** | 最新出处 |
|---|---|---|---|
| 1 | `05-系统测试.md:794` 记 **B31「删除用户不级联」未修复** | ✅ **2026-09-25 已修并实测闭环**（同事务级联软删 + 重算 `signed_count`；修复后 `verify.ps1` 跑一轮**不漂**） | `docs/待办清单.md:710-735`、`docs/_handoff/agent-2-b31.md` |
| 2 | `05-系统测试.md:823` 记**截图入库（D4）未做** | ✅ **已入库 28 张 / 2.73 MB**（真后端口径） | `docs/论文/README.md:332`、`docs/答辩前待办.md:160` |

> 附录 F **按最新状态标注**，同时在表里保留第五章的原记录与行号 —— 这样"论文写的是未修、
> 实际已修"这件事在稿子里是**自洽可查**的，答辩时不会被问住。
> **但请注意**：`docs/答辩前待办.md:141-145` 也仍写着 B31 未修，**该文件是共享总账，本代理未改**，
> 需要主会话决定是否同步（这是第 ⑤ 节的一个待办）。

### 附录 G 演示账号与演示数据口径

| 项 | 来源 |
|---|---|
| 账号表 | 根 `README.md:144-156`；**逐条核对了写入语句**：`sql/03_init_data.sql:11-14`、`sql/12_activity_images_demo.sql:34-38` |
| 口令为 BCrypt 密文 | 根 `README.md:155-156`、`sql/README.md:141-142` |
| `org_admin2`~`8` / `stu0001`~`30` / `stu100001`+ | `sql/README.md:149-150`、`:84` |
| 三套口径 | `docs/论文/README.md` §4.3（快照 `:280-296`）、`docs/下一步待办.md:72-73`、根 `README.md:62-63` |
| 两组真库口径的成因 | `docs/测试报告.md:287-291`、`CLAUDE.md` 踩坑第 20 条 |
| 当前库行数 | `docs/下一步待办.md:19`（2026-09-25 整库重建后实查） |

> **一处如实留白**：`sys_user` 1514 与 `student_info` 1503 差 **11**。
> 已定位到 `admin`/`org_admin`/`demo_school_01`/`org_admin2`~`8`（共 10 个），
> **与 11 差 1，本轮未定位到那一个账号** —— 附录 G.2 已**如实写明"本轮未核实、如实留白"**，
> 没有编造构成。（要坐实需查库，而云库连接被打满。）

---

## ④ 可直接粘贴的 Markdown 片段

> ⚠️ **先读这条**：`docs/论文/README.md` **在本代理工作期间正被另一个代理并发修改**
> （`git diff` 实测：`+24 −1`，新增了"合稿顺序"代码块、并已把"系统截图"行改成 ✅）。
> 因此下面给的**行号是 2026-09-25 本代理收尾时的快照**，粘贴前请**按内容定位**，不要只认行号。

### 片段 1：`docs/论文/README.md` 的「一、章节总览」表 —— 补一行

**插入位置**：在 `| README.md | 本文件（总览） | ... |` 那一行**之后**、
`| **合计（五章正文）** | ... |` 那一行**之前**（快照行号：第 22 行与第 23 行之间）。

```markdown
| `附录.md` | 附录（接口/表/权限/用例/脚本/限制/口径） | 889 | 50,974 | **11,740** | 0 |
```

**⚠️ 同时必须做的一件事（本代理不做，交给主会话）**：
本表下方两行合计**已经过时**，且**不只因为附录**——同批还有
`00-摘要与关键词.md`、`绪论.md`、`结论.md`、`参考文献.md`（外加待写的 `致谢.md`）
等新文件（`git status` 可见，均为未跟踪）。
**合计行必须在所有新文件定稿后统一重算**，不要只加附录这一行就完事。

参考：附录的"汉字数 11,740"是**全文口径**（含 44 张表），与本表其余行口径一致。
若院系要求按"不含表格"口径统计，附录另有 **4,056** 这个数（本代理已在附录产物清单里给出两个数）。

### 片段 2：`docs/论文/README.md` 的「5.1 本目录内待做」表 —— 更新「致谢 / 附录」行

**替换这一行**（快照行号：第 337 行）：

```markdown
| 7 | 致谢 / 附录 | ⚠️ **附录已产出、致谢未做** | **附录 ✅ 2026-09-25 已产出**：`附录.md`（889 行 / **11,740** 汉字全文、**4,056** 汉字不含表格 / **44 张表** / 0 Mermaid），含 A 接口清单（**OpenAPI 实测 59 路径 / 72 方法+路径**，与 18 个 Controller 源码解析结果**集合完全一致**）、B 表清单（16 表 / 149 基线列 + 25 真实补列 = **174 列** / 23 条关系）、C 权限矩阵（**29 条授予 / 24 个不同权限码**：学生 5 / 组织 7 / 学校 17）、D 用例索引（73 条 + 图片 8 条）、E 脚本清单（12 个 SQL + 3 个验证脚本，逐条标幂等性）、F 已知限制汇总（含 **B31 已修的更正**）、G 演示账号与三套口径。**致谢仍 ❌ 未做** |
```

> 说明：该行原文是 `| 7 | 致谢 / 附录 | ❌ 未做 | — |`。
> 若主会话更希望拆成两行（致谢、附录各一行），可自行拆分——内容可直接取用上面的文字。

---

## ⑤ 未做 / 待补项（如实列出）

### 5.1 本代理**没做**的事（按任务边界，不是遗漏）

| # | 未做 | 原因 |
|---|---|---|
| 1 | 未改 `docs/论文/README.md` | **任务明确要求由主会话统一更新**；片段已放在第 ④ 节 |
| 2 | 未改 7 个共享总账（含 `docs/答辩前待办.md`、`docs/待办清单.md`、`CLAUDE.md`） | 任务禁止 |
| 3 | 未改 01~05 章 | 任务禁止 |
| 4 | 未跑构建、未连数据库、无 git 写操作 | 任务禁止；且云库连接被打满 |
| 5 | 未更新 `论文初稿.docx` | DOCX 由主会话用 `build_docx.py` 统一重生成（附录是第 10 块，见 README 的"合稿顺序"） |
| 6 | 未做 Mermaid 图 | 附录是索引型，44 张表已足够；正文 18 图不动 |

### 5.2 需要主会话**决策**的两件事

| # | 事项 | 建议 |
|---|---|---|
| 1 | **`docs/答辩前待办.md:141-145` 仍写"B31 未修"**，与 `docs/待办清单.md:710` 矛盾 | 建议同步为"已修"，或在附录 F 的更正之外**再改一次总账**。附录 F 已用"两方出处并列"的方式兜住，但总账自身矛盾仍建议修 |
| 2 | **`docs/ER图.md` 的 2 处口径差异**（`:8` 的"10 个命名索引"应为 11；`:12`/`:305-307` 的"11 补列"实为 `02` 自带） | `docs/答辩材料/数据库说明.md:373-385` 已记录但**未回改 ER 图**。附录 B 已按脚本原文标注并写明更正；是否回改 ER 图请主会话定（**本代理未改**） |

### 5.3 附录自身的取数缺口（附录 F.7 已如实写明）

| 项 | 状态 | 原因 |
|---|---|---|
| 数据库**实时**行数与列数 | **未取到** | 云库 `FATAL: sorry, too many clients already`，**本次刻意不连库**；结构与列数以脚本原文为准，行数取 `docs/下一步待办.md:19` 的实查记录 |
| `sys_user` 1514 − `student_info` 1503 = **11** 的逐账号构成 | **未取到**（只定位到 10 个） | 同上，需查库 |
| OpenAPI 的 `summary` | **为空**（72 条 0 条有摘要） | 自研 `@Operation` 非 Swagger 注解；用途列改由 javadoc 首句提供 |
| 接口**请求/响应字段级**契约 | **未收录** | 属正文 3.7 节与 `/doc.html` 的职责，附录只做清单索引 |

### 5.4 三个可选增强（本代理认为**不必做**，列出来供判断）

| # | 想法 | 为什么建议不做 |
|---|---|---|
| 1 | 把附录 A 的 72 条再补"请求体字段" | 与正文 3.7 节重复，且会让附录从"索引"变成"第二份接口文档" |
| 2 | 把 20 项一致性检查逐项列进附录 | 正文 3.8 节与 5.7 节已逐项写过，附录只留了指向 `sql/09` 的索引 |
| 3 | 附录里加 Mermaid ER 图 | `docs/ER图.md` 已有总览图，正文 2.5 节也引了；重复会带来"两处不一致"的风险 |

---

## ⑥ 附：本代理产出的中间文件（**均未入库**，在 `.tmp-verify/`，可随时删）

| 文件 | 用途 |
|---|---|
| `_agent8_openapi.py` / `api-docs.json` | 拉取并落盘 OpenAPI（59/72 的实测来源） |
| `_agent8_controllers.py` / `agent8-appendixA-src.json` | 解析 18 个 Controller + 与 OpenAPI 集合比对 |
| `_agent8_mktables.py` / `agent8-appendixA-tables.md` | 生成附录 A 表格草稿 |
| `_agent8_multi.py` | 多权限码（`SaMode.OR`）清单 |
| `_agent8_perms.py` / `agent8-appendixC-report.txt` | 权限码统计（29 / 24） |
| `_agent8_schema.py` / `_agent8_er.py` / `_agent8_addcols.py` | 表结构、列数、真实补列（25）统计 |
| `_agent8_count.py` / `_agent8_validate.py` | 字数统计与表格完整性校验 |

> 这些都在 `.gitignore:36` 的 `.tmp-*` 覆盖范围内，**不会污染仓库**。
