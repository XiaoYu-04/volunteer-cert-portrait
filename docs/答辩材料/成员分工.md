# 成员分工说明（答辩材料）

> **用途**：答辩「小组分工」环节的说明材料，也是论文「致谢 / 分工」附页的来源。
> **整理时间**：2026-09-25，代码基线 `28c2ef6`。
>
> ## ⚠️ 关于姓名的如实声明（请先读这一段）
>
> 1. **本材料以「成员 A / 成员 B / 成员 C」为主标识** ——
>    这不是偷懒，而是因为 `docs/高校志愿服务时长认证与公益画像数据分析系统_开发计划与分工.md`
>    本身就是这么写的：该文档 `:935`~`:941` 原文为「**假设小组成员为：成员 A / 成员 B / 成员 C**」，
>    并注明「如果你们有真实姓名，可以把 A、B、C 替换成姓名」。
>    **仓库里没有任何一份文档把真实姓名映射到 A / B / C。**
> 2. **小组自报姓名确实存在**，来源是 `docs/第2组_高校志愿时长认证与公益画像系统_赖声煜_邓自洁_杨柏沣.docx`
>    的表头（第 0 张表第 1 行）：实验人 = **赖声煜 / 邓自洁 / 杨柏沣**，
>    学号 = **2024521109 / 2024521542 / 2024541250**（按出现顺序一一对应）。
>    文件名与表头内容**互相印证**，因此这三个姓名与学号**不是编造的**。
> 3. **但姓名 ↔ A/B/C 的对应关系未在任何文档中给出**，因此本材料**不擅自指派**，
>    一律标注「**待本人确认**」。
> 4. **指导教师**：该 docx 的表头里**只有「批阅人 / 批阅分数 / 批阅日期」三栏且均为空**，
>    **没有任何「指导教师」字段**；仓库其余文档也未提及。
>    因此本材料与 PPT 一律使用占位符「**指导教师：＿＿＿＿（待本人确认）**」。
> 5. 涉及姓名的最终定稿，**请三位同学本人各自确认后再替换占位符**。

---

## 一、小组构成与角色定位

**3 人小组**，按「**后端 + 数据库** / **前端 + 数据可视化** / **业务流程 + 测试 + 文档答辩**」
三条线分工。出处：`docs/高校志愿服务时长认证与公益画像数据分析系统_开发计划与分工.md:1358`~`:1362`。

| 标识 | 角色定位 | 负责主域 | 姓名（**待本人确认**） | 学号（**待本人确认**） |
|---|---|---|---|---|
| **成员 A** | 后端核心与数据库负责人 | 数据库设计 + Spring Boot 后端 + 部署 | ＿＿＿＿ | ＿＿＿＿ |
| **成员 B** | 前端页面与数据可视化负责人 | Vue 3 前端 + ECharts 图表 + 系统演示 | ＿＿＿＿ | ＿＿＿＿ |
| **成员 C** | 业务流程、测试与文档负责人 | 业务流程与状态设计 + 测试 + 论文/PPT | ＿＿＿＿ | ＿＿＿＿ |

**小组自报信息（来自 docx 表头，尚未与 A/B/C 对应）**：

| 项 | 内容 | 出处 |
|---|---|---|
| 组号 | **第 2 组** | docx 文件名 |
| 实验人 | 赖声煜、邓自洁、杨柏沣 | docx 第 0 张表第 1 行 |
| 学号 | 2024521109、2024521542、2024541250 | 同上（与上一行按顺序对应） |
| 所属班级 | 软件技术 2404 | docx 第 0 张表第 2 行 |
| 论文/实验方向 | 设计类（docx 实验类型栏勾选「设计」） | docx 第 0 张表第 5 行 |
| 指导教师 | **文档中无此字段，待本人确认** | —— |

---

## 二、按角色的职责与工作量占比

占比为 `docs/..._开发计划与分工.md` 给出的**建议值**（原文用代码块列出），
**不是实测工时统计**，如实标注。

### 成员 A —— 后端核心与数据库负责人

**职责**（出处 `:945`~`:955`）：搭建 Spring Boot 后端项目、设计数据库表结构、编写核心业务接口、
实现登录认证与权限控制、完成活动/报名/签到/时长审核接口、提供数据统计接口、负责后端部署。

**具体任务**（出处 `:959`~`:966`）：

| 阶段 | 任务 |
|---|---|
| 需求阶段 | 确定功能范围和技术方案 |
| 设计阶段 | 数据库设计、ER 图、表结构 |
| 开发阶段 | 用户、角色、活动、报名、签到、时长接口 |
| 统计阶段 | 数据看板 SQL 查询和统计接口 |
| 测试阶段 | 修复后端 Bug |
| 部署阶段 | 后端打包、接口部署 |

**负责模块**（出处 `:970`~`:976`）：登录认证 / 用户角色权限 / 志愿活动管理 / 活动报名 /
签到签退 / 服务时长审核 / 数据统计接口。

**工作量占比建议**（出处 `:980`~`:986`）：

```text
后端开发：45%    数据库设计：25%    接口联调：15%    部署：10%    文档协助：5%
```

### 成员 B —— 前端页面与数据可视化负责人

**职责**（出处 `:990`~`:1000`）：搭建 Vue 前端项目、完成页面布局、完成学生端页面、
完成管理员后台页面、使用 ECharts 实现数据看板、完成公益画像页面、与后端接口联调。

**具体任务**（出处 `:1004`~`:1011`）：

| 阶段 | 任务 |
|---|---|
| 原型阶段 | 页面结构、导航设计 |
| 开发阶段 | 前台学生页面 / 后台管理页面 |
| 统计阶段 | ECharts 图表展示 |
| 联调阶段 | 接口调用、页面数据渲染 |
| 答辩阶段 | 系统演示、页面截图 |

**负责模块**（出处 `:1015`~`:1023`）：登录注册页 / 首页 / 活动列表 / 活动详情 / 我的报名 /
我的时长 / 公益画像 / 后台管理页面 / 数据看板。

**工作量占比建议**（出处 `:1027`~`:1033`）：

```text
前端开发：55%    数据可视化：25%    接口联调：10%    页面测试：5%    文档协助：5%
```

### 成员 C —— 业务流程、测试与文档负责人

**职责**（出处 `:1037`~`:1048`）：梳理业务流程、设计状态字段、构造测试数据、编写测试用例、
负责系统测试、整理毕业设计文档、制作答辩 PPT、协助开发部分简单模块。

**具体任务**（出处 `:1052`~`:1059`）：

| 阶段 | 任务 |
|---|---|
| 需求阶段 | 需求说明、研究意义 |
| 设计阶段 | 流程图、状态设计 |
| 开发阶段 | 通知、日志、字典模块 |
| 测试阶段 | 测试用例、测试数据 |
| 文档阶段 | 论文整合、PPT |
| 答辩阶段 | 答辩稿、演示脚本 |

**负责模块**（出处 `:1063`~`:1071`）：需求分析 / 业务流程图 / 通知模块 / 操作日志 /
数据字典 / 测试用例 / 测试数据 / 论文材料 / 答辩 PPT。

**工作量占比建议**（出处 `:1075`~`:1081`）：

```text
文档与测试：40%    业务模块开发：25%    数据构造：20%    答辩材料：10%    协助联调：5%
```

---

## 三、按交付物的分工（对照实际产出）

下表把**已经真实产出的东西**归到三个角色上。归属依据是上面各角色的「负责模块」清单
+ `docs/会话记录.md` 的会话脉络 + `docs/待办清单.md` 的 D/E 组完成记录。

| 交付物 | 实际产出 | 归属（建议） | 依据 |
|---|---|---|---|
| **数据库** | 16 张表、23 条关系、`sql/01`~`12` 共 12 个脚本 | **成员 A** | 计划书 `:961`~`:962`「数据库设计、ER图、表结构」 |
| **ER 图** | `docs/ER图.md`（657 行，含 Mermaid 总览图） | **成员 A**（成员 B/C 复核） | 同上；`docs/待办清单.md:831` D1 |
| **后端 6 个业务模块** | `vcp-system` / `vcp-org` / `vcp-volunteer` / `vcp-certification` / `vcp-portrait` / `vcp-analytics` | **成员 A** | 计划书 `:970`~`:976` |
| **后端基础设施** | `vcp-common` + `vcp-framework`（`R<T>`、10 个状态枚举、Sa-Token、全局异常、操作日志切面、Knife4j） | **成员 A** | `docs/待办清单.md:923`~`:926`（B1+B2） |
| **安全三件套** | BCrypt 密文入库 + 5 次/15 分钟锁定 + 本人改密/管理员重置 | **成员 A** | `docs/答辩前待办.md:15` |
| **前端 29 个页面** | 学生端 / 组织端 / 学校端 + 登录注册 + 403 + 404；**纯手写水墨风组件库，不依赖第三方 UI 库** | **成员 B** | `docs/待办清单.md:912`~`:913` |
| **数据可视化** | **13 个 ECharts 图表**，配色由 CSS 令牌驱动 | **成员 B** | `docs/待办清单.md:914` |
| **前端 mock 层** | 60+ 接口，切换后端只需改 `VITE_USE_MOCK` | **成员 B** | `docs/待办清单.md:915` |
| **活动图片上传** | 封面 + 最多 6 张图文说明（前后端各一半） | **成员 A + 成员 B** | `docs/答辩前待办.md:20`；前端发布页 + 后端 `POST /attachments/images` |
| **测试用例表** | `docs/测试用例表.md`：**73 条**主体用例（功能 48 + 流程 10 + 异常 15）+ 8 条活动图片补充用例 | **成员 C** | 计划书 `:1057`；`docs/待办清单.md:833`~`:835` D2 |
| **测试报告** | `docs/测试报告.md`：含 20 项自检结果 + 如实记录的 5 项受限项与 4 项未执行项 | **成员 C** | `docs/待办清单.md:836`~`:839` D3 |
| **一致性自检脚本** | `sql/09_consistency_check.sql`（只读、20 项、可当场跑） | **成员 A 建脚本 / 成员 C 执行取证** | `docs/待办清单.md:906`~`:908` |
| **端到端验证脚本** | `.tmp-verify/verify.ps1`（**PASS 30 / SKIP 2 / FAIL 0**，未入库） | **成员 C** | `docs/答辩前待办.md:122` |
| **演示数据** | `sql/07_demo_scale.sql`（1500 学生 / 386 活动 / 10719 报名 / 19319.3 小时）+ `sql/12` 图文演示 | **成员 A 写脚本 / 成员 C 构造口径** | 计划书 `:1058`「测试数据」 |
| **部署文档与配置** | `docs/部署文档.md` + `deploy/nginx.conf` + `deploy/vcp.service`（**未实际部署**） | **成员 A** | 计划书 `:955`「负责后端部署」 |
| **系统截图（D4）** | **尚未入库**（仓库里 0 张图片） | **成员 B** | 计划书 `:1011`「系统演示、页面截图」 |
| **论文五章** | 未完成 | **成员 C 主导** | 计划书 `:1058`「论文整合」 |
| **答辩材料（本目录）** | PPT + 演示脚本 + 演示视频分镜 + 数据库说明 + 技术亮点 + 成员分工 | **成员 C 主导，A/B 供技术细节** | 计划书 `:922`「成员 C：论文整合与答辩 PPT」 |

> 计划书 `:920`~`:922` 的第十阶段负责人建议原文：
> 「成员 A：部署与后端文档 / 成员 B：系统截图与前端展示 / 成员 C：论文整合与答辩 PPT」。

---

## 四、开发阶段与角色配合（10 个阶段）

计划书把项目分成 **10 个阶段**，每阶段都给了「负责人建议」。逐阶段摘录如下
（行号为该阶段「负责人建议」段落的位置）：

| 阶段 | 名称 | 负责人建议 | 出处 |
|---|---|---|---|
| 第一阶段 | 需求确认与选题包装 | 成员 C 主导，A、B 配合 | `:418`、`:444`~`:447` |
| 第二阶段 | 原型设计与页面规划 | 成员 B 主导，C 协助，A 提后端字段需求 | `:451`、`:500`~`:504` |
| 第三阶段 | 数据库设计 | 成员 A 主导，B、C 参与讨论 | `:508`、`:533`~`:536` |
| 第四阶段 | 项目架构搭建 | A 负责后端架构，B 负责前端架构，C 协助测试登录流程 | `:549`、`:589`~`:593` |
| 第五阶段 | 基础管理模块开发 | A：用户/角色/权限接口；B：基础管理页面；C：组织/通知/字典数据 | `:597`、`:625`~`:629` |
| 第六阶段 | 志愿活动与报名模块开发 | A：活动/报名后端接口；B：活动列表/详情/报名页面；C：报名审核逻辑与测试数据 | `:639`、`:679`~`:683` |
| 第七阶段 | 签到签退与时长审核模块开发 | A：签到/时长/审核后端逻辑；B：签到管理与审核页面；C：流程/状态/异常测试 | `:693`、`:735`~`:739` |
| 第八阶段 | 公益画像与数据分析看板开发 | A：统计接口与 SQL；B：ECharts 图表与画像页；C：统计指标设计与测试数据构造 | `:750`、`:797`~`:801` |
| 第九阶段 | 系统测试与数据完善 | 成员 C 主导，A、B 配合修复问题 | `:812`、`:866`~`:869` |
| 第十阶段 | 部署、论文与答辩准备 | A：部署与后端文档；B：系统截图与前端展示；C：论文整合与答辩 PPT | `:880`、`:918`~`:922` |

**当前实际进度**：项目处在**第九阶段（系统测试与数据完善）已基本完成、正在进入第十阶段**
（部署 / 论文 / 答辩）。出处：`docs/答辩前待办.md:6`。

---

## 五、协作机制与实际会话脉络

### 5.1 计划书建议的协作机制

计划书第七节（`:1085` 起）建议**每周至少开一次小组会**，并列出会议内容与
「是否需要调整分工」的复盘项（`:1096`）。

### 5.2 实际工作脉络（有记录的部分）

`docs/会话记录.md` 从 **2026-09-24** 起按「会话」逐段记录过程、取舍与证据
（`:3`~`:9` 说明了三份文档的分工：待办清单=历史总账、下一步待办=还剩什么、会话记录=过程与证据）。
**2026-09-22 与 09-23 的工作没有单独的会话记录，只以「已完成」条目形式留档在
`docs/待办清单.md` 的 E 组。**

| 时间 | 主要产出 | 出处 |
|---|---|---|
| **2026-09-22** | 后端工程可构建可启动（修掉 BOM 循环依赖 + Boot 4 的 aop 改名）；**16 张表建成，20 项一致性自检全 0**；接口文档 `/doc.html` 可用；公益等级与标签规则确认并落地；**前端 28 个页面全部完成**（纯手写水墨风组件库）+ 13 个 ECharts 图表 + 60+ 接口 mock 层；`verify-mock-data.mjs` 18 项断言；**后端缺口全面审计**（4 路并行） | `docs/待办清单.md:905`~`:922` |
| **2026-09-23** | **B1+B2 基础设施完成并实跑验证**（`vcp-common` + `vcp-framework`，启动 3.3 秒、`/doc.html` 200、未登录返回 20001）；**A1–A5、A7 拍板并写进代码**；**6 个业务模块全部落地**（三角色 20 个接口全过、日志零异常）；**前后端联调 B12 审计通过**（前端 29 页、70 个调用与后端契约 0 缺失） | `docs/待办清单.md:923`~`:926`；`docs/待办清单.md:187`~`:189`；`CLAUDE.md`「当前进度」 |
| **2026-09-24（会话 1–10）** | 会话 1：P0 收口 → 第一批 P1 → **安全三件套**；会话 2：清 P1 文档小修 → **把「20 项一致性自检」落成可执行 SQL**；会话 3：修 C3 走查发现的 3 个 mock 缺陷 + 补断言；会话 4：**D5 部署的文档与配置**（未实际部署）；会话 5：git pull → 5 路只读摸底 → 2 个缺陷修复 + **D 组交付物**；会话 6：云库重置 + 修 B30 注册链路 + 学院字典与免登录接口；会话 7：**学院字典管理端**（4 接口 + 学院管理页）+ 去 AI 味文案改造；会话 8：**全站逐页走查** + 窄屏表格修复 + mock 学院口径收敛；会话 9：学生端页脚重做 + 375px 横向溢出修复；会话 10：**活动图片上传** + 图文演示数据 + 文档同步 | `docs/会话记录.md:13`、`:134`、`:215`、`:278`、`:348`、`:484`、`:644`、`:793`、`:854`、`:878` |
| **2026-09-25（会话 11）** | **云库整库重建 + 全量演示数据回灌**（`02`→`03`→`04`→`05`→`06`→`07`→`10`→`11`→`12`，随后只读复验 `09` 违规合计 0）；后端重新打包（11 模块 BUILD SUCCESS）并重启（pid 32936）；**三角色冒烟全过**；修好 `verify.ps1` 并实跑 **PASS 30 / SKIP 2 / FAIL 0**；**暴露新缺陷 B31（未修）**；重跑整链把库恢复干净；同步 7 个 Markdown | `docs/会话记录.md:949`~`:957` |

> **可引用的协作证据**：`docs/会话记录.md:988` 记录了一次**确定性实证** ——
> 整链重跑第二遍时 `07` 自检输出**与第一遍逐字相同**。
> 这证明「三个人各自执行得到同一结果」这个约定是**真的成立**的，
> 而不是写在文档里的口号（依据 `CLAUDE.md`「工作方式约定」的确定性算法要求）。

---

## 六、答辩现场可能被问到的问题

| 提问 | 建议回答 |
|---|---|
| 三个人具体怎么分的？ | 一条线一个人：A 后端 + 数据库、B 前端 + 可视化、C 业务流程 + 测试 + 文档。每阶段的负责人建议写在开发计划第四节的十个阶段里，我们按那个走。 |
| 谁的贡献最大？ | 三条线工作量按计划书的建议占比大致是 A 后端为主、B 前端为主、C 文档测试为主；**实际占比以三位同学自评与指导教师认定为准**，我们不单方面宣称。 |
| 有没有用 AI 辅助？ | 用了，作为编码与文档助手。但**所有结论都要求实跑验证**：数据改动必须实跑、`07` 自检带 9 条断言、`09` 20 项自检可当场跑、端到端脚本 PASS 30 / SKIP 2 / FAIL 0。踩过的坑（Boot 4 的 aop 改名、Jackson 3 换包名、Rolldown 的 `manualChunks` 等）都写进了 `CLAUDE.md` 的「踩过的坑」。 |
| 分工文档里的姓名为什么是空的？ | 因为仓库里的开发计划原文就是「成员 A/B/C」，且**没有任何文档把真实姓名映射到 A/B/C**。我们不擅自指派，留占位符由本人确认后填写 —— 这比写错更负责。 |
| 前后端联调谁负责？ | 计划书把「接口联调」同时列进了 A（15%）与 B（10%）；实际由 A 出契约、B 对接，C 在第九阶段做端到端验证。B12 联调审计的结论是「前端 29 个页面、70 个调用与后端 0 缺失」。 |

---

## 七、附：可直接使用的分工声明（模板，填完姓名即可）

> 本毕业设计《高校志愿服务时长认证与公益画像数据分析系统》由 **第 2 组** 3 人共同完成，
> 分工如下：
>
> - **＿＿＿＿（成员 A）**：负责数据库设计与实现（16 张表、23 条关系、12 个 SQL 脚本）、
>   后端工程搭建与 6 个业务模块开发、登录认证与权限控制、数据统计接口、
>   后端打包与部署文档。
> - **＿＿＿＿（成员 B）**：负责前端工程搭建与 29 个页面实现（纯手写水墨风组件库）、
>   13 个 ECharts 数据可视化图表、公益画像页面、前端 mock 层与接口联调、
>   系统截图与现场演示。
> - **＿＿＿＿（成员 C）**：负责业务流程梳理与状态字段设计、通知/日志/字典模块、
>   测试用例编写（73 条）与系统测试执行、演示数据构造与一致性取证、
>   论文整合与答辩材料（PPT、演示脚本、演示视频、数据库说明、技术亮点说明）。
>
> 指导教师：＿＿＿＿＿＿
> 日期：＿＿＿＿年＿＿月＿＿日

> ⚠️ 填写前请三位同学各自确认姓名与学号的对应关系；**指导教师一栏在仓库现有资料中无出处，
> 必须由本人提供**。
