# 演示视频分镜脚本（答辩材料）

> **用途**：录制 3~5 分钟系统演示视频的**镜头级**拍摄脚本。
> **整理时间**：2026-09-25，代码基线 `28c2ef6`。
> **2026-09-26 修正**：S15（签到）与实际前端对齐 —— 组织端签到管理页**没有**「签到」按钮，
> 学生端按钮**只在签到窗口已开时才渲染**，所以「点签到 → 提示窗口未开放」在 UI 上不成立；
> 同时录制红线第 3 条补上「要录学生端签到成功就得新发一场窗口内活动」。详见第五节后的注记。
>
> ## 🔴 录制状态：**未录制**
>
> **本视频尚未录制。** 本文件是**分镜脚本**（拍摄前的计划），不是录制结果。
> 仓库里目前 **0 个视频文件、0 张截图**〔`docs/答辩前待办.md:64`、`docs/待办清单.md:844`〕。
> **实际录制必须由人来做** —— 需要真人操作浏览器、配音（或配字幕）、剪辑。
> 录制完成后请把成片放到 `docs/答辩材料/` 或另行提交，并在本节把状态改为「已录制（文件名 / 时长）」。
>
> **录制的前置依赖**：`docs/screenshots/` 截图目录由另一路并行工作产出，
> **本分镜不依赖它** —— 分镜录的是**动态操作过程**，截图是**静态页面**，两者独立。
> 若录制时截图已就位，可复用其账号与页面口径。

---

## 一、成片规格

| 项 | 取值 | 说明 |
|---|---|---|
| **总时长** | **4 分 20 秒**（目标区间 3~5 分钟） | 各镜时长见下表，可整体 ±30 秒浮动 |
| **分辨率** | **1920 × 1080**（16:9） | 与浏览器 1440px 宽视口一致放大；**不要用 4K**，答辩投影仪通常只支持 1080p |
| **帧率** | 30 fps | 录屏足够，不必 60 fps |
| **浏览器缩放** | **100%** | 全站 29 页在 1440/620 两视口实测 0 横向溢出〔`docs/答辩前待办.md:23`~`:25`〕 |
| **浏览器** | **Chrome**：`C:\Program Files\Google\Chrome\Application\chrome.exe` | 本机**没有 Edge**〔`docs/会话记录.md:1125`~`:1128`〕 |
| **音频** | 旁白 + 背景音乐（音乐音量 -18dB 左右） | 无配音条件时**只保留字幕**，字幕内容即旁白原文 |
| **字幕** | 简体中文硬字幕，底部居中，字号 28~32px | 每个数字都必须与旁白一致 |
| **鼠标** | 建议录制时**显示鼠标高亮**（点击涟漪） | 观众能跟上操作位置 |

### ⚠️ 数据口径铁律

**全片统一使用真实后端口径**，数字如下（每个数字的出处见 `docs/答辩材料/演示脚本.md` 第十节）：

```text
活动 389 场 / 报名 10,719 人 / 累计时长 19,319.3 小时
签到率 90.1%（应签到 8,235 / 实签到 7,423 / 缺勤 812）
时长审核通过率 75.6%（5,614 通过 / 1,115 待审 / 694 驳回）
5 学院时长：4,258.2 / 3,839.7 / 3,761.9 / 3,759 / 3,700.5（合计 = 19,319.3）
学生 student：22.6 小时 / 9 场 / 四星志愿者 / 3 个标签
```

**绝对不要出现 mock 口径的 `12,480` 或 `86,420`** —— 一旦出现，全片数字就串口径了。

---

## 二、录制环境准备

### 2.1 起服务（两条命令）

```powershell
# ① 后端（若 8080 已有实例在跑可跳过；2026-09-25 的实例是 pid 32936）
cd volunteer-cert-portrait-server
java -jar vcp-boot/target/vcp-boot-1.0.0.jar
# 就绪判据：浏览器打开 http://localhost:8080/doc.html 能看到接口文档

# ② 前端 dev server（新开一个终端）
cd volunteer-cert-portrait-web
npm run dev
# 就绪判据：http://localhost:5173 打开登录页
```

> ⚠️ **打包前先停掉正在运行的 jar**，否则 Windows 会锁文件、`repackage` 报
> `Unable to rename 'vcp-boot-1.0.0.jar'`〔`CLAUDE.md` 踩坑第 15 条〕。
> ⚠️ **本次录制不需要重新打包** —— 用现成的 jar 即可。

### 2.2 确认走的是真实后端

| 检查 | 方法 | 期望 |
|---|---|---|
| 前端 mock 开关 | 看 `volunteer-cert-portrait-web/.env.development` 的 `VITE_USE_MOCK` | **`false`**〔`docs/会话记录.md:1111`~`:1112`〕 |
| 代理目标 | `.env.development` 的 `VITE_API_TARGET`（默认 `http://127.0.0.1:8080`） | 指向本机后端 |
| **一眼判定** | 打开数据看板看「累计志愿时长」 | **19,319.3 小时 = 对的**；86,420 = mock，**先改配置再录** |

### 2.3 录制前清场

- [ ] 关闭所有无关窗口、聊天软件通知、浏览器其他标签页
- [ ] 浏览器**清一次登录态**（或直接用无痕窗口），保证三个账号都从登录页开始
- [ ] Chrome 书签栏**隐藏**（`Ctrl+Shift+B`）
- [ ] 屏幕分辨率设为 **1920×1080**，缩放 100%
- [ ] 准备好录屏工具（OBS / Windows 自带 `Win+G`），**先试录 15 秒**确认音画同步
- [ ] **演示数据不要动** —— 不要为了录制方便去改活动时间或删用户（见第六节红线）

---

## 三、分镜表

> **图例**：`画面` = 观众看到的内容；`操作` = 录制者手上的动作；`旁白` = 配音原文；
> `字幕` = 屏幕下方文字（可与旁白相同，此处只标与旁白**不同**或**需要强调**的部分）。

### 片头（0:00 – 0:15）

| 镜号 | 时长 | 画面 | 操作 | 旁白 | 字幕 |
|---|---|---|---|---|---|
| **S01** | 8s | 标题卡（黑底白字）：「高校志愿服务时长认证与公益画像数据分析系统」+ 副标题「毕业设计 · 系统演示 · 第 2 组」 | 无（后期加） | 「本系统面向高校志愿服务场景，实现从活动发布到公益画像的完整闭环。」 | 高校志愿服务时长认证与公益画像数据分析系统 |
| **S02** | 7s | 标题卡切换：三行文字依次淡入 ——「学生 / 组织管理员 / 学校管理员」 | 无（后期加） | 「系统有三个角色：学生报名参与，组织管理员发布活动并审核，学校管理员终审时长并查看数据分析。」 | 三个角色 · 一条闭环 |

### 第一幕 · 学生（0:15 – 1:25，共 70 秒）

| 镜号 | 时长 | 画面 | 操作 | 旁白 | 字幕 |
|---|---|---|---|---|---|
| **S03** | 8s | 登录页 | 输入 `student` / `123456` → 点「登录」 | 「首先以学生身份登录。」 | 账号 student / 口令 123456 |
| **S04** | 10s | 学生端首页总览卡片 | 鼠标划过时长与等级卡片 | 「这位学生的累计有效时长是 **22.6 小时**，参与过 **9 场**活动，公益等级是**四星志愿者**。」 | 22.6 小时 · 9 场 · 四星志愿者 |
| **S05** | 10s | 活动列表页 | 滚动列表，停在带封面的三条活动上 | 「活动总数 **389 场**。列表前面这三场是带图文的演示活动，封面和图片说明都由系统管理。」 | 活动总数 389 场 |
| **S06** | 8s | 活动详情页（id 389） | 滚动展示封面与图文说明 | 「发布活动时支持一张封面，加最多六张带说明的图片。」 | 1 张封面 + 最多 6 张图文说明 |
| **S07** | 12s | 活动详情页 → 报名弹窗 | 点「立即报名」→ 填理由 → 提交 | 「学生报名时，系统用一条原子更新占用名额：已报名人数小于名额上限才会成功，等于上限就提示名额已满。这样不需要加锁，也不会出现两个人抢到同一个名额。」 | 一条原子 SQL 占名额，无锁无 TOCTOU |
| **S08** | 8s | 我的报名列表 | 滚动，指出最上面那条「待审核」 | 「报名提交后进入待审核状态，等组织管理员处理。」 | 新报名 → 待审核 |
| **S09** | 8s | 我的时长页 | 滚动列表 | 「已通过的服务时长合计 **22.6 小时**。」 | 已通过时长 22.6 小时 |
| **S10** | 10s | 我的公益画像页 | 依次指等级、标签、参与次数 | 「公益画像分**六个等级、八类标签**。四星对应二十到四十小时，这位学生是四星，标签是热心志愿者、长期坚持型、大型活动型。」 | 6 档等级 · 8 类标签 |

### 第二幕 · 组织管理员（1:25 – 2:45，共 80 秒）

| 镜号 | 时长 | 画面 | 操作 | 旁白 | 字幕 |
|---|---|---|---|---|---|
| **S11** | 6s | 退出 → 登录页 | 输入 `org_admin` / `123456` | 「切换到组织管理员。」 | 账号 org_admin |
| **S12** | 8s | 组织数据页 | 鼠标划过概览卡片 | 「组织管理员只能看到自己组织的数据，跨组织访问会被拦下。」 | 数据按组织隔离 |
| **S13** | 14s | 发布活动页 | 填名称/时间/名额/时长 → 上传封面 → 上传图文 → 点发布 | 「发布活动支持上传图片。系统会校验类型和大小，非图片或超过五兆会被拒绝；文件落盘前先规范化路径，防止路径穿越。」 | 图片上传：类型 + 大小 + 路径校验 |
| **S14** | 10s | 报名审核页 | 筛选「待审核」→ 找到 S07 那条 → 点「通过」 | 「组织管理员审核刚才那条报名。通过之后，系统会在同一事务里为这条报名生成一条签到记录。」 | 审核通过 → 同步生成签到记录 |
| **S15** | 14s | 签到管理页 → 学生端「我的报名」页 | 指该行状态「未签到」与操作列的**「修正」**按钮（**组织端不能替学生签到**）；再切到学生端「我的报名」，指**没有签到按钮**这一事实 | 「这里要说明两个边界。组织端只能查看和人工修正，不能替学生签到 —— 签到签退是学生本人的动作，只认登录态里的 studentId。学生端点签到还有一道时间窗口：活动开始前 30 分钟开放、结束后 30 分钟内必须签退；演示数据里已发布的活动都在未来，所以此刻窗口没开，**前端干脆不渲染签到按钮** —— 渲染一个点了必然报错的按钮比不渲染更糟；服务端同样拦得住，直接调接口会返回 10001。**这是设计内行为**，而且我们没有用定时任务去扫表改状态，是查询时实时判定，少一个调度组件，也不会出现任务挂了状态不更新的情况。」 | ⚠️ 设计内行为：窗口未开时**不渲染**签到按钮（接口层返回 10001「签到尚未开放」） |
| **S16** | 14s | 时长提交页 | 找到该学生的已通过报名 → 填 4.0 小时 → 提交 | 「组织为这次服务提交时长，状态进入待审核。时长有一个规则：最多按活动预计时长的一点五倍封顶，防止签到窗口被滥用刷时长。」 | 时长封顶 = 活动预计时长 × 1.5 |

### 第三幕 · 学校管理员（2:45 – 4:05，共 80 秒）

| 镜号 | 时长 | 画面 | 操作 | 旁白 | 字幕 |
|---|---|---|---|---|---|
| **S17** | 6s | 退出 → 登录页 | 输入 `admin` / `123456` | 「最后是学校管理员。」 | 账号 admin |
| **S18** | 22s | 数据看板（**全片重点**） | 缓慢滚动，逐个划过关键卡片与图表 | 「数据看板：活动 **389 场**、报名 **10,719 人**、累计志愿时长 **19,319.3 小时**、签到率 **90.1%**、时长审核通过率 **75.6%**。签到明细是应签到 **8,235**、实签到 **7,423**、缺勤 **812**。下面是五个学院的时长排行，**合计正好等于总时长**。」 | 389 场 · 10,719 人 · 19,319.3 小时<br>签到率 90.1% · 审核通过率 75.6% |
| **S19** | 12s | 时长审核页 | 筛选「待审核」→ 找到 S16 那条 → 点「通过」 | 「学校终审通过后，这条时长会累加到学生的累计时长上 —— 那是全系统唯一的权威值，画像里的那份只是可以随时重算的快照。」 | 终审通过 → 累加到权威值 |
| **S20** | 12s | 公益画像页（学校视角） | 滚动等级分布与标签分布 | 「等级分布六个档位齐全，从普通到五星；八类标签也全部能产出。」 | 6 档等级齐全 · 8 类标签齐全 |
| **S21** | 10s | 组织管理 → 分类管理 → 学院管理 | 依次点三个菜单，各停 3 秒 | 「组织资质、活动分类、学院字典都由学校统一管理，前端不写死任何一项。」 | 字典驱动，前端不写死 |
| **S22** | 10s | 操作日志页 | 滚动列表 | 「操作日志由切面采集，通过 Spring 事件解耦，日志模块和被记录的业务模块之间没有直接依赖。」 | 切面 + Spring Event 解耦 |
| **S23** | 8s | 时长审核页 → 挑一条点「驳回」→ 填理由 | 点驳回并确认 | 「驳回不是终点：组织可以修改后重新提交，状态回到待审核，而每一次提交都会往审核流水表追加一条记录，驳回历史不会被覆盖。」 | 驳回可重提，历史不覆盖 |

### 收尾（4:05 – 4:20，共 15 秒）

| 镜号 | 时长 | 画面 | 操作 | 旁白 | 字幕 |
|---|---|---|---|---|---|
| **S24** | 10s | 回到学生端「我的时长」→「我的公益画像」 | 刷新页面，指时长合计与等级 | 「回到学生端 —— 累计时长比刚才增加了，公益画像也随权威值重算。到这里，**发布 → 报名 → 组织审核 → 组织提交时长 → 学校审核 → 学生获得时长 → 画像与看板更新**，整个闭环就走完了。签到签退这一环前面 S15 已经说明：真后端下**要先造一场签到窗口内的活动**，学生端才会出现签到按钮。」 | 七步闭环跑通（签到入口见 S15 说明） |
| **S25** | 5s | 结束卡（黑底白字） | 无（后期加） | 「系统测试方面：二十项一致性自检违规合计为零，测试用例七十三条，端到端脚本全部通过。」 | 20 项自检 0 违规 · 73 条用例 · 端到端 PASS 30 / SKIP 2 / FAIL 0 |

> ⚠️ **S24 已于 2026-09-26 修正**：原旁白把「**签到签退**」列进了成片展示的闭环步骤，
> 但 S15 实际展示的是「真后端下**没有**签到按钮」，两处会互相打脸。现改为：
> 闭环按**画面里真做的步骤**念（发布 → 报名 → 组织审核 → 提交时长 → 学校审核 → 学生获得时长 → 画像与看板），
> 签到环节只做**口头指回 S15**。
> **若一定要在成片里出现"签到成功"的画面**，两条路：
> ① 按 `演示脚本.md` 第 2.4 步的 **(a) 路径**先造一场窗口内的活动（`time = 现在+30 分钟`、
> `deadline = 现在+29 分钟`）再录，**不要改旧活动的时间**（会破坏 `sql/09` 口径）；
> ② 或切 `VITE_USE_MOCK=true` 录一段 **mock 口径**的签到 → 签退，**并在字幕里标明是 mock 口径**
> （mock 刻意不做 30 分钟窗口校验，点签到一定能过）。
> 无论走哪条，**都不许**把没录到的环节说成录到了。

---

## 四、时长汇总

| 段落 | 镜号 | 时长 |
|---|---|---|
| 片头 | S01–S02 | 15s |
| 第一幕 · 学生 | S03–S10 | 74s |
| 第二幕 · 组织管理员 | S11–S16 | 66s |
| 第三幕 · 学校管理员 | S17–S23 | 90s |
| 收尾 | S24–S25 | 15s |
| **合计** | **S01–S25** | **260s ≈ 4 分 20 秒** |

> 若需压到 3 分钟：删 S06、S12、S21、S22、S23（省 46 秒），并把 S18 从 22 秒压到 16 秒。
> 若可放到 5 分钟：把 S18 拉长到 30 秒，并补一个「当场跑 `sql/09` 自检」的镜头（约 25 秒）。

---

## 五、后期处理要点

| 项 | 要求 |
|---|---|
| **数字打码/圈注** | S04、S18、S24 的关键数字用**细框圈注**（不要用大红圈，与系统水墨风不搭） |
| **速度** | 输入账号、加载页面等无信息量的片段**加速 2~4 倍**；关键数字停留处**放慢或定格 1 秒** |
| **转场** | 幕与幕之间用**淡入淡出**（0.3s），幕内不要转场，避免碎 |
| **字幕** | 与旁白逐字一致；数字前后不要漏字（如「19,319.3 小时」的千分位逗号要保留） |
| **BGM** | 轻音乐，音量压到旁白之下；**S15 讲签到窗口那段建议压低或去掉 BGM**，让说明更清楚 |

> ⚠️ **S15 已于 2026-09-26 修正**（B24 同步）：原分镜写的是「签到管理页 → 点「签到」→
> 出现窗口未开放提示」，但实测**这个提示在 UI 上点不出来** —— 组织端签到管理页只有「修正」按钮
> （`org/AttendanceView.vue:164`），签到 / 签退是学生专属动作（学生端 `MyAttendance` 按钮
> **只在服务端判定窗口已开时才渲染**，全仓唯一调用点是 `student/MySignupsView.vue:123`/`:133`）。
> 所以 S15 改成「指组织端的『修正』按钮 + 指学生端**没有**签到按钮」，
> 窗口校验的证明方式改为口头说明 + 接口层返回 `10001`。
| **片尾** | 不要加「谢谢观看」式空镜，S25 结束卡停 2 秒即黑 |

---

## 六、录制红线（**违反会让视频不可用**）

| # | 红线 | 原因 |
|---|---|---|
| 1 | ❌ **不要出现 mock 数字（12,480 / 86,420）** | 与全片口径冲突，一问就穿 |
| 2 | ❌ **演示时不要点「删除用户」** | **B31（删除用户不级联）已于 2026-09-25 修复并实测闭环**（同事务级联软删 + 重算 `signed_count`），所以不会再改坏看板；但删用户会**真的改动演示数据**，删完要重跑整链（约 10 秒）才能恢复干净口径。录制时不要点最稳。〔`CLAUDE.md`「当前进度」；`docs/答辩前待办.md:110`~`:114`〕 |
| 3 | ❌ **不要为了录签到而改数据库里的活动时间** | 会破坏 `sql/09` 的 20 项自检口径。**若一定要录到「学生端签到成功」的画面**（B24 新增能力），正确做法是**新发一场活动**：组织端发布一场开始时间设为**当前时间前 10 分钟**的活动 → 学生端报名 → 组织端审核通过（自动生成 `NOT_SIGNED` 签到记录）→ 学生端「我的报名」点签到、签退；**不要改旧活动的行** |
| 4 | ❌ **不要录到真实的数据库口令 / `application-local.yml`** | 仓库是公开仓库，明文口令进过 git 历史〔`CLAUDE.md` 踩坑第 17 条〕 |
| 5 | ❌ **不要录到聊天软件弹窗、其他项目窗口** | 信息泄露 + 观感差 |
| 6 | ⚠️ **等级没跳档就不要说跳了** | 22.6 小时加几小时仍在四星区间（20~40），如实说「还在四星区间内」 |
| 7 | ⚠️ **闭环里挑的活动 id 不要念出来** | 活动是否可报名取决于录制时刻，念 id 容易和画面对不上 |

---

## 七、录制完成后的收尾清单

- [ ] 成片文件命名：`docs/答辩材料/演示视频_第2组_v1.mp4`（或按学校要求命名）
- [ ] 时长确认在 3~5 分钟区间内
- [ ] 全片**逐帧核对数字**与本文第一节的「数据口径铁律」一致
- [ ] 把本文件顶部的「🔴 录制状态」改为「✅ 已录制（文件名 / 时长 / 录制日期）」
- [ ] 备份一份到网盘/U 盘（答辩现场别只带一个文件）
- [ ] 在答辩电脑上**试播一次**（确认解码器、音量、字幕渲染正常）

---

## 八、备用方案：录不出来怎么办

| 情况 | 替代方案 |
|---|---|
| 完全无法录屏 | 用 `docs/答辩材料/演示脚本.md` **现场实操**代替视频；把本分镜的 S18（数据看板）与 S10（公益画像）做成静态截图放进 PPT |
| 后端环境起不来 | 按 `docs/答辩材料/演示脚本.md` 第八节「降级 A」切 mock 录制，**但必须在片头加一行字幕说明口径是 12,480 / 86,420** |
| 只能录一部分 | **优先保证 S18（数据看板）+ S24（闭环收尾）** 两个镜头 —— 这两个镜头信息量最大 |
| 没有配音条件 | 只保留字幕，把旁白原文全部做成字幕；**S15 的签到窗口说明必须保留字幕** |
