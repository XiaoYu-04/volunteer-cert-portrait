<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.vcp</groupId>
        <artifactId>volunteer-cert-portrait-server</artifactId>
        <version>1.0.0</version>
    </parent>

    <artifactId>vcp-boot</artifactId>
    <name>vcp-boot</name>
    <description>启动模块：主类、配置文件，打包为唯一可执行 jar</description>

    <dependencies>
        <!-- 聚合全部业务模块 -->
        <dependency>
            <groupId>com.vcp</groupId>
            <artifactId>vcp-system</artifactId>
        </dependency>
        <dependency>
            <groupId>com.vcp</groupId>
            <artifactId>vcp-org</artifactId>
        </dependency>
        <dependency>
            <groupId>com.vcp</groupId>
            <artifactId>vcp-volunteer</artifactId>
        </dependency>
        <dependency>
            <groupId>com.vcp</groupId>
            <artifactId>vcp-certification</artifactId>
        </dependency>
        <dependency>
            <groupId>com.vcp</groupId>
            <artifactId>vcp-portrait</artifactId>
        </dependency>
        <dependency>
            <groupId>com.vcp</groupId>
            <artifactId>vcp-analytics</artifactId>
        </dependency>

        <!-- 数据库迁移：starter 只带入 flyway-core 与 JDBC 支持，不含任何数据库方言 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-flyway</artifactId>
        </dependency>
        <!-- PostgreSQL 方言必须单独声明，starter 不会传递进来；
             缺失时启动报 "Unsupported Database: PostgreSQL"，配置项 spring.flyway.postgresql.* 也不生效 -->
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
    </dependencies>

    <build>
        <resources>
            <!-- 第一段：application* 配置参与占位符过滤（@..@）；显式排除本机凭证文件 application-local.yml。
                 它的模板 application-local.yml.example 文件名结尾不同，不受此排除影响，仍随第二段原样打入 jar -->
            <resource>
                <directory>${basedir}/src/main/resources</directory>
                <filtering>true</filtering>
                <includes>
                    <include>**/application*.yml</include>
                    <include>**/application*.yaml</include>
                    <include>**/application*.properties</include>
                </includes>
                <excludes>
                    <exclude>application-local.yml</exclude>
                </excludes>
            </resource>
            <!-- 第二段：其余全部资源原样复制（mapper XML、db/migration 脚本、application-local.yml.example 等） -->
            <resource>
                <directory>${basedir}/src/main/resources</directory>
                <excludes>
                    <exclude>**/application*.yml</exclude>
                    <exclude>**/application*.yaml</exclude>
                    <exclude>**/application*.properties</exclude>
                </excludes>
            </resource>
        </resources>
        <plugins>
            <!-- 只有启动模块打可执行 jar；其余模块是普通内部 jar -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <mainClass>com.vcp.VcpApplication</mainClass>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>