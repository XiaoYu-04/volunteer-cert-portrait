/**
 * 复杂查询的结果载体（内部使用，不直接返回给前端）。
 *
 * <p>这些类只描述「SQL 查出来的原始行」：时间字段是 {@code LocalDateTime}、状态是库里的原值，
 * 不含任何格式化与惰性判定的结果。把它们和 vo 分开，是为了让
 * 「查出来的东西」与「返回给前端的东西」在类型上就不混用 ——
 * 后者要格式化时间、翻译分类状态、按 A1/A2 的口径算时长与缺勤。
 */
package com.vcp.volunteer.mapper.row;
