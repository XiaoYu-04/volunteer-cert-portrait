# 生产环境
#
# ⚠️ VITE_USE_MOCK 当前为 true —— 这是「纯前端独立演示」的配置：
#    构建产物直接用 src/mock 的本地数据，部署后即可展示，不依赖后端。
#    若置为 false，产物会去请求 /api，必须有 Nginx 反代到后端 8080，否则页面全空。
#
# 走「后端 + Nginx 反代」的正式部署时改成 false，
# 页面代码无需任何改动。同时确认 VITE_API_BASE_URL 与 Nginx 的 /api 反代一致。
VITE_API_BASE_URL=/api
VITE_USE_MOCK=true