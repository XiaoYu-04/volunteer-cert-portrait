# 生产环境
#
# VITE_USE_MOCK 当前为 false —— 这是「真实后端 + Nginx 反代」的正式部署配置（D5 口径）：
#    构建产物会去请求 /api，必须有 Nginx 把 /api 反代到后端 8080，否则页面全空。
#
# 需要「纯前端独立演示」（后端没起来时的兜底）时改成 true：
#    产物直接用 src/mock 的本地数据，不依赖后端，可离线展示。
#    ⚠️ 两套数据不是同一套：mock 是 12,480 报名 / 86,420 小时，
#    真实库是 10,719 报名 / 19,319.3 小时，答辩材料里的数字必须标明是哪一套。
#
# 切换只改这一个变量，页面代码无需任何改动。同时确认 VITE_API_BASE_URL 与 Nginx 的 /api 反代一致。
# 完整部署步骤与验收清单见 docs/部署说明.md。
VITE_API_BASE_URL=/api
VITE_USE_MOCK=false
