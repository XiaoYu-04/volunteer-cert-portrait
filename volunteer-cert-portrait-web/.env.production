# 生产环境
#
# ⚠️ VITE_USE_MOCK 当前为 true —— 因为后端尚未开发，此时若置为 false，
#    构建产物会去请求并不存在的 /api，页面全是空的。
#    这正是「纯前端独立演示」需要的配置（答辩演示前端时可直接部署）。
#
# 后端 volunteer-cert-portrait-server 就绪后，改成 false 即可，
# 页面代码无需任何改动。同时确认 VITE_API_BASE_URL 与 Nginx 的 /api 反代一致。
VITE_API_BASE_URL=/api
VITE_USE_MOCK=true