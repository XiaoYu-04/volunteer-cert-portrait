# 生产环境
#
# VITE_USE_MOCK=false（2026-09-27 起）—— 构建产物走真实后端：接口请求 /api，
# 必须有 Nginx（或其它反代）把 /api 转到后端 8080，否则页面全空。
# 生产构建的部署形态见 README「生产部署（Nginx + 后端）」一节。
#
# 若要「纯前端独立演示」（不依赖后端、不依赖数据库），把它改回 true 再构建：
#   产物直接吃 src/mock 的本地数据，部署后即可展示；代价是看不到真实数据。
#   判断产物用的是哪一套，看数据看板那组数字最快：
#     mock 口径 12,480 报名 / 86,420 小时；真实库 2,349 报名 / 9,163.0 小时。
VITE_API_BASE_URL=/api
VITE_USE_MOCK=false
